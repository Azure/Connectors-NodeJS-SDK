parameters:
  - name: IsPrerelease
    type: boolean
    default: true
  - name: NodeLtsVersion
    type: string
    default: 22.x
  - name: npmRegistry
    type: string
    default: 'https://pkgs.dev.azure.com/azfunc/internal/_packaging/upstream/npm/registry/'

trigger:
  batch: true
  branches:
    include:
      - main
      - release/*

pr:
  branches:
    include:
      - main
      - release/*

resources:
  repositories:
    - repository: 1es
      type: git
      name: 1ESPipelineTemplates/1ESPipelineTemplates
      ref: refs/tags/release

extends:
  template: v1/1ES.Official.PipelineTemplate.yml@1es
  parameters:
    pool:
      name: 1es-pool-azfunc
      image: 1es-windows-2022
      os: windows

    sdl:
      codeql:
        runSourceLanguagesInSourceAnalysis: true

    stages:
      - stage: LintAndTypecheck
        dependsOn: []
        jobs:
          - template: /azure-pipelines/templates/lint.yml@self
            parameters:
              NodeVersion: ${{ parameters.NodeLtsVersion }}
              npmRegistry: ${{ parameters.npmRegistry }}
        pool:
          name: 1es-pool-azfunc
          image: 1es-ubuntu-22.04
          os: linux

      - stage: WindowsUnitTests
        dependsOn:
          - LintAndTypecheck
        jobs:
          - template: /azure-pipelines/templates/test.yml@self
            parameters:
              NodeLtsVersion: ${{ parameters.NodeLtsVersion }}
              npmRegistry: ${{ parameters.npmRegistry }}

      - stage: LinuxUnitTests
        dependsOn:
          - LintAndTypecheck
        jobs:
          - template: /azure-pipelines/templates/test.yml@self
            parameters:
              NodeLtsVersion: ${{ parameters.NodeLtsVersion }}
              npmRegistry: ${{ parameters.npmRegistry }}
        pool:
          name: 1es-pool-azfunc
          image: 1es-ubuntu-22.04
          os: linux

      - stage: Build
        dependsOn:
          - WindowsUnitTests
          - LinuxUnitTests
        jobs:
          - template: /azure-pipelines/templates/build.yml@self
            parameters:
              IsPrerelease: ${{ parameters.IsPrerelease }}
              NodeVersion: ${{ parameters.NodeLtsVersion }}
              npmRegistry: ${{ parameters.npmRegistry }}
