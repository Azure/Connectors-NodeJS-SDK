// ZendeskExtensions.ts - Auto-generated DirectClient SDK for TypeScript
// Do not edit this file directly.

import type { TokenCredential } from "@azure/core-auth";
import { ConnectorClientBase } from "../azureConnectors/clientBase.ts";
import type { ConnectorPagedAsyncIterableIterator } from "../azureConnectors/clientBase.ts";
import type { ConnectorClientOptions, ConnectorOperationOptions } from "../azureConnectors/options.ts";
import { TriggerCallbackPayload } from "../azureConnectors/triggerPayload.ts";

// #region Types

/**
 * Definition: DataSetsMetadata
 */
export interface DataSetsMetadata {
    tabular?: TabularDataSetsMetadata;
    blob?: BlobDataSetsMetadata;
}

/**
 * Definition: TabularDataSetsMetadata
 */
export interface TabularDataSetsMetadata {
    source?: string;
    displayName?: string;
    urlEncoding?: string;
    tableDisplayName?: string;
    tablePluralName?: string;
}

/**
 * Definition: BlobDataSetsMetadata
 */
export interface BlobDataSetsMetadata {
    source?: string;
    displayName?: string;
    urlEncoding?: string;
}

/**
 * Definition: TableMetadata
 */
export interface TableMetadata {
    name?: string;
    title?: string;
    "x-ms-permission"?: string;
    "x-ms-capabilities"?: TableCapabilitiesMetadata;
    schema?: ObjectEntity;
    referencedEntities?: ObjectEntity;
}

/**
 * Definition: TableCapabilitiesMetadata
 */
export interface TableCapabilitiesMetadata {
    sortRestrictions?: TableSortRestrictionsMetadata;
    filterRestrictions?: TableFilterRestrictionsMetadata;
    selectRestrictions?: TableSelectRestrictionsMetadata;
    /** Server paging restrictions */
    isOnlyServerPagable?: boolean;
    filterFunctionSupport?: Array<string>;
}

/**
 * Definition: Object
 */
export interface ObjectEntity {
    [key: string]: unknown;
}

/**
 * Definition: TableSortRestrictionsMetadata
 */
export interface TableSortRestrictionsMetadata {
    /** Indicates whether this table has sortable columns */
    sortable?: boolean;
    /** List of unsortable properties */
    unsortableProperties?: Array<string>;
    /** List of properties which support ascending order only */
    ascendingOnlyProperties?: Array<string>;
}

/**
 * Definition: TableFilterRestrictionsMetadata
 */
export interface TableFilterRestrictionsMetadata {
    /** Indicates whether this table has filterable columns */
    filterable?: boolean;
    /** List of non filterable properties */
    nonFilterableProperties?: Array<string>;
    /** List of required properties */
    requiredProperties?: Array<string>;
}

/**
 * Definition: TableSelectRestrictionsMetadata
 */
export interface TableSelectRestrictionsMetadata {
    /** Indicates whether this table has selectable columns */
    selectable?: boolean;
}

/**
 * Definition: DataSetsList
 */
export interface DataSetsList {
    value?: Array<DataSet>;
}

/**
 * Definition: DataSet
 */
export interface DataSet {
    Name?: string;
    DisplayName?: string;
}

/**
 * Definition: Table
 */
export interface Table {
    Name?: string;
    DisplayName?: string;
}

/**
 * Definition: SearchResult
 */
export interface SearchResult {
    results?: Array<Record<string, unknown>>;
}

/**
 * Definition: Item
 */
export interface Item {
    dynamicProperties?: Record<string, unknown>;
}

/**
 * Definition: TablesList
 */
export interface TablesList {
    value?: Array<Table>;
}

/**
 * Definition: ItemsList
 */
export interface ItemsList {
    value?: Array<Item>;
    /** The URL to retrieve the next page. */
    "@odata.nextLink"?: string;
}

/**
 * Definition: GetCurrentUserInfoResponse
 */
export interface GetCurrentUserInfoResponse {
    [key: string]: unknown;
}

/**
 * Options for the getItems operation.
 */
export interface GetItemsOptions extends ConnectorOperationOptions {
    /** An ODATA filter query to restrict the entries returned (e.g. stringColumn eq 'string' OR numberColumn lt 123). */
    filter?: string;
    /** An ODATA orderBy query for specifying the order of entries */
    orderby?: string;
    /** Number of entries to skip (default = 0) */
    skip?: string;
    /** Maximum number of entries to retrieve (default = 512) */
    top?: string;
    /** Specific fields to retrieve from entries (default = all). */
    select?: string;
}

/**
 * Options for the searchArticles operation.
 */
export interface SearchArticlesOptions extends ConnectorOperationOptions {
    /** The locale the item is displayed in */
    locale?: string;
    /** Search for articles in the specified brand. */
    brandId?: string;
    /** Limit the search to this category id. */
    category?: string;
    /** Limit the search to this section id */
    section?: string;
    /** A comma-separated list of label names. */
    labelNames?: string;
    /** Enable search across all brands if true. */
    multibrand?: string;
}

/**
 * Typed callback payload for trigger operation 'GetOnNewItems'.
 */
export type ZendeskOnNewItemsTriggerPayload = TriggerCallbackPayload<Item>;

/**
 * Typed callback payload for trigger operation 'GetOnUpdatedItemsV2'.
 */
export type ZendeskOnUpdatedItemsTriggerPayload = TriggerCallbackPayload<Item>;

// #endregion Types

export const ZendeskTriggerOperations = {
    OnNewItems: "GetOnNewItems",
    OnUpdatedItems: "GetOnUpdatedItemsV2",
} as const;

export type ZendeskTriggerOperation = typeof ZendeskTriggerOperations[keyof typeof ZendeskTriggerOperations];

export const ZendeskTriggerParameters = {
    OnNewItems: {
        table: {
            name: "table",
            type: "string",
            required: true,
            description: "Name of Zendesk table",
            summary: "Table name",
            dynamicValuesOperationId: "GetTables",
        },
        filter: {
            name: "$filter",
            type: "string",
            required: false,
            description: "An ODATA filter query to restrict the entries returned (e.g. stringColumn eq 'string' OR numberColumn lt 123).",
            summary: "Filter Query",
        },
        orderby: {
            name: "$orderby",
            type: "string",
            required: false,
            description: "An ODATA orderBy query for specifying the order of entries",
            summary: "Order By",
        },
        skip: {
            name: "$skip",
            type: "integer",
            required: false,
            description: "Number of entries to skip (default = 0)",
            summary: "Skip Count",
        },
        top: {
            name: "$top",
            type: "integer",
            required: false,
            description: "Maximum number of entries to retrieve (default = 512)",
            summary: "Maximum Get Count",
        },
    },
    OnUpdatedItems: {
        table: {
            name: "table",
            type: "string",
            required: true,
            description: "Name of Zendesk table",
            summary: "Table name",
            dynamicValuesOperationId: "GetTables",
        },
        filter: {
            name: "$filter",
            type: "string",
            required: false,
            description: "An ODATA filter query to restrict the entries returned (e.g. stringColumn eq 'string' OR numberColumn lt 123).",
            summary: "Filter Query",
        },
        orderby: {
            name: "$orderby",
            type: "string",
            required: false,
            description: "An ODATA orderBy query for specifying the order of entries",
            summary: "Order By",
        },
        skip: {
            name: "$skip",
            type: "integer",
            required: false,
            description: "Number of entries to skip (default = 0)",
            summary: "Skip Count",
        },
        top: {
            name: "$top",
            type: "integer",
            required: false,
            description: "Maximum number of entries to retrieve (default = 512)",
            summary: "Maximum Get Count",
        },
    },
} as const;

// #region Client

/**
 * Typed client for the zendesk connector.
 */
export class ZendeskClient extends ConnectorClientBase {

    /**
     * Creates a new ZendeskClient.
     * @param connectionRuntimeUrl The connection runtime URL from Azure Portal.
     * @param credential The credential used for authentication.
     * @param options Optional connector client options.
     */
    constructor(connectionRuntimeUrl: string, credential: TokenCredential, options?: ConnectorClientOptions) {
        super(connectionRuntimeUrl, credential, options);
    }

    public get connectorName(): string {
        return "zendesk";
    }

    /**
     * Get tables
     * @remarks Retrieves Zendesk tables from a site
     */
    public async getTables(options: ConnectorOperationOptions = {}): Promise<TablesList> {
        const requestPath = `/datasets/default/tables`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<TablesList>("Zendesk.getTables", "GetTables", "GET", requestUrl, undefined, options);

        return httpResponse.value as TablesList;
    }

    /**
     * Get items
     * @remarks Retrieves Zendesk items of a certain Zendesk type (example: 'Ticket')
     */
    public getItems(table: string, options: GetItemsOptions = {}): ConnectorPagedAsyncIterableIterator<Item> {
        const queryParams: string[] = [];
        if (options.filter !== undefined) {
            queryParams.push(`$filter=${encodeURIComponent(String(options.filter))}`);
        }
        if (options.orderby !== undefined) {
            queryParams.push(`$orderby=${encodeURIComponent(String(options.orderby))}`);
        }
        if (options.skip !== undefined) {
            queryParams.push(`$skip=${encodeURIComponent(String(options.skip))}`);
        }
        if (options.top !== undefined) {
            queryParams.push(`$top=${encodeURIComponent(String(options.top))}`);
        }
        if (options.select !== undefined) {
            queryParams.push(`$select=${encodeURIComponent(String(options.select))}`);
        }
        const requestPath = `/datasets/default/tables/${table}/items` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        return this.createPageable<ItemsList, Item>(
            requestPath,
            async (requestUrl, isFirstPage) => {
                const requestMethod = isFirstPage ? "GET" : "GET";
                const httpResponse = await this.sendWithTracingAsync<ItemsList>("Zendesk.getItems", "GetItems", requestMethod, requestUrl, undefined, options);

                return httpResponse.value as ItemsList;
            },
            "value",
            "@odata.nextLink",
        );
    }

    /**
     * Create Item
     * @remarks Creates a Zendesk item
     */
    public async postItem(input: Item, table: string, options: ConnectorOperationOptions = {}): Promise<Item> {
        const requestPath = `/datasets/default/tables/${table}/items`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<Item>("Zendesk.postItem", "PostItem", "POST", requestUrl, input, options);

        return httpResponse.value as Item;
    }

    /**
     * Get item
     * @remarks Retrieves a Zendesk item
     */
    public async getItem(table: string, id: string, options: ConnectorOperationOptions = {}): Promise<Item> {
        const requestPath = `/datasets/default/tables/${table}/items/${id}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<Item>("Zendesk.getItem", "GetItem", "GET", requestUrl, undefined, options);

        return httpResponse.value as Item;
    }

    /**
     * Delete item
     * @remarks Deletes a Zendesk item
     */
    public async deleteItem(table: string, id: string, options: ConnectorOperationOptions = {}): Promise<void> {
        const requestPath = `/datasets/default/tables/${table}/items/${id}`;
        const requestUrl = this.resolveUrl(requestPath);
        await this.sendWithTracingAsync<void>("Zendesk.deleteItem", "DeleteItem", "DELETE", requestUrl, undefined, options);
    }

    /**
     * Update item
     * @remarks Updates an existing Zendesk item
     */
    public async patchItem(input: Item, table: string, id: string, options: ConnectorOperationOptions = {}): Promise<Item> {
        const requestPath = `/datasets/default/tables/${table}/items/${id}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<Item>("Zendesk.patchItem", "PatchItem", "PATCH", requestUrl, input, options);

        return httpResponse.value as Item;
    }

    /**
     * Search Articles
     * @remarks Returns a default number of 25 articles per page, up to a maximum of 1000 results.
     */
    public async searchArticles(query: string, options: SearchArticlesOptions = {}): Promise<SearchResult> {
        const queryParams: string[] = [];
        if (query !== undefined) {
            queryParams.push(`query=${encodeURIComponent(String(query))}`);
        }
        if (options.locale !== undefined) {
            queryParams.push(`locale=${encodeURIComponent(String(options.locale))}`);
        }
        if (options.brandId !== undefined) {
            queryParams.push(`brand_id=${encodeURIComponent(String(options.brandId))}`);
        }
        if (options.category !== undefined) {
            queryParams.push(`category=${encodeURIComponent(String(options.category))}`);
        }
        if (options.section !== undefined) {
            queryParams.push(`section=${encodeURIComponent(String(options.section))}`);
        }
        if (options.labelNames !== undefined) {
            queryParams.push(`label_names=${encodeURIComponent(String(options.labelNames))}`);
        }
        if (options.multibrand !== undefined) {
            queryParams.push(`multibrand=${encodeURIComponent(String(options.multibrand))}`);
        }
        const requestPath = `/api/v2/help_center/articles/search` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<SearchResult>("Zendesk.searchArticles", "SearchArticles", "GET", requestUrl, undefined, options);

        return httpResponse.value as SearchResult;
    }

}

// #endregion Client
