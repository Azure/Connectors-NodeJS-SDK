// ExcelonlineExtensions.ts - Auto-generated DirectClient SDK for TypeScript
// Do not edit this file directly.

import { ConnectorClientBase } from "../azureConnectors/clientBase.ts";
import { ConnectorException } from "../azureConnectors/connectorException.ts";
import { ConnectorClientOptions } from "../azureConnectors/options.ts";
import { TokenProvider } from "../azureConnectors/authentication.ts";

// #region Types

/**
 * Response for Get worksheets
 */
export interface GetAllWorksheetsResponse {
    value?: Array<WorksheetMetadata>;
}

/**
 * Create worksheet
 */
export interface CreateWorksheetInput {
    /** Worksheet name. */
    name?: string;
}

/**
 * Response for Get tables
 */
export interface GetTablesResponse {
    value?: Array<Record<string, unknown>>;
}

/**
 * Definition: DataSetsMetadata
 */
export interface DataSetsMetadata {
    tabular?: TabularDataSetsMetadata;
    blob?: BlobDataSetsMetadata;
}

/**
 * Definition: TabularDataSetsMetadata
 */
export interface TabularDataSetsMetadata {
    /** Dataset source */
    source?: string;
    /** Dataset display name */
    displayName?: string;
    /** Dataset url encoding */
    urlEncoding?: string;
    /** Table display name */
    tableDisplayName?: string;
    /** Table plural display name */
    tablePluralName?: string;
}

/**
 * Definition: BlobDataSetsMetadata
 */
export interface BlobDataSetsMetadata {
    /** Blob dataset source */
    source?: string;
    /** Blob dataset display name */
    displayName?: string;
    /** Blob dataset url encoding */
    urlEncoding?: string;
}

/**
 * Definition: TableToCreate
 */
export interface TableToCreate {
    /** Enter the Excel table name. */
    TableName?: string;
    /** Enter the table address using A1 notation. */
    Range: string;
    /** Enter the columns names separated by ';' or ','. */
    ColumnsNames?: string;
}

/**
 * Definition: TableMetadata
 */
export interface TableMetadata {
    /** Table name */
    name?: string;
    /** Table title */
    title?: string;
    /** Table permission */
    "x-ms-permission"?: string;
    "x-ms-capabilities"?: TableCapabilitiesMetadata;
    schema?: ObjectEntity;
    referencedEntities?: ObjectEntity;
    /** Url link */
    webUrl?: string;
}

/**
 * Definition: TableCapabilitiesMetadata
 */
export interface TableCapabilitiesMetadata {
    sortRestrictions?: TableSortRestrictionsMetadata;
    filterRestrictions?: TableFilterRestrictionsMetadata;
    selectRestrictions?: TableSelectRestrictionsMetadata;
    /** Server paging restrictions */
    isOnlyServerPagable?: boolean;
    /** List of supported filter capabilities */
    filterFunctionSupport?: Array<string>;
    /** List of supported server-driven paging capabilities */
    serverPagingOptions?: Array<string>;
}

/**
 * Definition: Object
 */
export interface ObjectEntity {
    [key: string]: unknown;
}

/**
 * Definition: TableSortRestrictionsMetadata
 */
export interface TableSortRestrictionsMetadata {
    /** Indicates whether this table has sortable columns */
    sortable?: boolean;
    /** List of unsortable properties */
    unsortableProperties?: Array<string>;
    /** List of properties which support ascending order only */
    ascendingOnlyProperties?: Array<string>;
}

/**
 * Definition: TableFilterRestrictionsMetadata
 */
export interface TableFilterRestrictionsMetadata {
    /** Indicates whether this table has filterable columns */
    filterable?: boolean;
    /** List of non filterable properties */
    nonFilterableProperties?: Array<string>;
    /** List of required properties */
    requiredProperties?: Array<string>;
}

/**
 * Definition: TableSelectRestrictionsMetadata
 */
export interface TableSelectRestrictionsMetadata {
    /** Indicates whether this table has selectable columns */
    selectable?: boolean;
}

/**
 * Definition: ItemsList
 */
export interface ItemsList {
    /** List of Items */
    value?: Array<Item>;
}

/**
 * Definition: Item
 */
export interface Item {
    dynamicProperties?: Record<string, unknown>;
}

/**
 * Definition: TablesList
 */
export interface TablesList {
    /** List of Tables */
    value?: Array<Table>;
}

/**
 * Definition: Table
 */
export interface Table {
    /** The name of the table. The name is used at runtime. */
    Name?: string;
    /** The display name of the table. */
    DisplayName?: string;
    /** Additional table properties provided by the connector to the clients. */
    DynamicProperties?: Record<string, unknown>;
}

/**
 * Definition: BlobMetadata
 */
export interface BlobMetadata {
    /** The unique id of the file or folder. */
    Id?: string;
    /** The name of the file or folder. */
    Name?: string;
    /** The display name of the file or folder. */
    DisplayName?: string;
    /** The path of the file or folder. */
    Path?: string;
    /** The media type of the file or folder. */
    MediaType?: string;
    /** A boolean value (true, false) to indicate whether or not the blob is a folder. */
    IsFolder?: boolean;
}

/**
 * Definition: WorksheetMetadata
 */
export interface WorksheetMetadata {
    /** Worksheet Id. */
    id?: string;
    /** Worksheet name. */
    name?: string;
    /** Worksheet position. */
    position?: number;
    /** Worksheet visibility. */
    visibility?: string;
}
// #endregion Types

// #region Client

/**
 * Typed client for the excelonline connector.
 */
export class ExcelonlineClient extends ConnectorClientBase {

    /**
     * Creates a new ExcelonlineClient.
     * @param connectionRuntimeUrl The connection runtime URL from Azure Portal.
     * @param tokenProvider The token provider for authentication.
     * @param options Optional connector client options.
     */
    constructor(connectionRuntimeUrl: string, tokenProvider: TokenProvider, options?: ConnectorClientOptions) {
        super(connectionRuntimeUrl, tokenProvider, options);
    }

    public get connectorName(): string {
        return "excelonline";
    }

    /**
     * Create table
     * @remarks Create a new table in the Excel workbook.
     */
    public async createTableAsync(input: TableToCreate, drive: string, file: string, source?: string, abortSignal?: AbortSignal): Promise<TableMetadata> {
        const queryParams: string[] = [];
        if (source !== undefined) {
            queryParams.push(`source=${encodeURIComponent(String(source))}`);
        }
        const requestPath = `/drives/${drive}/files/${file}/tables` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<TableMetadata>("POST", url, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as TableMetadata;
    }

    /**
     * Add a key column to a table
     * @remarks Add a key column to an Excel table. The new column will be appended to the right. The new key column must be unique in the table.
     */
    public async createIdColumnAsync(drive: string, file: string, table: string, source?: string, idColumn?: string, populateColumn?: string, abortSignal?: AbortSignal): Promise<void> {
        const queryParams: string[] = [];
        if (source !== undefined) {
            queryParams.push(`source=${encodeURIComponent(String(source))}`);
        }
        if (idColumn !== undefined) {
            queryParams.push(`idColumn=${encodeURIComponent(String(idColumn))}`);
        }
        if (populateColumn !== undefined) {
            queryParams.push(`populateColumn=${encodeURIComponent(String(populateColumn))}`);
        }
        const requestPath = `/drives/${drive}/files/${file}/tables/${table}/createIdColumn` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<void>("POST", url, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }
    }

    /**
     * List rows present in a table
     * @remarks List rows present in a table.
     */
    public async getItemsAsync(drive: string, file: string, table: string, source?: string, filter?: string, orderby?: string, top?: string, skip?: string, select?: string, idColumn?: string, dateTimeFormat?: string, abortSignal?: AbortSignal): Promise<ItemsList> {
        const queryParams: string[] = [];
        if (source !== undefined) {
            queryParams.push(`source=${encodeURIComponent(String(source))}`);
        }
        if (filter !== undefined) {
            queryParams.push(`$filter=${encodeURIComponent(String(filter))}`);
        }
        if (orderby !== undefined) {
            queryParams.push(`$orderby=${encodeURIComponent(String(orderby))}`);
        }
        if (top !== undefined) {
            queryParams.push(`$top=${encodeURIComponent(String(top))}`);
        }
        if (skip !== undefined) {
            queryParams.push(`$skip=${encodeURIComponent(String(skip))}`);
        }
        if (select !== undefined) {
            queryParams.push(`$select=${encodeURIComponent(String(select))}`);
        }
        if (idColumn !== undefined) {
            queryParams.push(`idColumn=${encodeURIComponent(String(idColumn))}`);
        }
        if (dateTimeFormat !== undefined) {
            queryParams.push(`dateTimeFormat=${encodeURIComponent(String(dateTimeFormat))}`);
        }
        const requestPath = `/drives/${drive}/files/${file}/tables/${table}/items` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<ItemsList>("GET", url, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as ItemsList;
    }

    /**
     * Get a row
     * @remarks Get a row using a key column. This action will retrieve all the values of the specified row given a column and key column.
     */
    public async getItemAsync(drive: string, file: string, table: string, id: string, source?: string, idColumn?: string, dateTimeFormat?: string, abortSignal?: AbortSignal): Promise<Item> {
        const queryParams: string[] = [];
        if (source !== undefined) {
            queryParams.push(`source=${encodeURIComponent(String(source))}`);
        }
        if (idColumn !== undefined) {
            queryParams.push(`idColumn=${encodeURIComponent(String(idColumn))}`);
        }
        if (dateTimeFormat !== undefined) {
            queryParams.push(`dateTimeFormat=${encodeURIComponent(String(dateTimeFormat))}`);
        }
        const requestPath = `/drives/${drive}/files/${file}/tables/${table}/items/${id}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Item>("GET", url, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Item;
    }

    /**
     * Delete a row
     * @remarks Delete a row using a key column.
     */
    public async deleteItemAsync(drive: string, file: string, table: string, id: string, source?: string, idColumn?: string, abortSignal?: AbortSignal): Promise<void> {
        const queryParams: string[] = [];
        if (source !== undefined) {
            queryParams.push(`source=${encodeURIComponent(String(source))}`);
        }
        if (idColumn !== undefined) {
            queryParams.push(`idColumn=${encodeURIComponent(String(idColumn))}`);
        }
        const requestPath = `/drives/${drive}/files/${file}/tables/${table}/items/${id}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<void>("DELETE", url, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `DELETE ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }
    }

    /**
     * Update a row
     * @remarks Update a row using a key column. The input value will overwrite the specified cells and columns left blank will not be updated. In order to append (instead of overwrite) a value, use the "Get a row" action to retrieve the content first.
     */
    public async patchItemAsync(input: Item, drive: string, file: string, table: string, id: string, source?: string, idColumn?: string, mode?: string, dateTimeFormat?: string, abortSignal?: AbortSignal): Promise<Item> {
        const queryParams: string[] = [];
        if (source !== undefined) {
            queryParams.push(`source=${encodeURIComponent(String(source))}`);
        }
        if (idColumn !== undefined) {
            queryParams.push(`idColumn=${encodeURIComponent(String(idColumn))}`);
        }
        if (mode !== undefined) {
            queryParams.push(`mode=${encodeURIComponent(String(mode))}`);
        }
        if (dateTimeFormat !== undefined) {
            queryParams.push(`dateTimeFormat=${encodeURIComponent(String(dateTimeFormat))}`);
        }
        const requestPath = `/drives/${drive}/files/${file}/tables/${table}/items/${id}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Item>("PATCH", url, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PATCH ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Item;
    }

    /**
     * Get worksheets
     * @remarks Get a list of worksheets in the Excel workbook.
     */
    public async getAllWorksheetsAsync(drive: string, file: string, source?: string, abortSignal?: AbortSignal): Promise<GetAllWorksheetsResponse> {
        const queryParams: string[] = [];
        if (source !== undefined) {
            queryParams.push(`source=${encodeURIComponent(String(source))}`);
        }
        const requestPath = `/codeless/v1.0/drives/${drive}/items/${file}/workbook/worksheets` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<GetAllWorksheetsResponse>("GET", url, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as GetAllWorksheetsResponse;
    }

    /**
     * Create worksheet
     * @remarks Create a new worksheet in the Excel workbook.
     */
    public async createWorksheetAsync(input: CreateWorksheetInput, drive: string, file: string, source?: string, abortSignal?: AbortSignal): Promise<WorksheetMetadata> {
        const queryParams: string[] = [];
        if (source !== undefined) {
            queryParams.push(`source=${encodeURIComponent(String(source))}`);
        }
        const requestPath = `/codeless/v1.0/drives/${drive}/items/${file}/workbook/worksheets` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<WorksheetMetadata>("POST", url, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as WorksheetMetadata;
    }

    /**
     * Get tables
     * @remarks Get a list of tables in the Excel workbook.
     */
    public async getTablesAsync(drive: string, file: string, source?: string, select?: string, abortSignal?: AbortSignal): Promise<GetTablesResponse> {
        const queryParams: string[] = [];
        if (source !== undefined) {
            queryParams.push(`source=${encodeURIComponent(String(source))}`);
        }
        if (select !== undefined) {
            queryParams.push(`$select=${encodeURIComponent(String(select))}`);
        }
        const requestPath = `/codeless/v1.0/drives/${drive}/items/${file}/workbook/tables` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<GetTablesResponse>("GET", url, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as GetTablesResponse;
    }

    /**
     * Add a row into a table
     * @remarks Add a new row into the Excel table.
     */
    public async addRowAsync(input: Item, drive: string, file: string, table: string, source?: string, dateTimeFormat?: string, abortSignal?: AbortSignal): Promise<Item> {
        const queryParams: string[] = [];
        if (source !== undefined) {
            queryParams.push(`source=${encodeURIComponent(String(source))}`);
        }
        if (dateTimeFormat !== undefined) {
            queryParams.push(`dateTimeFormat=${encodeURIComponent(String(dateTimeFormat))}`);
        }
        const requestPath = `/codeless/v1.2/drives/${drive}/items/${file}/workbook/tables/${table}/rows` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Item>("POST", url, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Item;
    }

}

// #endregion Client
