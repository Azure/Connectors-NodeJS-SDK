// SigninghubExtensions.ts - Auto-generated DirectClient SDK for TypeScript
// Do not edit this file directly.

import type { AbortSignalLike } from "@azure/abort-controller";
import type { TokenCredential } from "@azure/core-auth";
import { ConnectorClientBase } from "../azureConnectors/clientBase.ts";
import { ConnectorException } from "../azureConnectors/connectorException.ts";
import { ConnectorClientOptions } from "../azureConnectors/options.ts";

// #region Types

/**
 * Response for Download Attachment
 */
export interface AttachmentDownloadAttachmentResponse {
    [key: string]: unknown;
}

/**
 * Upload Attachment
 */
export type AttachmentUploadAttachmentInput = string;

/**
 * Response for Download Document
 */
export interface DocumentsDownloadDocumentBytesResponse {
    [key: string]: unknown;
}

/**
 * Upload Document
 */
export type DocumentsUploadStreamInput = string;

/**
 * Assign Document Field
 */
export interface FieldsAutoAssignFieldInput {
    [key: string]: unknown;
}

/**
 * Response for Download Package
 */
export interface PackageDownloadPackageBytesResponse {
    [key: string]: unknown;
}

/**
 * Response for Get Process Evidence Report
 */
export interface WorkflowEvidenceReportDownloadBytesResponse {
    [key: string]: unknown;
}

/**
 * Add Groups to Workflow
 */
export interface WorkflowWorkflowAddGroupInput {
    [key: string]: unknown;
}

/**
 * Add Placeholder to Workflow
 */
export interface WorkflowWorkflowAddPlaceholderInput {
    [key: string]: unknown;
}

/**
 * Add Users to Workflow
 */
export interface WorkflowWorkflowAddUserInput {
    [key: string]: unknown;
}

/**
 * Definition: ErrorWithDescriptionResponse
 */
export interface ErrorWithDescriptionResponse {
    /** Error key to identify the error in programming and applications can respond based on the different error keys. */
    error?: string;
    /** Error message against the error key returned. Client applications can present this error as it is or send their own error message. */
    error_description?: string;
}

/**
 * Definition: EmptyResponse
 */
export interface EmptyResponse {
    [key: string]: unknown;
}

/**
 * Definition: Response
 */
export interface Response {
    /** The service-specific error message returned by the API. This message provides useful information about why the service has returned an error response. */
    Message?: string;
}

/**
 * Definition: DetailedResponse
 */
export interface DetailedResponse {
    /** Custom error code */
    Error?: string;
    /** The service specific error message returned by the API. This message provides useful information about why the service has returned an error response. */
    Message?: string;
}

/**
 * Definition: AuthenticationRequest
 */
export interface AuthenticationRequest {
    /** Application Name that you have configured in your Enterprise Account when configuring the integration options. */
    client_id: string;
    /** API Key that you generated in your Enterprise Account when configuring the integration options. */
    client_secret: string;
    profile_name?: string;
}

/**
 * Definition: ElectronicSealRecipient
 */
export interface ElectronicSealRecipient {
    name?: string;
    level_of_assurance?: string;
}

/**
 * Definition: PermissionLegalNoticeResponse
 */
export interface PermissionLegalNoticeResponse {
    /** True, if legal notice is enabled and configured for the recipient. */
    enabled: boolean;
    /** List of legal notices allowed to the document owner from which he can select one for the recipient. */
    allowed_notices?: Array<LegalNoticeResponse>;
    /** Default or selected legal notice for the recipient. */
    default_notice?: Array<LegalNoticeResponse>;
}

/**
 * Definition: LegalNoticeResponse
 */
export interface LegalNoticeResponse {
    legal_notice_id: number;
    /** LegalNoticeResponse */
    legal_notice_name?: string;
    /** HTML content of the legal notice. */
    legal_notice_html?: string;
}

/**
 * Definition: AccessResponse
 */
export interface AccessResponse {
    authentication?: AuthenticationResponse;
    authentication_signing?: AuthenticationSigningResponse;
    access_duration?: PermissionAccessDurationResponse;
}

/**
 * Definition: AuthenticationResponse
 */
export interface AuthenticationResponse {
    /** True, if authentication is enabled for the package. */
    enabled: boolean;
    password?: AccessSecurityPasswordResponse;
    sms_otp?: AccessSecuritySmsOtpResponse;
    totp?: TotpPermissionResponse;
}

/**
 * Definition: AccessSecurityPasswordResponse
 */
export interface AccessSecurityPasswordResponse {
    /** True, if password authentication is enabled for the package. */
    enabled: boolean;
    /** Authentication password for document access. */
    user_password?: string;
}

/**
 * Definition: AccessSecuritySmsOtpResponse
 */
export interface AccessSecuritySmsOtpResponse {
    /** True, if SMS OTP authentication is enabled for the package. */
    enabled: boolean;
    otp_length: number;
    retry_duration: number;
    /** Mobile number of the recipient. Mobile number is needed to send the SMS to the recipient for the authentication. */
    mobile_number?: string;
    /** Allowed OTP Methods */
    methods?: Array<string>;
}

/**
 * Definition: TotpPermissionResponse
 */
export interface TotpPermissionResponse {
    /** Enabled */
    enabled: boolean;
}

/**
 * Definition: AuthenticationSigningResponse
 */
export interface AuthenticationSigningResponse {
    /** True, if authentication is enabled for the package. */
    enabled: boolean;
    sms_otp?: AuthenticationSigningSmsOtpResponse;
    totp?: TotpPermissionResponse;
}

/**
 * Definition: AuthenticationSigningSmsOtpResponse
 */
export interface AuthenticationSigningSmsOtpResponse {
    otp_length: number;
    retry_duration: number;
    /** Mobile number of the recipient. Mobile number is needed to send the SMS to the recipient for the authentication. */
    mobile_number?: string;
    /** Allowed OTP Methods */
    methods?: Array<string>;
}

/**
 * Definition: PermissionAccessDurationResponse
 */
export interface PermissionAccessDurationResponse {
    /** True, if access duration security is enabled for the package. */
    enabled: boolean;
    duration_by_date?: AccessDurationByDateResponse;
    duration_by_days?: AccessDurationByDaysResponse;
}

/**
 * Definition: AccessDurationByDateResponse
 */
export interface AccessDurationByDateResponse {
    /** True if access security is enabled for a duration by date and time. */
    enabled: boolean;
    accessible: boolean;
    duration?: DurationByDateResponse;
}

/**
 * Definition: DurationByDateResponse
 */
export interface DurationByDateResponse {
    /** Date and time from where the package will be accessible to the recipient. */
    start_date_time?: string;
    /** Date and time after which the package will not be accessible to the recipient. */
    end_date_time?: string;
}

/**
 * Definition: AccessDurationByDaysResponse
 */
export interface AccessDurationByDaysResponse {
    /** True, if access security is enabled for a duration by number of days. */
    enabled: boolean;
    accessible: boolean;
    duration?: DurationByDaysResponse;
}

/**
 * Definition: DurationByDaysResponse
 */
export interface DurationByDaysResponse {
    /** Total number of days for which the package will be accessible to the recipient after receiving the package. */
    total_days: number;
}

/**
 * Definition: ReminderResponse
 */
export interface ReminderResponse {
    /** True, if reminders are enabled for the recipient */
    enabled: boolean;
    /** Reminder Frequency. Possible Values are DAYS | HOURS.  */
    frequency?: string;
    /** The number of days after which the first reminder would be sent to workflow user. */
    remind_after: number;
    repeat?: ReminderRepeat;
}

/**
 * Definition: ReminderRepeat
 */
export interface ReminderRepeat {
    /** True, if subsequent reminders are enabled. */
    enabled: boolean;
    /** Repeat Reminder Frequency. Possible Values are DAYS | HOURS.  */
    frequency?: string;
    /** The number of days after which the subsequent reminders would be sent to workflow recipient. */
    keep_reminding_after: number;
    /** The number of subsequent reminders to be sent to workflow user. */
    total_reminders: number;
}

/**
 * Definition: AccessUpdateRequest
 */
export interface AccessUpdateRequest {
    /** True, if the access security or authentications are to be applied on all the recipients in the workflow. */
    apply_to_all: boolean;
    authentication: AccessAuthentication;
    authentication_signing?: SigningAuthentication;
    access_duration: PermissionAccessDuration;
}

/**
 * Definition: AccessAuthentication
 */
export interface AccessAuthentication {
    /** True, if authentication is enabled for the package. */
    enabled: boolean;
    password?: AuthenticationPassword;
    sms_otp?: AuthenticationSmsOtp;
    totp?: TotpPermissionResponse;
}

/**
 * Definition: AuthenticationPassword
 */
export interface AuthenticationPassword {
    /** True, if password authentication is enabled for the package. */
    enabled: boolean;
    /** Password string if password field is enabled. If a password is already set and owner do not wish to change it, remove the field from the response, or send null. */
    user_password?: string;
}

/**
 * Definition: AuthenticationSmsOtp
 */
export interface AuthenticationSmsOtp {
    /** True, if SMS OTP authentication is enabled for the package. */
    enabled: boolean;
    /** Mobile number of the recipient. Mobile number is needed to send the SMS to the recipient for the authentication. */
    mobile_number?: string;
}

/**
 * Definition: SigningAuthentication
 */
export interface SigningAuthentication {
    /** True, if authentication is enabled for the package. */
    enabled: boolean;
    sms_otp?: AuthenticationSigningSmsOtp;
    totp?: TotpPermissionResponse;
}

/**
 * Definition: AuthenticationSigningSmsOtp
 */
export interface AuthenticationSigningSmsOtp {
    /** Mobile number of the recipient. Mobile number is needed to send the SMS to the recipient for the authentication. */
    mobile_number?: string;
}

/**
 * Definition: PermissionAccessDuration
 */
export interface PermissionAccessDuration {
    /** True, if access duration security is enabled for the package. */
    enabled: boolean;
    duration_by_date?: AccessDurationByDate;
    duration_by_days?: AccessDurationByDays;
}

/**
 * Definition: AccessDurationByDate
 */
export interface AccessDurationByDate {
    /** True if access security is enabled for a duration by date and time. */
    enabled: boolean;
    duration?: DurationByDate;
}

/**
 * Definition: DurationByDate
 */
export interface DurationByDate {
    /** Date and time from where the package will be accessible to the recipient. */
    start_date_time?: string;
    /** Date and time after which the package will not be accessible to the recipient. */
    end_date_time?: string;
}

/**
 * Definition: AccessDurationByDays
 */
export interface AccessDurationByDays {
    /** True, if access security is enabled for a duration by number of days. */
    enabled: boolean;
    duration?: DurationByDays;
}

/**
 * Definition: DurationByDays
 */
export interface DurationByDays {
    /** Total number of days for which the package will be accessible to the recipient after receiving the package. */
    total_days: number;
}

/**
 * Definition: AddQrCodeResponse
 */
export interface AddQrCodeResponse {
    /** QR Code generated data */
    data: string;
    /** Name of the field created in the document. */
    field_name: string;
    /** Value will be the base64 of QrCode image */
    value: string;
}

/**
 * Definition: QrCodeRequest
 */
export interface QrCodeRequest {
    /** Page number on which the field is to be created. */
    page_no: number;
    /** Name of the provided field that is to be added. If not provided, system will assign an unique auto generated name to the field. */
    field_name?: string;
    dimensions: FieldDimension;
    /** The plain text data to be encoded in the QR code. Default is URL generated to access the related document. e.g. {base_url}/document/{document_unique_id} */
    data?: string;
    options?: QrCodeOptions;
    color?: QrCodeColor;
    logo?: QrCodeLogo;
}

/**
 * Definition: FieldDimension
 */
export interface FieldDimension {
    /** Left location of the field in pixel. */
    x: number;
    /** Top location of the field in pixel. */
    y: number;
    /** Width of the field in pixel. */
    width: number;
    /** Height of the field in pixel. */
    height: number;
}

/**
 * Definition: QrCodeOptions
 */
export interface QrCodeOptions {
    error_correction_level: ECCLevelAPI;
    encoding: EciModeAPI;
    /** Specifies the number of pixels per module in the QR code. A higher number results in a larger and more detailed QR code. Default is "20" Possible Values are : 20 | 30 | 40 | 50 */
    pixels_per_module: number;
    /** Indicates whether quiet zones (margins) should be drawn around the QR code. Quiet zones help scanners to detect and decode the QR code more accurately. Default is "true" */
    add_margins: boolean;
}

/**
 * Definition: ECCLevelAPI
 */
export type ECCLevelAPI = "L" | "M" | "Q" | "H" | "Default";

/**
 * Definition: EciModeAPI
 */
export type EciModeAPI = "Default" | "Iso8859_1" | "Iso8859_2" | "Utf8";

/**
 * Definition: QrCodeColor
 */
export interface QrCodeColor {
    /** Represents the dark color used for the dots in the QR code. Default is "black". e.g. "#000000" */
    dots?: string;
    /** Represents the light color used for the background of the QR code. Default is "white". e.g. "#ffffff" */
    background?: string;
}

/**
 * Definition: QrCodeLogo
 */
export interface QrCodeLogo {
    /** An optional icon (base64) to place in the center of the QR code. */
    base64?: string;
    /** The background color behind the icon, if an icon is provided. If color is not provided transparent background is implemented. */
    hide_background_dots: boolean;
    /** The size of the icon as a percentage of the QR code's size, if an icon is provided. Default is "15". Possible Values are : 10 | 15 | 20 | 25 | 30 | 35 | 40 */
    size: number;
}

/**
 * Definition: AddFieldResponse
 */
export interface AddFieldResponse {
    /** Name of the field created in the document. */
    field_name?: string;
    /** Date and time of field creation. */
    created_on?: string;
}

/**
 * Definition: ValidationRule
 */
export type ValidationRule = "OPTIONAL" | "MANDATORY";

/**
 * Definition: TextboxType
 */
export type TextboxType = "TEXT" | "NAME" | "EMAIL" | "COMPANY" | "JOBTITLE" | "DATE";

/**
 * Definition: FieldType
 */
export type FieldType = "Number" | "Text";

/**
 * Definition: SignatureFieldAuthentication
 */
export interface SignatureFieldAuthentication {
    /** True, if authentication is enabled for the field.  False otherwise. */
    enabled: boolean;
    otp?: SignatureFieldAuthenticationOtp;
    totp?: SignatureFieldAuthenticationTotp;
}

/**
 * Definition: SignatureFieldAuthenticationOtp
 */
export interface SignatureFieldAuthenticationOtp {
    /** True, if SMS/Email OTP authentication is enabled for the field.  False otherwise. */
    enabled: boolean;
    /** Mobile number of the recipient to which the field is assigned. */
    mobile_number?: string;
}

/**
 * Definition: SignatureFieldAuthenticationTotp
 */
export interface SignatureFieldAuthenticationTotp {
    /** True, if TOTP authentication is enabled for the field.  False otherwise. */
    enabled: boolean;
}

/**
 * Definition: SignatureFieldResponse
 */
export interface SignatureFieldResponse {
    /** Name of the field created in the document. */
    field_name?: string;
    /** Date and time of field creation. */
    created_on?: string;
}

/**
 * Definition: UpdateQrCodeResponse
 */
export interface UpdateQrCodeResponse {
    /** Value will be the base64 of QrCode image */
    value: string;
}

/**
 * Definition: UpdateQrCodeRequest
 */
export interface UpdateQrCodeRequest {
    /** Current name of the field, that is to be updated. */
    field_name: string;
    /** Updated name of the field if renaming is intended. */
    renamed_as?: string;
    /** Page number for which field is need to be updated */
    page_no: number;
    dimensions: FieldDimension;
    /** The plain text data to be encoded in the QR code. Default is URL generated to access the related document. e.g. {base_url}/document/{document_unique_id} */
    data?: string;
    options?: QrCodeOptions;
    color?: QrCodeColor;
    logo?: QrCodeLogo;
}

/**
 * Definition: FieldValidationValue
 */
export interface FieldValidationValue {
    required: boolean;
    rules?: Array<FieldValidationValueRule>;
}

/**
 * Definition: FieldValidationValueRule
 */
export interface FieldValidationValueRule {
    /** Specifies the type of validation rule, which is currently always set to EQUALS for text field. */
    type?: string;
    /** Represents the value used for validation. */
    value?: string;
}

/**
 * Definition: PostProcessResponse
 */
export interface PostProcessResponse {
    /** True, if post processing is enabled for the package. */
    enabled: boolean;
    /** List of email addresses to which the email will be sent when the workflow is completed. */
    contacts?: Array<string>;
    /** List of the name and email addresses to whom the emails are sent when the post processing is executed after the workflow completion. */
    recipients?: Array<PostProcessRecipientResponse>;
    /** Any custom string message that is to be sent to the contacts when the workflow is completed. */
    message?: string;
    /** True, if google drive uploading is turned on for the package. Documents will be uploaded to google drive when the workflow is completed. */
    google_drive: boolean;
    /** True, if one drive uploading is turned on for the package. Documents will be uploaded to one drive when the workflow is completed. */
    one_drive: boolean;
    /** True, if dropbox uploading is turned on for the package. Documents will be uploaded to google drive when the workflow is completed. */
    dropbox: boolean;
    /** True, if workflow completion report is to be sent to all recipients of the workflow when post processing is executed. Default value is false. */
    workflow_recipients: boolean;
    /** True, if Send the document processing report (XML) is to be enabled to for the workflow  */
    document_processing_report: boolean;
}

/**
 * Definition: PostProcessRecipientResponse
 */
export interface PostProcessRecipientResponse {
    name?: string;
    email?: string;
}

/**
 * Definition: CertifyResponse
 */
export interface CertifyResponse {
    /** True, if certify signature settings are enabled for the document. */
    enabled: boolean;
    /** Certify permission level set for the document with certify signature settings. */
    allowed_permissions?: Array<string>;
    default_permission?: string;
}

/**
 * Definition: GroupMemberResponse
 */
export interface GroupMemberResponse {
    /** Email address of the group member */
    user_email?: string;
    /** User name of the group member */
    user_name?: string;
}

/**
 * Definition: CollaboratorResponse
 */
export interface CollaboratorResponse {
    /** User email */
    user_email?: string;
    /** Signing order */
    signing_order: number;
    /** Is guest user */
    guest_user: boolean;
    /** Email language code */
    email_language_code?: string;
    permission?: WorkflowPermissionResponse;
    reminder?: ReminderResponse;
}

/**
 * Definition: WorkflowPermissionResponse
 */
export interface WorkflowPermissionResponse {
    /** True, if printing is allowed for this recipient. */
    print?: boolean;
    /** True, if download is allowed. */
    download?: boolean;
    /** True, if add text annotation on the document is allowed. */
    add_text?: boolean;
    /** True, if the recipient on the current order can change other recipients in the workflow after workflow is shared. */
    change_recipients?: boolean;
    /** True, if attachments and merging of PDF documents is allowed. */
    add_attachment?: boolean;
    legal_notice?: PermissionLegalNoticeResponse;
    attachment?: AttachmentPermissionResponse;
}

/**
 * Definition: AttachmentPermissionResponse
 */
export interface AttachmentPermissionResponse {
    /** Enabled */
    enabled: boolean;
}

/**
 * Definition: DeliveryMethod
 */
export type DeliveryMethod = "EMAIL" | "SMS" | "EMAIL_AND_SMS";

/**
 * Definition: WorkflowType
 */
export type WorkflowType = "SERIAL" | "INDIVIDUAL" | "PARALLEL" | "CUSTOM";

/**
 * Definition: PostProcessRecipientRequest
 */
export interface PostProcessRecipientRequest {
    name: string;
    email: string;
}

/**
 * Definition: UpdateCertifyPolicyRequest
 */
export interface UpdateCertifyPolicyRequest {
    certify?: CertifyPolicyRequest;
    /** True if form fields are to be locked after the last signature on the current document. */
    lock_form_fields: boolean;
}

/**
 * Definition: CertifyPolicyRequest
 */
export interface CertifyPolicyRequest {
    /** True if certify settings are to be enabled for the document. */
    enabled: boolean;
    permission: CertifyPermission;
}

/**
 * Definition: CertifyPermission
 */
export type CertifyPermission = "NO_CHANGES_ALLOWED" | "FORM_FILLING_ALLOWED" | "FORM_FILLING_WITH_ANNOTATIONS_ALLOWED";

/**
 * Definition: DeleteDocumentFieldRequest
 */
export interface DeleteDocumentFieldRequest {
    /** Name of the field to be deleted. */
    field_name: string;
}

/**
 * Definition: DocumentPackageStatus
 */
export type DocumentPackageStatus = "ALL" | "DRAFT" | "INPROGRESS" | "PENDING" | "SIGNED" | "COMPLETED" | "REVIEWED" | "DECLINED" | "EDITED" | "EXPIRING_IN_SEVEN_DAYS";

/**
 * Definition: DocumentType
 */
export type DocumentType = "ALL" | "PDF" | "DOCX" | "XML";

/**
 * Definition: DocumentOwnedBy
 */
export type DocumentOwnedBy = "ME_OTHERS" | "ME" | "OTHERS";

/**
 * Definition: EnterpriseTemplateResponse
 */
export interface EnterpriseTemplateResponse {
    /** Id of the template              */
    id: number;
    /** Name of the template. */
    name?: string;
    /** Response is "true" if enterprise template is marked as public. Otherwise "false" would be returned */
    public: boolean;
    /** Response of "true" if the template is read only, and user cannot make any changes to the workflow after applying this template.  Otherwise "false". */
    read_only: boolean;
    /** Description of the template */
    description?: string;
    /** Date and time when the template was created */
    created_on?: string;
    roles?: Array<EnterpriseTemplateRole>;
    documents?: Array<TemplateDocumentResponse>;
}

/**
 * Definition: EnterpriseTemplateRole
 */
export interface EnterpriseTemplateRole {
    /** Name of the enterprise role assigned to the user. */
    name?: string;
    /** Id of the enterprise role assigned to the user. */
    id: number;
}

/**
 * Definition: TemplateDocumentResponse
 */
export interface TemplateDocumentResponse {
    /** Id of the template document              */
    id: number;
    /** Name of the template document. */
    name?: string;
}

/**
 * Definition: TemplateSortBy
 */
export type TemplateSortBy = "CREATED_ON" | "NAME" | "DESCRIPTION" | "READ_ONLY" | "IS_PUBLIC";

/**
 * Definition: Next_signer_email
 */
export interface NextSignerEmail {
    /** Email address of the next signer. */
    user_email?: string;
    /** Name of the next signer. */
    user_name?: string;
}

/**
 * Definition: WorkflowRecipient
 */
export interface WorkflowRecipient {
    /** Order of the recipient in the workflow. */
    order: number;
    /** Email address of the recipient if user is not a group. */
    user_email?: string;
    /** Name of the recipient if user is not a group. */
    user_name?: string;
    /** Mobile number */
    mobile_number?: string;
    /** Delivery method of recipient Possible values: EMAIL, SMS, or EMAIL_AND_SMS */
    delivery_method?: string;
    /** API endpoint to access the resource, This will be provided only for main signers. e.g it will be null for group/delegator/gatekeeper etc.  This URL does not make sure that signer image is exist. If image exist it will be provided by hitting the defined url otherwise it will be an empty string. */
    user_photo_url?: string;
    /** Group name of the recipient if recipient is a group. */
    group_name?: string;
    /** A list of email addresses of the group members if recipient is a group. */
    group_members?: Array<GroupMemberResponse>;
    /** Delegatee email address, if recipient is not a group. */
    delegatee?: string;
    /** Name of the delegatee user, if recipient is not a group. */
    delegatee_name?: string;
    /** Workflow role in which the recipient was added. This role is not the enterprise role. Possible values are "SIGNER", "REVIEWER","CARBON_COPY" and "EDITOR". */
    role?: string;
    /** Reason provided by the user when declining, approving or submitting a document. */
    reason?: string;
    /** Document processing status for the recipient. Possible values are "UN_PROCESSED", "IN_PROGRESS", "SIGNED", "REVIEWED", "DECLINED", "EDITED" or "INVALID". */
    process_status?: string;
    /** Date and time on which the document was processed. */
    processed_on?: string;
    /** User may processed the field as COLLABORATOR , DELEGATOR, GROUP_MEMBER or OWNER. */
    processed_as?: string;
    /** Email address of the user who processed the document. */
    processed_by?: string;
    /** Placeholder text if the recipient is a placeholder */
    placeholder?: string;
    /** Order in which the workflow will be signed by the recipients. This signing order is important when workflow type is set to "CUSTOM". */
    signing_order: number;
    /** National identity number of the user. This helps to identify user in the workflow */
    user_national_id?: string;
    /** Gest user */
    guest_user: boolean;
    /** email language code */
    email_language_code?: string;
    electronic_seal?: ElectronicSealRecipient;
    gatekeepers?: WorkflowGatekeeperResponse;
}

/**
 * Definition: WorkflowGatekeeperResponse
 */
export interface WorkflowGatekeeperResponse {
    /** Getekeepers */
    emails?: Array<WorkflowGatekeeperUserResponse>;
}

/**
 * Definition: WorkflowGatekeeperUserResponse
 */
export interface WorkflowGatekeeperUserResponse {
    user_email?: string;
}

/**
 * Definition: EnterpriseAccessResponse
 */
export interface EnterpriseAccessResponse {
    authentication?: EnterpriseAuthenticationResponse;
    authentication_signing?: EnterpriseAuthenticationSigningResponse;
    access_duration?: PermissionAccessDurationResponse;
}

/**
 * Definition: EnterpriseAuthenticationResponse
 */
export interface EnterpriseAuthenticationResponse {
    /** True, if authentication is enabled for the package. */
    enabled: boolean;
    password?: AccessSecurityPasswordResponse;
    sms_otp?: EnterpriseAccessSecuritySmsOtpResponse;
    totp?: TotpPermissionResponse;
}

/**
 * Definition: EnterpriseAccessSecuritySmsOtpResponse
 */
export interface EnterpriseAccessSecuritySmsOtpResponse {
    /** True, if SMS OTP authentication is enabled for the package. */
    enabled: boolean;
    otp_length: number;
    retry_duration: number;
    /** Mobile number of the recipient. Mobile number is needed to send the SMS to the recipient for the authentication. */
    mobile_number?: string;
}

/**
 * Definition: EnterpriseAuthenticationSigningResponse
 */
export interface EnterpriseAuthenticationSigningResponse {
    /** True, if authentication is enabled for the package. */
    enabled: boolean;
    sms_otp?: EnterpriseAuthenticationSigningSmsOtpResponse;
    totp?: TotpPermissionResponse;
}

/**
 * Definition: EnterpriseAuthenticationSigningSmsOtpResponse
 */
export interface EnterpriseAuthenticationSigningSmsOtpResponse {
    otp_length: number;
    retry_duration: number;
    /** Mobile number of the recipient. Mobile number is needed to send the SMS to the recipient for the authentication. */
    mobile_number?: string;
}

/**
 * Definition: GetPackageDetailsResponse
 */
export interface GetPackageDetailsResponse {
    /** Name of the document package. */
    name?: string;
    owner?: DocumentOwner;
    /** True if the applied template is read only. User will not be able to make any changes if a read only template is applied. */
    read_only: boolean;
    /** Document status for the current logged in user. Possible values are "DRAFT", "INPROGRESS", "PENDING", "DECLINED", or "COMPLETED". */
    document_status?: string;
    documents?: Array<DocumentDetails>;
}

/**
 * Definition: DocumentOwner
 */
export interface DocumentOwner {
    /** Name of the document package owner. */
    name?: string;
    /** Email of the document package owner. */
    email?: string;
}

/**
 * Definition: DocumentDetails
 */
export interface DocumentDetails {
    /** ID of the newly created document. */
    id: number;
    /** The name of the document. */
    name?: string;
    /** Date and time when the document was uploaded. */
    uploaded_on?: string;
    /** Date and time when the document was last modified. */
    modified_on?: string;
    /** Size of the document in bytes. */
    size: number;
    /** Type of the document e.g. "PDF". */
    type?: string;
    /** Identifies where the document was uploaded e.g. "My App". */
    source?: string;
}

/**
 * Definition: ChangeServicePlanRequest
 */
export interface ChangeServicePlanRequest {
    /** Email address of the account owner. */
    user_email: string;
    /** New Service Plan name, which is to be assigned to the user. */
    service_plan: string;
}

/**
 * Definition: TemplateResponse
 */
export interface TemplateResponse {
    /** Id of the template is returned here              */
    template_id: number;
    /** Name of the template. */
    template_name?: string;
    /** Response is "true" if enterprise template is marked as public. Otherwise "false" would be returned */
    template_public: boolean;
    /** Response of "true" if the template is read only, and user cannot make any changes to the workflow after applying this template.  Otherwise "false". */
    read_only: boolean;
    /** Date and time when the template was created */
    created_on?: string;
    /** Description of the template */
    description?: string;
    /** True, if user belong to an enterprise. */
    enterprise: boolean;
    documents?: Array<TemplateDocumentResponse>;
    smart_form?: TemplateSmartFormResponse;
}

/**
 * Definition: TemplateSmartFormResponse
 */
export interface TemplateSmartFormResponse {
    /** Active count              */
    active_count: number;
    /** Total */
    total_count: number;
}

/**
 * Definition: ServicePlanLevelOfAssurance
 */
export interface ServicePlanLevelOfAssurance {
    /** Allowed level of assurances, Possible values are: ADVANCED_ELECTRONIC_SIGNATURE, HIGH_TRUST_ADVANCED, QUALIFIED_ELECTRONIC_SIGNATURE, ELECTRONIC_SEAL, ADVANCED_ELECTRONIC_SEAL, QUALIFIED_ELECTRONIC_SEAL, ELECTRONIC_SIGNATURE  */
    allowed?: Array<string>;
    /** Default level of assurances, Possible values are: ADVANCED_ELECTRONIC_SIGNATURE, HIGH_TRUST_ADVANCED, QUALIFIED_ELECTRONIC_SIGNATURE, ELECTRONIC_SEAL, ADVANCED_ELECTRONIC_SEAL, QUALIFIED_ELECTRONIC_SEAL, ELECTRONIC_SIGNATURE  */
    default?: Array<string>;
}

/**
 * Definition: ServicePlanCertifyOption
 */
export interface ServicePlanCertifyOption {
    /** Allowed document certify options, Possible values are: NO_CHANGES_ALLOWED, FORM_FILLING_ALLOWED, FORM_FILLING_WITH_ANNOTATIONS_ALLOWED  */
    allowed?: Array<string>;
    /** Default document certify options, Possible values are: NO_CHANGES_ALLOWED, FORM_FILLING_ALLOWED, FORM_FILLING_WITH_ANNOTATIONS_ALLOWED  */
    default?: string;
}

/**
 * Definition: ServicePlanAutoDeleteDocument
 */
export interface ServicePlanAutoDeleteDocument {
    /** True, If auto deletion of the unused documents is enabled. */
    enabled: boolean;
    /** Total number of unused days.              */
    duration: number;
    notify?: NotifyUnUsedDeleteDocument;
    email_document?: EmailDeletedDocument;
}

/**
 * Definition: NotifyUnUsedDeleteDocument
 */
export interface NotifyUnUsedDeleteDocument {
    /** True, If notify the user before days to deleting unused documents. */
    enabled: boolean;
    /** Total number of days before notifying the user.              */
    duration: number;
}

/**
 * Definition: EmailDeletedDocument
 */
export interface EmailDeletedDocument {
    /** True, If deleted document as attachment is enabled */
    enabled: boolean;
    /** True, Email the workflow evidence report as an attachment of the completed document is enabled */
    workflow_evidence_report: boolean;
}

/**
 * Definition: ServicePlanSigningServer
 */
export interface ServicePlanSigningServer {
    /** Key location of the signing server, possible values are SERVER, LOCAL */
    key_location?: string;
    /** Signing server order */
    order?: number;
    signing_profile?: ServicePlanProfile;
    electronic_seal?: SigningServerCapacity;
    advanced_electronic_seal?: SigningServerCapacity;
    qualified_electronic_seal?: SigningServerCapacity;
    advanced_electronic_signature?: SigningServerCapacity;
    high_trust_advanced_signature?: SigningServerCapacity;
    qualified_electronic_signature?: SigningServerCapacity;
    signature_appearances?: SigningServerSignatureAppearance;
    csp?: SigningServerCsp;
}

/**
 * Definition: ServicePlanProfile
 */
export interface ServicePlanProfile {
    /** ID of the profile */
    id: number;
    /** Name of the profile */
    name?: string;
}

/**
 * Definition: SigningServerCapacity
 */
export interface SigningServerCapacity {
    /** True, If to add the Signing Capacities. */
    enabled: boolean;
    /** Total number of Signing Capacities added are signing server. */
    capacities?: Array<ServicePlanCapacity>;
}

/**
 * Definition: ServicePlanCapacity
 */
export interface ServicePlanCapacity {
    /** Id of the Capacity */
    id: number;
    /** Name of the Capacity */
    name?: string;
    /** Level of assurance of the capacity, possible values are ADVANCED_ELECTRONIC_SIGNATURE, HIGH_TRUST_ADVANCED, QUALIFIED_ELECTRONIC_SIGNATURE, ELECTRONIC_SEAL, ADVANCED_ELECTRONIC_SEAL, QUALIFIED_ELECTRONIC_SEAL. */
    level_of_assurance?: string;
    /** Key protection option of the capacity, possible values are NO_PASSWORD, USER_PASSWORD, SYSTEM_PASSWORD, REMOTE_AUTHORISATION. */
    key_protection?: string;
}

/**
 * Definition: SigningServerSignatureAppearance
 */
export interface SigningServerSignatureAppearance {
    /** Appearances of the signature, possible values are COMPANY_LOGO, HAND_SIGNATURE, DETAILED_SIGNATURE */
    signature_appearance?: string;
    /** Logo of the signature appearance */
    signature_appearance_logo?: string;
}

/**
 * Definition: SigningServerCsp
 */
export interface SigningServerCsp {
    /** True, If to add Push Certificates to CSP. */
    enabled: boolean;
    virtual_profile?: ServicePlanProfile;
    /** True, If Automatically delete certificates and users from CSP when user is removed. */
    auto_delete_certificates: boolean;
}

/**
 * Definition: ServicePlanElectronicSealServer
 */
export interface ServicePlanElectronicSealServer {
    electronic_seal_profile?: ServicePlanProfile;
    electronic_seal?: SigningServerCapacity;
    advanced_electronic_seal?: SigningServerCapacity;
    qualified_electronic_seal?: SigningServerCapacity;
}

/**
 * Definition: ServicePlanAuthenticationProfile
 */
export interface ServicePlanAuthenticationProfile {
    /** Allowed authentication profile in service plan */
    allowed?: Array<ServicePlanProfile>;
    default?: ServicePlanProfile;
}

/**
 * Definition: ServicePlanEvidenceReport
 */
export interface ServicePlanEvidenceReport {
    /** Type of workflow evidence recording, Possible values are "BASIC", "DETAILED" */
    type?: string;
    /** True, If enable to delete document logs on generation of Workflow Evidence Report. */
    delete_document_logs: boolean;
}

/**
 * Definition: ServicePlanOneTimePassword
 */
export interface ServicePlanOneTimePassword {
    email_otp?: ServicePlanEmailOtp;
    sms_otp?: ServicePlanSmsOtp;
    /** Character count of OTP to be generated for authentication. */
    otp_length: number;
    /** OTP expiry duration in seconds */
    otp_expiry: number;
    /** OTP retry interval duration in seconds */
    otp_retry_interval: number;
}

/**
 * Definition: ServicePlanEmailOtp
 */
export interface ServicePlanEmailOtp {
    /** True, if Email OTP are enabled for the service plan. */
    enabled: boolean;
}

/**
 * Definition: ServicePlanSmsOtp
 */
export interface ServicePlanSmsOtp {
    /** True, if SMS OTP are enabled for the service plan. */
    enabled: boolean;
    /** SMS OTP server */
    server?: string;
}

/**
 * Definition: ServicePlanSmtpServer
 */
export interface ServicePlanSmtpServer {
    /** True, if SMTP Server is enabled for the service plan. */
    enabled: boolean;
    /** SMTP Server */
    Server?: string;
}

/**
 * Definition: ServicePlanSmsNotification
 */
export interface ServicePlanSmsNotification {
    /** True, if SMS notification gateway is enabled for the service plan. */
    enabled: boolean;
    /** SMS Gateway */
    Server?: string;
}

/**
 * Definition: ServicePlanAutoDeleteUser
 */
export interface ServicePlanAutoDeleteUser {
    /** True, If auto deletion of the inactive users is enabled. */
    enabled: boolean;
    /** Total number of inactive days.              */
    inactive_days: number;
    notify?: NotifyInactiveDeleteUser;
}

/**
 * Definition: NotifyInactiveDeleteUser
 */
export interface NotifyInactiveDeleteUser {
    /** True, If notify the user before days to deleting inactive users. */
    enabled: boolean;
    /** Total number of days before notifying the user.              */
    duration: number;
}

/**
 * Definition: ServicePlanCapacityRequest
 */
export interface ServicePlanCapacityRequest {
    /** Name of the Capacity */
    name?: string;
}

/**
 * Definition: StartWorkflowResponse
 */
export interface StartWorkflowResponse {
    /** Package ID of the package just shared. Incase of individual sharing, more then one packages are created and shared with the recipients. API returns an array of packages along with IDs of newly created documents. */
    package_id: number;
    /** Array of document IDs which were part of the package before sharing. */
    documents?: Array<number>;
}

/**
 * Definition: WorkflowDetailUpdateRequest
 */
export interface WorkflowDetailUpdateRequest {
    workflow_type?: WorkflowType;
    workflow_mode?: WorkflowModes;
    /** True, if workflow needs to continue even if any recipient declines the document. If no value is provided, old value will be retained. */
    continue_on_decline?: boolean;
    /** A custom string message from the document owner to every recipient, this message appears in the sharing email as well as on the screen. If no value is provided, old value will be retained. */
    message?: string;
    /** Allow comments */
    comments?: boolean;
}

/**
 * Definition: WorkflowModes
 */
export type WorkflowModes = "ME_AND_OTHERS" | "ONLY_OTHERS" | "ONLY_ME";

/**
 * Definition: PostProcessUpdateRequest
 */
export interface PostProcessUpdateRequest {
    /** True, if post processing is turned on for the package. */
    enabled: boolean;
    contacts?: Array<string>;
    /** List of the name and email addresses to whom the emails are sent when the post processing is executed after the workflow completion. */
    recipients?: Array<PostProcessRecipientRequest>;
    /** A custom string message for all the contacts. Message becomes part of the email sent to the contacts. */
    message?: string;
    /** True, if document is to be uploaded to the provided google account after the workflow completion. */
    google_drive?: boolean;
    /** True, if document is to be uploaded to the provided dropbox account after the workflow completion. */
    dropbox?: boolean;
    /** True, if document is to be uploaded to the provided oneDrive account after the workflow completion. */
    onedrive?: boolean;
    /** True, if workflow completion report is to be sent to all recipients of the workflow when post processing is executed. Default value is false. */
    workflow_recipients: boolean;
    /** True, if Send the document processing report (XML) is to be enabled to for the workflow  */
    document_processing_report: boolean;
}

/**
 * Definition: WorflowUserRequest
 */
export interface WorflowUserRequest {
    /** Email address of the user to be added in the workflow. */
    user_email?: string;
    /** Name of the recipient to be added in the workflow. */
    user_name: string;
    /** User national id of the recipient */
    user_national_id?: string;
    /** If set as true, SigningHub will send notifications to the user via email as per the document owner and user notification settings.  A value of false means no notifications will be sent to user throughout the workflow. */
    email_notification: boolean;
    role: CollaboratorRole;
    /** Order of the recipient in the workflow. This signing order is mandatory when workflow type is "CUSTOM". */
    signing_order?: number;
    delivery_method: DeliveryMethod;
    /** Mobile Number */
    mobile_number?: string;
}

/**
 * Definition: CollaboratorRole
 */
export type CollaboratorRole = "SIGNER" | "REVIEWER" | "EDITOR" | "INPERSON_HOST" | "CARBON_COPY" | "ELECTRONIC_SEAL";

/**
 * Definition: WorkflowDetailsResponse
 */
export interface WorkflowDetailsResponse {
    /** Package ID of the document package. */
    package_id: number;
    /** Name of the document package. */
    package_name?: string;
    /** shared package */
    shared_package: boolean;
    /** Email address of the document package owner. */
    package_owner?: string;
    /** Name of the document package owner. */
    owner_name?: string;
    /** Document status for the current logged in user. Possible values are "DRAFT", "INPROGRESS", "PENDING", "DECLINED", "APPROVED", "UPDATED" or "COMPLETED". */
    package_status?: string;
    /** Name of the folder which contains the document package. */
    folder?: string;
    /** ID of the folder which contains the document package. */
    folder_id: number;
    gatekeeper: boolean;
    /** The name of the next signer if the document is in shared state. */
    next_signer?: string;
    /** An object containing next signers username and email address. In case of group signer it will have a list of email addresses of all the group members. */
    next_signer_email?: Array<NextSignerEmail>;
    /** Date and time when the document was uploaded. */
    uploaded_on?: string;
    /** Date and time when the document was last modified. */
    modified_on?: string;
    workflow?: WorkflowResponse;
    /** List of documents that are uploaded in the package. */
    documents?: Array<WorkflowDocumentsResponse>;
    /** Recipients list for the workflow. */
    users?: Array<WorkflowUserResponse>;
}

/**
 * Definition: WorkflowResponse
 */
export interface WorkflowResponse {
    /** Type of the workflow, there are four types of workflows supported in the application. Possible values are "SERIAL", "PARALLEL", "INDIVIDUAL" and "CUSTOM". */
    workflow_type?: string;
    /** True, if workflow needs to be continued even after a recipient declines at his turn. */
    continue_on_decline: boolean;
    /** Processing status of the workflow, possible values are "DRAFT", "IN_PRGORESS" and "COMPLETED". */
    workflow_status?: string;
    /** Mode of the workflow, there are three types of workflow modes supported in application. Possible values are "ONLY_ME", "ME_AND_OTHERS" and "ONLY_OTHERS" */
    workflow_mode?: string;
    /** Any custom string message that is to be sent to all the recipient of the workflow by the document owner. */
    message?: string;
    /** If any template applied on the documents in this workflow is read only or locked template then the workflow is read only. SigningHub will not allow the end users to make any changes to this workflow in draft state and this workflow is to be shared as it is. */
    read_only: boolean;
    post_process?: PostProcessResponse;
    /** Comments */
    comments: boolean;
}

/**
 * Definition: WorkflowDocumentsResponse
 */
export interface WorkflowDocumentsResponse {
    /** ID of the document. */
    document_id: number;
    /** Name of the document. */
    document_name?: string;
    /** Original document extension when the document was uploaded before conversion to the PDF. */
    document_type?: string;
    /** Order of the document in the document package. */
    document_order: number;
    /** Source of the document from where the document was uploaded. */
    document_source?: string;
    /** True if document is required to update  */
    update_required: boolean;
    /** Height of the PDF document. */
    document_height: number;
    /** Width of the PDF document. */
    document_width: number;
    /** Total number of pages in the document. */
    document_pages: number;
    /** The date and time on which the document was uploaded. */
    created_on?: string;
    /** The date and time on which the document was last modified. */
    modified_on?: string;
    /** True, if document has form fields. */
    form_fields: boolean;
    template?: DocumentsTemplateResponse;
    certify?: CertifyResponse;
    /** True, if document is set to lock the form fields with the last digital signature on the document. */
    lock_form_fields: boolean;
    locked: boolean;
    /** Pdf document meta info: true if pdf document have signed signature fields */
    has_signed_signature_fields?: boolean;
    /** True, if document has attachments. */
    attachments: boolean;
    /** Document conformance level */
    conformance_level?: string;
    /** Size of document. */
    document_size: number;
    /** This property is for XML document, that either its formatted or not */
    formatted: boolean;
}

/**
 * Definition: DocumentsTemplateResponse
 */
export interface DocumentsTemplateResponse {
    /** ID of the template applied to the document. */
    template_id: number;
    /** Name of the template applied to the document. */
    template_name?: string;
    /** True, if template is configured as read only. No changes to the workflow should be made after a read only template is applied. */
    read_only: boolean;
}

/**
 * Definition: WorkflowUserResponse
 */
export interface WorkflowUserResponse {
    /** Order of the recipient in the workflow. */
    order: number;
    /** Name of the recipient if recipient is not a group. If recipient is a placeholder it will contain the name of the placeholder. */
    user_name?: string;
    /** Email address of the recipient if recipient is not a group or placeholder. */
    user_email?: string;
    /** Mobile number */
    mobile_number?: string;
    /** Delivery method of recipient Possible values: EMAIL, SMS, or EMAIL_AND_SMS */
    delivery_method?: string;
    /** API endpoint to access the resource, This will be provided only for main signers. e.g it will be null for group/delegator/gatekeeper etc.  This URL does not make sure that signer image is exist. If image exist it will be provided by hitting the defined url otherwise it will be an empty string. */
    user_photo_url?: string;
    /** Name of the group if recipient is a group. */
    group_name?: string;
    /** List of all the email addresses and names of the users. */
    group_members?: Array<string>;
    /** Email address of the user who is delegator to this recipient. */
    delegator?: string;
    /** Email address of the user who is gatekeeper to this recipient. */
    gatekeeper?: string;
    gatekeepers?: WorkflowGatekeeperResponse;
    /** Role of the recipient i.e., "SIGNER", "REVIEWER", "EDITOR","CARBON_COPY" or "INPERSON_HOST". */
    role?: string;
    /** Document processing status for the recipient. Possible values are "UN_PROCESSED", "IN_PROGRESS", "SIGNED", "REVIEWED", "DECLINED", "EDITED" or "INVALID". */
    process_status?: string;
    /** Date and time on which the document was processed. */
    processed_on?: string;
    /** User may processed the field as COLLABORATOR, DELEGATOR, GROUP_MEMBER or OWNER. */
    processed_as?: string;
    /** Email address of the user who processed the document. */
    processed_by?: string;
    /** Reason provided by the user when declining, approving or submitting a document. */
    reason?: string;
    /** Placeholder text if the recipient is a placeholder. */
    placeholder?: string;
    permission?: WorkflowPermissionResponse;
    authentications?: AccessResponse;
    reminder?: ReminderResponse;
    /** Order in which the workflow will be signed by the recipients. This signing order is important when workflow type is set to "CUSTOM". */
    signing_order: number;
    /** National identity number of the user. This helps to identify user in the workflow */
    user_national_id?: string;
    /** Gest user */
    guest_user: boolean;
    /** Email language code */
    email_language_code?: string;
    electronic_seal?: ElectronicSealRecipient;
}

/**
 * Definition: DocumentDetailsResponse
 */
export interface DocumentDetailsResponse {
    /** ID of the newly created document. */
    document_id: number;
    /** The name of the document. */
    document_name?: string;
    document_order: number;
    /** Type of the document e.g. "PDF". */
    document_type?: string;
    /** Width of the document in PDF format. */
    document_width: number;
    /** Height of the document in PDF format. */
    document_height: number;
    /** Identifies where the document was uploaded e.g. "My App". */
    document_source?: string;
    /** Total pages of the document. */
    document_pages: number;
    /** True if document has form fields. */
    form_fields: boolean;
    /** True if locking of form fields with the last signature is enabled. */
    lock_form_fields: boolean;
    /** The date and time when the document was uploaded. */
    uploaded_on?: string;
    /** The date and time when the document was last modified. */
    modified_on?: string;
    certify?: DocumentCertifyResponse;
    template?: DocumentTemplateResponse;
}

/**
 * Definition: DocumentCertifyResponse
 */
export interface DocumentCertifyResponse {
    /** True if certify signature is enabled for the document. */
    enabled: boolean;
    /** It is the certify permissions to be applied on the PDF document when the first signature is created. The possible values are "NO_CHANGES_ALLOWED", "FORM_FILLING_ALLOWED" and "FORM_FILLING_WITH_ANNOTATIONS_ALLOWED". */
    permission?: string;
}

/**
 * Definition: DocumentTemplateResponse
 */
export interface DocumentTemplateResponse {
    /** Name of the template applied on the document. */
    template_name?: string;
    /** True if the applied template is read only. User will not be able to make any changes if a read only template is applied. */
    read_only: boolean;
}

/**
 * Definition: ApplyTemplateRequest
 */
export interface ApplyTemplateRequest {
    /** Name of template to be applied on the document. */
    template_name: string;
    /** True, if template is to be applied on all the documents in the package. */
    apply_to_all: boolean;
}

/**
 * Definition: AddAttachmentResponse
 */
export interface AddAttachmentResponse {
    /** Attachment id */
    attachment_id: number;
}

/**
 * Definition: GetAttachmentResponse
 */
export interface GetAttachmentResponse {
    /** Attachment id */
    attachment_id: number;
    /** Attachment name */
    attachment_name?: string;
}

/**
 * Definition: AddPackageResponse
 */
export interface AddPackageResponse {
    /** The package ID to be used later to share, download and get status of the document. */
    package_id: number;
    /** The workflow mode that is set while adding package. */
    workflow_mode?: string;
    /** Type of the workflow. Possible values are "SERIAL", "PARALLEL", "INDIVIDUAL" and "CUSTOM". */
    workflow_type?: string;
}

/**
 * Definition: AddDocumentPackageRequest
 */
export interface AddDocumentPackageRequest {
    /** The name of the package. Default package name is always "Untitled" if the package_name is not provided. */
    package_name?: string;
    workflow_mode: WorkflowMode;
    /** The name of the folder. It will be used to upload package in any folder of the user, either it is a custom folder or a shared folder. */
    folder_name?: string;
}

/**
 * Definition: WorkflowMode
 */
export type WorkflowMode = "ME_AND_OTHERS" | "ONLY_OTHERS" | "ONLY_ME";

/**
 * Definition: ApproveRequest
 */
export interface ApproveRequest {
    /** The reason for approving a package. */
    reason?: string;
}

/**
 * Definition: DeclineRequest
 */
export interface DeclineRequest {
    /** Reason provided by the user for declining. */
    reason?: string;
}

/**
 * Definition: RenameDocumentPackageRequest
 */
export interface RenameDocumentPackageRequest {
    /** New name of the document package.              */
    package_name: string;
}

/**
 * Definition: GetPackagesResponse
 */
export interface GetPackagesResponse {
    /** Package ID of the document package. */
    package_id: number;
    /** Name of the document package. */
    package_name?: string;
    /** Email address of the document package owner. */
    package_owner?: string;
    /** Name of the document package owner. */
    owner_name?: string;
    /** Shared Package */
    shared_package: boolean;
    /** Gatekeeper Package */
    gatekeeper_package: boolean;
    /** Document status for the current logged in user. Possible values are "DRAFT", "INPROGRESS", "PENDING", "DECLINED", or "COMPLETED". */
    package_status?: string;
    /** Document Package extension */
    extension?: string;
    /** Workflow Mode */
    workflow_mode?: string;
    /** Name of the folder which contains the document package. */
    folder?: string;
    /** Id of the folder which contains the document package. */
    folder_id: number;
    /** True, if the document is not read by the recipient yet. */
    unread: boolean;
    /** The name of the next signer if the document is in shared state. */
    next_signer?: string;
    /** An object containing next signers username and email address. In case of group signer it will have a list of email addresses of all the group members. */
    next_signer_email?: Array<NextSignerEmail>;
    /** Date and time when the document was uploaded. */
    uploaded_on?: string;
    /** Date and time when the document was last modified. */
    modified_on?: string;
    access_duration?: RecipientAccessDurationDetails;
    decline?: RecipientDeclineDetails;
    /** Size of the document in bytes. */
    size: number;
}

/**
 * Definition: RecipientAccessDurationDetails
 */
export interface RecipientAccessDurationDetails {
    /** Start date/time for a recipient to access the document              */
    date_from?: string;
    /** End date/time for a recipient to access the document              */
    date_to?: string;
}

/**
 * Definition: RecipientDeclineDetails
 */
export interface RecipientDeclineDetails {
    /** Define reason of document decline */
    reason?: string;
    /** Indication of document is manually/auto declined  */
    auto_decline: boolean;
    declined_by?: DeclineByDetails;
}

/**
 * Definition: DeclineByDetails
 */
export interface DeclineByDetails {
    /** Name of user who have declined the workflow  */
    user_name?: string;
    /** Email of user who have declined the workflow               */
    user_email?: string;
}

/**
 * Definition: FolderItemSortBy
 */
export type FolderItemSortBy = "TITLE" | "WORKFLOW_TYPE" | "DATE_CREATED" | "LAST_MODIFIED" | "OWNER" | "LOCATION" | "STATUS";

/**
 * Definition: DocumentResponse
 */
export interface DocumentResponse {
    /** Document Package name */
    package_name?: string;
}

/**
 * Definition: UploadDocument
 */
export interface UploadDocument {
    /** This property is retained for backward compatibility and will be removed in later versions. Use document_id instead. */
    documentId: number;
    /** The document ID to be used later to share, download and get status of the document. will be removed later document_id will be used */
    documentid: number;
    /** The document ID to be used later to share, download and get status of the document. */
    document_id: number;
    /** The name of the document. */
    document_name?: string;
    /** Document order in package */
    document_order: number;
    /** Type of the document e.g. "PDF". */
    document_type?: string;
    /** Identifies where the document was uploaded e.g. "My App". */
    document_source?: string;
    /** Width of the document in PDF format. */
    document_width: number;
    /** Height of the document in PDF format. */
    document_height: number;
    /** Total pages of the document. */
    document_pages: number;
    /** The date and time when the document was uploaded. */
    uploaded_on: string;
    /** The date and time when the document was last modified. */
    modified_on: string;
    /** True if locking of form fields with the last signature is enabled. */
    lock_form_fields: boolean;
    certify?: CertifyPermissionResponse;
    /** Uploaded document size in bytes */
    document_size: number;
    metadata?: UploadDocumentMetaInfoDetail;
    /** The document package name */
    package_name?: string;
}

/**
 * Definition: CertifyPermissionResponse
 */
export interface CertifyPermissionResponse {
    /** True if certify signature is enabled for the document. */
    enabled: boolean;
    /** Set of allowed certify permission levels for the document. */
    allowed_permissions?: Array<string>;
    /** Selected permission level for the certify signature settings. */
    default_permission?: string;
}

/**
 * Definition: UploadDocumentMetaInfoDetail
 */
export interface UploadDocumentMetaInfoDetail {
    /** true if pdf document has any attachment */
    attachments: boolean;
    /** true if pdf document has any form field */
    form_fields: boolean;
    /** true if pdf document has any blank signature field */
    blank_signatures: boolean;
    /** true if pdf document has any signed signature field */
    signed_signatures: boolean;
    /** null/empty if document does not contains any conformance level otherwise contains formation like 1A, 1B etc */
    conformance?: string;
    /** true if pdf document is locked */
    locked: boolean;
    /** CertifyLevel of pdf document. Possible values NotCertified, NoChangesAllowed, FormFilling, FormFillingAndAnnotations */
    certify?: string;
    /** true if pdf document has any Qr Code, otherwise its false */
    qr_code: boolean;
}

/**
 * Definition: CertifyPolicyResponse
 */
export interface CertifyPolicyResponse {
    certify?: CertifyPermissionResponse;
    /** True if forms fields are to be locked in the PDF after the last digital signature on the current document. */
    lock_form_fields: boolean;
}

/**
 * Definition: RenameDocumentRequest
 */
export interface RenameDocumentRequest {
    /** New name of the document. */
    document_name: string;
}

/**
 * Definition: HandSignature
 */
export type HandSignature = "0" | "1" | "2" | "3" | "4";

/**
 * Definition: VerificationResponse
 */
export interface VerificationResponse {
    /** Name of the field for which the verification was requested. */
    field_name?: string;
    /** Name of the signer. */
    signer_name?: string;
    /** If the overall status of the signature is valid or not. Possible values are "VALID" or "INVALID". */
    signature_status?: string;
    /** Reason provided by the user while signing the field. In case of witness signing, custom signing reason would be set */
    signing_reason?: string;
    /** Location entered by the user while signing the field. */
    signing_location?: string;
    /** Contact information entered by the user while signing the field. */
    contact_information?: string;
    /** Date and time of signing in 8601 format. Default timezone from SH Admin is followed the the time. */
    signing_time?: string;
    /** True, if signature is LTV. */
    ltv: boolean;
    /** True, if signatures are qualified. */
    qualified: boolean;
    /** True, if signatures are certified. */
    certified: boolean;
    /** Permission level for certified digital signature. */
    certify_permission?: string;
    /** Date and time when signature was time-stamped. */
    timestamp_at?: string;
    /** Authority by which the signature was time-stamped. */
    timestamp_authority?: string;
    /** Subject DN from the certificate used while signing the field. */
    subject_dn?: string;
    /** Issuers DN from the certificate used while signing the field. */
    issuer_dn?: string;
    /** Date and time from which the certificate is valid. */
    cert_valid_from?: string;
    /** Date and time to which the certificate is valid. */
    cert_valid_to?: string;
    /** Type of signatures, possible values are "PADES_PART2" or "PADES_PART4". */
    signature_type?: string;
    /** Signature algorithm used while signing the field. */
    signature_algorithm?: string;
    /** Application name from where the field was signed. */
    signature_application?: string;
    /** Signature policy ID. */
    signature_policy_id: number;
    /** Signature policy URI. */
    signature_policy_uri?: string;
    /** Base64 image of the signer's avatar. */
    signer_photo?: string;
    signer_photo_url?: string;
    /** Hash of the document that was signed in respective signature. */
    signed_hash?: string;
    display?: string;
    /** Page number of the signature field */
    page_number: number;
    /** LEI number if availbale in certificate */
    lei_number?: string;
    /** LEI role if available in certificate */
    lei_role?: string;
    /** An error  message returns in case the application unable to get verification results. */
    error_message?: string;
}

/**
 * Definition: BulkSignResponse
 */
export interface BulkSignResponse {
    status?: string;
    transaction_id?: string;
}

/**
 * Definition: BulkSignRequest
 */
export interface BulkSignRequest {
    /** An array of the document package ids that are selected for bulk signing. */
    ids?: Array<number>;
    /** Base64 image used for the filling of the initials */
    hand_signature_initials_image?: string;
    hand_signature_initials_method?: HandSignature;
    /** Base64 encoded string image of the visible signature appearance */
    hand_signature_image?: string;
    hand_signature_method?: HandSignature;
    /** Reason of signing provided by the recipient. */
    signing_reason?: string;
    /** Locale of the signer provided by the recipient */
    signing_location?: string;
    /** Contact information of the signer provided by the recipient */
    contact_information?: string;
    /** Name of the signature appearance provided by user for signing. In case no appearance name is provided then default selected appearance would be used. Possible values are  "COMPANY_LOGO", "DETAILED_SIGNATURE", "HAND_SIGNATURE" */
    appearance_design?: string;
    /** Name of signing server using which the document is to be signed */
    signing_server?: string;
    /** Name of certification profile/signing capacity using which the document is to be signed */
    signing_capacity?: string;
    authentication?: BulkSignAuthentication;
    /** re-initiated signing process transaction id */
    transaction_id?: string;
}

/**
 * Definition: BulkSignAuthentication
 */
export interface BulkSignAuthentication {
    /** Mechanism of the authentication. Possible values for mechanism are: AUTHENTICATION_PASSWORD, AUTHENTICATION_ACTIVE_DIRECTORY, AUTHENTICATION_AZURE_ACTIVE_DIRECTORY, AUTHENTICATION_OFFICE_365, AUTHENTICATION_OAUTH2, AUTHENTICATION_OIDC */
    mechanism?: string;
    /** It is an optional parameter, but it's only required when using 'AUTHENTICATION_PASSWORD' as a parameter value under 'mechanism' */
    password?: string;
    /** It is an optional parameter that will be received from third party server, but it's only required when using  either  'AUTHENTICATION_AZURE_ACTIVE_DIRECTORY',  'AUTHENTICATION_OFFICE_365',  'AUTHENTICATION_OAUTH2'  'AUTHENTICATION_OIDC'  'AUTHENTICATION_GOOGLE'  'AUTHENTICATION_LINKED_IN'  'AUTHENTICATION_SALES_FORCE'  as a parameter value under 'mechanism' */
    token?: string;
    /** It is an optional parameter, but it's only required when using 'AUTHENTICATION_ACTIVE_DIRECTORY' or 'AUTHENTICATION_SAML' as a parameter value under 'mechanism' */
    user_identifier?: string;
    /** It is an optional parameter, but it's only required when using 'AUTHENTICATION_SALES_FORCE' or 'AUTHENTICATION_IDFY' as a parameter value under 'mechanism'. For 'AUTHENTICATION_IDFY' it's requestId */
    id?: string;
    /** Code Verifier if PKCE is enabled for the client. */
    code_verifier?: string;
    /** Nonce value if it is enabled for the client. */
    nonce?: string;
}

/**
 * Definition: ApiBulkSignAction
 */
export type ApiBulkSignAction = "SHARE" | "SIGN";

/**
 * Definition: BulkSignStatusResponse
 */
export interface BulkSignStatusResponse {
    status?: string;
    packages?: Array<BulkSignPackagesStatus>;
}

/**
 * Definition: BulkSignPackagesStatus
 */
export interface BulkSignPackagesStatus {
    id: number;
    name?: string;
    status?: string;
    /** Required action key if any */
    action?: string;
    error?: string;
    percentage: number;
    /** Documents in the current package */
    documents?: Array<BulkSignActionDocumentModel>;
}

/**
 * Definition: BulkSignActionDocumentModel
 */
export interface BulkSignActionDocumentModel {
    id: number;
    fields?: Array<BulkSignActionFieldModel>;
}

/**
 * Definition: BulkSignActionFieldModel
 */
export interface BulkSignActionFieldModel {
    name?: string;
    hash?: string;
    hashing_algo?: string;
}

/**
 * Definition: BulkSignStatusRequest
 */
export interface BulkSignStatusRequest {
    /** The identification number of the bulk signing transaction */
    transaction_id?: string;
}

/**
 * Definition: SignDocumentResponse
 */
export interface SignDocumentResponse {
    /** Name of the field for which the verification was requested. */
    field_name?: string;
    /** Returns the status of signature field. Values are SIGNED, PENDING. Status helps in eIDAS enabled remote authorized signing. If sign request is sent to mobile the status is pending. */
    status?: string;
    /** This parameter is used while RAS signing */
    transaction_id?: string;
    verification?: VerificationResponse;
    /** It will contain the OIDC id_token when user is authenticated via OIDC */
    authentication_access_token?: string;
}

/**
 * Definition: SignDocumentRequest
 */
export interface SignDocumentRequest {
    /** Unique identifier of the signature field in the document. */
    field_name: string;
    /** Base64 encoded string image of the visible signature appearance, which is placed onto the document. Note this can be retrieved from the user's personal settings using this [call](#operation/Settings_GetUserRoleSignatureAppearances).  (The response is binary, so the business application must then Base64 encode it before submitting in this API call.) */
    hand_signature_image?: string;
    hand_signature_method?: HandSignature;
    /** Reason of signing provided by the recipient. Note: Commitment type indication for XML documents */
    signing_reason?: string;
    /** Locale of the signer provided by the recipient. */
    signing_location?: string;
    /** Contact information of the signer provided by the recipient. */
    contact_information?: string;
    /** Name of the signer provided by the recipient.  Note this applies to in-person signing operations only. */
    user_name?: string;
    /** Password provided by the user subject to user's signature settings. */
    user_password?: string;
    /** Name of the signature appearance provided by user for signing. In case no appearance name is provided then default selected appearance would be used. Possible values are  "COMPANY_LOGO","DETAILED_SIGNATURE","HAND_SIGNATURE" */
    appearance_design?: string;
    /** Name of certification profile/signing capacity using which the document is to be signed. If not provided the default capacity will be used to sign. Provided name must be exactly same as of the actual profile due to case sensitivity. */
    signing_capacity?: string;
    /** No signature verification returns in response body when it is set as true. Default value for this parameter set to false. */
    skip_verification: boolean;
    /** Name of signing server using which the document is to be signed. */
    signing_server?: string;
    authentication?: Authentication;
}

/**
 * Definition: Authentication
 */
export interface Authentication {
    /** Mechanism of the authentication. Possible values for mechanism are: AUTHENTICATION_PASSWORD, AUTHENTICATION_ACTIVE_DIRECTORY, AUTHENTICATION_AZURE_ACTIVE_DIRECTORY, AUTHENTICATION_OFFICE_365, AUTHENTICATION_OAUTH2, AUTHENTICATION_OIDC */
    mechanism?: string;
    /** It is an optional parameter, but it's only required when using 'AUTHENTICATION_PASSWORD' as a parameter value under 'mechanism' */
    password?: string;
    /** It is an optional parameter that will be received from third party server, but it's only required when using  either  'AUTHENTICATION_AZURE_ACTIVE_DIRECTORY',  'AUTHENTICATION_OFFICE_365',  'AUTHENTICATION_OAUTH2'  'AUTHENTICATION_OIDC'  'AUTHENTICATION_GOOGLE'  'AUTHENTICATION_LINKED_IN'  'AUTHENTICATION_SALES_FORCE'  as a parameter value under 'mechanism' */
    token?: string;
    /** It is an optional parameter, but it's only required when using 'AUTHENTICATION_ACTIVE_DIRECTORY' or 'AUTHENTICATION_SAML' as a parameter value under 'mechanism' */
    user_identifier?: string;
    /** It is an optional parameter, but it's only required when using 'AUTHENTICATION_SALES_FORCE' or 'AUTHENTICATION_IDFY' as a parameter value under 'mechanism'. For 'AUTHENTICATION_IDFY' it's requestId */
    id?: string;
    /** Code Verifier if PKCE is enabled for the client. */
    code_verifier?: string;
    /** Nonce value if it is enabled for the client. */
    nonce?: string;
}

/**
 * Definition: AutoPlaceFieldsResponse
 */
export interface AutoPlaceFieldsResponse {
    /** Name of the field that was auto generated by the API. */
    field_name?: string;
}

/**
 * Definition: AutoPlaceFieldRequest
 */
export interface AutoPlaceFieldRequest {
    /** Word that needs to be searched in the document. */
    search_text?: string;
    /** Order of the user to which the fields will be assigned automatically.  Workflow in SigningHub orders recipients.  This list begins with “1” for the first designated signer. */
    order: number;
    /** If the text is found, fields are to be placed in the document. Placement of the field can be mentioned in this attribute. Possible values of placement of a field are LEFT, RIGHT, TOP, BOTTOM. If no value is provided the default value will be LEFT. */
    placement?: string;
    /** Type of field to be created in the document. Possible values are "SIGNATURE", "IN_PERSON_SIGNATURE", "INITIALS", "TEXT", "NUMBER" ,"NAME", "EMAIL", "COMPANY", "JOBTITLE", "RADIOBOX", "CHECKBOX", "DATE", "ATTACHMENT" */
    field_type?: string;
    /** Level Of Assurance to be updated. Possible Values are  "ELECTRONIC_SIGNATURE", "ELECTRONIC_SEAL", "ADVANCED_ELECTRONIC_SEAL", "QUALIFIED_ELECTRONIC_SEAL", "ADVANCED_ELECTRONIC_SIGNATURE", "HIGH_TRUST_ADVANCED", "QUALIFIED_ELECTRONIC_SIGNATURE" */
    level_of_assurance?: Array<string>;
    dimensions?: FieldDimension;
    /** Provide note in the case of Mandatory Attachment */
    placeholder?: string;
    /** The group name required only when adding a Radio Box type field to group multiple Radio boxes together. */
    radio_group_name?: string;
    /** <p>Text format of the field. Used for the date type field only. Possible values are</p><ul> <li>m/d</li> <li>m/d/yy</li> <li>m/d/yyyy</li> <li>mm/dd/yy</li> <li>mm/dd/yyyy</li> <li>mm/yy</li> <li>mm/yyyy</li> <li>d-mmm</li> <li>d-mmm-yy</li> <li>d-mmm-yyyy</li> <li>dd-mmm-yy</li> <li>dd-mmm-yyyy</li> <li>yy-mm-dd</li> <li>yyyy-mm-dd</li> <li>mmm-yy</li> <li>mmm-yyyy</li> <li>mmmm-yy</li> <li>mmmm-yyyy</li> <li>mmmm d, yyyy</li> <li>dd/mm/yy</li> <li>ddmmmyyyy</li></ul> */
    format?: string;
    /** Value that user want to show in the field. */
    value?: string;
    /** Maximum length of the value allowed in the field. Must between 1 - 9999 */
    max_length: number;
    /** One or more rules for validation of the fields possible values are "MANDATORY" or "OPTIONAL". */
    validation_rule?: string;
    validation?: FieldValidationValue;
    font?: Font;
    /** This belongs to Text Area field type and If set to true, text area field would be created with multi line option.              */
    multiline: boolean;
}

/**
 * Definition: Font
 */
export interface Font {
    /** Name of the font selected for the text field. Possible values are "COURIER", "HELVETICA". */
    name?: string;
    /** Size of the font selected for the text field. Possible values are 8, 10, 12, 14, 16, 18, 20 */
    size?: string;
    /** Size of the font in the original PDF document. */
    embedded_size?: string;
}

/**
 * Definition: FormFillingRequest
 */
export interface FormFillingRequest {
    /** Default value is false. True, if the form fields are being saved without user intervention e.g., while closing the document or pressing back. For Signing, Initials, In-persons, Reviewing, Submit actions the value is false. */
    auto_save: boolean;
    /** It has the list of textboxes data to save */
    text?: Array<TextFormFillingRequest>;
    /** It has the list of radios value to save */
    radio?: Array<RadioFormFillingRequest>;
    /** It has the list of checkboxes value to save */
    checkbox?: Array<CheckBoxFormFillingRequest>;
    /** It has the list of dropdowns value to save */
    dropdown?: Array<DropDownFormFillingRequest>;
    /** It has the list of listboxes value to save */
    listbox?: Array<ListBoxFormFillingRequest>;
}

/**
 * Definition: TextFormFillingRequest
 */
export interface TextFormFillingRequest {
    /** Unique identifier of the field in the document. */
    field_name: string;
    /** Value of the field to be saved in the document. */
    value?: string;
}

/**
 * Definition: RadioFormFillingRequest
 */
export interface RadioFormFillingRequest {
    /** Unique identifier of the field in the document. */
    field_name: string;
    /** Group name to which the radio button belongs. */
    radio_group_name?: string;
    /** Value of the field to be saved in the document. */
    value: boolean;
}

/**
 * Definition: CheckBoxFormFillingRequest
 */
export interface CheckBoxFormFillingRequest {
    /** Unique identifier of the field in the document. */
    field_name: string;
    /** Value of the field to be saved in the document. */
    value: boolean;
}

/**
 * Definition: DropDownFormFillingRequest
 */
export interface DropDownFormFillingRequest {
    /** Unique identifier of the field in the document. */
    field_name: string;
    /** Value of the field to be saved in the document. */
    value?: string;
}

/**
 * Definition: ListBoxFormFillingRequest
 */
export interface ListBoxFormFillingRequest {
    /** Unique identifier of the field in the document. */
    field_name: string;
    /** Value of the field to be saved in the document. */
    value?: string;
}

/**
 * Definition: InitialFieldRequest
 */
export interface InitialFieldRequest {
    /** The order of the user in workflow for which the field is being added. */
    order: number;
    /** Page number on which the field is to be created. */
    page_no: number;
    /** Name of the field that is to be added. If not provided, system will assign an unique auto generated name to the field. */
    field_name?: string;
    dimensions: FieldDimension;
}

/**
 * Definition: InitialFillingRequest
 */
export interface InitialFillingRequest {
    /** Unique identifier of the field in the document. */
    field_name: string;
    /** Base64 image used for the filling of the initials. */
    image: string;
    method?: HandSignature;
    /** True if all initials are to be filled. */
    apply_to_all: boolean;
}

/**
 * Definition: UpdateInitialFieldRequest
 */
export interface UpdateInitialFieldRequest {
    /** Current name of the field, that is to be updated. */
    field_name: string;
    /** Updated name of the field if renaming is intended. */
    renamed_as?: string;
    /** Page number on which the field is to be created. */
    page_no: number;
    dimensions: FieldDimension;
}

/**
 * Definition: InPersonFieldRequest
 */
export interface InPersonFieldRequest {
    /** The order of the user in workflow for which the field is being added. */
    order: number;
    /** Page number on which the field is to be created. */
    page_no: number;
    /** Name of the field that is to be added. If not provided, system will assign an unique auto generated name to the field. */
    field_name?: string;
    /** String identifier for the inperson field, it can be Customer, Jack, CEO etc. */
    placeholder: string;
    dimensions: FieldDimension;
    /** Visibility of the field that is to be added, possible values are "VISIBLE" and "INVISIBLE" */
    display?: string;
    /** Level Of Assurance to be updated. Possible Values are  "ELECTRONIC_SEAL", "ADVANCED_ELECTRONIC_SEAL", "QUALIFIED_ELECTRONIC_SEAL", "ELECTRONIC_SIGNATURE" */
    level_of_assurance?: Array<string>;
    authentication_signing?: SignatureFieldAuthentication;
}

/**
 * Definition: UpdateInPersonFieldRequest
 */
export interface UpdateInPersonFieldRequest {
    /** Current name of the field, that is to be updated. */
    field_name: string;
    /** updated name of the field, if renaming is intended. */
    renamed_as?: string;
    /** Page number on which the field is to be created. */
    page_no: number;
    /** String identifier for the in-person field, it can be Customer, Jack, CEO etc. */
    placeholder?: string;
    dimensions?: FieldDimension;
    /** Visibility of the field that is to be updated, possible values are "VISIBLE" and "INVISIBLE" */
    display?: string;
    /** Level Of Assurance to be updated. Possible Values are  "ELECTRONIC_SEAL", "ADVANCED_ELECTRONIC_SEAL", "QUALIFIED_ELECTRONIC_SEAL", "ELECTRONIC_SIGNATURE" */
    level_of_assurance?: Array<string>;
    authentication_signing?: SignatureFieldAuthentication;
}

/**
 * Definition: DigitalSignatureFieldRequest
 */
export interface DigitalSignatureFieldRequest {
    /** Order of the recipient for which the field is being created. */
    order: number;
    /** Page number at which the field is about to be created. */
    page_no: number;
    /** Name of the field that is to be added. If not provided, system will assign an unique auto generated name to the field. */
    field_name?: string;
    /** Level Of Assurance to be added. Possible Values are  "ELECTRONIC_SIGNATURE", "ELECTRONIC_SEAL", "ADVANCED_ELECTRONIC_SEAL", "QUALIFIED_ELECTRONIC_SEAL", "ADVANCED_ELECTRONIC_SIGNATURE", "HIGH_TRUST_ADVANCED", "QUALIFIED_ELECTRONIC_SIGNATURE" */
    level_of_assurance?: Array<string>;
    dimensions?: FieldDimension;
    /** Visibility of the field that is to be added, possible values are "VISIBLE" and "INVISIBLE" */
    display?: string;
    authentication_signing?: SignatureFieldAuthentication;
}

/**
 * Definition: UpdateDigitalSignatureFieldRequest
 */
export interface UpdateDigitalSignatureFieldRequest {
    /** Current name of the field, which is to be updated. */
    field_name: string;
    /** Level Of Assurance to be updated. Possible Values are  "ELECTRONIC_SIGNATURE", "ELECTRONIC_SEAL", "ADVANCED_ELECTRONIC_SEAL", "QUALIFIED_ELECTRONIC_SEAL", "ADVANCED_ELECTRONIC_SIGNATURE", "HIGH_TRUST_ADVANCED", "QUALIFIED_ELECTRONIC_SIGNATURE" */
    level_of_assurance?: Array<string>;
    /** Updated name of the field if renaming of the field is intended. */
    renamed_as?: string;
    /** Page number on which the field is to be created. */
    page_no: number;
    dimensions?: FieldDimension;
    /** Visibility of the field that is to be updated, possible values are "VISIBLE" and "INVISIBLE" */
    display?: string;
    authentication_signing?: SignatureFieldAuthentication;
}

/**
 * Definition: CheckBoxFieldRequest
 */
export interface CheckBoxFieldRequest {
    /** The order of the user in workflow for which the field is being added. */
    order: number;
    /** Page number on which the field is to be created. */
    page_no: number;
    /** Name of the provided field that is to be added. If not provided, system will assign an unique auto generated name to the field. */
    field_name?: string;
    /** Value of the field. Possible values are "true" or "false" */
    value?: string;
    dimensions: FieldDimension;
    /** One or more rules for validation of the fields possible values are "MANDATORY" or "OPTIONAL". */
    validation_rule?: string;
}

/**
 * Definition: UpdateCheckBoxFieldRequest
 */
export interface UpdateCheckBoxFieldRequest {
    /** Current name of the field, that is to be updated. */
    field_name: string;
    /** Updated name of the field if renaming is intended. */
    renamed_as?: string;
    /** Page number on which the field is to be created. */
    page_no: number;
    /** Value of the field. Possible values are "true" or "false" */
    value?: string;
    dimensions: FieldDimension;
    /** One or more rules for validation of the fields possible values are "MANDATORY" or "OPTIONAL". */
    validation_rule?: string;
}

/**
 * Definition: TextBoxFieldRequest
 */
export interface TextBoxFieldRequest {
    /** The order of the user in workflow for which the field is being added. */
    order: number;
    /** Page number on which the field is to be created. */
    page_no: number;
    type: TextboxType;
    /** Value of the field. For type = DATE, the value is expected to follow ISO 8601 format.Following the format YYYY-MM-DD hh:mm:ss +00. As the values are for date fields client applications can send YYYY-MM-DD and ignore hh:mm:ss +00. If value is not in proper format an error will be returned. */
    value?: string;
    /** Placeholder text for the text field. For name, email, company, job title and date, placeholder value can be "NAME", "EMAIL", "COMPANY", "JOBTITLE", "DATE". Developers can send their own placeholders to overwrite the default values. For "TEXT" developers can provide a their own placeholder texts. These placeholders appear in the text fields while viewing the document in viewer. */
    placeholder?: string;
    /** Maximum length of the value allowed in the field. Must between 1 - 9999 */
    max_length: number;
    /** <p>Text format of the field. Used for the date type field only. Possible values are</p><ul> <li>m/d</li> <li>m/d/yy</li> <li>m/d/yyyy</li> <li>mm/dd/yy</li> <li>mm/dd/yyyy</li> <li>mm/yy</li> <li>mm/yyyy</li> <li>d-mmm</li> <li>d-mmm-yy</li> <li>d-mmm-yyyy</li> <li>dd-mmm-yy</li> <li>dd-mmm-yyyy</li> <li>yy-mm-dd</li> <li>yyyy-mm-dd</li> <li>mmm-yy</li> <li>mmm-yyyy</li> <li>mmmm-yy</li> <li>mmmm-yyyy</li> <li>mmmm d, yyyy</li> <li>dd/mm/yy</li> <li>ddmmmyyyy</li></ul> */
    format?: string;
    /** Name of the field that is to be added. If not provided, system will assign an unique auto generated name to the field. */
    field_name?: string;
    field_type: FieldType;
    validation_rule: ValidationRule;
    validation?: FieldValidationValue;
    font: Font;
    dimensions: FieldDimension;
    /** If set to true, text area field would be created */
    multiline: boolean;
}

/**
 * Definition: UpdateTextBoxFieldRequest
 */
export interface UpdateTextBoxFieldRequest {
    /** Current name of the field, that is to be updated. */
    field_name: string;
    /** Updated name of the field if renaming is intended. */
    renamed_as?: string;
    /** Page number on which the field is to be created. */
    page_no: number;
    /** Value of the field. For type = DATE, the value is expected to follow ISO 8601 format.Following the format YYYY-MM-DD hh:mm:ss +00. As the values are for date fields client applications can send YYYY-MM-DD and ignore hh:mm:ss +00. If value is not in proper format an error will be returned. */
    value?: string;
    /** Maximum length of the value allowed in the field. Must between 1 - 9999 */
    max_length: number;
    field_type: FieldType;
    validation_rule: ValidationRule;
    font?: Font;
    dimensions?: FieldDimension;
    /** Developers can provide a their own placeholder texts. These placeholders appear in the text fields while viewing the document in viewer. */
    placeholder?: string;
    /** <p>Text format of the field. Used for the date type field only. Possible values are</p><ul> <li>m/d</li> <li>m/d/yy</li> <li>m/d/yyyy</li> <li>mm/dd/yy</li> <li>mm/dd/yyyy</li> <li>mm/yy</li> <li>mm/yyyy</li> <li>d-mmm</li> <li>d-mmm-yy</li> <li>d-mmm-yyyy</li> <li>dd-mmm-yy</li> <li>dd-mmm-yyyy</li> <li>yy-mm-dd</li> <li>yyyy-mm-dd</li> <li>mmm-yy</li> <li>mmm-yyyy</li> <li>mmmm-yy</li> <li>mmmm-yyyy</li> <li>mmmm d, yyyy</li> <li>dd/mm/yy</li> <li>ddmmmyyyy</li></ul> */
    format?: string;
    validation?: FieldValidationValue;
}

/**
 * Definition: RadioBoxFieldRequest
 */
export interface RadioBoxFieldRequest {
    /** The order of the user in workflow for which the field is being added. */
    order: number;
    /** Page number on which the field is to be created. */
    page_no: number;
    /** Name of the provided field that is to be added. If not provided, system will assign an unique auto generated name to the field. */
    field_name?: string;
    /** Value of the field. Possible values are "true" or "false" */
    value?: string;
    validation_rule: ValidationRule;
    radio_group_name: string;
    dimensions: FieldDimension;
}

/**
 * Definition: UpdateRadioBoxFieldRequest
 */
export interface UpdateRadioBoxFieldRequest {
    /** Current name of the field, that is to be updated. */
    field_name: string;
    /** Updated name of the field if renaming is intended. */
    renamed_as?: string;
    /** Page number on which the field is to be created. */
    page_no: number;
    /** Value of the field. Possible values are "true" or "false" */
    value?: string;
    validation_rule: ValidationRule;
    /** The group name to which the field belongs. */
    radio_group_name: string;
    dimensions: FieldDimension;
}

/**
 * Definition: ContactResponse
 */
export interface ContactResponse {
    /** id of the contact. */
    id: number;
    /** User name of the contact. */
    user_name?: string;
    /** Email address of the contact. */
    user_email?: string;
    /** Mobile number of the contact. */
    mobile_number?: string;
    /** True, if contact is enterprise. */
    enterprise: boolean;
    /** National identity number of the contact */
    user_national_id?: string;
    /** DateTime when contact is added. */
    created_on?: string;
}

/**
 * Definition: ContactSortBy
 */
export type ContactSortBy = "CREATEDON" | "NAME" | "EMAIL";

/**
 * Definition: WorkSpaceMembers
 */
export interface WorkSpaceMembers {
    /** Name of work space member */
    name: string;
    /** Email Address of work space member */
    email: string;
}

/**
 * Definition: WorkSpaceResponse
 */
export interface WorkSpaceResponse {
    /** Id of shared space */
    id: number;
    /** Name of shared space */
    name: string;
    owner: WorkSpaceOwner;
    /** Members of shared space */
    members: Array<WorkSpaceMembers>;
}

/**
 * Definition: WorkSpaceOwner
 */
export interface WorkSpaceOwner {
    /** Name of work space owner */
    name: string;
    /** Email Address of work space owner */
    email: string;
}

/**
 * Definition: UploadDocumentLibraryResponse
 */
export interface UploadDocumentLibraryResponse {
    /** ID of the newly created document. */
    document_id: number;
    /** The name of the document. */
    document_name?: string;
    /** Uploaded document size in bytes */
    document_size: number;
    /** Order of the document */
    document_order: number;
    /** Type of the document e.g. "PDF". */
    document_type?: string;
    /** Identifies where the document was uploaded e.g. "My App". */
    document_source?: string;
    /** Width of the document in PDF format. */
    document_width: number;
    /** Height of the document in PDF format. */
    document_height: number;
    /** Total pages of the document. */
    document_pages: number;
    /** The date and time when the document was uploaded. */
    uploaded_on: string;
    /** The date and time when the document was last modified. */
    modified_on: string;
    /** True if document has form fields. */
    form_fields: boolean;
    /** True if locking of form fields with the last signature is enabled. */
    lock_form_fields: boolean;
    certify?: UploadDocumentLibraryCertifyResponse;
    template?: UploadDocumentLibraryTemplateResponse;
    /** Package name */
    package_name?: string;
    metadata?: UploadDocumentMetaInfoDetail;
}

/**
 * Definition: UploadDocumentLibraryCertifyResponse
 */
export interface UploadDocumentLibraryCertifyResponse {
    /** True if certify signature is enabled for the document. */
    enabled: boolean;
    /** Set of allowed certify permission levels for the document. */
    allowed_permissions?: Array<string>;
    /** Selected permission level for the certify signature settings. */
    default_permission?: string;
    /** It is the certify permissions to be applied on the PDF document when the first signature is created. The possible values are "NO_CHANGES_ALLOWED", "FORM_FILLING_ALLOWED" and "FORM_FILLING_WITH_ANNOTATIONS_ALLOWED". */
    permission?: string;
}

/**
 * Definition: UploadDocumentLibraryTemplateResponse
 */
export interface UploadDocumentLibraryTemplateResponse {
    /** Name of the template applied on the document. */
    template_name?: string;
    /** True if the applied template is read only. User will not be able to make any changes if a read only template is applied. */
    read_only: boolean;
}

/**
 * Definition: WorkflowPermissionsUpdateRequest
 */
export interface WorkflowPermissionsUpdateRequest {
    /** True, if the permissions are to be applied on all the recipients in the workflow. */
    apply_to_all: boolean;
    permissions: WorkflowPermission;
}

/**
 * Definition: WorkflowPermission
 */
export interface WorkflowPermission {
    /** It defines whether recipient is allowed to print the document. */
    print?: boolean;
    /** It defines whether recipient is allowed to download the document. */
    download?: boolean;
    /** It defines whether recipient is allowed to add text fields in the document. */
    add_text?: boolean;
    /** It allows the user to change any recipient who has not signed yet. */
    change_recipients?: boolean;
    /** It defines whether recipient is allowed to add attachments in the PDF document. */
    add_attachment?: boolean;
    legal_notice?: PermissionLegalNotice;
    attachment?: AttachmentPermissionRequest;
}

/**
 * Definition: AttachmentPermissionRequest
 */
export interface AttachmentPermissionRequest {
    /** Enabled */
    enabled: boolean;
}

/**
 * Definition: PermissionLegalNotice
 */
export interface PermissionLegalNotice {
    /** True, if legal notice is enabled. */
    enabled: boolean;
    /** Name of the legal notice selected for  the recipient. */
    legal_notice_name?: string;
}

/**
 * Definition: WorkflowHistoryResponse
 */
export interface WorkflowHistoryResponse {
    /** ID of the package for which the workflow history is requested. */
    package_id: number;
    package_name?: string;
    /** Email address of the package owner. */
    package_owner?: string;
    /** Name of the package owner. */
    owner_name?: string;
    /** Status of the package/document with the current logged in user. Possible values are "DRAFT", "INPROGRESS", "PENDING", "DECLINED", "APPROVED", "UPDATED" or "COMPLETED". */
    package_status?: string;
    /** Name of the next recipient. */
    next_signer?: string;
    /** Object having name and email address of the next recipient. */
    next_signer_email?: Array<NextSignerEmail>;
    documents?: Array<WorkflowDocumentsResponse>;
    actions?: Array<DocumentLogsActions>;
}

/**
 * Definition: DocumentLogsActions
 */
export interface DocumentLogsActions {
    /** ID of an action performed on the document. */
    log_id: number;
    /** Date and time when the action was performed. */
    date_time?: string;
    action_type?: string;
    /** Email of the user who performed the action. */
    user_email?: string;
    /** Name of the user who performed the action. */
    user_name?: string;
    /** The name of Enterprise to which the user belongs. If the user doesn't belong to an enterprise, then this element is not present. */
    enterprise_name?: string;
    information?: DocumentLogsActionsInformation;
}

/**
 * Definition: DocumentLogsActionsInformation
 */
export interface DocumentLogsActionsInformation {
    /** The type of extra information about the action e.g. "SIGNING_METHOD" which means what signing method user used to sign the document. */
    type?: string;
    /** The value of the extra information about the action e.g. "LOCAL" which means user signed the document using locally held credentials. */
    value?: string;
}

/**
 * Definition: WorkflowUserUpdateRequest
 */
export interface WorkflowUserUpdateRequest {
    /** The email address of the new user to be updated in workflow. If no value is provided, old value will be retained. */
    user_email?: string;
    /** Name of the recipient to be updated. If no value is provided, old value will be retained. */
    user_name?: string;
    /** Setting its value to "true" sends an email notification to the user when its turn arrives in workflow. Setting its value to "false" does not send the email notification to the user on its turn. If no value is provided, old value will be retained. */
    email_notification?: boolean;
    /** Mobile number. If no value is provided, old value will be retained. */
    mobile_number?: string;
    role: WorkflowCollaboratorRole;
    /** Order in which the workflow will be signed by the recipients. This signing order is important when workflow type is set to "CUSTOM". */
    signing_order?: number;
    /** email language code */
    email_language_code?: string;
    delivery_method: DeliveryMethod;
}

/**
 * Definition: WorkflowCollaboratorRole
 */
export type WorkflowCollaboratorRole = "SIGNER" | "REVIEWER" | "EDITOR" | "INPERSON_HOST" | "CARBON_COPY";

/**
 * Definition: UpdateCollaboratorResponse
 */
export interface UpdateCollaboratorResponse {
    /** User email */
    user_email?: string;
    /** Signing order */
    signing_order: number;
    /** Is guest user */
    guest_user: boolean;
    /** Email language code */
    email_language_code?: string;
}

/**
 * Definition: WorkflowUserReorderRequest
 */
export interface WorkflowUserReorderRequest {
    /** The order of the recipient which is to be updated. */
    order: number;
}

/**
 * Definition: UpdateReminderRequest
 */
export interface UpdateReminderRequest {
    /** True, if reminder settings are to be applied on all the recipients in the workflow. */
    apply_to_all: boolean;
    /** True, if reminder settings are to be enabled. */
    enabled: boolean;
    /** Reminder Frequency. Possible Values are DAYS | HOURS.  */
    frequency?: string;
    /** Required, in case of enabled property will true. The number of days after which the first reminder would be sent to workflow user. */
    remind_after: number;
    repeat?: RepeatRequest;
}

/**
 * Definition: RepeatRequest
 */
export interface RepeatRequest {
    /** True, if subsequent reminders are enabled. */
    enabled: boolean;
    /** Repeat Reminder Frequency. Possible Values are DAYS | HOURS.  */
    frequency?: string;
    /** Required, in case of enabled property will true. The number of days after which the subsequent reminders would be sent to workflow user. */
    keep_reminding_after: number;
    /** Required, in case of enabled property will true. The number of subsequent reminders would be sent to workflow user. */
    total_reminders: number;
}

/**
 * Definition: WorflowGroupRequest
 */
export interface WorflowGroupRequest {
    /** The name of the new group to be added in workflow. */
    group_name?: string;
    /** Setting its value to "true" sends an email notification to the user when its turn arrives in workflow. Setting its value to "false" does not send the email notification to the user on its turn. If no value is provided, default value of "true" will be set. */
    email_notification: boolean;
    role: CollaboratorRole;
    /** Order in which the workflow will be signed by the recipients. This signing order is important when workflow type is set to "CUSTOM". */
    signing_order?: number;
}

/**
 * Definition: CollaboratorGroupResponse
 */
export interface CollaboratorGroupResponse {
    permission?: WorkflowPermissionResponse;
    reminder?: ReminderResponse;
    /** Signing order */
    signing_order: number;
}

/**
 * Definition: PlaceholderRequest
 */
export interface PlaceholderRequest {
    /** The name of the new placeholder to be added in workflow. */
    placeholder?: string;
    /** Setting its value to "true" sends an email notification to the user when its turn arrives in workflow. Setting its value to "false" does not send the email notification to the user on its turn. */
    email_notification: boolean;
    role: CollaboratorRole;
    /** Order in which the workflow will be signed by the recipients. This signing order is important when workflow type is set to "CUSTOM". */
    signing_order?: number;
}

/**
 * Definition: CollaboratorPlaceholderResponse
 */
export interface CollaboratorPlaceholderResponse {
    permission?: WorkflowPermissionResponse;
    reminder?: ReminderResponse;
    /** Signing order */
    signing_order: number;
}

/**
 * Definition: WorkflowPlaceholderUpdateRequest
 */
export interface WorkflowPlaceholderUpdateRequest {
    /** The name of the new placeholder to be added in workflow. If no value is provided, old value will be retained. */
    placeholder?: string;
    /** Setting its value to "true" sends an email notification to the user when its turn arrives in workflow. Setting its value to "false" does not send the email notification to the user on its turn. If no value is provided, old value will be retained. */
    email_notification?: boolean;
    role: WorkflowCollaboratorRole;
    /** Order in which the workflow will be signed by the recipients. This signing order is important when workflow type is set to "CUSTOM". */
    signing_order?: number;
}

/**
 * Definition: UpdateWorkSpaceRequest
 */
export interface UpdateWorkSpaceRequest {
    /** Name of shared space */
    name?: string;
    /** Members of shared space */
    members?: Array<WorkSpaceMembers>;
}

/**
 * Definition: MoveToRequest
 */
export interface MoveToRequest {
    /** Target folder name in which package need to move */
    folder_name: string;
}

/**
 * Definition: FieldsResponse
 */
export interface FieldsResponse {
    /** A list of all digital signatures in the document. */
    signature?: Array<DigitalSignatureFieldResponse>;
    hand_signature?: Array<HandSignatureFieldResponse>;
    electronic_signature?: Array<ElectronicSignatureFieldResponse>;
    initials?: Array<InitialFieldResponse>;
    in_person_signature?: Array<Record<string, unknown>>;
    text?: Array<TextFieldResponse>;
    radio?: Array<RadioFieldResponse>;
    checkbox?: Array<CheckboxFieldResponse>;
    dropdown?: Array<DropdownFieldResponse>;
    listbox?: Array<ListBoxFieldResponse>;
    qrcode?: Array<QrCodeResponse>;
    attachment?: Array<AttachmentFieldResponse>;
    comment?: Array<CommentFieldResponse>;
    unique_identifier?: Array<UniqueIdentifierFieldResponse>;
}

/**
 * Definition: DigitalSignatureFieldResponse
 */
export interface DigitalSignatureFieldResponse {
    /** Order of the recipient to whom this field is assigned. If the field is not assigned to anyone, value will be null. */
    order: number;
    /** Display order according to the SigningHub viewer. */
    display_order: number;
    /** Unique identifier of the field. */
    field_name?: string;
    /** Page number of the document on which the field exists. */
    page_no: number;
    /** True, if field is part of the PDF document. */
    embedded: boolean;
    /** If field is assigned or not, possible values are ASSIGNED, UN_ASSIGNED, SIGNED, LOGGED_IN_USER, HIDDEN and PLACEHOLDER. */
    type?: string;
    /** Whether the field is processed or not. Possible values are UN_PROCESSED, PROCESSED. */
    process_status?: string;
    /** Date time on which the document is processed. */
    processed_on?: string;
    /** User who processed the field as COLLABORATOR, DELEGATEE, GROUP_MEMBER or OWNER. */
    processed_as?: string;
    /** Date time on which the field is added */
    created_on?: string;
    /** Email address of the user who processed the field. */
    processed_by?: string;
    dimensions?: FieldDimenssionResponse;
    verification?: VerificationResponse;
    nid?: string;
    /** Returns the visibility value of the digital signature field, possible values are "VISIBLE" and "INVISIBLE". */
    display?: string;
    level_of_assurance?: Array<string>;
    authentication?: FieldAuthentication;
}

/**
 * Definition: FieldDimenssionResponse
 */
export interface FieldDimenssionResponse {
    field?: FieldResponse;
    page?: PageResponse;
}

/**
 * Definition: FieldResponse
 */
export interface FieldResponse {
    /** Left location of the field in pixel. */
    x: number;
    /** Top location of the field in pixel. */
    y: number;
    /** Width of the field in pixel. */
    width: number;
    /** Height of the field in pixel. */
    height: number;
}

/**
 * Definition: PageResponse
 */
export interface PageResponse {
    /** Width of the page where field is placed in the PDF. */
    width: number;
    /** Height of the page where field is placed in the PDF. */
    height: number;
}

/**
 * Definition: FieldAuthentication
 */
export interface FieldAuthentication {
    /** True,  if authentications are enabled for the electronic signature fields. */
    enabled: boolean;
    sms_otp?: AccessSecuritySmsOtpResponse;
    totp?: AccessSecurityTOTPResponse;
}

/**
 * Definition: AccessSecurityTOTPResponse
 */
export interface AccessSecurityTOTPResponse {
    /** True, if TOTP authentication is enabled for the package. */
    enabled: boolean;
}

/**
 * Definition: HandSignatureFieldResponse
 */
export interface HandSignatureFieldResponse {
    /** Order of the recipient to whom this field is assigned. If the field is not assigned to anyone, value will be null. */
    order: number;
    /** Display order according to the SigningHub viewer. */
    display_order: number;
    /** Unique identifier of the field. */
    field_name?: string;
    /** Page number of the document on which the field exists. */
    page_no: number;
    /** True, if field is part of the PDF document. */
    embedded: boolean;
    /** If field is assigned or not, possible values are ASSIGNED, UN_ASSIGNED, SIGNED, LOGGED_IN_USER, HIDDEN and PLACEHOLDER. */
    type?: string;
    /** Whether the field is processed or not. Possible values are UN_PROCESSED, PROCESSED. */
    process_status?: string;
    /** Date time on which the document is processed. */
    processed_on?: string;
    /** User who processed the field as COLLABORATOR,DELEGATEE,GROUP_MEMBER or OWNER. */
    processed_as?: string;
    /** Email address of the user who processed the field. */
    processed_by?: string;
    dimensions?: FieldDimenssionResponse;
    nid?: string;
}

/**
 * Definition: ElectronicSignatureFieldResponse
 */
export interface ElectronicSignatureFieldResponse {
    /** Email address of the user who processed the field. */
    order: number;
    /** Display order according to the SigningHub viewer. */
    display_order: number;
    /** Unique identifier of the field. */
    field_name?: string;
    /** Page number of the document on which the field exists. */
    page_no: number;
    /** True, if field is part of the PDF document. */
    embedded: boolean;
    /** If field is assigned or not, possible values are ASSIGNED, UN_ASSIGNED, SIGNED, LOGGED_IN_USER, HIDDEN and PLACEHOLDER. */
    type?: string;
    /** Whether the field is processed or not. Possible values are UN_PROCESSED, PROCESSED. */
    process_status?: string;
    /** Date time on which the document is processed. */
    processed_on?: string;
    /** User who processed the field as COLLABORATOR,DELEGATEE,GROUP_MEMBER or OWNER. */
    processed_as?: string;
    /** Email address of the user who processed the field. */
    processed_by?: string;
    dimensions?: FieldDimenssionResponse;
    /** The signature sub-type can be a WITNESS signature. If witness signatures are enabled in service plan, E-Signature fields are signed with a common certificate shared by all the users in the service plan. Possible values are WITNESS or NONE. */
    signature_sub_type?: string;
    authentication?: FieldAuthentication;
    verification?: VerificationResponse;
    nid?: string;
    /** Returns the visibility value of the electronic signature field, possible values are "VISIBLE" and "INVISIBLE". */
    display?: string;
}

/**
 * Definition: InitialFieldResponse
 */
export interface InitialFieldResponse {
    /** Order of the recipient to whom this field is assigned. If the field is not assigned to anyone, value will be null. */
    order: number;
    /** Display order according to the SigningHub viewer. */
    display_order: number;
    /** Unique identifier of the field. */
    field_name?: string;
    /** Page number of the document on which the field exists. */
    page_no: number;
    /** Whether the field is processed or not. Possible values are UN_PROCESSED, PROCESSED. */
    process_status?: string;
    /** Date time on which the document is processed. */
    processed_on?: string;
    /** User who processed the field as COLLABORATOR,DELEGATEE,GROUP_MEMBER or OWNER. */
    processed_as?: string;
    /** Email address of the user who processed the field. */
    processed_by?: string;
    dimensions?: FieldDimenssionResponse;
    /** True, if field is part of the PDF document. */
    embedded: boolean;
}

/**
 * Definition: InPersonFieldResponse
 */
export interface InPersonFieldResponse {
    /** Order of the recipient to whom this field is assigned. If the field is not assigned to anyone, value will be null. */
    order: number;
    /** Display order according to the SigningHub viewer. */
    display_order: number;
    /** Unique identifier of the field. */
    field_name?: string;
    /** Page number of the document on which the field exists. */
    page_no: number;
    /** True, if field is part of the PDF document. */
    embedded: boolean;
    type?: string;
    /** Whether the field is processed or not. Possible values are UN_PROCESSED, PROCESSED. */
    process_status?: string;
    /** Date time on which the document is processed. */
    processed_on?: string;
    /** User who processed the field as COLLABORATOR,DELEGATEE,GROUP_MEMBER or OWNER. */
    processed_as?: string;
    /** Email address of the user who processed the field. */
    processed_by?: string;
    /** Name of the in-person signer or placeholder e.g., Customer, Sales Representative etc. */
    placeholder?: string;
    /** The signature sub-type for in-person signatures can be a WITNESS signature. If witness signatures are enabled in service plan, In-person fields are signed with a common certificate shared by all the users in the service plan. Possible values are WITNESS or NONE. */
    signature_sub_type?: string;
    dimensions?: FieldDimenssionResponse;
    verification?: VerificationResponse;
    /** Returns the visibility value of the inperson field, possible values are "VISIBLE" and "INVISIBLE". */
    display?: string;
    /** In Person selected level of assurances */
    level_of_assurance?: Array<string>;
    authentication?: FieldAuthentication;
}

/**
 * Definition: TextFieldResponse
 */
export interface TextFieldResponse {
    /** True if field required in pdf */
    required: boolean;
    /** Order of the recipient to whom this field is assigned. If the field is not assigned to anyone, value will be null. */
    order: number;
    /** Tab order in the PDF document */
    tab_order: number;
    /** Display order according to the SigningHub viewer. */
    display_order: number;
    /** Unique identifier of the field. */
    field_name?: string;
    /** Page number of the document on which the field exists. */
    page_no: number;
    /** True, if field is part of the PDF document. */
    embedded: boolean;
    /** Type of the text field, possible values are "TEXT", "NAME", "COMPANY", "JOBTITLE", "EMAIL", "DATE". */
    type?: string;
    /** Value of the field. For type = DATE or format_type = DATE, the value is expected to follow ISO 8601 format.Following the format YYYY-MM-DD hh:mm:ss +00. As the values are for date fields client applications can send YYYY-MM-DD and ignore hh:mm:ss +00. If value is not in proper format an error will be returned. */
    value?: string;
    /** True, if the field is marked read only in the PDF document. */
    read_only: boolean;
    /** One or more rules for validation of the fields, possible values are "MANDATORY" or "OPTIONAL". */
    validation_rule?: string;
    validation?: FieldValidationValue;
    /** True, if the field is not hidden in the PDF document. */
    visible: boolean;
    /** True, if the field is multi line in the PDF document. */
    multiline: boolean;
    font?: FontResponse;
    /** Maximum content length allowed to be filled in the field. */
    max_length: number;
    /** If field is a textbox it can follow different input types. Possible values are ALPHANUMERIC, NUMBER, TEXT, EMAIL, TIME or DATE. */
    format_type?: string;
    /** Placeholder text for the textbox field. */
    placeholder?: string;
    /** <p>Text format of the field. Used for the date type field only. Possible values are</p><ul> <li>m/d</li> <li>m/d/yy</li> <li>m/d/yyyy</li> <li>mm/dd/yy</li> <li>mm/dd/yyyy</li> <li>mm/yy</li> <li>mm/yyyy</li> <li>d-mmm</li> <li>d-mmm-yy</li> <li>d-mmm-yyyy</li> <li>dd-mmm-yy</li> <li>dd-mmm-yyyy</li> <li>yy-mm-dd</li> <li>yyyy-mm-dd</li> <li>mmm-yy</li> <li>mmm-yyyy</li> <li>mmmm-yy</li> <li>mmmm-yyyy</li> <li>mmmm d, yyyy</li> <li>dd/mm/yy</li> <li>ddmmmyyyy</li></ul> */
    format?: string;
    /** Color of the text in the form fields. */
    color?: string;
    /** Border color of the form field. */
    border_color?: string;
    /** tooltip of the form field. */
    tooltip?: string;
    /** Field Rotation */
    field_rotation: number;
    /** Text alignment for the form field. Possible values are "RIGHT", "LEFT", "CENTER" or "JUSTIFY" */
    align?: string;
    dimensions?: FieldDimenssionResponse;
}

/**
 * Definition: RadioFieldResponse
 */
export interface RadioFieldResponse {
    /** True if field required in pdf */
    required: boolean;
    /** Order of the recipient to whom this field is assigned. If the field is not assigned to anyone, value will be null. */
    order: number;
    /** Tab order in the PDF document. */
    tab_order: number;
    /** Display order according to the SigningHub viewer. */
    display_order: number;
    /** Unique identifier of the field. */
    field_name?: string;
    /** Page number of the document on which the field exists. */
    page_no: number;
    /** True, if field is part of the PDF document. */
    embedded: boolean;
    type?: string;
    /** Value of the field. */
    value: boolean;
    /** True, if the field is marked readonly in the PDF document. */
    read_only: boolean;
    /** One or more rules for validation of the fields possible values are "MANDATORY" or "OPTIONAL". */
    validation_rule?: string;
    /** The group name to which the field belongs. */
    radio_group_name?: string;
    /** True, if the field is selected */
    checked: boolean;
    /** True if the field is not hidden in the PDF document. */
    visible: boolean;
    dimensions?: FieldDimenssionResponse;
    /** Color of the text in the form fields. */
    color?: string;
    /** Border color of the form field. */
    border_color?: string;
    /** tooltip of the form field. */
    tooltip?: string;
    /** Field Rotation */
    field_rotation: number;
}

/**
 * Definition: CheckboxFieldResponse
 */
export interface CheckboxFieldResponse {
    /** True if field required in pdf */
    required: boolean;
    /** Order of the recipient to whom this field is assigned. If the field is not assigned to anyone, value will be null. */
    order: number;
    /** Tab order in the PDF document */
    tab_order: number;
    /** Display order according to the SigningHub viewer. */
    display_order: number;
    /** Unique identifier of the field. */
    field_name?: string;
    /** Page number of the document on which the field exists. */
    page_no: number;
    /** True, if field is part of the PDF document. */
    embedded: boolean;
    type?: string;
    /** Value of the field. */
    value: boolean;
    /** True, if the field is marked read only in the PDF document. */
    read_only: boolean;
    /** True if the field is selected. */
    checked: boolean;
    /** True if the field is not hidden in the PDF document. */
    visible: boolean;
    /** One or more rules for validation of the fields possible values are "MANDATORY" or "OPTIONAL". */
    validation_rule?: string;
    dimensions?: FieldDimenssionResponse;
    /** Color of the text in the form fields. */
    color?: string;
    /** Border color of the form field. */
    border_color?: string;
    /** tooltip of the form field. */
    tooltip?: string;
    /** Field Rotation */
    field_rotation: number;
}

/**
 * Definition: DropdownFieldResponse
 */
export interface DropdownFieldResponse {
    /** True if field required in pdf */
    required: boolean;
    /** Order of the recipient to whom this field is assigned. If the field is not assigned to anyone, value will be null. */
    order: number;
    /** Tab order in the PDF document. */
    tab_order: number;
    /** Display order according to the SigningHub viewer. */
    display_order: number;
    /** Unique identifier of the field. */
    field_name?: string;
    /** Page number of the document on which the field exists. */
    page_no: number;
    /** True, if field is part of the PDF document. */
    embedded: boolean;
    /** One or more rules for validation of the fields possible values are "MANDATORY" or "OPTIONAL". */
    validation_rule?: string;
    /** True, if the field is marked read only in the PDF document. */
    read_only: boolean;
    /** Value of the field . */
    value?: string;
    /** Choices available for the dropdown to choose a value from. */
    choices?: Array<string>;
    /** True, if the field is not hidden in the PDF document. */
    visible: boolean;
    dimensions?: FieldDimenssionResponse;
    font?: FontResponse;
    /** Color of the text in the form fields. */
    color?: string;
    /** Border color of the form field. */
    border_color?: string;
    /** tooltip of the form field. */
    tooltip?: string;
    /** Field Rotation */
    field_rotation: number;
}

/**
 * Definition: ListBoxFieldResponse
 */
export interface ListBoxFieldResponse {
    /** Order of the recipient to whom this field is assigned. If the field is not assigned to anyone, value will be null. */
    order: number;
    /** Tab order in the PDF document. */
    tab_order: number;
    /** Display order according to the SigningHub viewer. */
    display_order: number;
    /** Unique identifier of the field. */
    field_name?: string;
    /** Page number of the document on which the field exists. */
    page_no: number;
    /** True, if field is part of the PDF document. */
    embedded: boolean;
    /** One or more rules for validation of the fields possible values are "MANDATORY" or "OPTIONAL". */
    validation_rule?: string;
    /** True, if the field is marked read only in the PDF document. */
    read_only: boolean;
    /** Value of the field. */
    value?: string;
    /** Choices available for the dropdown to choose a value from. */
    choices?: Array<string>;
    /** True, if the field is not hidden in the PDF document. */
    visible: boolean;
    dimensions?: FieldDimenssionResponse;
    font?: FontResponse;
    /** Color of the text in the form fields. */
    color?: string;
    /** Border color of the form field. */
    border_color?: string;
    /** tooltip of the form field. */
    tooltip?: string;
    /** Field Rotation */
    field_rotation: number;
}

/**
 * Definition: FontResponse
 */
export interface FontResponse {
    /** Font name for the text field. For user defined fields SigningHub only support "COURIER", "HELVETICA". In case of existing fields in a PDF file, font value can be anything that is set in the PDF. */
    name?: string;
    /** Font size for the text field. */
    size: number;
    /** Size of the font in the original PDF document. */
    embedded_size: number;
}

/**
 * Definition: QrCodeResponse
 */
export interface QrCodeResponse {
    /** Unique identifier of the field. */
    field_name?: string;
    /** Field Type PDF_FIELD_TYPE like QR */
    type?: string;
    /** Page number of the document on which the field exists. */
    page_no: number;
    /** QrCode generated base64 will be provided inly in draft mode */
    value?: string;
    dimensions?: FieldDimenssionResponse;
    /** The plain text data to be encoded in the QR code. Default is URL generated to access the related document. e.g. {base_url}/document/{document_unique_id} */
    data?: string;
    options?: QrCodeOptions;
    color?: QrCodeColor;
    logo?: QrCodeLogo;
}

/**
 * Definition: AttachmentFieldResponse
 */
export interface AttachmentFieldResponse {
    /** Order of the recipient to whom this field is assigned. */
    order: number;
    /** Display order according to the SigningHub viewer. */
    display_order: number;
    /** Unique identifier of the field. */
    field_name?: string;
    /** Page number of the document on which the field exists. */
    page_no: number;
    /** Whether the field is processed or not. Possible values are UN_PROCESSED, PROCESSED. */
    process_status?: string;
    /** Date time on which the document is processed. */
    processed_on?: string;
    /** User who processed the field as COLLABORATOR,DELEGATEE,GROUP_MEMBER or OWNER. */
    processed_as?: string;
    /** Email address of the user who processed the field. */
    processed_by?: string;
    /** Attachmet ref id */
    attachmentRefId?: number;
    /** Attachmet note Only in case of "MANDATORY" attachment rule */
    note?: string;
    validation_rule: ValidationRule;
    dimensions?: FieldDimenssionResponse;
}

/**
 * Definition: CommentFieldResponse
 */
export interface CommentFieldResponse {
    /** Comment field id */
    id: number;
    /** Page number of the document on which the field exists. */
    page_no: number;
    /** Unread comments */
    unread_count: number;
    /** Private */
    private: boolean;
    /** Recipients */
    recipients?: Array<CommentFieldRecipientResponse>;
    dimensions?: FieldDimenssionResponse;
}

/**
 * Definition: UniqueIdentifierFieldResponse
 */
export interface UniqueIdentifierFieldResponse {
    /** Unique identifier of the field. */
    field_name?: string;
    /** Value of the field. */
    value?: string;
    /** Page number of the document on which the field exists. */
    page_no: number;
    font?: FontResponse;
    dimensions?: FieldDimenssionResponse;
}

/**
 * Definition: CommentFieldRecipientResponse
 */
export interface CommentFieldRecipientResponse {
    /** User name */
    user_name?: string;
    /** User email */
    user_email?: string;
}

/**
 * Definition: AssignFieldRequest
 */
export interface AssignFieldRequest {
    /** Name of the signature field that is to be assigned. */
    field_name: string;
    /** provide group name for radio box */
    radio_group_name?: string;
    /** The order of the user in workflow, to which the field is being assigned. */
    order: number;
    /** Level Of Assurance to be added. Possible Values are  "ELECTRONIC_SIGNATURE", "ELECTRONIC_SEAL", "ADVANCED_ELECTRONIC_SEAL", "QUALIFIED_ELECTRONIC_SEAL", "ADVANCED_ELECTRONIC_SIGNATURE", "HIGH_TRUST_ADVANCED", "QUALIFIED_ELECTRONIC_SIGNATURE" */
    level_of_assurance?: Array<string>;
}
// #endregion Types

// #region Client

/**
 * Typed client for the signinghub connector.
 */
export class SigninghubClient extends ConnectorClientBase {

    /**
     * Creates a new SigninghubClient.
     * @param connectionRuntimeUrl The connection runtime URL from Azure Portal.
     * @param credential The credential used for authentication.
     * @param options Optional connector client options.
     */
    constructor(connectionRuntimeUrl: string, credential: TokenCredential, options?: ConnectorClientOptions) {
        super(connectionRuntimeUrl, credential, options);
    }

    public get connectorName(): string {
        return "signinghub";
    }

    /**
     * Delete Attachment
     * @remarks Business applications can use this service API to delete the attachment of a document.
     */
    public async attachmentDeleteAttachmentAsync(packageId: string, documentId: string, attachmentId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/attachments/${attachmentId}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("DELETE", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `DELETE ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Download Attachment
     * @remarks Business applications can use this service API to download the attachment of a document.
     */
    public async attachmentDownloadAttachmentAsync(packageId: string, documentId: string, attachmentId: string, abortSignal?: AbortSignalLike): Promise<AttachmentDownloadAttachmentResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/attachments/${attachmentId}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<AttachmentDownloadAttachmentResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as AttachmentDownloadAttachmentResponse;
    }

    /**
     * Get Attachments
     * @remarks Business applications can use this service API to get the attachments of a document.
     */
    public async attachmentGetAttachmentsAsync(packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<Array<GetAttachmentResponse>> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/attachments`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<GetAttachmentResponse>>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<GetAttachmentResponse>;
    }

    /**
     * Upload Attachment
     * @remarks Business applications can use this service API to upload a attachment in a document.
     */
    public async attachmentUploadAttachmentAsync(input: AttachmentUploadAttachmentInput, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<AddAttachmentResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/attachments`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<AddAttachmentResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as AddAttachmentResponse;
    }

    /**
     * Add CheckBox Field
     * @remarks Business applications can use this service API to add a checkbox to a document in a package.               At least one user must exist in a workflow before fields(input and signature) can be added to the document in this way.The order number corresponds to the recipient in the workflow.Therefore, this number must be equal or less than the total number of users in the workflow.  The below example assigns this new signature field to the first person in the workflow.               See [Add Users to Workflow](#operation/Workflow_WorkflowAddUser) for more information.  Note if you have a document with a template applied, or have applied one using the APIs, then the workflow will already contain users.               If width and height parameters are not provided, a default size of 10x10 will be assigned. If either width or height (or both) is provided, the system will compare these provided values with each other. The larger value between width and height will be assigned to both dimensions.
     */
    public async checkboxAddCheckBoxAsync(input: CheckBoxFieldRequest, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<AddFieldResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/fields/checkbox`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<AddFieldResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as AddFieldResponse;
    }

    /**
     * Update CheckBox Field
     * @remarks Business applications can use this service API to update a checkbox field of a document in a package.               Note all of the input parameters can be changed.Where you wish to retain the current setting submit it in the call unchanged.You can therefore use the same parameters as when you added the input field, or the information as returned by [Get Document Fields](#operation/Documents_GetDocumentFields).The latter can be used when a template was applied or the document was created using the GUI interface.               It's width and height cannot be updated. They will remain the same as when they were added.
     */
    public async checkboxUpdateCheckBoxAsync(input: UpdateCheckBoxFieldRequest, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/fields/checkbox`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Get Contacts
     * @remarks Business applications can use this service API to retieve contacts from both personal and enterprise contacts for the current user.               It is possible to search based upon the criterion supplied under the "x-search-text" header.Furthermore, it is possible to search a user's specific contacts or that of the enterprise to which the user belongs.               The search response information contains the user email address and respective user name.
     */
    public async contactsGetAsync(recordPerPage: string, pageNo: string, sortBy?: string, asc?: string, abortSignal?: AbortSignalLike): Promise<Array<ContactResponse>> {
        const queryParams: string[] = [];
        if (sortBy !== undefined) {
            queryParams.push(`sort-by=${encodeURIComponent(String(sortBy))}`);
        }
        if (asc !== undefined) {
            queryParams.push(`asc=${encodeURIComponent(String(asc))}`);
        }
        const requestPath = `/v4/settings/contacts/${recordPerPage}/${pageNo}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<ContactResponse>>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<ContactResponse>;
    }

    /**
     * Delete Document
     * @remarks Business applications can use this service API to delete a document in a package.
     */
    public async documentsDeleteDocumentAsync(packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<DocumentResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DocumentResponse>("DELETE", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `DELETE ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DocumentResponse;
    }

    /**
     * Download Document
     * @remarks Business applications can use this service API to download the document bytes. The package ID and document ID is provided in the resource URL.
     */
    public async documentsDownloadDocumentBytesAsync(packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<DocumentsDownloadDocumentBytesResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DocumentsDownloadDocumentBytesResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DocumentsDownloadDocumentBytesResponse;
    }

    /**
     * Get Certify Policy for a document
     * @remarks Business applications can use this service API to get certify signature settings of a document in a package.
     */
    public async documentsGetCertifyPolicyAsync(packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<CertifyPolicyResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/certify`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<CertifyPolicyResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as CertifyPolicyResponse;
    }

    /**
     * Get Document Details
     * @remarks Business applications can use this service API to get the document details. The document ID is provided in the URL as “{document_id}”.
     */
    public async documentsGetDocumentDetailsAsync(packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<DocumentDetailsResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/details`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DocumentDetailsResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DocumentDetailsResponse;
    }

    /**
     * Rename Document
     * @remarks Business applications can use this service API to rename a document in a package.
     */
    public async documentsRenameDocumentAsync(input: RenameDocumentRequest, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<DocumentResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DocumentResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DocumentResponse;
    }

    /**
     * Update Certify Policy for a document
     * @remarks Business applications can use this service API to update certify signature settings for a document in a package.
     */
    public async documentsUpdateCertifyPolicyAsync(input: UpdateCertifyPolicyRequest, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/certify`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Add or Update Document from Library
     * @remarks Business applications can use this service API to add a document from the user’s library to a package. Package ID is provided in the URL, the ID of the document should also be provided as “document_id” in the resource URL to identify the library document to be copied.               Note a package must already exist before you can add a document using this call.
     */
    public async documentsUploadFromLibraryAsync(packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<UploadDocumentLibraryResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/library/${documentId}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<UploadDocumentLibraryResponse>("POST", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as UploadDocumentLibraryResponse;
    }

    /**
     * Upload Document
     * @remarks Business applications can use this service API to add a document to a document package linked to an enterprise user’s account. The document information is sent in the HTTP request header and document bytes are sent in the HTTP request body.  Note SigningHub will convert supported document formats to PDF if the header "x-convert-document" is set to a value of "true".  The only case supported where this value is set to "false" is to retain Word format and XML documents.                SigningHub supports a wide variety of document formats, each of which can be converted to PDF format upon upload.Click here for the full list.                Note PDF documents are not altered upon upload to the system.                Note a package must already exist before you can add a document using this call.
     */
    public async documentsUploadStreamAsync(input: DocumentsUploadStreamInput, packageId: string, abortSignal?: AbortSignalLike): Promise<UploadDocument> {
        const requestPath = `/v4/packages/${packageId}/documents`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<UploadDocument>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as UploadDocument;
    }

    /**
     * Get Workflow User Authentication (Document Opening)  of Enterprise Package
     * @remarks Business applications can use this service API to get the access security settings enabled for the package. Recipients for whom the security is configured are identified by the order in the URL. Package ID is also identified in the request URL. Recipients will not be able to access document package outside the scope of this duration if the access security is enabled. Document owner can also configure authentication based security of the package for a recipient.
     */
    public async enterpriseDocumentsGetEnterpriseWorkflowAccessAsync(packageId: string, order: string, abortSignal?: AbortSignalLike): Promise<EnterpriseAccessResponse> {
        const requestPath = `/v4/enterprise/packages/${packageId}/workflow/${order}/authentication`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EnterpriseAccessResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EnterpriseAccessResponse;
    }

    /**
     * Update Workflow User Authentication (Document Opening) of Enterprise Package
     * @remarks Business applications can use this service API to update the package authentications and access duration for the recipients. This API can also be used by the enterprise administrator or the enterprise user if the "scope" variable was used in the authentication request
     */
    public async enterpriseDocumentsUpdateEnterpriseWorkflowAccessAsync(input: AccessUpdateRequest, packageId: string, order: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/enterprise/packages/${packageId}/workflow/${order}/authentication`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Assign Document Field
     * @remarks Business applications can use this service API to assign users to input fields, e.g. signature fields, and hence define the signing order.  The fields must already be present in the document and the users must be present in the workflow. The ID of the workflow document is provided in the resource URL, along with the package identifier.               When recipients are in a workflow they are numbered, beginning at one and counting up for each extra one.Therefore, if there are three users in a workflow they are numbered "1", "2", and "3" respectfully.This API call allows you to assign a user by their workflow order number, to an input field on the document.
     */
    public async fieldsAutoAssignFieldAsync(input: FieldsAutoAssignFieldInput, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/fields/assign`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * AutoPlace Fields
     * @remarks Business applications can call this API to add signature or user defined form fields to a document based upon the search criterion supplied. The placement options are: to the left, to the right, top and bottom.  The document is search and all instances of the search criteria met are processed.  This allows you to add multiple fields with one call.  For example, upload a document and automatically place a signature field to the right of all instances of the word "Sign here:".  This saves time and ensures no signatures are missed from the workflow.               The API call supports all types of fields; namely electronic, digital, in-person, initials, and text field etc.               In addition, the API call supports the "order" variable.This means you can assign a set of signature fields automatically to a single user.Hence, the call can be repeated to place and assign signature fields to more than one user in the workflow.               At least one user must exist in a workflow before signature fields can be added to the document in this way.The order number corresponds to the recipient in the workflow.  Therefore, this number must be equal or less than the total number of users in the workflow.  The below example assigns this new signature field to the first person in the workflow.               This API call can be used multiple times on the same document.The reason for doing so is to cater for multiple signatories on a document.The recipient or signatory is identified by the "order" variable passed in the call.               See [Add Users to Workflow](#operation/Workflow_WorkflowAddUser) for more information.  Note if you have a document with a template applied, or have applied one using the APIs, then the workflow will already contain users.
     */
    public async fieldsAutoPlaceAsync(input: AutoPlaceFieldRequest, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<Array<AutoPlaceFieldsResponse>> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/fields/autoplace`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<AutoPlaceFieldsResponse>>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<AutoPlaceFieldsResponse>;
    }

    /**
     * Delete Document Field
     * @remarks Business applications can use this service API to delete a field of document in a package.
     */
    public async fieldsDeleteDocumentFieldAsync(input: DeleteDocumentFieldRequest, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/fields`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("DELETE", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `DELETE ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Fill Form Fields
     * @remarks Business applications can use this service API to fill one or more form fields in a document by a specified user in the order.
     */
    public async fieldsFillFormFieldsAsync(input: FormFillingRequest, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/fields`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Get Document Fields
     * @remarks Business applications can use this service API to get document fields i.e., initials, in-persons, signature fields or form fields.
     */
    public async fieldsGetAllDocumentFieldsAsync(packageId: string, documentId: string, pageNo: string, abortSignal?: AbortSignalLike): Promise<FieldsResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/fields/${pageNo}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<FieldsResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as FieldsResponse;
    }

    /**
     * Move Package to Custom or Shared Space folder
     * @remarks Business applications can use this service API to move the document package to a shared space or user's custom folder.
     */
    public async folderMovePackageAsync(input: MoveToRequest, packageId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/move_to`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Add Initial Field
     * @remarks Business applications can use this service API to add an initials field to a document in a package.               At least one user must exist in a workflow before fields(input and signature) can be added to the document in this way.The order number corresponds to the recipient in the workflow.Therefore, this number must be equal or less than the total number of users in the workflow.  The below example assigns this new signature field to the first person in the workflow.               See [Add Users to Workflow](#operation/Workflow_WorkflowAddUser) for more information.  Note if you have a document with a template applied, or have applied one using the APIs, then the workflow will already contain users.
     */
    public async initialsAddInitialAsync(input: InitialFieldRequest, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<AddFieldResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/fields/initials`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<AddFieldResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as AddFieldResponse;
    }

    /**
     * Fill Initials
     * @remarks Business applications can use this service API to fill an initials field in a document by a specified user in the order.
     */
    public async initialsFillAsync(input: InitialFillingRequest, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/initial`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Update Initial Field
     * @remarks Business applications can use this service API to update an initials field of a document in a package.               Note all of the input parameters can be changed.Where you wish to retain the current setting submit it in the call unchanged.               You can therefore use the same parameters as when you added the input field, or the information as returned by [Get Document Fields](#operation/Documents_GetDocumentFields).The latter can be used when a template was applied or the document was created using the GUI interface.
     */
    public async initialsUpdateInitialAsync(input: UpdateInitialFieldRequest, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/fields/initials`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Add In-person Field
     * @remarks Business applications can use this service API to an add in-person signature field to a document in a package.               At least one user must exist in a workflow before fields(input and signature) can be added to the document in this way.The order number corresponds to the recipient in the workflow.Therefore, this number must be equal or less than the total number of users in the workflow.  The below example assigns this new signature field to the first person in the workflow.               See [Add Users to Workflow](#operation/Workflow_WorkflowAddUser) for more information.  Note if you have a document with a template applied, or have applied one using the APIs, then the workflow will already contain users.               In order to host an in-person meeting a SigningHub user must be part of the workflow.They must either be added with Role "INPERSON_HOST" or "SIGNER".  For the host option they do not require a signature or input field on the document, and the in-person signature field can be signed by the non-SigningHub registered user.               Note this is a special workflow scenario whereby it is expected that the SigningHub user and recipient would be in the same location.This type of signature is not intended for use whereby a document is shared with recipients over email link in the normal loose integration method.
     */
    public async inPersonAddInPersonAsync(input: InPersonFieldRequest, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<AddFieldResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/fields/in_person_signature`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<AddFieldResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as AddFieldResponse;
    }

    /**
     * Update In-person Field
     * @remarks Business applications can use this service API to update an in-person signature field of a document in a package.               Note all of the input parameters can be changed.Where you wish to retain the current setting submit it in the call unchanged.You can therefore use the same parameters as when you added the input field, or the information as returned by [Get Document Fields](#operation/Documents_GetDocumentFields).The latter can be used when a template was applied or the document was created using the GUI interface.
     */
    public async inPersonUpdateInPersonAsync(input: UpdateInPersonFieldRequest, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/fields/in_person_signature`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Add Package
     * @remarks This service API is used to add/create a document package to hold one or more documents. This package can then be used to add documents and recipients in order to start a new workflow.               As previously stated, this call is mandatory if you wish to work with documents. A package must exist before you can upload or add documents to SigningHub..               Even a single document is a part of a package: a package containing one document.               Note: A package can be renamed if required once created.
     */
    public async packageAddPackageAsync(input: AddDocumentPackageRequest, abortSignal?: AbortSignalLike): Promise<AddPackageResponse> {
        const requestPath = `/v4/packages`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<AddPackageResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as AddPackageResponse;
    }

    /**
     * Approve Document
     * @remarks Business applications can use this service API to approve a document by a specified user in the order.
     */
    public async packageApproveAsync(input: ApproveRequest, packageId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/approve`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Decline Document
     * @remarks Business applications can use this service API to decline a document by a specified user in the order.
     */
    public async packageDeclineAsync(input: DeclineRequest, packageId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/decline`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Delete Package
     * @remarks Business applications can use this service API to delete a document from the user inbox. The package ID is provided in the resource URL as "{package_id}". If the document status is PENDING, then it is automatically declined as result of delete operation. If the document status is SHARED, then the document is automatically recalled and workflow is stopped before the document is deleted.
     */
    public async packageDeletePackageAsync(packageIdBulkAction: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageIdBulkAction}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("DELETE", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `DELETE ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Download Package
     * @remarks Business applications can use this service API to download the document package in binary format.               The package ID is provided in the resource URL.               If the package contains only one document, the download is the binary PDF document.               Alternatively, if the package has more than one document, the download is the binary zip file of all documents.               The x-password and x-otp headers are optional.  They are required if the document owner set them during the workflow creation phase.
     */
    public async packageDownloadPackageBytesAsync(packageIdBulkAction: string, documentIds?: string, abortSignal?: AbortSignalLike): Promise<PackageDownloadPackageBytesResponse> {
        const queryParams: string[] = [];
        if (documentIds !== undefined) {
            queryParams.push(`document-ids=${encodeURIComponent(String(documentIds))}`);
        }
        const requestPath = `/v4/packages/${packageIdBulkAction}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<PackageDownloadPackageBytesResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as PackageDownloadPackageBytesResponse;
    }

    /**
     * Finish Processing
     * @remarks Business applications can use this service API to finish the document processing after signing all the signature fields.  This method is primarily used by native SigningHub mobile apps for iOS and Android, and mobile web use cases.  General business applications employing tight integration into their respective portal do not need to call this method.  However, when using the Sign Document API (and hence the user is not presented with a visual representation of the document they are requested to sign) calling this API is required in order to ensure the respective workflow continues or completes.  For example, once all signatures have been applied using the "blind" Sign Document API call the document will not show as status "Completed" to the owner until this API is invoked.  The document owner will see a status of "In Progress" until this API is called.               Within native SigningHub mobile apps and mobile web use cases, this call is necessary to ensure that each user completes their respective actions with respect to SigningHub.For example, after a signatory has signed a document in SigningHub App, this method is invoked by the application to ensure the workflow continues to process and the next signatory is notified, and the document status is available via the configured call-back URL.
     */
    public async packageFinishAsync(packageId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/finish`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("POST", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Gatekeeper Approve Document
     * @remarks Business applications can use this service API to gatekeeper approve a document by a specified user in the order.
     */
    public async packageGatekeeperApproveAsync(input: ApproveRequest, packageId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/gatekeeper/approve`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Gatekeeper Decline Document
     * @remarks Business applications can use this service API to gatekeeper decline a document by a specified user in the order.
     */
    public async packageGatekeeperDeclineAsync(input: DeclineRequest, packageId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/gatekeeper/decline`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Get Packages
     * @remarks Business applications can use this service API to get a list of documents filtered by different statuses. Users can divide the records into pages by providing a number of records per page.
     */
    public async packageGetAllPackagesAsync(documentStatus: string, pageNo: string, recordPerPage: string, packageName?: string, packageId?: string, recipientFrom?: string, recipientTo?: string, documentId?: string, expiry?: string, modifiedFrom?: string, modifiedTo?: string, sizeFrom?: string, sizeTo?: string, certifiedDocuments?: string, formFields?: string, attachments?: string, documentType?: string, sortBy?: string, asc?: string, documentStatuses?: string, ownedBy?: string, smartForm?: string, abortSignal?: AbortSignalLike): Promise<Array<GetPackagesResponse>> {
        const queryParams: string[] = [];
        if (packageName !== undefined) {
            queryParams.push(`package-name=${encodeURIComponent(String(packageName))}`);
        }
        if (packageId !== undefined) {
            queryParams.push(`package-id=${encodeURIComponent(String(packageId))}`);
        }
        if (recipientFrom !== undefined) {
            queryParams.push(`recipient-from=${encodeURIComponent(String(recipientFrom))}`);
        }
        if (recipientTo !== undefined) {
            queryParams.push(`recipient-to=${encodeURIComponent(String(recipientTo))}`);
        }
        if (documentId !== undefined) {
            queryParams.push(`document-id=${encodeURIComponent(String(documentId))}`);
        }
        if (expiry !== undefined) {
            queryParams.push(`expiry=${encodeURIComponent(String(expiry))}`);
        }
        if (modifiedFrom !== undefined) {
            queryParams.push(`modified-from=${encodeURIComponent(String(modifiedFrom))}`);
        }
        if (modifiedTo !== undefined) {
            queryParams.push(`modified-to=${encodeURIComponent(String(modifiedTo))}`);
        }
        if (sizeFrom !== undefined) {
            queryParams.push(`size-from=${encodeURIComponent(String(sizeFrom))}`);
        }
        if (sizeTo !== undefined) {
            queryParams.push(`size-to=${encodeURIComponent(String(sizeTo))}`);
        }
        if (certifiedDocuments !== undefined) {
            queryParams.push(`certified-documents=${encodeURIComponent(String(certifiedDocuments))}`);
        }
        if (formFields !== undefined) {
            queryParams.push(`form-fields=${encodeURIComponent(String(formFields))}`);
        }
        if (attachments !== undefined) {
            queryParams.push(`attachments=${encodeURIComponent(String(attachments))}`);
        }
        if (documentType !== undefined) {
            queryParams.push(`document-type=${encodeURIComponent(String(documentType))}`);
        }
        if (sortBy !== undefined) {
            queryParams.push(`sort-by=${encodeURIComponent(String(sortBy))}`);
        }
        if (asc !== undefined) {
            queryParams.push(`asc=${encodeURIComponent(String(asc))}`);
        }
        if (documentStatuses !== undefined) {
            queryParams.push(`document-statuses=${encodeURIComponent(String(documentStatuses))}`);
        }
        if (ownedBy !== undefined) {
            queryParams.push(`owned-by=${encodeURIComponent(String(ownedBy))}`);
        }
        if (smartForm !== undefined) {
            queryParams.push(`smart-form=${encodeURIComponent(String(smartForm))}`);
        }
        const requestPath = `/v4/packages/${documentStatus}/${pageNo}/${recordPerPage}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<GetPackagesResponse>>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<GetPackagesResponse>;
    }

    /**
     * Get Package Details
     * @remarks Business applications can use this service API to get the details of an package. This API can also be used by the business application to perform action on behalf of the enterprise user via "Scope Authentication".
     */
    public async packageGetPackageDetailsAsync(packageId: string, abortSignal?: AbortSignalLike): Promise<GetPackageDetailsResponse> {
        const requestPath = `/v4/packages/${packageId}/details`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<GetPackageDetailsResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as GetPackageDetailsResponse;
    }

    /**
     * Rename Package
     * @remarks This service API is used to rename a document package.               The document package is identified by its unique identifier in the URL of the call.
     */
    public async packageRenamePackageAsync(input: RenameDocumentPackageRequest, packageIdBulkAction: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageIdBulkAction}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Submit Document
     * @remarks One of the SigningHub roles is called "Editor".  This allows a recipient to edit input fields on a document, but does not require a signature.  Once completed the recipient submits their changes.  The button used in the GUI is called "Submit".               This is the equivalent operation for an editor to submit their changes via an API call.               Click [SigningHub Roles](http://manuals.ascertia.com/SigningHubv7/default.aspx#pageid=1056) for a description of all available workflow recipient roles.
     */
    public async packageSubmitAsync(packageId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/submit`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("POST", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Add QR Code
     * @remarks Business applications can use this service API to add a QR Code to a document in a package.
     */
    public async qraddQRCodeAsync(input: QrCodeRequest, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<AddQrCodeResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/fields/qrcode`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<AddQrCodeResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as AddQrCodeResponse;
    }

    /**
     * Update QR Code
     * @remarks Business applications can use this service API to update a QR Code of a document in a package.
     */
    public async qrupdateQRCodeAsync(input: UpdateQrCodeRequest, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<UpdateQrCodeResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/fields/qrcode`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<UpdateQrCodeResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as UpdateQrCodeResponse;
    }

    /**
     * Add RadioBox Field
     * @remarks Business applications can use this service API to add a checkbox to a document in a package.               At least one user must exist in a workflow before fields(input and signature) can be added to the document in this way.The order number corresponds to the recipient in the workflow.Therefore, this number must be equal or less than the total number of users in the workflow.  The below example assigns this new signature field to the first person in the workflow.               See [Add Users to Workflow](#operation/Workflow_WorkflowAddUser) for more information.  Note if you have a document with a template applied, or have applied one using the APIs, then the workflow will already contain users.               If width and height parameters are not provided, a default size of 10x10 will be assigned. If either width or height (or both) is provided, the system will compare these provided values with each other. The larger value between width and height will be assigned to both dimensions.
     */
    public async radioAddRadioBoxAsync(input: RadioBoxFieldRequest, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<AddFieldResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/fields/radio`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<AddFieldResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as AddFieldResponse;
    }

    /**
     * Update RadioBox Field
     * @remarks Business applications can use this service API to update a radio button of a document in a package.               Note all of the input parameters can be changed.Where you wish to retain the current setting submit it in the call unchanged.You can therefore use the same parameters as when you added the input field, or the information as returned by [Get Document Fields](#operation/Documents_GetDocumentFields).The latter can be used when a template was applied or the document was created using the GUI interface.               It's width and height cannot be updated. They will remain the same as when they were added.
     */
    public async radioUpdateRadioBoxAsync(input: UpdateRadioBoxFieldRequest, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/fields/radio`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Get Templates
     * @remarks Business applications can use this service API to get list of templates for the current user.               It is possible to search based upon the criterion supplied under the "x-search-text" header.Furthermore, it is possible to search a user's personal templates or the enterprise templates as allowed against their user role by their enterprise admin.
     */
    public async settingsGetTemplatesAsync(recordPerPage: string, pageNo: string, sortBy?: string, asc?: string, abortSignal?: AbortSignalLike): Promise<Array<TemplateResponse>> {
        const queryParams: string[] = [];
        if (sortBy !== undefined) {
            queryParams.push(`sort-by=${encodeURIComponent(String(sortBy))}`);
        }
        if (asc !== undefined) {
            queryParams.push(`asc=${encodeURIComponent(String(asc))}`);
        }
        const requestPath = `/v4/settings/templates/${recordPerPage}/${pageNo}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<TemplateResponse>>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<TemplateResponse>;
    }

    /**
     * Add Digital Signature Field
     * @remarks Business applications can use this service API to add a digital signature field to a document in a package.               At least one user must exist in a workflow before fields(input and signature) can be added to the document in this way.The order number corresponds to the recipient in the workflow.Therefore, this number must be equal or less than the total number of users in the workflow.  The below example assigns this new signature field to the first person in the workflow.               See [Add Users to Workflow](#operation/Workflow_WorkflowAddUser) for more information.Note if you have a document with a template applied, or have applied one using the APIs, then the workflow will already contain users.
     */
    public async signatureAddSignatureAsync(input: DigitalSignatureFieldRequest, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<SignatureFieldResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/fields/signature`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<SignatureFieldResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as SignatureFieldResponse;
    }

    /**
     * Update Digital Signature Field
     * @remarks Business applications can use this service API to update a digital signature field of a document in a package.               Note all of the input parameters can be changed.Where you wish to retain the current setting submit it in the call unchanged.You can therefore use the same parameters as when you added the input field, or the information as returned by [Get Document Fields](#operation/Documents_GetDocumentFields).The latter can be used when a template was applied or the document was created using the GUI interface.
     */
    public async signatureUpdateSignatureAsync(input: UpdateDigitalSignatureFieldRequest, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/fields/signature`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Bulk Sign Packages
     * @remarks Business applications can use this API to sign/share/sign and share multiple documents (both electronic and digital) without displaying the documents to the end user. This API needs information from the business application about document packages and the details of signatures, in-person and initials fields. On the successful completion of bulk signing transaction, the API will return the statuses and transaction ids of the document packages.               Unlike the Sign Document API, this API not only signs a document package but also marks it as approved and reviewed based on whether the recipient is a Signer, Editor or Reviewer. Any document package for which the status returns as COMPLETED has been signed, approved or reviewed by this API.               Bulk Signing works with all the signing-time authentication methods.               If SIGN is passed as bulk_action and package is in Draft state, Sign and Share will be performed automatically.               You must call this API after the [Pre Bulk Sign Documents](#operation/V4_Documents_ValidateBulkSignPackages) API.               In case you need to make changes in any of the document before signing, the [Fill Form Fields](#operation/V4_Documents_FillFormFields) API should be called before calling the Pre Bulk Sign Document API.  Remember, any mandatory input fields on a document require completing before this API will successfully complete; whereas, the auto-populated fields (like Name, Email, Date, Job Title, etc.) will be automatically filled.               The signatory is identified by the access token presented in the call.  Therefore, [authentication](#tag/Authentication) of the signatory is required prior to making this call.  You cannot authenticate as an Enterprise Admin with the scope variable, and sign a document on behalf of a user.  The access token must be issued to the signatory as a result of direct authentication.               Once document is signed, the verification response can be seen from the [Bulk Signing Status](#operation/V4_Documents_BulkSignStatus) API.               First or Second Factor OTP Usage for Authentication               In case OTP authentication is turned on for the server side signing operation, the client applications will need to generate an OTP for the mobile number using the [Bulk Sign Authentication via OTP](#operation/V4_Documents_SendsOTP) API call.  Respective business applications must retrieve the OTP from the use and submit it when making the API call.  This is done using the "x-otp" header in the request.
     */
    public async signingBulkSignDocumentsAsync(input: BulkSignRequest, packageIdBulkAction: string, abortSignal?: AbortSignalLike): Promise<BulkSignResponse> {
        const requestPath = `/v4/packages/${packageIdBulkAction}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<BulkSignResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as BulkSignResponse;
    }

    /**
     * Bulk Signing Status
     * @remarks Business applications can use this API to get the status of a specific bulk signing transaction along with the details of document packages that were processed by the [Bulk Signing Packages](#operation/V4_Documents_BulkSignDocuments) API.               You must call this API after the Bulk Signing Packages API.               The signatory is identified by the access token presented in the call.  Therefore, [authentication](#tag/Authentication) of the signatory is required prior to making this call.  You cannot authenticate as an Enterprise Admin with the scope variable, and sign a document on behalf of a user.  The access token must be issued to the signatory as a result of direct authentication.               For RAS signing, the ACTION property will be REMOTE_AUTHORIZATION_REQURIED and the STATUS property will be PENDING, indicating that the request needs to be authorized using the mobile device. If the signing process fails in some document due to different levels of assurance configured and the user wants to re-initiate the signing process to complete the signing process on failed documents, the ACTION property will be REINITIATE_SIGNING_PROCESS, and the STATUS property will be PENDING.
     */
    public async signingBulkSignStatusAsync(input: BulkSignStatusRequest, bulkAction: string, abortSignal?: AbortSignalLike): Promise<BulkSignStatusResponse> {
        const requestPath = `/v4/packages/${bulkAction}/status`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<BulkSignStatusResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as BulkSignStatusResponse;
    }

    /**
     * Sign Document
     * @remarks Business applications can use this service API to sign a document (both electronic and digital) without displaying the document to the end user using SigningHub.  For example, a business application may have another method to display the document to the user, and only the signing functionality of SigningHub is required, or for bulk operations it may be justified to sign without requiring the user to view and approve every document individually.  If there are changes to be saved in the document before signing, [Fill Form Fields](#operation/Documents_FillFormFields) API should be called before calling the Sign Document API.  Note any mandatory input fields on a document require completing before this API will successfully complete.               The signatory is identified by the access token presented in the call.Therefore, [authentication](#tag/Authentication) of the signatory is required prior to making this call.You cannot authenticate as an Enterprise Admin with the scope variable, and sign a document on behalf of a user.The access token must be issued to the signatory as a result of direct authentication.               When this call completes it is important that if the signatory was the last signer for a document that the [Finish Document](#operation/Documents_Finish) API call is invoked.The reason is because without calling that API the document will remain in a status of "In Progress" to the document owner.  Once the API has been called, the status will change to "Completed" for the document owner.               Once document is signed, the verification response can be seen from [Get Document Verification](#operation/Documents_GetDocumentPackageVerification) API.               First or Second Factor OTP Usage for Authentication               If OTP authentication is turned on for the server side signing operation, client applications will need to generate a OTP for the mobile number using [Signer Authentication via OTP](#operation/Documents_SendOtpForField) API call.  Respective business applications must retrieve the OTP from the use and submit it when making the API call.  This is done using the "x-otp" header in the request.
     */
    public async signingSignDocumentAsync(input: SignDocumentRequest, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<SignDocumentResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/sign`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<SignDocumentResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as SignDocumentResponse;
    }

    /**
     * Get Enterprise Templates
     * @remarks Business applications can use this service API to get list of enterprise templates of the current user. This means the enterprise administrator or the enterprise user if the "scope" variable was used in the authentication request.               It is possible to search based upon the criterion supplied under the "x-search-text" header.
     */
    public async templateGetEnterpriseTemplatesAsync(recordPerPage: string, pageNo: string, sortBy?: string, asc?: string, id?: string, abortSignal?: AbortSignalLike): Promise<Array<EnterpriseTemplateResponse>> {
        const queryParams: string[] = [];
        if (sortBy !== undefined) {
            queryParams.push(`sort-by=${encodeURIComponent(String(sortBy))}`);
        }
        if (asc !== undefined) {
            queryParams.push(`asc=${encodeURIComponent(String(asc))}`);
        }
        if (id !== undefined) {
            queryParams.push(`id=${encodeURIComponent(String(id))}`);
        }
        const requestPath = `/v4/enterprise/templates/${recordPerPage}/${pageNo}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<EnterpriseTemplateResponse>>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<EnterpriseTemplateResponse>;
    }

    /**
     * Add TextBox Field
     * @remarks Business applications can use this service API to add a text input field to a document in a package.               At least one user must exist in a workflow before fields(input and signature) can be added to the document in this way.The order number corresponds to the recipient in the workflow.Therefore, this number must be equal or less than the total number of users in the workflow.  The below example assigns this new signature field to the first person in the workflow.               See [Add Users to Workflow](#operation/Workflow_WorkflowAddUser) for more information.  Note if you have a document with a template applied, or have applied one using the APIs, then the workflow will already contain users.
     */
    public async textBoxAddTextBoxAsync(input: TextBoxFieldRequest, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<AddFieldResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/fields/text`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<AddFieldResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as AddFieldResponse;
    }

    /**
     * Update TextBox Field
     * @remarks Business applications can use this service API to update a text box of a document in a package.               Note all of the input parameters can be changed.Where you wish to retain the current setting submit it in the call unchanged.             You can therefore use the same parameters as when you added the input field, or the information as returned by [Get Document Fields](#operation/Documents_GetDocumentFields).             The latter can be used when a template was applied or the document was created using the GUI interface.
     */
    public async textBoxUpdateTextBoxAsync(input: UpdateTextBoxFieldRequest, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/fields/text`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Apply Workflow Template
     * @remarks Business applications can use this service API to apply a workflow template to a document. The document ID on which template has to be applied is provided in the resource URL. When document ID is set to 0 then template will be applied to to all of the doucments in a pacakge.               While applying the template it is important to remember these two important points:               Point 1 - If the template being applied is created using a PDF which already contained form fields then ensure that the document on which this template is now being applied must contain these form fields in advance and SigningHub will NOT create those form fields via the template rather only apply the form data and assign to the respective users.               Point 2 - If the template being applied is created where form fields are manually added (hence not present in the PDF originally) via SigningHub e.g. signature field, initial, in-person signature, check boxes, radio buttons, text fields etc. then on applying such a template, these form fields will be created on the target document even if these form fields were already present in the document.
     */
    public async workflowApplyTemplateAsync(input: ApplyTemplateRequest, packageId: string, documentId: string, abortSignal?: AbortSignalLike): Promise<DocumentDetailsResponse> {
        const requestPath = `/v4/packages/${packageId}/documents/${documentId}/template`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DocumentDetailsResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DocumentDetailsResponse;
    }

    /**
     * Get Process Evidence Report
     * @remarks Business applications can use this service API to download the workflow process evidence report of a document.
     */
    public async workflowEvidenceReportDownloadBytesAsync(packageId: string, abortSignal?: AbortSignalLike): Promise<WorkflowEvidenceReportDownloadBytesResponse> {
        const requestPath = `/v4/packages/${packageId}/report`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<WorkflowEvidenceReportDownloadBytesResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as WorkflowEvidenceReportDownloadBytesResponse;
    }

    /**
     * Get Workflow Details
     * @remarks Business applications can use this service API to get workflow details for the package.
     */
    public async workflowGetWorkflowDetailAsync(packageId: string, abortSignal?: AbortSignalLike): Promise<WorkflowDetailsResponse> {
        const requestPath = `/v4/packages/${packageId}/workflow`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<WorkflowDetailsResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as WorkflowDetailsResponse;
    }

    /**
     * Get Workflow History
     * @remarks Business applications can utilize this service API to retrieve the list of actions performed on a document. The package ID is provided in the resource URL. The search field (x-search-text header) is intended for email, document name, action, infokey, and infovalue.
     */
    public async workflowGetWorkflowHistoryAsync(packageId: string, pageNo: string, recordsPerPage: string, abortSignal?: AbortSignalLike): Promise<WorkflowHistoryResponse> {
        const requestPath = `/v4/packages/${packageId}/log/${pageNo}/${recordsPerPage}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<WorkflowHistoryResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as WorkflowHistoryResponse;
    }

    /**
     * Get Workflow Reminder
     * @remarks Business applications can use this service API to update the reminders for the recipients that has already been added to a workflow. Normally this call is useful after a template has been applied to a document and business applications want to override the reminders settings within the workflow. The ID of the workflow document is provided in the resource URL, and the workflow recipient is identified by the order at which it is added to the workflow.
     */
    public async workflowGetWorkflowReminderAsync(packageId: string, order: string, abortSignal?: AbortSignalLike): Promise<ReminderResponse> {
        const requestPath = `/v4/packages/${packageId}/workflow/${order}/reminders`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<ReminderResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as ReminderResponse;
    }

    /**
     * Get Workflow Users
     * @remarks Business applications can use this service API to get workflow details for the package.
     */
    public async workflowGetWorkflowUsersAsync(packageId: string, abortSignal?: AbortSignalLike): Promise<Array<WorkflowRecipient>> {
        const requestPath = `/v4/packages/${packageId}/workflow/users`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<WorkflowRecipient>>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<WorkflowRecipient>;
    }

    /**
     * Complete Workflow in the Middle (Terminate Workflow)
     * @remarks Business applications can mark the workflow completed even in the middle of the workflow. Use this service API to change the status of the workflow to completed. If there are recipients who have not signed the document yet. Their signature will not be required any more and they will not be able to see the document in their inbox any further.             Bearer token should belong to document owner or enterprise admin can use scope variable to get the access token on behalf of document owner to perform this action.
     */
    public async workflowMarkWorkflowCompletedAsync(packageId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/complete`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("POST", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Get Workflow User Permissions
     * @remarks Business applications can use this service API to get document permissions for a recipient. Recipient is identified by the order in the workflow. The ID of the package is provided in the resource URL.
     */
    public async workflowPermissionGetWorkflowPermissionsAsync(packageId: string, order: string, abortSignal?: AbortSignalLike): Promise<WorkflowPermissionResponse> {
        const requestPath = `/v4/packages/${packageId}/workflow/${order}/permissions`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<WorkflowPermissionResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as WorkflowPermissionResponse;
    }

    /**
     * Update Workflow User Permissions
     * @remarks Business applications can use this service API to update the workflow permissions for a user that has already been added to a workflow. Normally this call is useful after a template has been applied to a document and business applications want to override the permissions within the workflow. The ID of the document package is provided in the resource URL, and the workflow user is identified by the order at which it is added to the workflow.
     */
    public async workflowPermissionUpdateWorkflowPermissionsAsync(input: WorkflowPermissionsUpdateRequest, packageId: string, order: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/workflow/${order}/permissions`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Recall Document
     * @remarks Business applications can use this service API to stop a workflow in progress. The document ID is provided in the resource URL. After recall the document status automatically changes to “DRAFT”.
     */
    public async workflowRecallWorkflowAsync(packageId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/workflow`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("DELETE", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `DELETE ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Share Document Package
     * @remarks Business applications can use this service API to share a document package with the signers and start a new workflow. The document should already have been prepared by applying a template and optionally updating the users and actions defined in template. The package ID to be shared is provided in the resource URL.
     */
    public async workflowStartWorkflowAsync(packageId: string, abortSignal?: AbortSignalLike): Promise<Array<StartWorkflowResponse>> {
        const requestPath = `/v4/packages/${packageId}/workflow`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<StartWorkflowResponse>>("POST", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<StartWorkflowResponse>;
    }

    /**
     * Update Workflow Details
     * @remarks Business applications can use this service API to update the workflow details. Normally this call is useful after a template has been applied to a document, but business applications wants to override the certify permission or post processing details. The package ID is provided in the resource URL.
     */
    public async workflowUpdateWorkflowAsync(input: WorkflowDetailUpdateRequest, packageId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/workflow`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Update Post Processing
     * @remarks Business applications can use this service API to add placeholder to a workflow a document in a package.
     */
    public async workflowUpdateWorkflowPostProcessAsync(input: PostProcessUpdateRequest, packageId: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/workflow/post_process`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Update Workflow Reminders
     * @remarks Business applications can use this service API to update the workflow reminders for a recipient that has already been added to a workflow. Normally this call is useful after a template has been applied to a document and business applications want to override the reminders settings within the workflow. The ID of the workflow document is provided in the resource URL, and the workflow recipient is identified by the order at which it is added to the workflow.
     */
    public async workflowUpdateWorkflowReminderAsync(input: UpdateReminderRequest, packageId: string, order: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/workflow/${order}/reminders`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Add Groups to Workflow
     * @remarks Business applications can use this service API to add groups as a recipient to a workflow.  A SigningHub group means any member of the group can perform the action, i.e. sign, on behalf of all members of the group.               Note the group will be added as the last user/group in the workflow.Hence further work may be required to remove a current user/group, already present in the workflow. While XML type document preparation, only supported role types are "SIGNER", "REVIEWER" and "CARBON_COPY"
     */
    public async workflowWorkflowAddGroupAsync(input: WorkflowWorkflowAddGroupInput, packageId: string, abortSignal?: AbortSignalLike): Promise<Array<CollaboratorGroupResponse>> {
        const requestPath = `/v4/packages/${packageId}/workflow/groups`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<CollaboratorGroupResponse>>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<CollaboratorGroupResponse>;
    }

    /**
     * Add Placeholder to Workflow
     * @remarks Business applications can use this service API to add placeholder to a workflow in a package.  A place holder is required when you do not know the identity of the intended recipient in a workflow.               Note the placeholder will be added as the last user in the workflow.Hence further work may be required to remove a current user/group, already present in the workflow.               Adding a place holder allows you to proceed and add signature and/or input fields to the document, which can then be assigned to individual users at a later stage. While XML type document preparation, only supported role types are "SIGNER", "REVIEWER" and "CARBON_COPY"
     */
    public async workflowWorkflowAddPlaceholderAsync(input: WorkflowWorkflowAddPlaceholderInput, packageId: string, abortSignal?: AbortSignalLike): Promise<Array<CollaboratorPlaceholderResponse>> {
        const requestPath = `/v4/packages/${packageId}/workflow/placeholder`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<CollaboratorPlaceholderResponse>>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<CollaboratorPlaceholderResponse>;
    }

    /**
     * Add Users to Workflow
     * @remarks Business applications can use this service API to add recipients to a workflow. This call will only add a recipient to the workflow.  That is, no fields currently on the document will be automatically assigned to the new recipient(s), nor will this create any new fields.               Note the recipient(s) will be added as the last person in the workflow.Hence further work may be required to remove a current recipient(s) already present in the workflow.               At least one user must exist in a workflow before fields (input and signature) can be added to the document.               Note the input accepts one or more users in a single call.               In this specific call the user is a known natural person.  However, the same rules apply to Groups and Placeholders.  In either of these cases the  recipient becomes a Group of users whereby any member of the group can sign the document, or a Placeholder.The latter case is used when you do not know the identity of the intended signatory.               Note SigningHub workflow signing order starts at "1".  Hence, adding a user to workflow with no current recipient(s) begins a new signing order count.The "signing_order" parameter is mandatory if the workflow type is "custom".               Important, using this API call may affect the workflow type.If there are current recipients in a sequential workflow and a new one is added with the same signing order as a current recipient, then that part of the workflow changes automatically from sequential to parallel.Hence, the workflow overall is now of type custom as opposed to purely sequential.  Adding a recipient with the same signing order as two or parallel recipients does not change the workflow.There are merely, three or more recipients at that particular point of the workflow; all in parallel order.               Note, while XML type document preparation, only supported role types are "SIGNER", "REVIEWER" and "CARBON_COPY"
     */
    public async workflowWorkflowAddUserAsync(input: WorkflowWorkflowAddUserInput, packageId: string, abortSignal?: AbortSignalLike): Promise<Array<CollaboratorResponse>> {
        const requestPath = `/v4/packages/${packageId}/workflow/users`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<CollaboratorResponse>>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<CollaboratorResponse>;
    }

    /**
     * Delete Workflow User
     * @remarks Business applications can use this service API to delete workflow recipient.
     */
    public async workflowWorkflowDeleteUserAsync(packageId: string, order: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/workflow/${order}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("DELETE", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `DELETE ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Update Placeholder
     * @remarks Business applications can use this service API to update a placeholder in the workflow.  Normally this call is useful after a template has been applied to a document and business applications wants to override the details of a specific place holder within the workflow. The ID of the workflow document is provided in the resource URL, and the workflow place holder is identified by the order at which it is added to the workflow. While XML type document preparation, only supported role types are "SIGNER", "REVIEWER" and "CARBON_COPY"
     */
    public async workflowWorkflowUpdatePlaceholderAsync(input: WorkflowPlaceholderUpdateRequest, packageId: string, order: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/workflow/${order}/placeholder`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Update Workflow User
     * @remarks Business applications can use this service API to update the details of a recipient, group or place holder who has already been added to a workflow. Normally this call is useful after a template has been applied to a document and business applications wants to override the details of a specific user within the workflow. The ID of the workflow document is provided in the resource URL, and the workflow recipient is identified by the order at which it is added to the workflow. Note, while XML type document preparation, only supports role types "SIGNER", "REVIEWER" and "CARBON_COPY".
     */
    public async workflowWorkflowUserUpdateAsync(input: WorkflowUserUpdateRequest, packageId: string, order: string, abortSignal?: AbortSignalLike): Promise<UpdateCollaboratorResponse> {
        const requestPath = `/v4/packages/${packageId}/workflow/${order}/user`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<UpdateCollaboratorResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as UpdateCollaboratorResponse;
    }

    /**
     * Update Workflow Users Order
     * @remarks Business applications can use this service API to update order of the recipient in the workflow.
     */
    public async workflowWorkflowUserUpdateOrderAsync(input: WorkflowUserReorderRequest, packageId: string, order: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/packages/${packageId}/workflow/${order}/reorder`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Delete Shared Space
     * @remarks Business applications can use this service API to delete shared space. The availability of deleting shared spaces is subject to the assigned enterprise user role. To allow this provision Enterprise Admin will enable the ‘Manage Shared Space’ option in Roles>Document Settings.
     */
    public async workSpaceDeleteSharedSpaceAsync(id: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/shared_spaces/${id}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("DELETE", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `DELETE ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

    /**
     * Get Shared Space
     * @remarks Business applications can use this service API to get a specific shared space. This API returns information about shared space, its owner and collaborator.
     */
    public async workSpaceGetSharedSpaceAsync(id: string, abortSignal?: AbortSignalLike): Promise<WorkSpaceResponse> {
        const requestPath = `/v4/shared_spaces/${id}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<WorkSpaceResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as WorkSpaceResponse;
    }

    /**
     * Update Shared Space
     * @remarks Business applications can use this service API to update shared space. The availability of updating shared spaces is subject to the assigned enterprise user role. To allow this provision Enterprise Admin will enable the ‘Manage Shared Space’ option in Roles>Document Settings.
     */
    public async workSpaceUpdateSharedSpaceAsync(input: UpdateWorkSpaceRequest, id: string, abortSignal?: AbortSignalLike): Promise<EmptyResponse> {
        const requestPath = `/v4/shared_spaces/${id}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<EmptyResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as EmptyResponse;
    }

}

// #endregion Client
