// StarmindExtensions.ts - Auto-generated DirectClient SDK for TypeScript
// Do not edit this file directly.

import type { AbortSignalLike } from "@azure/abort-controller";
import { ConnectorClientBase } from "../azureConnectors/clientBase.ts";
import { ConnectorException } from "../azureConnectors/connectorException.ts";
import { ConnectorClientOptions } from "../azureConnectors/options.ts";
import { TokenProvider } from "../azureConnectors/authentication.ts";

// #region Types

/**
 * Find experts
 */
export interface FindExpertsInput {
    language?: Language;
    /** Primary search text to identify experts. Can be a question, a summary of a text, one or multiple topics. */
    text_query: string;
}

/**
 * Response for Find experts
 */
export interface FindExpertsResponse {
    /** List of experts matching the search query. May be empty if no experts are found. */
    experts?: Array<Record<string, unknown>>;
    /** The matched labels for the expert search query. May be empty if no labels match. */
    label_matches?: Array<ConceptLabelMatch>;
}

/**
 * Response for Find questions
 */
export interface FindQuestionsResponse {
    /** _links */
    _links?: Record<string, unknown>;
    /** Paginated results of the question search. */
    items?: Array<Question>;
    /** Total number of questions matching the request. */
    total?: number;
}

/**
 * Create a question draft
 */
export interface PostQuestionDraftInput {
    /** The optional id of the category to assign the question draft to */
    category?: number;
    /** Question title, Between 4 and 100 characters by default. Depending on the network configuration the title max length may be limited or extended further. */
    title: string;
    /** The optional question description providing detailed context and information about the question. Depending on the network configuration the description might be enforced to be non-empty. */
    description?: string;
    language?: Language;
    /** the id of the knowledge space to assign the question to */
    knowledge_space?: number;
}

/**
 * Response for Publish a question
 */
export interface PublishQuestionDraftResponse {
    /** number_of_experts */
    number_of_experts?: number;
}

/**
 * Definition: Question
 */
export interface Question {
    /** The unique knowledge space id to which the question belongs. */
    knowledge_space_id?: number;
    /** The total number of upvotes the question has received. */
    upvote_count?: number;
    /** The number of times the question has been viewed by users. */
    view_count?: number;
    /** Timestamp indicating when the question was published (yyyy-MM-ddTHH:mm:ss.fffZ) */
    published?: string;
    /** Indicates if the question is deleted. Deleted questions are not visible to users, but can still be accessed by the administrators. */
    is_deleted?: boolean;
    /** The question title */
    title?: string;
    /** Timestamp indicating the last activity with the question (yyyy-MM-ddTHH:mm:ss.fffZ) */
    last_activity?: string;
    /** The question is_published flag indicates whether the question is published or not. A published solution is visible to all users, while an unpublished solution is in draft state only visible to the author. */
    is_published?: boolean;
    /** Timestamp indicating when the question was created (yyyy-MM-ddTHH:mm:ss.fffZ) */
    created?: string;
    /** Question description providing detailed context and information about the question */
    description?: string;
    /** This is the number of solutions that have been provided for this question. */
    solution_count?: number;
    /** Timestamp indicating when the question's content or attachments were last updated (yyyy-MM-ddTHH:mm:ss.fffZ) */
    content_updated?: string;
    /** The unique question id */
    id?: number;
    /** Last time the resource has been updated in the DB. This date does not reflect content updates but any update to any fields of the record. Use content_updated for information about content updates. */
    updated?: string;
    /** The question language */
    language?: string;
}

/**
 * Definition: QuestionComplete
 */
export interface QuestionComplete {
    [key: string]: unknown;
}

/**
 * Definition: ConceptLabelMatch
 */
export interface ConceptLabelMatch {
    /** The unique concept id */
    concept_id?: string;
    /** The concept description */
    description?: string;
    /** is_flat */
    is_flat?: boolean;
    /** The label that matched the concept */
    label_match?: string;
    /** Concept language */
    language?: string;
    /** The unique ontology id to which the concept belongs */
    ontology_id?: string;
    /** Concept primary label */
    primary_label?: string;
}

/**
 * Definition: Attachment
 */
export interface Attachment {
    /** Timestamp indicating when the attachment was created (yyyy-MM-ddTHH:mm:ss.fffZ) */
    created?: string;
    /** The attachment hash */
    hash?: string;
    /** the unique attachment id */
    id?: number;
    /** The relative url to the attachment resource */
    link?: string;
    /** The attachment file mime_type */
    mime_type?: string;
    /** The attachment file name */
    name?: string;
    /** The attachment file size */
    size?: number;
}

/**
 * Definition: Comment
 */
export interface Comment {
    /** Timestamp indicating when the comment's description was last updated (yyyy-MM-ddTHH:mm:ss.fffZ) */
    content_updated?: string;
    /** Timestamp indicating when the comment was created (yyyy-MM-ddTHH:mm:ss.fffZ) */
    created?: string;
    /** This is the main text of the comment that provides additional information or context to the question or solution. */
    description?: string;
    /** The unique comment id */
    id?: number;
    /** The comment language */
    language?: string;
    /** Last time the resource has been updated in the DB. This date does not reflect content updates but any update to any fields of the record. Use content_updated for information about content updates. */
    updated?: string;
    /** The number of upvotes the comment has received. */
    upvote_count?: number;
    user?: UserLegacy;
}

/**
 * Definition: Solution
 */
export interface Solution {
    /** Timestamp indicating whether the solution has been accepted by the question poser (yyyy-MM-ddTHH:mm:ss.fffZ) */
    accepted_by_question_poser?: string;
    /** The solution attachments */
    attachments?: Array<Attachment>;
    /** The solution comments */
    comments?: Array<Comment>;
    /** Timestamp indicating when the solution's content was last updated (yyyy-MM-ddTHH:mm:ss.fffZ) */
    content_updated?: string;
    /** Timestamp indicating when the solution was created (yyyy-MM-ddTHH:mm:ss.fffZ) */
    created?: string;
    /** This is the main text of the solution that provides the answer or guidance to the question posed. */
    description?: string;
    /** Timestamp indicating when the solution is about to expiry and requires revalidation (yyyy-MM-ddTHH:mm:ss.fffZ) */
    expires?: string;
    /** the unique solution id */
    id?: number;
    /** The solution is_published flag indicates whether the solution is published or not. A published solution is visible to all users. */
    is_published?: boolean;
    /** The solution language */
    language?: string;
    /** Timestamp indicating when the solution was published (yyyy-MM-ddTHH:mm:ss.fffZ) */
    published?: string;
    /** Total number of ratings for the solution. */
    rating_count?: number;
    solution_authority_approval?: SolutionAuthorityApproval;
    /** Last time the resource has been updated in the DB. This date does not reflect content updates but any update to any fields of the record. Use content_updated for information about content updates. */
    updated?: string;
    /** The total number of upvotes the solution has received. */
    upvote_count?: number;
    /** The user who created the solution */
    user?: UserLegacy;
    validated?: SolutionValidation;
    /** Validity period until the solution is considered expired */
    validity_period?: string;
}

/**
 * Definition: SolutionValidation
 */
export interface SolutionValidation {
    /** The date when the solution was revalidated */
    revalidatedAt?: string;
    /** The validity period until the solution is considered expired */
    validityPeriod?: string;
    /** The user who validated the solution */
    user?: UserLegacy;
}

/**
 * Definition: SolutionAuthorityApproval
 */
export interface SolutionAuthorityApproval {
    /** Date of the solution authority approval (yyyy-MM-ddTHH:mm:ss.fffZ) */
    approval_date?: string;
    /** ID of the solution authority */
    solution_authority_id?: number;
    /** Name of the solution authority */
    solution_authority_name?: string;
    user?: UserLegacy;
}

/**
 * Definition: UserLegacy
 */
export interface UserLegacy {
    /** email */
    email?: string;
    /** global_user_id */
    globalUserId?: string;
    /** image */
    image?: string;
    /** firstname */
    firstname?: string;
    /** lastname */
    lastname?: string;
}

/**
 * Definition: UserId
 */
export type UserId = string;

/**
 * Definition: User
 */
export interface User {
    /** Custom text entered by the user to describe themselves in their 'About Me' section */
    about?: string;
    /** URL to the user's avatar image */
    avatar_url?: string;
    /** Company of the user */
    company?: string;
    /** ISO 3166-1 alpha-2 country code (e.g. ch, us, de) of the user */
    country?: string;
    /** Timestamp indicating when the user was created (yyyy-MM-ddTHH:mm:ss.fffZ) */
    created?: string;
    /** Timestamp indicating when the user was deleted (yyyy-MM-ddTHH:mm:ss.fffZ). Without admin permissions, only non deleted user will be returned */
    deleted?: string;
    /** Department of the user */
    department?: string;
    /** Email of the user */
    email?: string;
    /** First name of the user. */
    first_name?: string;
    /** Timestamp indicating when the user logged in the first time (yyyy-MM-ddTHH:mm:ss.fffZ) */
    first_seen?: string;
    id?: UserId;
    /** Indicates whether the user authenticates using local credentials (email/password) rather than an external SSO provider (SAML, OpenID Connect) */
    is_federated?: boolean;
    /** Indicates whether the user is a technical API user (non-interactive/machine account); when true the user typically authenticates via API key */
    is_technical?: boolean;
    /** The user's interface language preference */
    language?: Language;
    /** Last name of the user. */
    last_name?: string;
    /** Location of the user */
    location?: string;
    /** Company of the user */
    position?: string;
}

/**
 * Definition: KnowledgeSpace
 */
export interface KnowledgeSpace {
    /** Indicates if new users are automatically assigned */
    automatic_new_user_assignment?: boolean;
    /** Timestamp indicating when the knowledge space was created (yyyy-MM-ddTHH:mm:ss.fffZ) */
    created?: string;
    /** Description of the knowledge space */
    description?: string;
    /** The unique knowledge space id */
    id?: number;
    /** Indicates if the knowledge space is non public */
    is_closed?: boolean;
    /** Number of members */
    member_count?: number;
    /** Name of the knowledge space */
    name?: string;
    /** Last time the resource has been updated in the DB. */
    updated?: string;
}

/**
 * Definition: Language
 */
export type Language = "de" | "en" | "es" | "fr" | "hr" | "it" | "pt" | "ro" | "xx" | "zh";

/**
 * Definition: ExpertConceptScores
 */
export interface ExpertConceptScores {
    [key: string]: unknown;
}

/**
 * Definition: GraphQLUserResponse
 */
export interface GraphQLUserResponse {
    data: Record<string, unknown>;
    /** Optional array of GraphQL errors; only present if errors occurred */
    errors?: Array<GraphQLError>;
}

/**
 * Definition: GraphQLError
 */
export interface GraphQLError {
    extensions?: Record<string, unknown>;
    locations?: Array<Record<string, unknown>>;
    message?: string;
    path?: Array<string>;
}
// #endregion Types

// #region Client

/**
 * Typed client for the starmind connector.
 */
export class StarmindClient extends ConnectorClientBase {

    /**
     * Creates a new StarmindClient.
     * @param connectionRuntimeUrl The connection runtime URL from Azure Portal.
     * @param tokenProvider The token provider for authentication.
     * @param options Optional connector client options.
     */
    constructor(connectionRuntimeUrl: string, tokenProvider: TokenProvider, options?: ConnectorClientOptions) {
        super(connectionRuntimeUrl, tokenProvider, options);
    }

    public get connectorName(): string {
        return "starmind";
    }

    /**
     * Find experts
     * @remarks Finds experts based on the provided text_query. The search leverages the knowledge graph to identify users with relevant expertise and knowledge related to the query. Provide the optional language to specify the preferred language for concept label filtering. This does not affect the search matching itself; it only filters which expertise we show from returned users by selecting concepts based on the primary label in the chosen language. Returned concepts are deduplicated when multiple concepts share the same primary label.
     */
    public async findExpertsAsync(input: FindExpertsInput, abortSignal?: AbortSignalLike): Promise<FindExpertsResponse> {
        const requestPath = `/api/v3/experts`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<FindExpertsResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as FindExpertsResponse;
    }

    /**
     * Find questions
     * @remarks Searches questions using optional `query`, `limit` (default 10), `filter`, and `sort`; returns a paginated collection with question `items`. By default, all published questions are returned, ordered by their last activity (descending). A combination of filters, search queries and ordering criteria can be applied to the result.
     */
    public async findQuestionsAsync(query?: string, limit?: string, filter?: string, sort?: string, abortSignal?: AbortSignalLike): Promise<FindQuestionsResponse> {
        const queryParams: string[] = [];
        if (query !== undefined) {
            queryParams.push(`query=${encodeURIComponent(String(query))}`);
        }
        if (limit !== undefined) {
            queryParams.push(`limit=${encodeURIComponent(String(limit))}`);
        }
        if (filter !== undefined) {
            queryParams.push(`filter=${encodeURIComponent(String(filter))}`);
        }
        if (sort !== undefined) {
            queryParams.push(`sort=${encodeURIComponent(String(sort))}`);
        }
        const requestPath = `/api/v3/questions` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<FindQuestionsResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as FindQuestionsResponse;
    }

    /**
     * Get user by id
     * @remarks Retrieves user information based on the provided user id.
     */
    public async getUserByIdAsync(id: string, abortSignal?: AbortSignalLike): Promise<GraphQLUserResponse> {
        const requestPath = `/api/v3/users/${id}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<GraphQLUserResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as GraphQLUserResponse;
    }

    /**
     * Create a question draft
     * @remarks Creates a new question draft. The draft is not yet searchable or answerable by others. After creation, publish the draft (via the publish endpoint) to trigger expert search, make the question discoverable, and allow other users to answer it. Providing the optional description helps experts understand the question better and increases the chances of getting high-quality answers.
     */
    public async postQuestionDraftAsync(input: PostQuestionDraftInput, abortSignal?: AbortSignalLike): Promise<Question> {
        const requestPath = `/api/v3/questions`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Question>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Question;
    }

    /**
     * Publish a question
     * @remarks Publishes a question draft. Publishing the question triggers expert search, makes the question discoverable, and allows other users to answer it. Questions are always published anonymously.
     */
    public async publishQuestionDraftAsync(questionId: string, abortSignal?: AbortSignalLike): Promise<PublishQuestionDraftResponse> {
        const requestPath = `/api/v3/questions/${questionId}/publish`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<PublishQuestionDraftResponse>("PUT", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as PublishQuestionDraftResponse;
    }

}

// #endregion Client
