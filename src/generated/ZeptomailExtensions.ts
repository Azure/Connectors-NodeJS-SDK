// ZeptomailExtensions.ts - Auto-generated DirectClient SDK for TypeScript
// Do not edit this file directly.

import type { AbortSignalLike } from "@azure/abort-controller";
import type { TokenCredential } from "@azure/core-auth";
import { ConnectorClientBase } from "../azureConnectors/clientBase.ts";
import { ConnectorException } from "../azureConnectors/connectorException.ts";
import { ConnectorClientOptions } from "../azureConnectors/options.ts";

// #region Types

/**
 * Response for Get all Mail Agent
 */
export interface GetMailAgentResponse {
    /** data */
    data?: Array<Record<string, unknown>>;
    /** message */
    message?: string;
}

/**
 * Response for Get processed email details
 */
export interface GetProcessedEmailsResponse {
    /** data */
    data?: Record<string, unknown>;
    /** message */
    message?: string;
}

/**
 * Send mail
 */
export interface SendMailInput {
    /** Choose Mail Agent or enter the Mail Agent key. */
    mailagent_key: string;
    /** The email address to which bounced emails will be sent. */
    bounce_address?: string;
    from: FromAddress;
    /** Recipient email address. */
    to: Array<EmailAddressItems>;
    /** Email addresses of cc'd recipients. */
    cc?: Array<EmailAddressItems>;
    /** Email addresses of bcc'd recipients. */
    bcc?: Array<EmailAddressItems>;
    /** Format of the mail to be sent */
    mailtype?: string;
    /** Subject line of your email */
    subject: string;
    /** This is the body content for your email. */
    htmlbody?: string;
    /** The email address to which the recipient's email responses will be addressed. */
    reply_to?: Array<ReplyToAddress>;
    /** Attachments */
    attachments?: Array<Record<string, unknown>>;
}

/**
 * Send template mail
 */
export interface SendTemplateMailInput {
    /** Choose Mail Agent or enter the Mail Agent key. */
    mailagent_key: string;
    /** the email address to which bounced emails will be sent. */
    bounce_address?: string;
    /** Choose the Mail template */
    mail_template_key: string;
    from: FromAddress;
    /** Recipient email address. */
    to?: Array<EmailAddressItems>;
    /** Email addresses of cc'd recipients. */
    cc?: Array<EmailAddressItems>;
    /** Email addresses of bcc'd recipients. */
    bcc?: Array<EmailAddressItems>;
    /** You can use merge tags to replace the placeholders with multiple values for different recipients.  */
    merge_key_detail?: Array<Record<string, unknown>>;
    /** Reply To */
    reply_to?: Array<ReplyToAddress>;
}

/**
 * Response for Processed emails analytics
 */
export interface ProcessedMailStatsResponse {
    /** Emails that have hard bounced */
    hb?: Record<string, unknown>;
    /** sb */
    sb?: Record<string, unknown>;
    /** Emails that have been sent */
    sent?: Record<string, unknown>;
}

/**
 * Definition: FromAddress
 */
export interface FromAddress {
    "from-detail": Record<string, unknown>;
    /** Sender's user name */
    name: string;
}

/**
 * Definition: EmailAddress
 */
export interface EmailAddress {
    /** Recipient's email address. */
    address: string;
    /** Recipient's name. */
    name?: string;
}

/**
 * Definition: ReplyToAddress
 */
export interface ReplyToAddress {
    /** A valid email address containing a domain that is verified in your Mail Agent. */
    address?: string;
    /** Name for the reply-to. */
    name?: string;
}

/**
 * Definition: EmailAddressItems
 */
export interface EmailAddressItems {
    email_address?: EmailAddress;
}

/**
 * Definition: SuccessMessage
 */
export interface SuccessMessage {
    /** data */
    data?: Array<Record<string, unknown>>;
    /** The status of the request made. */
    message?: string;
    /** Unique reference ID of the request. */
    request_id?: string;
}
// #endregion Types

// #region Client

/**
 * Typed client for the zeptomail connector.
 */
export class ZeptomailClient extends ConnectorClientBase {

    /**
     * Creates a new ZeptomailClient.
     * @param connectionRuntimeUrl The connection runtime URL from Azure Portal.
     * @param credential The credential used for authentication.
     * @param options Optional connector client options.
     */
    constructor(connectionRuntimeUrl: string, credential: TokenCredential, options?: ConnectorClientOptions) {
        super(connectionRuntimeUrl, credential, options);
    }

    public get connectorName(): string {
        return "zeptomail";
    }

    /**
     * Get all Mail Agent
     * @remarks This endpoint fetches details of all the Mail Agents in the account. It returns data including Mail Agent name, Mail Agent key, Created time, Status.  \n\nData Fields Explained:\n- mailagent_name: Name of the Mail Agent.\n- mailagent_key: Mail Agent Key.\n- mailagent_alias: Mail Agent Alias.\n- created_time: Time at which the Mail Agent was created.\n- status: Status of the Mail Agent.\n\nExample 1: To retrieve details of all Mail Agents in the ZeptoMail account. Sample prompt 1: Get me details of all the Mail Agents in the account.
     */
    public async getMailAgentAsync(abortSignal?: AbortSignalLike): Promise<GetMailAgentResponse> {
        const requestPath = `/portal/v1.0/mailagents`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<GetMailAgentResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as GetMailAgentResponse;
    }

    /**
     * Get processed email details
     * @remarks This endpoint fetches detailed information about emails processed by Mail Agent identified by its unique ID. It returns data including the Subject, From Address, Sent time and Request ID for emails sent for the time range. The mailagent_key parameter should match the  Mail Agent key of the Mail Agent as listed in our system.
     */
    public async getProcessedEmailsAsync(mailagentKey: string, subject?: string, from?: string, to?: string, dateFrom?: string, dateTo?: string, requestId?: string, isHb?: boolean, isSb?: boolean, abortSignal?: AbortSignalLike): Promise<GetProcessedEmailsResponse> {
        const queryParams: string[] = [];
        queryParams.push(`mailagent_key=${encodeURIComponent(String(mailagentKey))}`);
        if (subject !== undefined) {
            queryParams.push(`subject=${encodeURIComponent(String(subject))}`);
        }
        if (from !== undefined) {
            queryParams.push(`from=${encodeURIComponent(String(from))}`);
        }
        if (to !== undefined) {
            queryParams.push(`to=${encodeURIComponent(String(to))}`);
        }
        if (dateFrom !== undefined) {
            queryParams.push(`date_from=${encodeURIComponent(String(dateFrom))}`);
        }
        if (dateTo !== undefined) {
            queryParams.push(`date_to=${encodeURIComponent(String(dateTo))}`);
        }
        if (requestId !== undefined) {
            queryParams.push(`request_id=${encodeURIComponent(String(requestId))}`);
        }
        if (isHb !== undefined) {
            queryParams.push(`is_hb=${encodeURIComponent(String(isHb))}`);
        }
        if (isSb !== undefined) {
            queryParams.push(`is_sb=${encodeURIComponent(String(isSb))}`);
        }
        const requestPath = `/v1.0/email` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<GetProcessedEmailsResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as GetProcessedEmailsResponse;
    }

    /**
     * Send mail
     * @remarks To send an email to the desired recipient.
     */
    public async sendMailAsync(input: SendMailInput, abortSignal?: AbortSignalLike): Promise<SuccessMessage> {
        const requestPath = `/v1.0/email`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<SuccessMessage>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as SuccessMessage;
    }

    /**
     * Send template mail
     * @remarks Sending a single email using Templates.
     */
    public async sendTemplateMailAsync(input: SendTemplateMailInput, abortSignal?: AbortSignalLike): Promise<SuccessMessage> {
        const requestPath = `/v1.0/email/template`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<SuccessMessage>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as SuccessMessage;
    }

    /**
     * Processed emails analytics
     * @remarks This endpoint fetches emails analytics for emails sent by Mail Agent identified by its Mail Agent name. It returns data including total count of Hardbounce, Softbounce and Sent statistics over a period of time and each day . The mailagent parameter should match the Mail Agent name as listed in our system.
     */
    public async processedMailStatsAsync(mailagent: string, fromTime?: string, toTime?: string, abortSignal?: AbortSignalLike): Promise<ProcessedMailStatsResponse> {
        const queryParams: string[] = [];
        queryParams.push(`mailagent=${encodeURIComponent(String(mailagent))}`);
        if (fromTime !== undefined) {
            queryParams.push(`from_time=${encodeURIComponent(String(fromTime))}`);
        }
        if (toTime !== undefined) {
            queryParams.push(`to_time=${encodeURIComponent(String(toTime))}`);
        }
        const requestPath = `/v1.0/stats/email` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<ProcessedMailStatsResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as ProcessedMailStatsResponse;
    }

}

// #endregion Client
