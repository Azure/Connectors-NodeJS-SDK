// CommondataserviceExtensions.ts - Auto-generated DirectClient SDK for TypeScript
// Do not edit this file directly.

import type { AbortSignalLike } from "@azure/abort-controller";
import { ConnectorClientBase } from "../azureConnectors/clientBase.ts";
import { ConnectorException } from "../azureConnectors/connectorException.ts";
import { ConnectorClientOptions } from "../azureConnectors/options.ts";
import { TokenProvider } from "../azureConnectors/authentication.ts";
import { TriggerCallbackPayload } from "../azureConnectors/triggerPayload.ts";

// #region Types

/**
 * Response for Get row (legacy)
 */
export interface GetItemResponse {
    [key: string]: unknown;
}

/**
 * Update a row (legacy)
 */
export interface PatchItemInput {
    [key: string]: unknown;
}

/**
 * Response for Update a row (legacy)
 */
export interface PatchItemResponse {
    [key: string]: unknown;
}

/**
 * Add a new row (legacy)
 */
export interface PostItemInput {
    [key: string]: unknown;
}

/**
 * Response for Add a new row (legacy)
 */
export interface PostItemResponse {
    [key: string]: unknown;
}

/**
 * Definition: ItemsList
 */
export interface ItemsList {
    /** List of Items */
    value?: Array<Item>;
    /** The URL to retrieve the next page. */
    "@odata.nextLink"?: string;
}

/**
 * Definition: Item
 */
export interface Item {
    dynamicProperties?: Record<string, unknown>;
}

/**
 * Definition: SubscriptionRequest
 */
export interface SubscriptionRequest {
    /** Callback url to the flow engine. Expected as part of the request and provided by Flow. */
    NotificationUrl: string;
}

/**
 * Definition: SubscriptionResponse
 */
export interface SubscriptionResponse {
    /** Id of the subscription */
    id?: string;
    /** Resource of the subscription request */
    resource?: string;
    /** Notification Type */
    notificationType?: string;
    /** Notification Url */
    notificationUrl?: string;
}

/**
 * Definition: UpdateTriggerSubscriptionRequest
 */
export interface UpdateTriggerSubscriptionRequest {
    /** Column filters */
    AttributeFilters?: Array<string>;
    /** Callback url to the flow engine. Expected as part of the request and provided by Flow. */
    NotificationUrl: string;
}

/**
 * Definition: Procedure
 */
export interface Procedure {
    /** Procedure name */
    Name?: string;
    /** Procedure display name */
    DisplayName?: string;
}

/**
 * Definition: DisplayCollectionName
 */
export interface DisplayCollectionName {
    /** The user localized display name label object. */
    userLocalizedLabel?: UserLocalizedLabel;
}

/**
 * Definition: EntitiesDynamicValuesList
 */
export interface EntitiesDynamicValuesList {
    /** List of tables as dynamic values. */
    value?: Array<EntitiesDynamicValuesListItem>;
}

/**
 * Definition: EntitiesDynamicValuesListItem
 */
export interface EntitiesDynamicValuesListItem {
    /** The metadata id. */
    metadataId?: string;
    /** The name of the table. */
    entitySetName?: string;
    /** The logical name of the table. */
    logicalName?: string;
    /** The display name of the table. */
    displayCollectionName?: DisplayCollectionName;
}

/**
 * Definition: OrganizationsDynamicValuesList
 */
export interface OrganizationsDynamicValuesList {
    /** List of organizations as dynamic values. */
    value?: Array<OrganizationsDynamicValuesListItem>;
}

/**
 * Definition: OrganizationsDynamicValuesListItem
 */
export interface OrganizationsDynamicValuesListItem {
    /** The organization id. */
    Id?: string;
    /** The name of the organization. */
    FriendlyName?: string;
    /** The URL of the organization. */
    Url?: string;
}

/**
 * Definition: EntityMetadata
 */
export interface EntityMetadata {
    /** Swagger schema */
    schema?: Record<string, unknown>;
}

/**
 * Definition: EntityItemList
 */
export interface EntityItemList {
    /** List of Items */
    value?: Array<EntityItem>;
    /** The url to fetch next page data. */
    "@odata.nextLink"?: string;
}

/**
 * Definition: EntityItem
 */
export interface EntityItem {
    dynamicProperties?: Record<string, unknown>;
}

/**
 * Definition: CallbackRegistration
 */
export interface CallbackRegistration {
    /** The callback registration table version */
    version: number;
    /** Callback url to the flow engine. Expected as part of the request and provided by Flow. */
    url: string;
    /** Callback registration name. Value will be replaced with workflow id */
    name?: string;
    /** Choose a table */
    entityname: string;
    /** Choose when the flow triggers */
    message: number;
    /** Choose an option or add your own */
    sdkmessagename?: string;
    /** Choose a scope to limit which rows can trigger the flow */
    scope: number;
    /** Enter a comma-separated list of column unique names. The flow triggers if any of them are modified */
    filteringattributes?: string;
    /** Enter an OData style filter expression to determine which rows can trigger the flow */
    filterexpression?: string;
    /** Enter a time to delay the trigger evaluation, eg. 2020-01-01T10:10:00Z */
    postponeuntil?: string;
    /** Choose the running user for steps where invoker connections are used */
    runas?: number;
}

/**
 * Definition: UserLocalizedLabel
 */
export interface UserLocalizedLabel {
    /** The display name label of the table. */
    label?: string;
}

/**
 * Definition: WebhookNotificationContent
 */
export interface WebhookNotificationContent {
    /** Swagger schema */
    schema?: Record<string, unknown>;
}

/**
 * Definition: EntityRelationshipsDynamicValuesList
 */
export interface EntityRelationshipsDynamicValuesList {
    /** List of table relationships as dynamic values. */
    value?: Array<Record<string, unknown>>;
}

/**
 * Definition: AssociateEntityRequest
 */
export interface AssociateEntityRequest {
    /** Enter the row URL using OData ID from a previous step or typing the full URL (eg. https://org0.crm.dynamics.com/api/data/v9.0/faxes(3ce6c728-3c8a-4b55-a4ee-a251b253c3ee) */
    "@odata.id": string;
}

/**
 * Definition: ActionsDynamicValuesList
 */
export interface ActionsDynamicValuesList {
    /** List of actions as dynamic values */
    value?: Array<Record<string, unknown>>;
}

/**
 * Definition: SearchRequestBody
 */
export interface SearchRequestBody {
    /** Enter a search term, eg. Contoso. Searches modifiers like boolean operators, wildcards, fuzzy search, proximity search etc. require the search type full */
    search: string;
    /** Enter whether simple or full search syntax should be used (default is simple) */
    searchtype?: string;
    /** Enter whether any or all of the search terms must be matched (default is any) */
    searchmode?: string;
    /** Enter the number of search results to be listed (default = 50) */
    top?: number;
    /** Enter an Odata style filter expression to narrow the search */
    filter?: string;
    /** Enter a comma-separated list of tables to be searched (default is all tables) */
    entities?: Array<string>;
    /** Enter a comma-separated list of column unique names followed by asc or desc */
    orderby?: Array<string>;
    /** Enter a comma-separated list of facet queries to narrow the search */
    facets?: Array<string>;
    /** Enter the number of search results to be skipped */
    skip?: number;
    /** Choose an option */
    returntotalrecordcount?: boolean;
}

/**
 * Definition: SearchOutput
 */
export interface SearchOutput {
    /** List of rows */
    value?: Array<Record<string, unknown>>;
    /** Total count of results (-1 if returntotalrecordcount is set to false) */
    totalrecordcount?: number;
    /** Facet results */
    facets?: Record<string, unknown>;
}

/**
 * Definition: WhenAnActionIsPerformedSubscriptionRequest
 */
export interface WhenAnActionIsPerformedSubscriptionRequest {
    /** The callback registration table version. */
    version: number;
    /** Callback url to the flow engine. Expected as part of the request and provided by Flow. */
    url: string;
    /** Callback registration name. Value will be replaced with workflow id. */
    name?: string;
    /** Choose a scope to limit which rows can trigger the flow. */
    scope: number;
    /** Choose a table to filter actions. */
    entityname: string;
    /** Choose an action. */
    sdkmessagename: string;
}

/**
 * Definition: WebhookNotificationContentForWhenAnActionIsPerformed
 */
export interface WebhookNotificationContentForWhenAnActionIsPerformed {
    /** Swagger schema */
    schema?: Record<string, unknown>;
}

/**
 * Typed callback payload for trigger operation 'GetOnNewItems_V2'.
 */
export type CommondataserviceOnNewItemsTriggerPayload = TriggerCallbackPayload<Item>;

/**
 * Typed callback payload for trigger operation 'GetOnUpdatedItems_V2'.
 */
export type CommondataserviceOnUpdatedItemsTriggerPayload = TriggerCallbackPayload<Item>;

// #endregion Types

export const CommondataserviceTriggerOperations = {
    OnNewItems: "GetOnNewItems_V2",
    OnUpdatedItems: "GetOnUpdatedItems_V2",
} as const;

export type CommondataserviceTriggerOperation = typeof CommondataserviceTriggerOperations[keyof typeof CommondataserviceTriggerOperations];

export const CommondataserviceTriggerParameters = {
    OnNewItems: {
        dataset: {
            name: "dataset",
            type: "string",
            required: true,
            description: "Select an Environment",
            summary: "Environment",
            dynamicValuesOperationId: "GetDataSets",
        },
        table: {
            name: "table",
            type: "string",
            required: true,
            description: "Select a table",
            summary: "Table Name",
            dynamicValuesOperationId: "GetTables",
        },
        filter: {
            name: "$filter",
            type: "string",
            required: false,
            description: "An ODATA filter query to restrict the entries returned (e.g. stringColumn eq 'string' OR numberColumn lt 123).",
            summary: "Filter Query",
        },
        orderby: {
            name: "$orderby",
            type: "string",
            required: false,
            description: "An ODATA orderBy query for specifying the order of entries.",
            summary: "Order By",
        },
        top: {
            name: "$top",
            type: "integer",
            required: false,
            description: "Total number of entries to retrieve (default = all).",
            summary: "Top Count",
        },
        skip: {
            name: "$skip",
            type: "integer",
            required: false,
            description: "The number of entries to skip (default = 0).",
            summary: "Skip Count",
        },
    },
    OnUpdatedItems: {
        dataset: {
            name: "dataset",
            type: "string",
            required: true,
            description: "Select an Environment",
            summary: "Environment",
            dynamicValuesOperationId: "GetDataSets",
        },
        table: {
            name: "table",
            type: "string",
            required: true,
            description: "Select a table",
            summary: "Table Name",
            dynamicValuesOperationId: "GetTables",
        },
        filter: {
            name: "$filter",
            type: "string",
            required: false,
            description: "An ODATA filter query to restrict the entries returned (e.g. stringColumn eq 'string' OR numberColumn lt 123).",
            summary: "Filter Query",
        },
        orderby: {
            name: "$orderby",
            type: "string",
            required: false,
            description: "An ODATA orderBy query for specifying the order of entries.",
            summary: "Order By",
        },
        top: {
            name: "$top",
            type: "integer",
            required: false,
            description: "Total number of entries to retrieve (default = all).",
            summary: "Top Count",
        },
        skip: {
            name: "$skip",
            type: "integer",
            required: false,
            description: "The number of entries to skip (default = 0).",
            summary: "Skip Count",
        },
    },
} as const;

// #region Client

/**
 * Typed client for the commondataservice connector.
 */
export class CommondataserviceClient extends ConnectorClientBase {

    /**
     * Creates a new CommondataserviceClient.
     * @param connectionRuntimeUrl The connection runtime URL from Azure Portal.
     * @param tokenProvider The token provider for authentication.
     * @param options Optional connector client options.
     */
    constructor(connectionRuntimeUrl: string, tokenProvider: TokenProvider, options?: ConnectorClientOptions) {
        super(connectionRuntimeUrl, tokenProvider, options);
    }

    public get connectorName(): string {
        return "commondataservice";
    }

    /**
     * Get row (legacy)
     * @remarks This operation retrieves the specified row for a table
     */
    public async getItemAsync(dataset: string, table: string, id: string, abortSignal?: AbortSignalLike): Promise<GetItemResponse> {
        const requestPath = `/v2/datasets/${encodeURIComponent(encodeURIComponent(String(dataset)))}/tables/${encodeURIComponent(encodeURIComponent(String(table)))}/items/${encodeURIComponent(encodeURIComponent(String(id)))}`;
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<GetItemResponse>("GET", url, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as GetItemResponse;
    }

    /**
     * List rows (legacy)
     * @remarks This operation gets rows for a table
     */
    public async *getItemsAsync(dataset: string, table: string, apply?: string, filter?: string, orderby?: string, top?: number, skip?: number, expand?: string, abortSignal?: AbortSignalLike): AsyncIterable<Item> {
        const queryParams: string[] = [];
        if (apply !== undefined) {
            queryParams.push(`$apply=${encodeURIComponent(String(apply))}`);
        }
        if (filter !== undefined) {
            queryParams.push(`$filter=${encodeURIComponent(String(filter))}`);
        }
        if (orderby !== undefined) {
            queryParams.push(`$orderby=${encodeURIComponent(String(orderby))}`);
        }
        if (top !== undefined) {
            queryParams.push(`$top=${encodeURIComponent(String(top))}`);
        }
        if (skip !== undefined) {
            queryParams.push(`$skip=${encodeURIComponent(String(skip))}`);
        }
        if (expand !== undefined) {
            queryParams.push(`$expand=${encodeURIComponent(String(expand))}`);
        }
        const requestPath = `/v2/datasets/${encodeURIComponent(encodeURIComponent(String(dataset)))}/tables/${encodeURIComponent(encodeURIComponent(String(table)))}/items` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        let nextRequest = requestPath;
        let requestBody = undefined;
        while (true) {
            const url = this.resolveUrl(nextRequest);
            const httpResponse = await this.httpClient.sendAsync<ItemsList>("GET", url, undefined, requestBody, abortSignal);

            if (!httpResponse.isSuccessStatusCode) {
                const operationPath = this.getOperationPath(url);
                throw new ConnectorException(this.connectorName, `GET ${operationPath}`, httpResponse.statusCode, httpResponse.text);
            }

            const page = httpResponse.value as ItemsList;
            for (const item of page["value"] ?? []) {
                yield item;
            }

            const nextLink = page["@odata.nextLink"];
            if (!nextLink) {
                return;
            }

            nextRequest = nextLink;
            requestBody = undefined;
        }
    }

    /**
     * Update a row (legacy)
     * @remarks This operation updates an existing row for a table
     */
    public async patchItemAsync(input: PatchItemInput, dataset: string, table: string, id: string, abortSignal?: AbortSignalLike): Promise<PatchItemResponse> {
        const requestPath = `/v2/datasets/${encodeURIComponent(encodeURIComponent(String(dataset)))}/tables/${encodeURIComponent(encodeURIComponent(String(table)))}/items/${encodeURIComponent(encodeURIComponent(String(id)))}`;
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<PatchItemResponse>("PATCH", url, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PATCH ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as PatchItemResponse;
    }

    /**
     * Add a new row (legacy)
     * @remarks This operation adds a new row of a table
     */
    public async postItemAsync(input: PostItemInput, dataset: string, table: string, abortSignal?: AbortSignalLike): Promise<PostItemResponse> {
        const requestPath = `/v2/datasets/${encodeURIComponent(encodeURIComponent(String(dataset)))}/tables/${encodeURIComponent(encodeURIComponent(String(table)))}/items`;
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<PostItemResponse>("POST", url, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as PostItemResponse;
    }

}

// #endregion Client
