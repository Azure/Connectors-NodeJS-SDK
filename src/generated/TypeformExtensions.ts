// TypeformExtensions.ts - Auto-generated DirectClient SDK for TypeScript
// Do not edit this file directly.

import { ConnectorClientBase } from "../azureConnectors/clientBase.ts";
import { ConnectorException } from "../azureConnectors/connectorException.ts";
import { ConnectorClientOptions } from "../azureConnectors/options.ts";
import { TokenProvider } from "../azureConnectors/authentication.ts";

// #region Types

/**
 * When a response is submitted
 */
export interface NewResponseWebhookInput {
    /** Webhook URL. */
    url: string;
    /** Yes if you want to send responses to the webhook immediately. Otherwise, no. */
    enabled: boolean;
}

/**
 * Definition: ListFormsResponse-Old
 */
export interface ListFormsResponseOld {
    [key: string]: unknown;
}

/**
 * Definition: ResponseList
 */
export interface ResponseList {
    responses?: Array<Item>;
}

/**
 * Definition: Item
 */
export interface Item {
    ItemInternalId?: string;
}

/**
 * Definition: Item-New
 */
export interface ItemNew {
    ItemInternalId?: string;
}

/**
 * Definition: ListFormsResponse
 */
export interface ListFormsResponse {
    /** Total number of forms. */
    total_items?: number;
    /** Number of pages of results. */
    page_count?: number;
    /** Forms in response. */
    items?: Array<Record<string, unknown>>;
}

/**
 * Definition: WebhookCreationResponse
 */
export interface WebhookCreationResponse {
    /** Unique ID of the webhook */
    id: string;
    /** Unique ID of the typeform */
    form_id?: string;
    /** Unique name you want to use for the webhook */
    tag?: string;
    /** Webhook URL */
    url?: string;
    /** True if you want to send responses to the webhook immediately. Otherwise, false. */
    enabled?: boolean;
    /** Date and time when webhook was created. In ISO 8601 format, UTC time, to the second, with T as a delimiter between the date and time. */
    created_at?: string;
    /** Date of last update to webhook. In ISO 8601 format, UTC time, to the second, with T as a delimeter between the date and time. */
    updated_at?: string;
}

/**
 * Definition: WebhookPayload
 */
export interface WebhookPayload {
    /** Unique ID for the webhook. Automatically assigned by Typeform. */
    event_id: string;
    /** Reason the webhook is being sent. */
    event_type: string;
    /** Object that contains information about the typeform and account associated with the webhook. */
    form_response?: Record<string, unknown>;
}
// #endregion Types

export const TypeformTriggerOperations = {
    OnNewResponseWebhook: "NewResponseWebhook_V2",
} as const;

export type TypeformTriggerOperation = typeof TypeformTriggerOperations[keyof typeof TypeformTriggerOperations];

export const TypeformTriggerParameters = {
    OnNewResponseWebhook: {
        formId: {
            name: "form_id",
            type: "string",
            required: true,
            description: "Select a form from the dropdown.",
            summary: "Select a form",
            dynamicValuesOperationId: "ListForms_V2",
        },
        tag: {
            name: "tag",
            type: "string",
            required: true,
            description: "Unique name you want to use for the webhook.",
            summary: "Specify a tag for the webhook",
        },
    },
} as const;

// #region Client

/**
 * Typed client for the typeform connector.
 */
export class TypeformClient extends ConnectorClientBase {

    /**
     * Creates a new TypeformClient.
     * @param connectionRuntimeUrl The connection runtime URL from Azure Portal.
     * @param tokenProvider The token provider for authentication.
     * @param options Optional connector client options.
     */
    constructor(connectionRuntimeUrl: string, tokenProvider: TokenProvider, options?: ConnectorClientOptions) {
        super(connectionRuntimeUrl, tokenProvider, options);
    }

    public get connectorName(): string {
        return "typeform";
    }

}

// #endregion Client
