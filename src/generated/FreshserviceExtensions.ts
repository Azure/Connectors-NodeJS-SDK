// FreshserviceExtensions.ts - Auto-generated DirectClient SDK for TypeScript
// Do not edit this file directly.

import type { AbortSignalLike } from "@azure/abort-controller";
import type { TokenCredential } from "@azure/core-auth";
import { ConnectorClientBase } from "../azureConnectors/clientBase.ts";
import { ConnectorException } from "../azureConnectors/connectorException.ts";
import { ConnectorClientOptions } from "../azureConnectors/options.ts";
import { TriggerCallbackPayload } from "../azureConnectors/triggerPayload.ts";

// #region Types

/**
 * Definition: CreateTicket_Request
 */
export interface CreateTicketRequest {
    /** User-id of the requester. One of the Requester ID, Requester Email and Requester Phone is required */
    requester_id?: number;
    /** Email of the requester. One of the Requester ID, Requester Email and Requester Phone is required */
    email?: string;
    /** Phone number of the requester. One of the Requester ID, Requester Email and Requester Phone is required */
    phone?: string;
    /** Name of the requester. This is a required field only if Requester Phone value is provided */
    name?: string;
    /** The subject of the ticket. */
    subject: string;
    /** Status of the ticket. */
    status: string;
    /** The priority of the ticket (such as Low, Medium, or High). */
    priority: string;
    /** The description of the ticket. */
    description: string;
    /** Urgency of the problem (such as Low, Medium or High). */
    urgency?: string;
    /** Impact of the problem (such as Low, Medium or High). */
    impact?: string;
    /** The channel through which the ticket was created. */
    source?: string;
}

/**
 * Definition: UpdateTicket_Request
 */
export interface UpdateTicketRequest {
    /** User-id of the requester. For existing contacts, requester_id can be passed instead of email. */
    requester_id?: number;
    /** The subject of the ticket. */
    subject?: string;
    /** Status of the ticket. */
    status?: string;
    /** The priority of the ticket (such as Low, Medium, or High). */
    priority?: string;
    /** Urgency of the problem (such as Low, Medium or High). */
    urgency?: string;
    /** Impact of the problem (such as Low, Medium or High). */
    impact?: string;
    /** The channel through which the ticket was created. */
    source?: string;
}

/**
 * Definition: AddNote_Request
 */
export interface AddNoteRequest {
    /** The note to be added to the ticket. */
    body: string;
    /** Set to true if message is private. */
    private?: boolean;
}

/**
 * Definition: Ticket_Response
 */
export interface TicketResponse {
    /** yyyy-MM-ddTHH:mm:ss.fffZ */
    created_at?: string;
    /** Set as true if the ticket is deleted/trashed. Deleted tickets will not be considered in any views except 'deleted' filter. */
    deleted?: boolean;
    department_id_value?: number;
    /** Ticket Id specific to your account. */
    display_id?: number;
    /** yyyy-MM-ddTHH:mm:ss.fffZ */
    due_by?: string;
    /** Id of Group to which the ticket is assigned. */
    group_id?: number;
    /** Unique Id of the ticket. */
    id?: number;
    /** A number representing the impact of the problem. */
    impact?: number;
    /** Set to true if an escalation was sent. */
    isescalated?: boolean;
    /** Id of the agent to whom the problem is assigned. */
    owner_id?: string;
    /** A number representing the priority of the ticket. */
    priority?: number;
    /** User-id of the requester. For existing contacts, requester_id can be passed instead of email. */
    requester_id?: number;
    /** Id of the agent to whom the ticket is assigned. */
    responder_id?: number;
    /** A number representing the channel through which the ticket was created. */
    source?: number;
    /** Set as true if the ticket is marked as spam. */
    spam?: boolean;
    /** A number representing the status of the ticket. */
    status?: number;
    /** Ticket subject. */
    subject?: string;
    /** Type property field as defined in ticket fields. */
    ticket_type?: string;
    /** Email address to which the incoming ticket email was sent. */
    to_email?: string;
    /** yyyy-MM-ddTHH:mm:ss.fffZ */
    updated_at?: string;
    /** A number representing the urgency of the problem. */
    urgency?: number;
    /** Description of the problem. */
    description?: string;
    /** HTML content of the ticket. Description and description_html should not be passed together. */
    description_html?: string;
    /** Status of the ticket. */
    status_name?: string;
    /** Priority of the ticket. */
    priority_name?: string;
    /** The channel through which the ticket was created. */
    source_name?: string;
    /** Name of the agent to whom the ticket is assigned. */
    responder_name?: string;
    /** Email addresses to which the incoming ticket email was sent. */
    to_emails?: Array<string>;
    /** Name of the department. */
    department_name?: string;
    /** Id of the associated problem to the ticket. */
    assoc_problem_id?: number;
    /** Id of the associated change to the problem. */
    assoc_change_id?: number;
    /** Id of the change causing the ticket. */
    assoc_change_cause_id?: number;
    /** Id of the asset associated to the ticket. */
    assoc_asset_id?: number;
    /** Urgency of the problem. */
    urgency_name?: string;
    /** Impact of the problem. */
    impact_name?: string;
}

/**
 * Definition: CreateUpdateTicket_Response
 */
export interface CreateUpdateTicketResponse {
    ticket?: Record<string, unknown>;
}

/**
 * Definition: ListTicket_Response
 */
export interface ListTicketResponse {
    /** yyyy-MM-ddTHH:mm:ss.fffZ */
    created_at?: string;
    department_id?: number;
    /** yyyy-MM-ddTHH:mm:ss.fffZ */
    due_by?: string;
    /** Id of Group to which the ticket is assigned. */
    group_id?: number;
    /** Unique Id of the ticket. */
    id?: number;
    /** Set to true if an escalation was sent. */
    is_escalated?: boolean;
    /** A number representing the priority of the ticket. */
    priority?: number;
    /** User-id of the requester. For existing contacts, requester_id can be passed instead of email. */
    requester_id?: number;
    /** Id of the agent to whom the ticket is assigned. */
    responder_id?: number;
    /** A number representing the channel through which the ticket was created. */
    source?: number;
    /** Set as true if the ticket is marked as spam. */
    spam?: boolean;
    /** A number representing the status of the ticket. */
    status?: number;
    /** Ticket subject. */
    subject?: string;
    /** Type property field as defined in ticket fields. */
    type?: string;
    /** yyyy-MM-ddTHH:mm:ss.fffZ */
    updated_at?: string;
    /** Description of the problem. */
    description_text?: string;
    /** HTML content of the ticket. Description and description_html should not be passed together. */
    description?: string;
    /** Email addresses to which the incoming ticket email was sent. */
    to_emails?: Array<string>;
}

/**
 * Definition: UpdateTicket_Response
 */
export interface UpdateTicketResponse {
    ticket?: Record<string, unknown>;
}

/**
 * Definition: AddNote_Response
 */
export interface AddNoteResponse {
    conversation?: Record<string, unknown>;
}

/**
 * Definition: ListUsers_Response
 */
export interface ListUsersResponse {
    [key: string]: unknown;
}

/**
 * Typed callback payload for trigger operation 'OnTicketCreatedV2'.
 */
export type FreshserviceOnTicketCreatedTriggerPayload = TriggerCallbackPayload<ListTicketResponse>;

// #endregion Types

export const FreshserviceTriggerOperations = {
    OnTicketCreated: "OnTicketCreatedV2",
} as const;

export type FreshserviceTriggerOperation = typeof FreshserviceTriggerOperations[keyof typeof FreshserviceTriggerOperations];

// #region Client

/**
 * Typed client for the freshservice connector.
 */
export class FreshserviceClient extends ConnectorClientBase {

    /**
     * Creates a new FreshserviceClient.
     * @param connectionRuntimeUrl The connection runtime URL from Azure Portal.
     * @param credential The credential used for authentication.
     * @param options Optional connector client options.
     */
    constructor(connectionRuntimeUrl: string, credential: TokenCredential, options?: ConnectorClientOptions) {
        super(connectionRuntimeUrl, credential, options);
    }

    public get connectorName(): string {
        return "freshservice";
    }

    /**
     * Add a note to a ticket
     * @remarks Add a private or public note to a ticket.
     */
    public async addNoteAsync(input: AddNoteRequest, ticketId: string, abortSignal?: AbortSignalLike): Promise<AddNoteResponse> {
        const requestPath = `/api/v2/tickets/${ticketId}/notes`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<AddNoteResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as AddNoteResponse;
    }

    /**
     * Create a ticket
     * @remarks Create a ticket.
     */
    public async createTicketAsync(input: CreateTicketRequest, abortSignal?: AbortSignalLike): Promise<CreateUpdateTicketResponse> {
        const requestPath = `/api/v2/tickets`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<CreateUpdateTicketResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as CreateUpdateTicketResponse;
    }

    /**
     * Update a ticket
     * @remarks Update a ticket (only specified values will be updated).
     */
    public async updateTicketAsync(input: UpdateTicketRequest, ticketId: string, abortSignal?: AbortSignalLike): Promise<CreateUpdateTicketResponse> {
        const requestPath = `/api/v2/tickets/${ticketId}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<CreateUpdateTicketResponse>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as CreateUpdateTicketResponse;
    }

}

// #endregion Client
