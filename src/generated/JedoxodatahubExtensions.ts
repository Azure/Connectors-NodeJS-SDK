// JedoxodatahubExtensions.ts - Auto-generated DirectClient SDK for TypeScript
// Do not edit this file directly.

import type { TokenCredential } from "@azure/core-auth";
import { ConnectorClientBase } from "../azureConnectors/clientBase.ts";
import type { ConnectorClientOptions, ConnectorOperationOptions } from "../azureConnectors/options.ts";

// #region Types

/**
 * Response for Get databases
 */
export interface DatabasesResponse {
    value?: DatabaseArray;
}

/**
 * Response for Get cubes
 */
export interface CubesResponse {
    value?: CubeArray;
}

/**
 * Response for Get cube cells
 */
export interface CubeCellsResponse {
    value?: CellArray;
}

/**
 * Response for Get dimensions
 */
export interface DimensionsResponse {
    value?: DimensionArray;
}

/**
 * Response for Get elements
 */
export interface ElementsResponse {
    value?: ElementArray;
}

/**
 * Response for Get stored views
 */
export interface ViewsResponse {
    value?: ViewArray;
}

/**
 * Response for Get stored view cells
 */
export interface ViewCellsResponse {
    value?: ViewCellArray;
}

/**
 * Response for Get Integrator project groups
 */
export interface IntegratorProjectGroupsResponse {
    value?: IntegratorProjectGroupArray;
}

/**
 * Response for Get Integrator projects
 */
export interface IntegratorProjectsResponse {
    value?: IntegratorProjectArray;
}

/**
 * Response for Get extracts
 */
export interface ExtractsResponse {
    value?: IntegratorComponentArray;
}

/**
 * Response for Get extract rows
 */
export interface ExtractRowsResponse {
    value?: IntegratorExtractComponentRowArray;
}

/**
 * Response for Get jobs
 */
export interface JobsResponse {
    value?: IntegratorComponentArray;
}

/**
 * Response for Get loads
 */
export interface LoadsResponse {
    value?: IntegratorComponentArray;
}

/**
 * Response for Get transforms
 */
export interface TransformsResponse {
    value?: IntegratorComponentArray;
}

/**
 * Response for Get transform Rows
 */
export interface TransformRowsResponse {
    value?: IntegratorTransformComponentRowArray;
}

/**
 * Definition: Cell
 */
export interface Cell {
    [key: string]: unknown;
}

/**
 * Definition: CellArray
 */
export interface CellArray {
    [key: string]: unknown;
}

/**
 * Definition: Cube
 */
export interface Cube {
    FilledCellCount?: number;
    Id?: number;
    Name?: string;
    Type?: string;
}

/**
 * Definition: CubeArray
 */
export interface CubeArray {
    [key: string]: unknown;
}

/**
 * Definition: Database
 */
export interface Database {
    CubeCount?: number;
    DimensionCount?: number;
    Id?: number;
    Name?: string;
    Type?: string;
}

/**
 * Definition: DatabaseArray
 */
export interface DatabaseArray {
    [key: string]: unknown;
}

/**
 * Definition: Dimension
 */
export interface Dimension {
    ElementCount?: number;
    Id?: number;
    Name?: string;
    Type?: string;
}

/**
 * Definition: DimensionArray
 */
export interface DimensionArray {
    [key: string]: unknown;
}

/**
 * Definition: Element
 */
export interface Element {
    Id?: number;
    Name?: string;
    Position?: number;
    Type?: string;
    Weight?: number;
}

/**
 * Definition: ElementArray
 */
export interface ElementArray {
    [key: string]: unknown;
}

/**
 * Definition: Error
 */
export interface Error {
    error?: string;
}

/**
 * Definition: IntegratorComponent
 */
export interface IntegratorComponent {
    Description?: string;
    ModificationDate?: string;
    Name?: string;
    Type?: string;
}

/**
 * Definition: IntegratorComponentArray
 */
export interface IntegratorComponentArray {
    [key: string]: unknown;
}

/**
 * Definition: IntegratorExtractComponentRow
 */
export interface IntegratorExtractComponentRow {
    [key: string]: unknown;
}

/**
 * Definition: IntegratorExtractComponentRowArray
 */
export interface IntegratorExtractComponentRowArray {
    [key: string]: unknown;
}

/**
 * Definition: IntegratorProject
 */
export interface IntegratorProject {
    Description?: string;
    ModificationDate?: string;
    Name?: string;
    Version?: string;
}

/**
 * Definition: IntegratorProjectArray
 */
export interface IntegratorProjectArray {
    [key: string]: unknown;
}

/**
 * Definition: IntegratorProjectGroup
 */
export interface IntegratorProjectGroup {
    Description?: string;
    Developer?: string;
    FriendlyName?: string;
    Id?: string;
    Name?: string;
    Namespace?: string;
    Version?: string;
}

/**
 * Definition: IntegratorProjectGroupArray
 */
export interface IntegratorProjectGroupArray {
    [key: string]: unknown;
}

/**
 * Definition: IntegratorRunResult
 */
export interface IntegratorRunResult {
    errors?: number;
    executionType?: string;
    id?: number;
    startDate?: string;
    status?: string;
    traceAvailable?: boolean;
    warnings?: number;
}

/**
 * Definition: IntegratorTransformComponentRow
 */
export interface IntegratorTransformComponentRow {
    [key: string]: unknown;
}

/**
 * Definition: IntegratorTransformComponentRowArray
 */
export interface IntegratorTransformComponentRowArray {
    [key: string]: unknown;
}

/**
 * Definition: View
 */
export interface View {
    CreationDate?: string;
    CubeId?: number;
    CubeName?: string;
    Description?: string;
    FriendlyName?: string;
    Global?: boolean;
    Id?: string;
    UserName?: string;
}

/**
 * Definition: ViewArray
 */
export interface ViewArray {
    [key: string]: unknown;
}

/**
 * Definition: ViewCell
 */
export interface ViewCell {
    [key: string]: unknown;
}

/**
 * Definition: ViewCellArray
 */
export interface ViewCellArray {
    [key: string]: unknown;
}
// #endregion Types

// #region Client

/**
 * Typed client for the jedoxodatahub connector.
 */
export class JedoxodatahubClient extends ConnectorClientBase {

    /**
     * Creates a new JedoxodatahubClient.
     * @param connectionRuntimeUrl The connection runtime URL from Azure Portal.
     * @param credential The credential used for authentication.
     * @param options Optional connector client options.
     */
    constructor(connectionRuntimeUrl: string, credential: TokenCredential, options?: ConnectorClientOptions) {
        super(connectionRuntimeUrl, credential, options);
    }

    public get connectorName(): string {
        return "jedoxodatahub";
    }

    /**
     * Get databases
     * @remarks Get a list of databases found in the server. System and config databases are excluded from the list, but can be requested by providing the ID.
     */
    public async databases(top?: string, skip?: string, filter?: string, options: ConnectorOperationOptions = {}): Promise<DatabasesResponse> {
        const queryParams: string[] = [];
        if (top !== undefined) {
            queryParams.push(`$top=${encodeURIComponent(String(top))}`);
        }
        if (skip !== undefined) {
            queryParams.push(`$skip=${encodeURIComponent(String(skip))}`);
        }
        if (filter !== undefined) {
            queryParams.push(`$filter=${encodeURIComponent(String(filter))}`);
        }
        const requestPath = `/Databases` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DatabasesResponse>("Jedoxodatahub.databases", "Databases", "GET", requestUrl, undefined, options);

        return httpResponse.value as DatabasesResponse;
    }

    /**
     * Get database by id
     * @remarks Get the database with the given ID.
     */
    public async databaseById(databaseId: string, options: ConnectorOperationOptions = {}): Promise<Database> {
        const requestPath = `/Databases(${databaseId})`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<Database>("Jedoxodatahub.databaseById", "DatabaseById", "GET", requestUrl, undefined, options);

        return httpResponse.value as Database;
    }

    /**
     * Get cubes
     * @remarks Get a list of cubes in the given database. To prevent issues with the URL encodings, Attribute cubes will be renamed, e.g. \#_Years to ATT_Years.
     */
    public async cubes(databaseId: string, top?: string, skip?: string, filter?: string, options: ConnectorOperationOptions = {}): Promise<CubesResponse> {
        const queryParams: string[] = [];
        if (top !== undefined) {
            queryParams.push(`$top=${encodeURIComponent(String(top))}`);
        }
        if (skip !== undefined) {
            queryParams.push(`$skip=${encodeURIComponent(String(skip))}`);
        }
        if (filter !== undefined) {
            queryParams.push(`$filter=${encodeURIComponent(String(filter))}`);
        }
        const requestPath = `/Databases(${databaseId})/Cubes` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<CubesResponse>("Jedoxodatahub.cubes", "Cubes", "GET", requestUrl, undefined, options);

        return httpResponse.value as CubesResponse;
    }

    /**
     * Get cube by ID
     * @remarks Get the cube with the given ID in the given database. To prevent issues with the URL encodings, Attribute cubes will be renamed, e.g. \#_Years to ATT_Years.
     */
    public async cubeById(databaseId: string, cubeId: string, options: ConnectorOperationOptions = {}): Promise<Cube> {
        const requestPath = `/Databases(${databaseId})/Cubes(${cubeId})`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<Cube>("Jedoxodatahub.cubeById", "CubeById", "GET", requestUrl, undefined, options);

        return httpResponse.value as Cube;
    }

    /**
     * Get cube cells
     * @remarks Get the cells from a cube. This returns the cells' values and element names. If the cell has a string value, the value is instead stored in the stringValue field. Element names are stored in dynamic properties.
     */
    public async cubeCells(databaseId: string, cubeId: string, top?: string, skip?: string, filter?: string, baseonly?: string, userules?: string, zerosupression?: string, disablepaging?: string, options: ConnectorOperationOptions = {}): Promise<CubeCellsResponse> {
        const queryParams: string[] = [];
        if (top !== undefined) {
            queryParams.push(`$top=${encodeURIComponent(String(top))}`);
        }
        if (skip !== undefined) {
            queryParams.push(`$skip=${encodeURIComponent(String(skip))}`);
        }
        if (filter !== undefined) {
            queryParams.push(`$filter=${encodeURIComponent(String(filter))}`);
        }
        if (baseonly !== undefined) {
            queryParams.push(`baseonly=${encodeURIComponent(String(baseonly))}`);
        }
        if (userules !== undefined) {
            queryParams.push(`userules=${encodeURIComponent(String(userules))}`);
        }
        if (zerosupression !== undefined) {
            queryParams.push(`zerosupression=${encodeURIComponent(String(zerosupression))}`);
        }
        if (disablepaging !== undefined) {
            queryParams.push(`disablepaging=${encodeURIComponent(String(disablepaging))}`);
        }
        const requestPath = `/Databases(${databaseId})/Cubes(${cubeId})/Cells` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<CubeCellsResponse>("Jedoxodatahub.cubeCells", "CubeCells", "GET", requestUrl, undefined, options);

        return httpResponse.value as CubeCellsResponse;
    }

    /**
     * Get dimensions
     * @remarks Get a list of dimensions in the given database.
     */
    public async dimensions(databaseId: string, top?: string, skip?: string, filter?: string, options: ConnectorOperationOptions = {}): Promise<DimensionsResponse> {
        const queryParams: string[] = [];
        if (top !== undefined) {
            queryParams.push(`$top=${encodeURIComponent(String(top))}`);
        }
        if (skip !== undefined) {
            queryParams.push(`$skip=${encodeURIComponent(String(skip))}`);
        }
        if (filter !== undefined) {
            queryParams.push(`$filter=${encodeURIComponent(String(filter))}`);
        }
        const requestPath = `/Databases(${databaseId})/Dimensions` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DimensionsResponse>("Jedoxodatahub.dimensions", "Dimensions", "GET", requestUrl, undefined, options);

        return httpResponse.value as DimensionsResponse;
    }

    /**
     * Get dimension by ID
     * @remarks Get the dimension with the given ID in the given database.
     */
    public async dimensionById(databaseId: string, dimensionId: string, options: ConnectorOperationOptions = {}): Promise<Dimension> {
        const requestPath = `/Databases(${databaseId})/Dimensions(${dimensionId})`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<Dimension>("Jedoxodatahub.dimensionById", "DimensionById", "GET", requestUrl, undefined, options);

        return httpResponse.value as Dimension;
    }

    /**
     * Get elements
     * @remarks Get a list of elements in the given dimension.
     */
    public async elements(databaseId: string, dimensionId: string, top?: string, skip?: string, filter?: string, options: ConnectorOperationOptions = {}): Promise<ElementsResponse> {
        const queryParams: string[] = [];
        if (top !== undefined) {
            queryParams.push(`$top=${encodeURIComponent(String(top))}`);
        }
        if (skip !== undefined) {
            queryParams.push(`$skip=${encodeURIComponent(String(skip))}`);
        }
        if (filter !== undefined) {
            queryParams.push(`$filter=${encodeURIComponent(String(filter))}`);
        }
        const requestPath = `/Databases(${databaseId})/Dimensions(${dimensionId})/Elements` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<ElementsResponse>("Jedoxodatahub.elements", "Elements", "GET", requestUrl, undefined, options);

        return httpResponse.value as ElementsResponse;
    }

    /**
     * Get element by ID
     * @remarks Returns the element with the given ID in the given dimension.
     */
    public async elementById(databaseId: string, dimensionId: string, elementId: string, options: ConnectorOperationOptions = {}): Promise<Element> {
        const requestPath = `/Databases(${databaseId})/Dimensions(${dimensionId})/Elements(${elementId})`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<Element>("Jedoxodatahub.elementById", "ElementById", "GET", requestUrl, undefined, options);

        return httpResponse.value as Element;
    }

    /**
     * Get stored views
     * @remarks Get a list of stored views in the given database.
     */
    public async views(databaseId: string, top?: string, skip?: string, filter?: string, options: ConnectorOperationOptions = {}): Promise<ViewsResponse> {
        const queryParams: string[] = [];
        if (top !== undefined) {
            queryParams.push(`$top=${encodeURIComponent(String(top))}`);
        }
        if (skip !== undefined) {
            queryParams.push(`$skip=${encodeURIComponent(String(skip))}`);
        }
        if (filter !== undefined) {
            queryParams.push(`$filter=${encodeURIComponent(String(filter))}`);
        }
        const requestPath = `/Databases(${databaseId})/Views` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<ViewsResponse>("Jedoxodatahub.views", "Views", "GET", requestUrl, undefined, options);

        return httpResponse.value as ViewsResponse;
    }

    /**
     * Get stored view by ID
     * @remarks Get the view with the given ID in the given database.
     */
    public async viewById(databaseId: string, viewId: string, options: ConnectorOperationOptions = {}): Promise<View> {
        const requestPath = `/Databases(${databaseId})/Views(${viewId})`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<View>("Jedoxodatahub.viewById", "ViewById", "GET", requestUrl, undefined, options);

        return httpResponse.value as View;
    }

    /**
     * Get stored view cells
     * @remarks Get all cells from a view. This returns the cells' values and element names. If the cell has a string value, the value is instead stored in the stringValue field. Element names are stored in dynamic properties.
     */
    public async viewCells(databaseId: string, viewId: string, top?: string, skip?: string, filter?: string, baseonly?: string, userules?: string, zerosupression?: string, disablepaging?: string, options: ConnectorOperationOptions = {}): Promise<ViewCellsResponse> {
        const queryParams: string[] = [];
        if (top !== undefined) {
            queryParams.push(`$top=${encodeURIComponent(String(top))}`);
        }
        if (skip !== undefined) {
            queryParams.push(`$skip=${encodeURIComponent(String(skip))}`);
        }
        if (filter !== undefined) {
            queryParams.push(`$filter=${encodeURIComponent(String(filter))}`);
        }
        if (baseonly !== undefined) {
            queryParams.push(`baseonly=${encodeURIComponent(String(baseonly))}`);
        }
        if (userules !== undefined) {
            queryParams.push(`userules=${encodeURIComponent(String(userules))}`);
        }
        if (zerosupression !== undefined) {
            queryParams.push(`zerosupression=${encodeURIComponent(String(zerosupression))}`);
        }
        if (disablepaging !== undefined) {
            queryParams.push(`disablepaging=${encodeURIComponent(String(disablepaging))}`);
        }
        const requestPath = `/Databases(${databaseId})/Views(${viewId})/Cells` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<ViewCellsResponse>("Jedoxodatahub.viewCells", "ViewCells", "GET", requestUrl, undefined, options);

        return httpResponse.value as ViewCellsResponse;
    }

    /**
     * Get Integrator project groups
     * @remarks Get a list of integrator project groups found in the server.
     */
    public async integratorProjectGroups(top?: string, skip?: string, filter?: string, options: ConnectorOperationOptions = {}): Promise<IntegratorProjectGroupsResponse> {
        const queryParams: string[] = [];
        if (top !== undefined) {
            queryParams.push(`$top=${encodeURIComponent(String(top))}`);
        }
        if (skip !== undefined) {
            queryParams.push(`$skip=${encodeURIComponent(String(skip))}`);
        }
        if (filter !== undefined) {
            queryParams.push(`$filter=${encodeURIComponent(String(filter))}`);
        }
        const requestPath = `/Integrator` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<IntegratorProjectGroupsResponse>("Jedoxodatahub.integratorProjectGroups", "IntegratorProjectGroups", "GET", requestUrl, undefined, options);

        return httpResponse.value as IntegratorProjectGroupsResponse;
    }

    /**
     * Get Integrator project group by identifier
     * @remarks Get the project group with the given ID.
     */
    public async integratorProjectsById(groupIdentifier: string, options: ConnectorOperationOptions = {}): Promise<IntegratorProjectGroup> {
        const requestPath = `/Integrator('${groupIdentifier}')`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<IntegratorProjectGroup>("Jedoxodatahub.integratorProjectsById", "IntegratorProjectsById", "GET", requestUrl, undefined, options);

        return httpResponse.value as IntegratorProjectGroup;
    }

    /**
     * Get Integrator projects
     * @remarks Get a list of integrator projects found in the server.
     */
    public async integratorProjects(groupIdentifier: string, top?: string, skip?: string, filter?: string, options: ConnectorOperationOptions = {}): Promise<IntegratorProjectsResponse> {
        const queryParams: string[] = [];
        if (top !== undefined) {
            queryParams.push(`$top=${encodeURIComponent(String(top))}`);
        }
        if (skip !== undefined) {
            queryParams.push(`$skip=${encodeURIComponent(String(skip))}`);
        }
        if (filter !== undefined) {
            queryParams.push(`$filter=${encodeURIComponent(String(filter))}`);
        }
        const requestPath = `/Integrator('${groupIdentifier}')/Projects` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<IntegratorProjectsResponse>("Jedoxodatahub.integratorProjects", "IntegratorProjects", "GET", requestUrl, undefined, options);

        return httpResponse.value as IntegratorProjectsResponse;
    }

    /**
     * Get Integrator project by name
     * @remarks Get the integrator project with the given name.
     */
    public async integratorProjectsByName(groupIdentifier: string, projectName: string, options: ConnectorOperationOptions = {}): Promise<IntegratorProject> {
        const requestPath = `/Integrator('${groupIdentifier}')/Projects('${projectName}')`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<IntegratorProject>("Jedoxodatahub.integratorProjectsByName", "IntegratorProjectsByName", "GET", requestUrl, undefined, options);

        return httpResponse.value as IntegratorProject;
    }

    /**
     * Get extracts
     * @remarks Get a list of extracts in the given integrator project.
     */
    public async extracts(groupIdentifier: string, projectName: string, top?: string, skip?: string, filter?: string, options: ConnectorOperationOptions = {}): Promise<ExtractsResponse> {
        const queryParams: string[] = [];
        if (top !== undefined) {
            queryParams.push(`$top=${encodeURIComponent(String(top))}`);
        }
        if (skip !== undefined) {
            queryParams.push(`$skip=${encodeURIComponent(String(skip))}`);
        }
        if (filter !== undefined) {
            queryParams.push(`$filter=${encodeURIComponent(String(filter))}`);
        }
        const requestPath = `/Integrator('${groupIdentifier}')/Projects('${projectName}')/Extracts` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<ExtractsResponse>("Jedoxodatahub.extracts", "Extracts", "GET", requestUrl, undefined, options);

        return httpResponse.value as ExtractsResponse;
    }

    /**
     * Get extract by Name
     * @remarks Get the extract with the given name in the given integrator project.
     */
    public async extractByName(groupIdentifier: string, projectName: string, extractName: string, options: ConnectorOperationOptions = {}): Promise<IntegratorComponent> {
        const requestPath = `/Integrator('${groupIdentifier}')/Projects('${projectName}')/Extracts('${extractName}')`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<IntegratorComponent>("Jedoxodatahub.extractByName", "ExtractByName", "GET", requestUrl, undefined, options);

        return httpResponse.value as IntegratorComponent;
    }

    /**
     * Get extract rows
     * @remarks Stream the rows of the extract with the given name in the given integrator project.
     */
    public async extractRows(groupIdentifier: string, projectName: string, extractName: string, top?: string, skip?: string, filter?: string, options: ConnectorOperationOptions = {}): Promise<ExtractRowsResponse> {
        const queryParams: string[] = [];
        if (top !== undefined) {
            queryParams.push(`$top=${encodeURIComponent(String(top))}`);
        }
        if (skip !== undefined) {
            queryParams.push(`$skip=${encodeURIComponent(String(skip))}`);
        }
        if (filter !== undefined) {
            queryParams.push(`$filter=${encodeURIComponent(String(filter))}`);
        }
        const requestPath = `/Integrator('${groupIdentifier}')/Projects('${projectName}')/Extracts('${extractName}')/Rows` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<ExtractRowsResponse>("Jedoxodatahub.extractRows", "ExtractRows", "GET", requestUrl, undefined, options);

        return httpResponse.value as ExtractRowsResponse;
    }

    /**
     * Get jobs
     * @remarks Get a list of jobs in the given integrator project.
     */
    public async jobs(groupIdentifier: string, projectName: string, top?: string, skip?: string, filter?: string, options: ConnectorOperationOptions = {}): Promise<JobsResponse> {
        const queryParams: string[] = [];
        if (top !== undefined) {
            queryParams.push(`$top=${encodeURIComponent(String(top))}`);
        }
        if (skip !== undefined) {
            queryParams.push(`$skip=${encodeURIComponent(String(skip))}`);
        }
        if (filter !== undefined) {
            queryParams.push(`$filter=${encodeURIComponent(String(filter))}`);
        }
        const requestPath = `/Integrator('${groupIdentifier}')/Projects('${projectName}')/Jobs` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<JobsResponse>("Jedoxodatahub.jobs", "Jobs", "GET", requestUrl, undefined, options);

        return httpResponse.value as JobsResponse;
    }

    /**
     * Get job by name
     * @remarks Get the jobs with the given name in the given integrator project.
     */
    public async jobByName(groupIdentifier: string, projectName: string, jobName: string, options: ConnectorOperationOptions = {}): Promise<IntegratorComponent> {
        const requestPath = `/Integrator('${groupIdentifier}')/Projects('${projectName}')/Jobs('${jobName}')`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<IntegratorComponent>("Jedoxodatahub.jobByName", "JobByName", "GET", requestUrl, undefined, options);

        return httpResponse.value as IntegratorComponent;
    }

    /**
     * Run job
     * @remarks Run the job with the given name in the given integrator project. The execution will be added to the queue.
     */
    public async runJob(groupIdentifier: string, projectName: string, jobName: string, options: ConnectorOperationOptions = {}): Promise<IntegratorRunResult> {
        const requestPath = `/Integrator('${groupIdentifier}')/Projects('${projectName}')/Jobs('${jobName}')/Run`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<IntegratorRunResult>("Jedoxodatahub.runJob", "RunJob", "GET", requestUrl, undefined, options);

        return httpResponse.value as IntegratorRunResult;
    }

    /**
     * Run job with variables
     * @remarks Run the job with the given name in the given integrator project. The execution will be added to the queue.
     */
    public async runJobWithVariables(groupIdentifier: string, projectName: string, jobName: string, variables: string, options: ConnectorOperationOptions = {}): Promise<IntegratorRunResult> {
        const requestPath = `/Integrator('${groupIdentifier}')/Projects('${projectName}')/Jobs('${jobName}')/Run(Variables='${variables}')`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<IntegratorRunResult>("Jedoxodatahub.runJobWithVariables", "RunJobWithVariables", "GET", requestUrl, undefined, options);

        return httpResponse.value as IntegratorRunResult;
    }

    /**
     * Get loads
     * @remarks Get a list of loads in the given integrator project.
     */
    public async loads(groupIdentifier: string, projectName: string, top?: string, skip?: string, filter?: string, options: ConnectorOperationOptions = {}): Promise<LoadsResponse> {
        const queryParams: string[] = [];
        if (top !== undefined) {
            queryParams.push(`$top=${encodeURIComponent(String(top))}`);
        }
        if (skip !== undefined) {
            queryParams.push(`$skip=${encodeURIComponent(String(skip))}`);
        }
        if (filter !== undefined) {
            queryParams.push(`$filter=${encodeURIComponent(String(filter))}`);
        }
        const requestPath = `/Integrator('${groupIdentifier}')/Projects('${projectName}')/Loads` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<LoadsResponse>("Jedoxodatahub.loads", "Loads", "GET", requestUrl, undefined, options);

        return httpResponse.value as LoadsResponse;
    }

    /**
     * Get load by name
     * @remarks Get the transform with the given name in the given integrator project.
     */
    public async loadByName(groupIdentifier: string, projectName: string, loadName: string, options: ConnectorOperationOptions = {}): Promise<IntegratorComponent> {
        const requestPath = `/Integrator('${groupIdentifier}')/Projects('${projectName}')/Loads('${loadName}')`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<IntegratorComponent>("Jedoxodatahub.loadByName", "LoadByName", "GET", requestUrl, undefined, options);

        return httpResponse.value as IntegratorComponent;
    }

    /**
     * Run load
     * @remarks Run the load with the given name in the given integrator project. The execution will be added to the queue.
     */
    public async runLoad(groupIdentifier: string, projectName: string, loadName: string, options: ConnectorOperationOptions = {}): Promise<IntegratorRunResult> {
        const requestPath = `/Integrator('${groupIdentifier}')/Projects('${projectName}')/Loads('${loadName}')/Run()`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<IntegratorRunResult>("Jedoxodatahub.runLoad", "RunLoad", "GET", requestUrl, undefined, options);

        return httpResponse.value as IntegratorRunResult;
    }

    /**
     * Run load with variables
     * @remarks Run the load with the given name in the given integrator project. The execution will be added to the queue.
     */
    public async runLoadWithVariables(groupIdentifier: string, projectName: string, loadName: string, variables: string, options: ConnectorOperationOptions = {}): Promise<IntegratorRunResult> {
        const requestPath = `/Integrator('${groupIdentifier}')/Projects('${projectName}')/Loads('${loadName}')/Run(Variables='${variables}')`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<IntegratorRunResult>("Jedoxodatahub.runLoadWithVariables", "RunLoadWithVariables", "GET", requestUrl, undefined, options);

        return httpResponse.value as IntegratorRunResult;
    }

    /**
     * Get transforms
     * @remarks Get a list of transforms in the given integrator project.
     */
    public async transforms(groupIdentifier: string, projectName: string, top?: string, skip?: string, filter?: string, options: ConnectorOperationOptions = {}): Promise<TransformsResponse> {
        const queryParams: string[] = [];
        if (top !== undefined) {
            queryParams.push(`$top=${encodeURIComponent(String(top))}`);
        }
        if (skip !== undefined) {
            queryParams.push(`$skip=${encodeURIComponent(String(skip))}`);
        }
        if (filter !== undefined) {
            queryParams.push(`$filter=${encodeURIComponent(String(filter))}`);
        }
        const requestPath = `/Integrator('${groupIdentifier}')/Projects('${projectName}')/Transforms` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<TransformsResponse>("Jedoxodatahub.transforms", "Transforms", "GET", requestUrl, undefined, options);

        return httpResponse.value as TransformsResponse;
    }

    /**
     * Get transform by name
     * @remarks Get the transform with the given name in the given integrator project.
     */
    public async transformByName(groupIdentifier: string, projectName: string, transformName: string, options: ConnectorOperationOptions = {}): Promise<IntegratorComponent> {
        const requestPath = `/Integrator('${groupIdentifier}')/Projects('${projectName}')/Transforms('${transformName}')`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<IntegratorComponent>("Jedoxodatahub.transformByName", "TransformByName", "GET", requestUrl, undefined, options);

        return httpResponse.value as IntegratorComponent;
    }

    /**
     * Get transform Rows
     * @remarks Stream the rows of the transform with the given name in the given integrator project.
     */
    public async transformRows(groupIdentifier: string, projectName: string, transformName: string, top?: string, skip?: string, filter?: string, options: ConnectorOperationOptions = {}): Promise<TransformRowsResponse> {
        const queryParams: string[] = [];
        if (top !== undefined) {
            queryParams.push(`$top=${encodeURIComponent(String(top))}`);
        }
        if (skip !== undefined) {
            queryParams.push(`$skip=${encodeURIComponent(String(skip))}`);
        }
        if (filter !== undefined) {
            queryParams.push(`$filter=${encodeURIComponent(String(filter))}`);
        }
        const requestPath = `/Integrator('${groupIdentifier}')/Projects('${projectName}')/Transforms('${transformName}')/Rows` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<TransformRowsResponse>("Jedoxodatahub.transformRows", "TransformRows", "GET", requestUrl, undefined, options);

        return httpResponse.value as TransformRowsResponse;
    }

}

// #endregion Client
