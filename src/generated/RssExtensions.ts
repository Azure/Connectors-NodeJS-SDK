// RssExtensions.ts - Auto-generated DirectClient SDK for TypeScript
// Do not edit this file directly.

import { ConnectorClientBase } from "../azureConnectors/clientBase.ts";
import { ConnectorException } from "../azureConnectors/connectorException.ts";
import { ConnectorClientOptions } from "../azureConnectors/options.ts";
import { TokenProvider } from "../azureConnectors/authentication.ts";
import { TriggerCallbackPayload } from "../azureConnectors/triggerPayload.ts";

// #region Types

/**
 * Definition: TriggerBatchResponse[FeedItem]
 */
export interface TriggerBatchResponseFeedItem {
    /** A list of the response objects */
    value?: Array<FeedItem>;
}

/**
 * Definition: FeedItem
 */
export interface FeedItem {
    /** Feed ID */
    id?: string;
    /** Feed title */
    title: string;
    /** Primary feed link */
    primaryLink?: string;
    /** Feed links */
    links?: Array<string>;
    /** Feed updated on */
    updatedOn?: string;
    /** Feed published date */
    publishDate?: string;
    /** Feed item summary */
    summary?: string;
    /** Copyright information */
    copyright?: string;
    /** Feed item categories */
    categories?: Array<string>;
}

/**
 * Typed callback payload for trigger operation 'OnNewFeed'.
 */
export type RssOnNewFeedTriggerPayload = TriggerCallbackPayload<FeedItem>;

// #endregion Types

export const RssTriggerOperations = {
    OnNewFeed: "OnNewFeed",
} as const;

export type RssTriggerOperation = typeof RssTriggerOperations[keyof typeof RssTriggerOperations];

export const RssTriggerParameters = {
    OnNewFeed: {
        feedUrl: {
            name: "feedUrl",
            type: "string",
            required: true,
            description: "The RSS feed URL (Example: http://rss.cnn.com/rss/cnn_topstories.rss).",
            summary: "The RSS feed URL",
        },
        sinceProperty: {
            name: "sinceProperty",
            type: "string",
            required: false,
            description: "Chosen property will be used for trigger state.",
            summary: "Chosen property will be used to determine which items are new.",
            defaultValue: "PublishDate",
            allowedValues: ["PublishDate", "UpdatedOn"],
        },
    },
} as const;

// #region Client

/**
 * Typed client for the rss connector.
 */
export class RssClient extends ConnectorClientBase {

    /**
     * Creates a new RssClient.
     * @param connectionRuntimeUrl The connection runtime URL from Azure Portal.
     * @param tokenProvider The token provider for authentication.
     * @param options Optional connector client options.
     */
    constructor(connectionRuntimeUrl: string, tokenProvider: TokenProvider, options?: ConnectorClientOptions) {
        super(connectionRuntimeUrl, tokenProvider, options);
    }

    public get connectorName(): string {
        return "rss";
    }

    /**
     * List all RSS feed items
     * @remarks This operation retrieves all items from an RSS feed.
     */
    public async listFeedItemsAsync(feedUrl?: string, since?: string, sinceProperty?: string, abortSignal?: AbortSignal): Promise<Array<FeedItem>> {
        const queryParams: string[] = [];
        if (feedUrl !== undefined) {
            queryParams.push(`feedUrl=${encodeURIComponent(String(feedUrl))}`);
        }
        if (since !== undefined) {
            queryParams.push(`since=${encodeURIComponent(String(since))}`);
        }
        if (sinceProperty !== undefined) {
            queryParams.push(`sinceProperty=${encodeURIComponent(String(sinceProperty))}`);
        }
        const requestPath = `/ListFeedItems` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<FeedItem>>("GET", url, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<FeedItem>;
    }

}

// #endregion Client
