// RevaiExtensions.ts - Auto-generated DirectClient SDK for TypeScript
// Do not edit this file directly.

import { AbortSignalLike } from "@azure/abort-controller";
import { ConnectorClientBase } from "../azureConnectors/clientBase.ts";
import { ConnectorException } from "../azureConnectors/connectorException.ts";
import { ConnectorClientOptions } from "../azureConnectors/options.ts";
import { TokenProvider } from "../azureConnectors/authentication.ts";

// #region Types

/**
 * Response for Get transcription job
 */
export interface TranscriptionGetResponse {
    /** The identifier. */
    id?: string;
    /** The status. */
    status?: string;
    /** The language. */
    language?: string;
    /** When created on. */
    created_on?: string;
    /** The transcriber. */
    transcriber?: string;
}

/**
 * Submit transcription job
 */
export interface TranscriptionInput {
    source_config?: Record<string, unknown>;
    /** The metadata. */
    metadata?: string;
    notification_config: Record<string, unknown>;
    /** The delete after seconds. */
    delete_after_seconds?: number;
    /** The transcriber. */
    transcriber?: string;
    /** Whether verbatim. */
    verbatim?: boolean;
    /** Whether to rush. */
    rush?: boolean;
    /** Whether test mode. */
    test_mode?: boolean;
    segments_to_transcribe?: Array<Record<string, unknown>>;
    speakers_names?: Array<Record<string, unknown>>;
    /** Whether to skip diarization. */
    skip_diarization?: boolean;
    /** Whether to skip postprocessing. */
    skip_postprocessing?: boolean;
    /** Whether to skip punctuations. */
    skip_punctuation?: boolean;
    /** Whether to remove disfluencies. */
    remove_disfluencies?: boolean;
    /** Whether to remove atmospherics. */
    remove_atmospherics?: boolean;
    /** Whether to filter profanity. */
    filter_profanity?: boolean;
    /** The speaker channels count. */
    speaker_channels_count?: number;
    /** The speakers count. */
    speakers_count?: number;
    /** The custom vocabulary identifier. */
    custom_vocabulary_id?: string;
    custom_vocabularies?: Array<Record<string, unknown>>;
    /** The language. */
    language?: string;
}

/**
 * Response for Submit transcription job
 */
export interface TranscriptionPostResponse {
    /** The identifier. */
    id?: string;
    /** The status. */
    status?: string;
    /** The language. */
    language?: string;
    /** When created on. */
    created_on?: string;
    /** The transcriber. */
    transcriber?: string;
}

/**
 * Response for Get transcript
 */
export interface TranscriptGetResponse {
    monologues?: Array<Record<string, unknown>>;
}

/**
 * Response for Get account
 */
export interface AccountGetResponse {
    /** The email address. */
    email?: string;
    /** The free balance. */
    free_balance?: number;
    /** The purchased balance. */
    purchased_balance?: number;
    /** The total balance. */
    total_balance?: number;
    /** The invoiced balance. */
    invoiced_balance?: number;
    /** The balance seconds. */
    balance_seconds?: number;
    /** Whether HIPAA is enabled. */
    hipaa_enabled?: boolean;
}

/**
 * Submit custom vocabulary
 */
export interface VocabularyInput {
    /** The metadata. */
    metadata?: string;
    notification_config?: Record<string, unknown>;
    custom_vocabularies?: Array<Record<string, unknown>>;
}

/**
 * Response for Submit custom vocabulary
 */
export interface VocabularyPostResponse {
    /** The identifier. */
    id?: string;
    /** The status. */
    status?: string;
    /** When created on. */
    created_on?: string;
    /** The callback URL address. */
    callback_url?: string;
}

/**
 * Response for Get vocabulary
 */
export interface VocabularyGetResponse {
    /** The identifier. */
    id?: string;
    /** The status. */
    status?: string;
    /** When created on. */
    created_on?: string;
    /** The callback URL address. */
    callback_url?: string;
}

/**
 * Submit topic extraction job
 */
export interface ExtractionInput {
    /** The language. */
    language?: string;
    /** The metadata. */
    metadata?: string;
    notification_config?: Record<string, unknown>;
    /** The delete after seconds. */
    delete_after_seconds?: number;
    json?: Record<string, unknown>;
}

/**
 * Response for Submit topic extraction job
 */
export interface ExtractionPostResponse {
    /** The identifier. */
    id?: string;
    /** The status. */
    status?: string;
    /** The language. */
    language?: string;
    /** When created on. */
    created_on?: string;
    /** The type. */
    type?: string;
}

/**
 * Response for Get topic extraction job
 */
export interface ExtractionGetResponse {
    /** The identifier. */
    id?: string;
    /** The status. */
    status?: string;
    /** The language. */
    language?: string;
    /** When created on. */
    created_on?: string;
    /** The type. */
    type?: string;
}

/**
 * Response for Get topic extraction result
 */
export interface ExtractionResultGetResponse {
    topics?: Array<Record<string, unknown>>;
}

/**
 * Submit sentiment analysis job
 */
export interface AnalysisInput {
    /** The language. */
    language?: string;
    /** The metadata. */
    metadata?: string;
    notification_config?: Record<string, unknown>;
    /** The delete after seconds. */
    delete_after_seconds?: number;
    json?: Record<string, unknown>;
}

/**
 * Response for Submit sentiment analysis job
 */
export interface AnalysisPostResponse {
    /** The identifier. */
    id?: string;
    /** The status. */
    status?: string;
    /** The language. */
    language?: string;
    /** When created on. */
    created_on?: string;
    /** The callback URL address. */
    callback_url?: string;
    /** The type. */
    type?: string;
}

/**
 * Response for Get sentiment analysis job
 */
export interface AnalysisGetResponse {
    /** The identifier. */
    id?: string;
    /** The status. */
    status?: string;
    /** The language. */
    language?: string;
    /** When created on. */
    created_on?: string;
    /** The callback URL address. */
    callback_url?: string;
    /** The type. */
    type?: string;
}

/**
 * Response for Get sentiment analysis result
 */
export interface AnalysisResultGetResponse {
    messages?: Array<Record<string, unknown>>;
}

/**
 * Submit language identification job
 */
export interface IdentificationInput {
    /** The metadata. */
    metadata?: string;
    notification_config?: Record<string, unknown>;
    /** The delete after seconds. */
    delete_after_seconds?: number;
    source_config?: Record<string, unknown>;
}

/**
 * Response for Submit language identification job
 */
export interface IdentificationPostResponse {
    /** The identifier. */
    id?: string;
    /** The status. */
    status?: string;
    /** When created on. */
    created_on?: string;
    /** The type. */
    type?: string;
}

/**
 * Response for Get language identification job
 */
export interface IdentificationGetResponse {
    /** The identifier. */
    id?: string;
    /** The status. */
    status?: string;
    /** When created on. */
    created_on?: string;
    /** The type. */
    type?: string;
}

/**
 * Response for Get language identification result
 */
export interface IdentificationResultGetResponse {
    /** The top language. */
    top_language?: string;
    language_confidences?: Array<Record<string, unknown>>;
}

/**
 * Submit forced alignment job
 */
export interface AlignmentInput {
    /** The metadata. */
    metadata?: string;
    notification_config?: Record<string, unknown>;
    /** The delete after seconds. */
    delete_after_seconds?: number;
    source_config?: Record<string, unknown>;
    /** The transcript text. */
    transcript_text?: string;
    /** The language. */
    language?: string;
}

/**
 * Response for Submit forced alignment job
 */
export interface AlignmentPostResponse {
    /** The identifier. */
    id?: string;
    /** The status. */
    status?: string;
    /** When created on. */
    created_on?: string;
    /** The type. */
    type?: string;
}

/**
 * Response for Get forced alignment job
 */
export interface AlignmentGetResponse {
    /** The identifier. */
    id?: string;
    /** The status. */
    status?: string;
    /** When created on. */
    created_on?: string;
    /** The type. */
    type?: string;
}

/**
 * Response for Get forced alignment transcript
 */
export interface AlignmentTranscriptGetResponse {
    monologues?: Array<Record<string, unknown>>;
}
// #endregion Types

// #region Client

/**
 * Typed client for the revai connector.
 */
export class RevaiClient extends ConnectorClientBase {

    /**
     * Creates a new RevaiClient.
     * @param connectionRuntimeUrl The connection runtime URL from Azure Portal.
     * @param tokenProvider The token provider for authentication.
     * @param options Optional connector client options.
     */
    constructor(connectionRuntimeUrl: string, tokenProvider: TokenProvider, options?: ConnectorClientOptions) {
        super(connectionRuntimeUrl, tokenProvider, options);
    }

    public get connectorName(): string {
        return "revai";
    }

    /**
     * Get transcription job
     * @remarks Returns information about a transcription job.
     */
    public async transcriptionGetAsync(id: string, abortSignal?: AbortSignalLike): Promise<TranscriptionGetResponse> {
        const requestPath = `/speechtotext/v1/jobs/${id}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<TranscriptionGetResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as TranscriptionGetResponse;
    }

    /**
     * Delete transcription job
     * @remarks Deletes a transcription job. All data related to the job, such as input media and transcript, will be permanently deleted. A job can only be deleted once it's completed (either with success or failure).
     */
    public async transcriptionDeleteAsync(id: string, abortSignal?: AbortSignalLike): Promise<string> {
        const requestPath = `/speechtotext/v1/jobs/${id}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<string>("DELETE", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `DELETE ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as string;
    }

    /**
     * Get transcription job list
     * @remarks Retrieve a list of transcription jobs submitted within the last 30 days.
     */
    public async transcriptionsGetAsync(limit?: string, startingAfter?: string, abortSignal?: AbortSignalLike): Promise<Array<Record<string, unknown>>> {
        const queryParams: string[] = [];
        if (limit !== undefined) {
            queryParams.push(`limit=${encodeURIComponent(String(limit))}`);
        }
        if (startingAfter !== undefined) {
            queryParams.push(`starting_after=${encodeURIComponent(String(startingAfter))}`);
        }
        const requestPath = `/speechtotext/v1/jobs` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<Record<string, unknown>>>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<Record<string, unknown>>;
    }

    /**
     * Submit transcription job
     * @remarks Starts an asynchronous job to transcribe speech-to-text for a media file.
     */
    public async transcriptionAsync(input: TranscriptionInput, abortSignal?: AbortSignalLike): Promise<TranscriptionPostResponse> {
        const requestPath = `/speechtotext/v1/jobs`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<TranscriptionPostResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as TranscriptionPostResponse;
    }

    /**
     * Get transcript
     * @remarks Returns the transcript for a completed transcription job.
     */
    public async transcriptGetAsync(id: string, abortSignal?: AbortSignalLike): Promise<TranscriptGetResponse> {
        const requestPath = `/speechtotext/v1/jobs/${id}/transcript`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<TranscriptGetResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as TranscriptGetResponse;
    }

    /**
     * Get captions
     * @remarks Returns the caption output for a transcription job. We currently support the SubRip (SRT) format.
     */
    public async captionsGetAsync(id: string, abortSignal?: AbortSignalLike): Promise<string> {
        const requestPath = `/speechtotext/v1/jobs/${id}/captions`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<string>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as string;
    }

    /**
     * Get account
     * @remarks Get the developer's account information.
     */
    public async accountGetAsync(abortSignal?: AbortSignalLike): Promise<AccountGetResponse> {
        const requestPath = `/speechtotext/v1/account`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<AccountGetResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as AccountGetResponse;
    }

    /**
     * Get list of vocabularies
     * @remarks Retrieves a list of most recent custom vocabularies' processing information.
     */
    public async vocabulariesGetAsync(limit?: string, abortSignal?: AbortSignalLike): Promise<Array<Record<string, unknown>>> {
        const queryParams: string[] = [];
        if (limit !== undefined) {
            queryParams.push(`limit=${encodeURIComponent(String(limit))}`);
        }
        const requestPath = `/speechtotext/v1/vocabularies` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<Record<string, unknown>>>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<Record<string, unknown>>;
    }

    /**
     * Submit custom vocabulary
     * @remarks Submits a Custom Vocabulary for asynchronous processing.
     */
    public async vocabularyAsync(input: VocabularyInput, abortSignal?: AbortSignalLike): Promise<VocabularyPostResponse> {
        const requestPath = `/speechtotext/v1/vocabularies`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<VocabularyPostResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as VocabularyPostResponse;
    }

    /**
     * Get vocabulary
     * @remarks Retrieve a custom vocabulary processing information.
     */
    public async vocabularyGetAsync(id: string, abortSignal?: AbortSignalLike): Promise<VocabularyGetResponse> {
        const requestPath = `/speechtotext/v1/vocabularies/${id}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<VocabularyGetResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as VocabularyGetResponse;
    }

    /**
     * Delete vocabulary
     * @remarks Deletes a custom vocabulary. All data related to the custom vocabulary, such as webhooks and metadata, will be permanently deleted.
     */
    public async vocabularyDeleteAsync(id: string, abortSignal?: AbortSignalLike): Promise<string> {
        const requestPath = `/speechtotext/v1/vocabularies/${id}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<string>("DELETE", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `DELETE ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as string;
    }

    /**
     * Get topic extraction jobs
     * @remarks Retrieves a list of topic extraction jobs submitted within the last 30 days in reverse chronological order.
     */
    public async extractionsGetAsync(limit?: string, startingAfter?: string, abortSignal?: AbortSignalLike): Promise<Array<Record<string, unknown>>> {
        const queryParams: string[] = [];
        if (limit !== undefined) {
            queryParams.push(`limit=${encodeURIComponent(String(limit))}`);
        }
        if (startingAfter !== undefined) {
            queryParams.push(`starting_after=${encodeURIComponent(String(startingAfter))}`);
        }
        const requestPath = `/topic_extraction/v1/jobs` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<Record<string, unknown>>>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<Record<string, unknown>>;
    }

    /**
     * Submit topic extraction job
     * @remarks Starts an asynchronous job to extract topics from a transcript.
     */
    public async extractionAsync(input: ExtractionInput, abortSignal?: AbortSignalLike): Promise<ExtractionPostResponse> {
        const requestPath = `/topic_extraction/v1/jobs`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<ExtractionPostResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as ExtractionPostResponse;
    }

    /**
     * Get topic extraction job
     * @remarks Returns information about a topic extraction job.
     */
    public async extractionGetAsync(id: string, abortSignal?: AbortSignalLike): Promise<ExtractionGetResponse> {
        const requestPath = `/topic_extraction/v1/jobs/${id}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<ExtractionGetResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as ExtractionGetResponse;
    }

    /**
     * Delete topic extraction jo
     * @remarks Deletes a topic extraction job. All data related to the job will be permanently deleted.
     */
    public async extractionDeleteAsync(id: string, abortSignal?: AbortSignalLike): Promise<string> {
        const requestPath = `/topic_extraction/v1/jobs/${id}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<string>("DELETE", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `DELETE ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as string;
    }

    /**
     * Get topic extraction result
     * @remarks Returns the results for a completed topic extraction job.
     */
    public async extractionResultGetAsync(id: string, threshold?: string, abortSignal?: AbortSignalLike): Promise<ExtractionResultGetResponse> {
        const queryParams: string[] = [];
        if (threshold !== undefined) {
            queryParams.push(`threshold=${encodeURIComponent(String(threshold))}`);
        }
        const requestPath = `/topic_extraction/v1/jobs/${id}/result` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<ExtractionResultGetResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as ExtractionResultGetResponse;
    }

    /**
     * Get sentiment analysis jobs
     * @remarks Gets a list of sentiment analysis jobs submitted within the last 30 days in reverse chronological order.
     */
    public async analysisesGetAsync(limit?: string, startingAfter?: string, abortSignal?: AbortSignalLike): Promise<void> {
        const queryParams: string[] = [];
        if (limit !== undefined) {
            queryParams.push(`limit=${encodeURIComponent(String(limit))}`);
        }
        if (startingAfter !== undefined) {
            queryParams.push(`starting_after=${encodeURIComponent(String(startingAfter))}`);
        }
        const requestPath = `/sentiment_analysis/v1/jobs` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<void>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }
    }

    /**
     * Submit sentiment analysis job
     * @remarks Starts an asynchronous job to analyze sentiments in a transcript.
     */
    public async analysisAsync(input: AnalysisInput, abortSignal?: AbortSignalLike): Promise<AnalysisPostResponse> {
        const requestPath = `/sentiment_analysis/v1/jobs`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<AnalysisPostResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as AnalysisPostResponse;
    }

    /**
     * Get sentiment analysis job
     * @remarks Returns information about a sentiment analysis job.
     */
    public async analysisGetAsync(id: string, abortSignal?: AbortSignalLike): Promise<AnalysisGetResponse> {
        const requestPath = `/sentiment_analysis/v1/jobs/${id}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<AnalysisGetResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as AnalysisGetResponse;
    }

    /**
     * Delete sentiment analysis job
     * @remarks Deletes a sentiment analysis job. All data related to the job will be permanently deleted.
     */
    public async analysisDeleteAsync(id: string, abortSignal?: AbortSignalLike): Promise<string> {
        const requestPath = `/sentiment_analysis/v1/jobs/${id}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<string>("DELETE", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `DELETE ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as string;
    }

    /**
     * Get sentiment analysis result
     * @remarks Returns the results for a completed sentiment analysis job.
     */
    public async analysisResultGetAsync(id: string, filterFor?: string, abortSignal?: AbortSignalLike): Promise<AnalysisResultGetResponse> {
        const queryParams: string[] = [];
        if (filterFor !== undefined) {
            queryParams.push(`filter_for=${encodeURIComponent(String(filterFor))}`);
        }
        const requestPath = `/sentiment_analysis/v1/jobs/${id}/result` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<AnalysisResultGetResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as AnalysisResultGetResponse;
    }

    /**
     * Get language identification jobs
     * @remarks Retrieves a list of language identification jobs submitted within the last 30 days in reverse chronological order.
     */
    public async identificationsGetAsync(limit?: string, startingAfter?: string, abortSignal?: AbortSignalLike): Promise<Array<Record<string, unknown>>> {
        const queryParams: string[] = [];
        if (limit !== undefined) {
            queryParams.push(`limit=${encodeURIComponent(String(limit))}`);
        }
        if (startingAfter !== undefined) {
            queryParams.push(`starting_after=${encodeURIComponent(String(startingAfter))}`);
        }
        const requestPath = `/languageid/v1/jobs` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<Record<string, unknown>>>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<Record<string, unknown>>;
    }

    /**
     * Submit language identification job
     * @remarks Starts an asynchronous job to identify the most probable language in the audio.
     */
    public async identificationAsync(input: IdentificationInput, abortSignal?: AbortSignalLike): Promise<IdentificationPostResponse> {
        const requestPath = `/languageid/v1/jobs`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<IdentificationPostResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as IdentificationPostResponse;
    }

    /**
     * Get language identification job
     * @remarks Returns information about a language identification job.
     */
    public async identificationGetAsync(id: string, abortSignal?: AbortSignalLike): Promise<IdentificationGetResponse> {
        const requestPath = `/languageid/v1/jobs/${id}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<IdentificationGetResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as IdentificationGetResponse;
    }

    /**
     * Delete language identification job
     * @remarks Deletes a language identification job. All data related to the job will be permanently deleted.
     */
    public async identificationDeleteAsync(id: string, abortSignal?: AbortSignalLike): Promise<string> {
        const requestPath = `/languageid/v1/jobs/${id}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<string>("DELETE", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `DELETE ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as string;
    }

    /**
     * Get language identification result
     * @remarks Returns the results for a completed language identification job.
     */
    public async identificationResultGetAsync(id: string, abortSignal?: AbortSignalLike): Promise<IdentificationResultGetResponse> {
        const requestPath = `/languageid/v1/jobs/${id}/result`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<IdentificationResultGetResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as IdentificationResultGetResponse;
    }

    /**
     * Get forced alignment jobs
     * @remarks Retrieve a list of alignment jobs submitted within the last 30 days in reverse chronological order.
     */
    public async alignmentsGetAsync(limit?: string, startingAfter?: string, abortSignal?: AbortSignalLike): Promise<Array<Record<string, unknown>>> {
        const queryParams: string[] = [];
        if (limit !== undefined) {
            queryParams.push(`limit=${encodeURIComponent(String(limit))}`);
        }
        if (startingAfter !== undefined) {
            queryParams.push(`starting_after=${encodeURIComponent(String(startingAfter))}`);
        }
        const requestPath = `/alignment/v1/jobs` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<Record<string, unknown>>>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<Record<string, unknown>>;
    }

    /**
     * Submit forced alignment job
     * @remarks Starts an asynchronous job to obtain precise word timings for an audio file and text transcript.
     */
    public async alignmentAsync(input: AlignmentInput, abortSignal?: AbortSignalLike): Promise<AlignmentPostResponse> {
        const requestPath = `/alignment/v1/jobs`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<AlignmentPostResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as AlignmentPostResponse;
    }

    /**
     * Get forced alignment job
     * @remarks Returns information about a forced alignment job.
     */
    public async alignmentGetAsync(id: string, abortSignal?: AbortSignalLike): Promise<AlignmentGetResponse> {
        const requestPath = `/alignment/v1/jobs/${id}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<AlignmentGetResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as AlignmentGetResponse;
    }

    /**
     * Delete forced alignment job
     * @remarks Deletes a forced alignment job. All data related to the job will be permanently deleted.
     */
    public async alignmentDeleteAsync(id: string, abortSignal?: AbortSignalLike): Promise<string> {
        const requestPath = `/alignment/v1/jobs/${id}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<string>("DELETE", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `DELETE ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as string;
    }

    /**
     * Get forced alignment transcript
     * @remarks Returns the results for a completed forced alignment job.
     */
    public async alignmentTranscriptGetAsync(id: string, abortSignal?: AbortSignalLike): Promise<AlignmentTranscriptGetResponse> {
        const requestPath = `/alignment/v1/jobs/${id}/transcript`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<AlignmentTranscriptGetResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as AlignmentTranscriptGetResponse;
    }

}

// #endregion Client
