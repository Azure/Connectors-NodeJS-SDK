// MeetingroommapExtensions.ts - Auto-generated DirectClient SDK for TypeScript
// Do not edit this file directly.

import { ConnectorClientBase } from "../azureConnectors/clientBase.ts";
import { ConnectorException } from "../azureConnectors/connectorException.ts";
import { ConnectorClientOptions } from "../azureConnectors/options.ts";
import { TokenProvider } from "../azureConnectors/authentication.ts";

// #region Types

/**
 * Response for Get locations by image
 */
export interface GetCustomLocationsByImageNameResponse {
    /** customLocationCollection */
    customLocationCollection?: Array<Record<string, unknown>>;
    /** mapImage */
    mapImage?: Record<string, unknown>;
}

/**
 * Response for Get office locations on image
 */
export interface GetOfficeLocationsByImageResponse {
    /** image */
    image?: Record<string, unknown>;
    /** attachedOfficeLocations */
    attachedOfficeLocations?: Array<Record<string, unknown>>;
}

/**
 * Response for Get office location details
 */
export interface GetRoomWithPersonsDetailsResponse {
    /** officeLocationMap */
    officeLocationMap?: Record<string, unknown>;
    /** mapImage */
    mapImage?: Record<string, unknown>;
}

/**
 * Response for Get all meeting rooms by image
 */
export interface GetRoomsByImageNameResponse {
    /** meetingRoomCollection */
    meetingRoomCollection?: Record<string, unknown>;
    /** mapImage */
    mapImage?: Record<string, unknown>;
}

/**
 * Definition: GetCustomLocationsByCategoriesQuery
 */
export interface GetCustomLocationsByCategoriesQuery {
    categories?: Array<string>;
}

/**
 * Definition: GetCustomLocationListByImageNameQuery
 */
export interface GetCustomLocationListByImageNameQuery {
    /** The unique image name */
    imageName: string;
    categories?: Array<string>;
}

/**
 * Definition: AADMeetingRoom
 */
export interface AADMeetingRoom {
    name?: string;
    address?: string;
}

/**
 * Definition: AADMeetingRoomCollection
 */
export interface AADMeetingRoomCollection {
    rooms?: Array<AADMeetingRoom>;
}

/**
 * Definition: UserDetailsDTO
 */
export interface UserDetailsDTO {
    tenantId?: string;
    email?: string;
}
// #endregion Types

// #region Client

/**
 * Typed client for the meetingroommap connector.
 */
export class MeetingroommapClient extends ConnectorClientBase {

    /**
     * Creates a new MeetingroommapClient.
     * @param connectionRuntimeUrl The connection runtime URL from Azure Portal.
     * @param tokenProvider The token provider for authentication.
     * @param options Optional connector client options.
     */
    constructor(connectionRuntimeUrl: string, tokenProvider: TokenProvider, options?: ConnectorClientOptions) {
        super(connectionRuntimeUrl, tokenProvider, options);
    }

    public get connectorName(): string {
        return "meetingroommap";
    }

    /**
     * Get all custom locations
     * @remarks Get all custom locations
     */
    public async getCustomLocationsAsync(input: GetCustomLocationsByCategoriesQuery, abortSignal?: AbortSignal): Promise<void> {
        const requestPath = `/api/CustomLocations/GetCustomLocations`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<void>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }
    }

    /**
     * Get locations by image
     * @remarks Get all custom locations for a given image
     */
    public async getCustomLocationsByImageNameAsync(input: GetCustomLocationListByImageNameQuery, abortSignal?: AbortSignal): Promise<GetCustomLocationsByImageNameResponse> {
        const requestPath = `/api/CustomLocations/GetCustomLocationsByImageName`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<GetCustomLocationsByImageNameResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as GetCustomLocationsByImageNameResponse;
    }

    /**
     * GetCategories
     * @remarks Get custom categories
     */
    public async getCategoriesAsync(abortSignal?: AbortSignal): Promise<Array<string>> {
        const requestPath = `/api/CustomLocations/categories`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<string>>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<string>;
    }

    /**
     * Get location by id
     * @remarks Get location details including image url by the location id
     */
    public async locationDetailsAsync(locationId: string, abortSignal?: AbortSignal): Promise<void> {
        const requestPath = `/api/CustomLocations/${locationId}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<void>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }
    }

    /**
     * Search location by name
     * @remarks Search custom locations by name
     */
    public async searchLocationsAsync(locationName: string, category?: string, abortSignal?: AbortSignal): Promise<Array<Record<string, unknown>>> {
        const queryParams: string[] = [];
        if (category !== undefined) {
            queryParams.push(`Category=${encodeURIComponent(String(category))}`);
        }
        const requestPath = `/api/CustomLocations/findbyname/${locationName}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<Record<string, unknown>>>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<Record<string, unknown>>;
    }

    /**
     * Get image for location
     * @remarks Get image for specific custom location
     */
    public async getCustomLocationImageAsync(locationId: string, large?: string, abortSignal?: AbortSignal): Promise<Blob> {
        const queryParams: string[] = [];
        if (large !== undefined) {
            queryParams.push(`Large=${encodeURIComponent(String(large))}`);
        }
        const requestPath = `/api/CustomLocations/createimage/${locationId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Blob>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Blob;
    }

    /**
     * Get all images
     * @remarks Get list of all floorplans/images
     */
    public async imagesAsync(abortSignal?: AbortSignal): Promise<Array<Record<string, unknown>>> {
        const requestPath = `/api/MapImage/thumbnails`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<Record<string, unknown>>>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<Record<string, unknown>>;
    }

    /**
     * Get image for meeting room
     * @remarks Get image for a specific meeting room
     */
    public async getMeetingRoomImageAsync(roomName: string, large?: string, abortSignal?: AbortSignal): Promise<Blob> {
        const queryParams: string[] = [];
        if (large !== undefined) {
            queryParams.push(`Large=${encodeURIComponent(String(large))}`);
        }
        const requestPath = `/api/MapImage/create/${roomName}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Blob>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Blob;
    }

    /**
     * Get next meetings
     * @remarks Get the next meetings for the current user
     */
    public async nextMeetingsAsync(meetingCount: string, abortSignal?: AbortSignal): Promise<void> {
        const requestPath = `/api/MapImage/meetings/${meetingCount}/roomdetails`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<void>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }
    }

    /**
     * Get meetingroom details
     * @remarks Get meetingroom details
     */
    public async getMeetingRoomDetailsAsync(roomName: string, abortSignal?: AbortSignal): Promise<void> {
        const requestPath = `/api/MapImage/roomdetails_v2/${roomName}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<void>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }
    }

    /**
     * Get all office locations
     * @remarks Get all office locations
     */
    public async getOfficeLocationsAsync(abortSignal?: AbortSignal): Promise<void> {
        const requestPath = `/api/officelocations`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<void>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }
    }

    /**
     * Search coworker
     * @remarks Search coworker by name/email
     */
    public async searchCoworkersAsync(personSearch: string, abortSignal?: AbortSignal): Promise<Array<Record<string, unknown>>> {
        const requestPath = `/api/officelocations/searchCoworkers/${personSearch}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<Record<string, unknown>>>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<Record<string, unknown>>;
    }

    /**
     * Get office locations on image
     * @remarks Get all office locations mapped to a specific image
     */
    public async getOfficeLocationsByImageAsync(imageName: string, abortSignal?: AbortSignal): Promise<GetOfficeLocationsByImageResponse> {
        const requestPath = `/api/officelocations/bymapimage/${imageName}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<GetOfficeLocationsByImageResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as GetOfficeLocationsByImageResponse;
    }

    /**
     * Get office location details
     * @remarks Get office location details including list of people for that office location
     */
    public async getRoomWithPersonsDetailsAsync(officeLocationName: string, inludeUserInfo?: string, abortSignal?: AbortSignal): Promise<GetRoomWithPersonsDetailsResponse> {
        const queryParams: string[] = [];
        if (inludeUserInfo !== undefined) {
            queryParams.push(`InludeUserInfo=${encodeURIComponent(String(inludeUserInfo))}`);
        }
        const requestPath = `/api/officelocations/mapimagewithpersoninfo/${officeLocationName}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<GetRoomWithPersonsDetailsResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as GetRoomWithPersonsDetailsResponse;
    }

    /**
     * Get image of office location
     * @remarks Get image of specific office location
     */
    public async getOfficeLocationImageAsync(officeLocationName: string, large?: string, abortSignal?: AbortSignal): Promise<Blob> {
        const queryParams: string[] = [];
        if (large !== undefined) {
            queryParams.push(`Large=${encodeURIComponent(String(large))}`);
        }
        const requestPath = `/api/officelocationimage/create/${officeLocationName}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Blob>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Blob;
    }

    /**
     * Get all rooms
     * @remarks Get all rooms (defined in Azure AD as meeting rooms)
     */
    public async getRoomsAsync(abortSignal?: AbortSignal): Promise<AADMeetingRoomCollection> {
        const requestPath = `/api/rooms`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<AADMeetingRoomCollection>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as AADMeetingRoomCollection;
    }

    /**
     * Search for meeting room
     * @remarks Get rooms by name search (from Azure AD meeting rooms)
     */
    public async searchMeetingRoomsAsync(name: string, abortSignal?: AbortSignal): Promise<AADMeetingRoomCollection> {
        const requestPath = `/api/rooms/findbyname/${name}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<AADMeetingRoomCollection>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as AADMeetingRoomCollection;
    }

    /**
     * Get meeting rooms lists
     * @remarks Get meeting rooms lists as defined in Azure AD
     */
    public async roomListsAsync(abortSignal?: AbortSignal): Promise<AADMeetingRoomCollection> {
        const requestPath = `/api/rooms/lists`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<AADMeetingRoomCollection>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as AADMeetingRoomCollection;
    }

    /**
     * Get meeting rooms by list
     * @remarks Get all meeting rooms from specific list name
     */
    public async roomsByListAddressAsync(meetingRoomListAddress: string, abortSignal?: AbortSignal): Promise<AADMeetingRoomCollection> {
        const requestPath = `/api/rooms/${meetingRoomListAddress}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<AADMeetingRoomCollection>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as AADMeetingRoomCollection;
    }

    /**
     * Get all meeting rooms by image
     * @remarks Get all meeting rooms mapped to a specific image
     */
    public async getRoomsByImageNameAsync(imageName: string, abortSignal?: AbortSignal): Promise<GetRoomsByImageNameResponse> {
        const requestPath = `/api/rooms/GetRoomsByImageName/${imageName}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<GetRoomsByImageNameResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as GetRoomsByImageNameResponse;
    }

}

// #endregion Client
