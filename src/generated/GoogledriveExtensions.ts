// GoogledriveExtensions.ts - Auto-generated DirectClient SDK for TypeScript
// Do not edit this file directly.

import type { TokenCredential } from "@azure/core-auth";
import { ConnectorClientBase } from "../azureConnectors/clientBase.ts";
import type { ConnectorPagedAsyncIterableIterator } from "../azureConnectors/clientBase.ts";
import type { ConnectorClientOptions, ConnectorOperationOptions } from "../azureConnectors/options.ts";

// #region Types

/**
 * Update file
 */
export type UpdateFileInput = string;

/**
 * Create file
 */
export type CreateFileInput = string;

/**
 * Definition: DataSetsMetadata
 */
export interface DataSetsMetadata {
    tabular?: TabularDataSetsMetadata;
    blob?: BlobDataSetsMetadata;
}

/**
 * Definition: TabularDataSetsMetadata
 */
export interface TabularDataSetsMetadata {
    /** Dataset source */
    source?: string;
    /** Dataset display name */
    displayName?: string;
    /** Dataset url encoding */
    urlEncoding?: string;
    /** Table display name */
    tableDisplayName?: string;
    /** Table plural display name */
    tablePluralName?: string;
}

/**
 * Definition: BlobDataSetsMetadata
 */
export interface BlobDataSetsMetadata {
    /** Blob dataset source */
    source?: string;
    /** Blob dataset display name */
    displayName?: string;
    /** Blob dataset url encoding */
    urlEncoding?: string;
}

/**
 * Definition: BlobMetadata
 */
export interface BlobMetadata {
    /** The unique id of the file or folder. */
    Id?: string;
    /** The name of the file or folder. */
    Name?: string;
    /** The display name of the file or folder. */
    DisplayName?: string;
    /** The path of the file or folder. */
    Path?: string;
    /** The unique id of the parent folder of the file or folder. */
    FolderId?: string;
    /** The current path of the parent folder of the file or folder. */
    FolderPath?: string;
    /** The date and time the file or folder was last modified. */
    LastModified?: string;
    /** The size of the file or folder. */
    Size?: number;
    /** The media type of the file or folder. */
    MediaType?: string;
    /** A boolean value (true, false) to indicate whether or not the blob is a folder. */
    IsFolder?: boolean;
    /** The etag of the file or folder. */
    ETag?: string;
    /** The filelocator of the file or folder. */
    FileLocator?: string;
    /** The user who last modified the file or folder. */
    LastModifiedBy?: string;
}

/**
 * Definition: Object
 */
export interface ObjectEntity {
    [key: string]: unknown;
}

/**
 * Definition: TableMetadata
 */
export interface TableMetadata {
    /** Table name */
    name?: string;
    /** Table title */
    title?: string;
    /** Table permission */
    "x-ms-permission"?: string;
    "x-ms-capabilities"?: TableCapabilitiesMetadata;
    schema?: Record<string, unknown>;
    referencedEntities?: Record<string, unknown>;
    /** Url link */
    webUrl?: string;
}

/**
 * Definition: TableCapabilitiesMetadata
 */
export interface TableCapabilitiesMetadata {
    sortRestrictions?: TableSortRestrictionsMetadata;
    filterRestrictions?: TableFilterRestrictionsMetadata;
    selectRestrictions?: TableSelectRestrictionsMetadata;
    /** Server paging restrictions */
    isOnlyServerPagable?: boolean;
    /** List of supported filter capabilities */
    filterFunctionSupport?: Array<string>;
    /** List of supported server-driven paging capabilities */
    serverPagingOptions?: Array<string>;
}

/**
 * Definition: TableSortRestrictionsMetadata
 */
export interface TableSortRestrictionsMetadata {
    /** Indicates whether this table has sortable columns */
    sortable?: boolean;
    /** List of unsortable properties */
    unsortableProperties?: Array<string>;
    /** List of properties which support ascending order only */
    ascendingOnlyProperties?: Array<string>;
}

/**
 * Definition: TableFilterRestrictionsMetadata
 */
export interface TableFilterRestrictionsMetadata {
    /** Indicates whether this table has filterable columns */
    filterable?: boolean;
    /** List of non filterable properties */
    nonFilterableProperties?: Array<string>;
    /** List of required properties */
    requiredProperties?: Array<string>;
}

/**
 * Definition: TableSelectRestrictionsMetadata
 */
export interface TableSelectRestrictionsMetadata {
    /** Indicates whether this table has selectable columns */
    selectable?: boolean;
}

/**
 * Definition: TablesList
 */
export interface TablesList {
    /** List of Tables */
    value?: Array<Table>;
}

/**
 * Definition: Table
 */
export interface Table {
    /** The name of the table. The name is used at runtime. */
    Name?: string;
    /** The display name of the table. */
    DisplayName?: string;
    /** Additional table properties provided by the connector to the clients. */
    DynamicProperties?: Record<string, unknown>;
}

/**
 * Definition: Item
 */
export interface Item {
    dynamicProperties?: Record<string, unknown>;
}

/**
 * Definition: ItemsList
 */
export interface ItemsList {
    /** List of Items */
    value?: Array<Item>;
}

/**
 * Definition: DataSetsList
 */
export interface DataSetsList {
    /** List of datasets */
    value?: Array<DataSet>;
}

/**
 * Definition: DataSet
 */
export interface DataSet {
    /** Dataset name */
    Name?: string;
    /** Dataset display name */
    DisplayName?: string;
    /** Pass-through Native Queries */
    query?: Array<PassThroughNativeQuery>;
}

/**
 * Definition: Procedure
 */
export interface Procedure {
    /** Procedure name */
    Name?: string;
    /** Procedure display name */
    DisplayName?: string;
}

/**
 * Definition: PassThroughNativeQuery
 */
export interface PassThroughNativeQuery {
    /** Query language */
    Language?: string;
}
// #endregion Types

// #region Client

/**
 * Typed client for the googledrive connector.
 */
export class GoogledriveClient extends ConnectorClientBase {

    /**
     * Creates a new GoogledriveClient.
     * @param connectionRuntimeUrl The connection runtime URL from Azure Portal.
     * @param credential The credential used for authentication.
     * @param options Optional connector client options.
     */
    constructor(connectionRuntimeUrl: string, credential: TokenCredential, options?: ConnectorClientOptions) {
        super(connectionRuntimeUrl, credential, options);
    }

    public get connectorName(): string {
        return "googledrive";
    }

    /**
     * Get file metadata using id
     * @remarks Retrieves file metadata from Google Drive using id
     */
    public async getFileMetadata(id: string, options: ConnectorOperationOptions = {}): Promise<BlobMetadata> {
        const requestPath = `/datasets/default/files/${id}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<BlobMetadata>("Googledrive.getFileMetadata", "GetFileMetadata", "GET", requestUrl, undefined, options);

        return httpResponse.value as BlobMetadata;
    }

    /**
     * Update file
     * @remarks Updates a file in Google Drive
     */
    public async updateFile(input: UpdateFileInput, id: string, options: ConnectorOperationOptions = {}): Promise<BlobMetadata> {
        const requestPath = `/datasets/default/files/${id}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<BlobMetadata>("Googledrive.updateFile", "UpdateFile", "PUT", requestUrl, input, options);

        return httpResponse.value as BlobMetadata;
    }

    /**
     * Delete file
     * @remarks Deletes a file from Google Drive
     */
    public async deleteFile(id: string, options: ConnectorOperationOptions = {}): Promise<void> {
        const requestPath = `/datasets/default/files/${id}`;
        const requestUrl = this.resolveUrl(requestPath);
        await this.sendWithTracingAsync<void>("Googledrive.deleteFile", "DeleteFile", "DELETE", requestUrl, undefined, options);
    }

    /**
     * Get file metadata using path
     * @remarks Retrieves file metadata from Google Drive using path
     */
    public async getFileMetadataByPath(path?: string, queryParametersSingleEncoded?: string, options: ConnectorOperationOptions = {}): Promise<BlobMetadata> {
        const queryParams: string[] = [];
        if (path !== undefined) {
            queryParams.push(`path=${encodeURIComponent(String(path))}`);
        }
        if (queryParametersSingleEncoded !== undefined) {
            queryParams.push(`queryParametersSingleEncoded=${encodeURIComponent(String(queryParametersSingleEncoded))}`);
        }
        const requestPath = `/datasets/default/GetFileByPath` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<BlobMetadata>("Googledrive.getFileMetadataByPath", "GetFileMetadataByPath", "GET", requestUrl, undefined, options);

        return httpResponse.value as BlobMetadata;
    }

    /**
     * Get file content using path
     * @remarks Retrieves file content from Google Drive using path
     */
    public async getFileContentByPath(path?: string, inferContentType?: string, queryParametersSingleEncoded?: string, options: ConnectorOperationOptions = {}): Promise<Blob> {
        const queryParams: string[] = [];
        if (path !== undefined) {
            queryParams.push(`path=${encodeURIComponent(String(path))}`);
        }
        if (inferContentType !== undefined) {
            queryParams.push(`inferContentType=${encodeURIComponent(String(inferContentType))}`);
        }
        if (queryParametersSingleEncoded !== undefined) {
            queryParams.push(`queryParametersSingleEncoded=${encodeURIComponent(String(queryParametersSingleEncoded))}`);
        }
        const requestPath = `/datasets/default/GetFileContentByPath` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<Blob>("Googledrive.getFileContentByPath", "GetFileContentByPath", "GET", requestUrl, undefined, options);

        return httpResponse.value as Blob;
    }

    /**
     * Get file content using id
     * @remarks Retrieves file content from Google Drive using id
     */
    public async getFileContent(id: string, inferContentType?: string, options: ConnectorOperationOptions = {}): Promise<Blob> {
        const queryParams: string[] = [];
        if (inferContentType !== undefined) {
            queryParams.push(`inferContentType=${encodeURIComponent(String(inferContentType))}`);
        }
        const requestPath = `/datasets/default/files/${id}/content` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<Blob>("Googledrive.getFileContent", "GetFileContent", "GET", requestUrl, undefined, options);

        return httpResponse.value as Blob;
    }

    /**
     * Copy file
     * @remarks Copies a file on Google Drive
     */
    public async copyFile(source?: string, destination?: string, overwrite?: string, queryParametersSingleEncoded?: string, options: ConnectorOperationOptions = {}): Promise<BlobMetadata> {
        const queryParams: string[] = [];
        if (source !== undefined) {
            queryParams.push(`source=${encodeURIComponent(String(source))}`);
        }
        if (destination !== undefined) {
            queryParams.push(`destination=${encodeURIComponent(String(destination))}`);
        }
        if (overwrite !== undefined) {
            queryParams.push(`overwrite=${encodeURIComponent(String(overwrite))}`);
        }
        if (queryParametersSingleEncoded !== undefined) {
            queryParams.push(`queryParametersSingleEncoded=${encodeURIComponent(String(queryParametersSingleEncoded))}`);
        }
        const requestPath = `/datasets/default/copyFile` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<BlobMetadata>("Googledrive.copyFile", "CopyFile", "POST", requestUrl, undefined, options);

        return httpResponse.value as BlobMetadata;
    }

    /**
     * List files in folder
     * @remarks List files in a Google Drive folder
     */
    public listFolder(id: string, options: ConnectorOperationOptions = {}): ConnectorPagedAsyncIterableIterator<BlobMetadata> {
        const requestPath = `/datasets/default/folders/${id}`;
        return this.createPageable<Array<BlobMetadata>, BlobMetadata>(
            requestPath,
            async (requestUrl) => {
                const httpResponse = await this.sendWithTracingAsync<Array<BlobMetadata>>("Googledrive.listFolder", "ListFolder", "GET", requestUrl, undefined, options);

                return httpResponse.value as Array<BlobMetadata>;
            },
            null,
            undefined,
        );
    }

    /**
     * List files in root folder
     * @remarks Lists files in the Google Drive root folder
     */
    public listRootFolder(options: ConnectorOperationOptions = {}): ConnectorPagedAsyncIterableIterator<BlobMetadata> {
        const requestPath = `/datasets/default/folders`;
        return this.createPageable<Array<BlobMetadata>, BlobMetadata>(
            requestPath,
            async (requestUrl) => {
                const httpResponse = await this.sendWithTracingAsync<Array<BlobMetadata>>("Googledrive.listRootFolder", "ListRootFolder", "GET", requestUrl, undefined, options);

                return httpResponse.value as Array<BlobMetadata>;
            },
            null,
            undefined,
        );
    }

    /**
     * Create file
     * @remarks Uploads a file to a Google Drive folder selected by its identifier
     */
    public async createFile(input: CreateFileInput, folderId?: string, name?: string, queryParametersSingleEncoded?: string, options: ConnectorOperationOptions = {}): Promise<BlobMetadata> {
        const queryParams: string[] = [];
        if (folderId !== undefined) {
            queryParams.push(`folderId=${encodeURIComponent(String(folderId))}`);
        }
        if (name !== undefined) {
            queryParams.push(`name=${encodeURIComponent(String(name))}`);
        }
        if (queryParametersSingleEncoded !== undefined) {
            queryParams.push(`queryParametersSingleEncoded=${encodeURIComponent(String(queryParametersSingleEncoded))}`);
        }
        const requestPath = `/datasets/default/v2/files` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<BlobMetadata>("Googledrive.createFile", "CreateFileV2", "POST", requestUrl, input, options);

        return httpResponse.value as BlobMetadata;
    }

    /**
     * Extract archive to folder
     * @remarks Extracts an archive file into a folder in Google Drive (example: .zip)
     */
    public async extractFolder(source?: string, destination?: string, overwrite?: string, queryParametersSingleEncoded?: string, options: ConnectorOperationOptions = {}): Promise<Array<BlobMetadata>> {
        const queryParams: string[] = [];
        if (source !== undefined) {
            queryParams.push(`source=${encodeURIComponent(String(source))}`);
        }
        if (destination !== undefined) {
            queryParams.push(`destination=${encodeURIComponent(String(destination))}`);
        }
        if (overwrite !== undefined) {
            queryParams.push(`overwrite=${encodeURIComponent(String(overwrite))}`);
        }
        if (queryParametersSingleEncoded !== undefined) {
            queryParams.push(`queryParametersSingleEncoded=${encodeURIComponent(String(queryParametersSingleEncoded))}`);
        }
        const requestPath = `/datasets/default/extractFolderV2` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<Array<BlobMetadata>>("Googledrive.extractFolder", "ExtractFolderV2", "POST", requestUrl, undefined, options);

        return httpResponse.value as Array<BlobMetadata>;
    }

}

// #endregion Client
