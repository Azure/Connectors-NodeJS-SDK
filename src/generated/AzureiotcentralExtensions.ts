// AzureiotcentralExtensions.ts - Auto-generated DirectClient SDK for TypeScript
// Do not edit this file directly.

import type { TokenCredential } from "@azure/core-auth";
import { ConnectorClientBase } from "../azureConnectors/clientBase.ts";
import type { ConnectorPagedAsyncIterableIterator } from "../azureConnectors/clientBase.ts";
import type { ConnectorClientOptions, ConnectorOperationOptions } from "../azureConnectors/options.ts";

// #region Types

/**
 * Definition: Action
 */
export interface Action {
    /** Unique ID of the action. */
    id?: string;
    /** Display name of the action. */
    displayName?: string;
    /** Type of the action. */
    type: string;
    /** Unique IDs of the rules that are tied to the action. */
    rules: Array<string>;
}

/**
 * Definition: EmailAction
 */
export interface EmailAction {
    /** Unique ID of the email action. */
    id?: string;
    /** Display name of the email action. */
    displayName?: string;
    /** Type of the email action. */
    type: string;
    /** Unique IDs of the rules that are tied to the email action. */
    rules: Array<string>;
}

/**
 * Definition: WebhookAction
 */
export interface WebhookAction {
    /** Unique ID of the webhook action. */
    id?: string;
    /** Display name of the webhook action. */
    displayName?: string;
    /** Type of the webhook action. */
    type: string;
    /** Unique IDs of the rules that are tied to the webhook action. */
    rules: Array<string>;
    /** Callback URL of the webhook. */
    url: string;
}

/**
 * Definition: PowerAutomateAction
 */
export interface PowerAutomateAction {
    /** Unique ID of the Power Automate action. */
    id?: string;
    /** Display name of the Power Automate action. */
    displayName?: string;
    /** Type of the Power Automate action. */
    type: string;
    /** Unique IDs of the rules that are tied to the Power Automate action. */
    rules: Array<string>;
}

/**
 * Definition: LogicAppsAction
 */
export interface LogicAppsAction {
    /** Unique ID of the Logic Apps action. */
    id?: string;
    /** Display name of the Logic Apps action. */
    displayName?: string;
    /** Type of the Logic Apps action. */
    type: string;
    /** Unique IDs of the rules that are tied to the Logic Apps action. */
    rules: Array<string>;
}

/**
 * Definition: ActionGroupsAction
 */
export interface ActionGroupsAction {
    /** Unique ID of the Action Groups action. */
    id?: string;
    /** Display name of the Action Groups action. */
    displayName?: string;
    /** Type of the Action Groups action. */
    type: string;
    /** Unique IDs of the rules that are tied to the Action Groups action. */
    rules: Array<string>;
}

/**
 * Definition: Application
 */
export interface Application {
    /** Unique ID of the application. */
    id?: string;
    /** Display name of the application. */
    displayName?: string;
    /** The URL subdomain of the application. */
    subdomain: string;
    /** The URL host of the application. */
    host: string;
}

/**
 * Definition: ApplicationCollection
 */
export interface ApplicationCollection {
    /** The collection of applications. */
    value: Array<Application>;
    /** URL to get the next page of applications. */
    nextLink?: string;
}

/**
 * Definition: Device
 */
export interface Device {
    /** Unique ID of the device. */
    id?: string;
    /** ETag used to prevent conflict in device updates. */
    etag?: string;
    /** Display name of the device. */
    displayName?: string;
    /** The device template definition for the device. */
    instanceOf?: string;
    /** Whether the device is simulated. */
    simulated?: boolean;
    /** Whether the device has been approved to connect to IoT Central. */
    approved?: boolean;
    /** Whether resources have been allocated for the device. */
    provisioned?: boolean;
}

/**
 * Definition: DeviceV1
 */
export interface DeviceV1 {
    /** Unique ID of the device. */
    id?: string;
    /** ETag used to prevent conflict in device updates. */
    etag?: string;
    /** Display name of the device. */
    displayName?: string;
    /** The device template definition for the device. */
    template?: string;
    /** Whether the device is simulated. */
    simulated?: boolean;
    /** Whether the device connection to IoT Central has been enabled. */
    enabled?: boolean;
    /** Whether resources have been allocated for the device. */
    provisioned?: boolean;
}

/**
 * Definition: DeviceV2
 */
export interface DeviceV2 {
    /** Unique ID of the device. */
    id?: string;
    /** ETag used to prevent conflict in device updates. */
    etag?: string;
    /** Display name of the device. */
    displayName?: string;
    /** The device template definition for the device. */
    template?: string;
    /** Whether the device is simulated. */
    simulated?: boolean;
    /** Whether the device connection to IoT Central has been enabled. */
    enabled?: boolean;
    /** List of organizations of the device, only one organization is supported today, multiple organizations will be supported soon. */
    organizations?: Array<string>;
    /** Whether resources have been allocated for the device. */
    provisioned?: boolean;
}

/**
 * Definition: DeviceCollection
 */
export interface DeviceCollection {
    /** The collection of devices. */
    value: Array<DeviceV1>;
    /** URL to get the next page of devices. */
    nextLink?: string;
}

/**
 * Definition: DeviceCloudProperties
 */
export interface DeviceCloudProperties {
    [key: string]: unknown;
}

/**
 * Definition: DeviceCommand
 */
export interface DeviceCommand {
    /** The request ID of the device command execution. */
    id?: string;
    /** The payload for the device command. */
    request?: unknown;
    /** The payload of the device command response. */
    response?: unknown;
    /** Connection timeout in seconds to wait for a disconnected device to come online. Defaults to 0 seconds. */
    connectionTimeout?: number;
    /** Response timeout in seconds to wait for a command completion on a device. Defaults to 30 seconds. */
    responseTimeout?: number;
    /** The status code of the device command response. */
    responseCode?: number;
}

/**
 * Definition: DeviceCommandV1
 */
export interface DeviceCommandV1 {
    /** The request ID of the device command execution. */
    id?: string;
    /** The payload for the device command. */
    request?: unknown;
    /** The payload of the device command response. */
    response?: unknown;
    /** Connection timeout in seconds to wait for a disconnected device to come online. Defaults to 0 seconds. */
    connectionTimeout?: number;
    /** Response timeout in seconds to wait for a command completion on a device. Defaults to 30 seconds. */
    responseTimeout?: number;
    /** The status code of the device command response. */
    responseCode?: number;
}

/**
 * Definition: DeviceComponentCommand
 */
export interface DeviceComponentCommand {
    /** The request ID of the device command execution. */
    id?: string;
    /** The payload for the device command. */
    request?: unknown;
    /** The payload of the device command response. */
    response?: unknown;
    /** Connection timeout in seconds to wait for a disconnected device to come online. Defaults to 0 seconds. */
    connectionTimeout?: number;
    /** Response timeout in seconds to wait for a command completion on a device. Defaults to 30 seconds. */
    responseTimeout?: number;
    /** The status code of the device command response. */
    responseCode?: number;
}

/**
 * Definition: DeviceModuleCommand
 */
export interface DeviceModuleCommand {
    /** The request ID of the device command execution. */
    id?: string;
    /** The payload for the device command. */
    request?: unknown;
    /** The payload of the device command response. */
    response?: unknown;
    /** Connection timeout in seconds to wait for a disconnected device to come online. Defaults to 0 seconds. */
    connectionTimeout?: number;
    /** Response timeout in seconds to wait for a command completion on a device. Defaults to 30 seconds. */
    responseTimeout?: number;
    /** The status code of the device command response. */
    responseCode?: number;
}

/**
 * Definition: DeviceModuleComponentCommand
 */
export interface DeviceModuleComponentCommand {
    /** The request ID of the device command execution. */
    id?: string;
    /** The payload for the device command. */
    request?: unknown;
    /** The payload of the device command response. */
    response?: unknown;
    /** Connection timeout in seconds to wait for a disconnected device to come online. Defaults to 0 seconds. */
    connectionTimeout?: number;
    /** Response timeout in seconds to wait for a command completion on a device. Defaults to 30 seconds. */
    responseTimeout?: number;
    /** The status code of the device command response. */
    responseCode?: number;
}

/**
 * Definition: DeviceGroup
 */
export interface DeviceGroup {
    /** Unique ID of the device group. */
    id?: string;
    /** Display name of the device group. */
    displayName: string;
    /** Query defining which devices should be in this group. */
    filter: string;
    /** Short summary of device group. */
    description?: string;
    /** ETag used to prevent conflict in device group updates. */
    etag?: string;
    /** List of organizations of the device group, only one organization is supported today, multiple organizations will be supported soon. */
    organizations?: Array<string>;
}

/**
 * Definition: DeviceGroupCollection
 */
export interface DeviceGroupCollection {
    /** The collection of device groups. */
    value: Array<DeviceGroup>;
    /** URL to get the next page of device groups. */
    nextLink?: string;
}

/**
 * Definition: DeviceGroupDeviceCollection
 */
export interface DeviceGroupDeviceCollection {
    /** The collection of devices belonging to the device group. */
    value: Array<DeviceV1>;
    /** URL to get the next page of devices in the group. */
    nextLink?: string;
}

/**
 * Definition: DeviceProperties
 */
export interface DeviceProperties {
    [key: string]: unknown;
}

/**
 * Definition: ModuleProperties
 */
export interface ModuleProperties {
    [key: string]: unknown;
}

/**
 * Definition: DeviceRelationship
 */
export interface DeviceRelationship {
    /** Unique ID of the device relationship. */
    id?: string;
    /** The device ID of the source (child) device. */
    target: string;
}

/**
 * Definition: DeviceRelationshipStatic
 */
export interface DeviceRelationshipStatic {
    /** Unique ID of the device relationship. */
    id?: string;
    /** The device ID of the source (parent) device. */
    source?: string;
    /** The device ID of the source (child) device. */
    target?: string;
}

/**
 * Definition: DeviceRelationshipCollection
 */
export interface DeviceRelationshipCollection {
    /** The collection of device relationships. */
    value: Array<DeviceRelationshipStatic>;
    /** URL to get the next page of device relationships. */
    nextLink?: string;
}

/**
 * Definition: DeviceTelemetry
 */
export interface DeviceTelemetry {
    /** The last known value of this device telemetry. */
    value?: unknown;
    /** String-formatted date representing the time when the telemetry value was sent. */
    timestamp?: string;
}

/**
 * Definition: DeviceComponentTelemetry
 */
export interface DeviceComponentTelemetry {
    /** The last known value of this device telemetry. */
    value?: unknown;
    /** String-formatted date representing the time when the telemetry value was sent. */
    timestamp?: string;
}

/**
 * Definition: DeviceModuleTelemetry
 */
export interface DeviceModuleTelemetry {
    /** The last known value of this device telemetry. */
    value?: unknown;
    /** String-formatted date representing the time when the telemetry value was sent. */
    timestamp?: string;
}

/**
 * Definition: DeviceModuleComponentTelemetry
 */
export interface DeviceModuleComponentTelemetry {
    /** The last known value of this device telemetry. */
    value?: unknown;
    /** String-formatted date representing the time when the telemetry value was sent. */
    timestamp?: string;
}

/**
 * Definition: DeviceTemplate
 */
export interface DeviceTemplate {
    /** Unique ID of the device template. */
    "@id"?: string;
    /** ETag used to prevent conflict in device template updates. */
    etag?: string;
    /** The JSON-LD types of this device template. */
    "@type": Array<string>;
    /** Display name of the device template. */
    displayName?: string;
    /** Detailed description of the device template. */
    description?: string;
    /** The capability model utilized by this device template. */
    capabilityModel?: unknown;
}

/**
 * Definition: DeviceTemplateCollection
 */
export interface DeviceTemplateCollection {
    /** The collection of device templates. */
    value: Array<DeviceTemplate>;
    /** URL to get the next page of device templates. */
    nextLink?: string;
}

/**
 * Definition: PropertyJobData
 */
export interface PropertyJobData {
    /** Data related to the operation being performed by this job. All entries must be of the same type. */
    data: Array<Record<string, unknown>>;
}

/**
 * Definition: CommandJobData
 */
export interface CommandJobData {
    /** Data related to the operation being performed by this job. All entries must be of the same type. */
    data: Array<Record<string, unknown>>;
}

/**
 * Definition: CloudPropertyJobData
 */
export interface CloudPropertyJobData {
    /** The collection of job data. */
    data: Array<Record<string, unknown>>;
}

/**
 * Definition: DeviceTemplateMigrationJobData
 */
export interface DeviceTemplateMigrationJobData {
    /** The collection of job data. */
    data: Array<Record<string, unknown>>;
}

/**
 * Definition: JobDeviceStatus
 */
export interface JobDeviceStatus {
    /** ID of the device whose job status is being provided. */
    id?: string;
    /** Indicates whether the job is starting, running, etc. for the given device. */
    status?: string;
}

/**
 * Definition: JobDeviceStatusCollection
 */
export interface JobDeviceStatusCollection {
    /** The collection of job device statuses. */
    value: Array<JobDeviceStatus>;
    /** URL to get the next page of job device statuses. */
    nextLink?: string;
}

/**
 * Definition: JobBatch
 */
export interface JobBatch {
    /** Whether batching is done on a specified number of devices or a percentage of the total devices. */
    type: string;
    /** The number or percentage of devices on which batching is done. */
    value: number;
}

/**
 * Definition: JobCancellationThreshold
 */
export interface JobCancellationThreshold {
    /** Whether the cancellation threshold is per a specified number of devices or a percentage of the total devices. */
    type: string;
    /** The number or percentage of devices on which the cancellation threshold is applied. */
    value: number;
    /** Whether the cancellation threshold applies per-batch or to the overall job. */
    batch?: boolean;
}

/**
 * Definition: Job
 */
export interface Job {
    /** Unique ID of the job. */
    id?: string;
    /** Display name of the job. */
    displayName: string;
    /** Detailed description of the job. */
    description?: string;
    /** The ID of the device group on which to execute the job. */
    group: string;
    /** The batching configuration for the job. */
    batch?: JobBatch;
    /** The cancellation threshold for the job. */
    cancellationThreshold?: JobCancellationThreshold;
    /** List of organizations of the job, only one organization is supported today, multiple organizations will be supported soon. */
    organizations?: Array<string>;
    /** Id of the scheduled job definition that created this job. */
    scheduledJobId?: string;
}

/**
 * Definition: JobStatic
 */
export interface JobStatic {
    /** Unique ID of the job. */
    id?: string;
    /** Display name of the job. */
    displayName: string;
    /** Detailed description of the job. */
    description?: string;
    /** The ID of the device group on which to execute the job. */
    group: string;
    /** The batching configuration for the job. */
    batch?: JobBatch;
    /** The cancellation threshold for the job. */
    cancellationThreshold?: JobCancellationThreshold;
    /** Data related to the operation being performed by this job. All entries must be of the same type. */
    data: Array<Record<string, unknown>>;
    /** List of organizations of the job, only one organization is supported today, multiple organizations will be supported soon. */
    organizations?: Array<string>;
    /** Id of the scheduled job definition that created this job. */
    scheduledJobId?: string;
}

/**
 * Definition: JobCollection
 */
export interface JobCollection {
    /** The collection of jobs. */
    value: Array<JobStatic>;
    /** URL to get the next page of jobs. */
    nextLink?: string;
}

/**
 * Definition: Organization
 */
export interface Organization {
    /** Unique ID of the organization. */
    id?: string;
    /** Display name of the organization. */
    displayName?: string;
    /** Display name of the parent organization. */
    parent?: string;
}

/**
 * Definition: OrganizationStatic
 */
export type OrganizationStatic = string;

/**
 * Definition: OrganizationCollection
 */
export interface OrganizationCollection {
    /** The collection of organizations. */
    value: Array<Organization>;
    /** URL to get the next page of organizations. */
    nextLink?: string;
}

/**
 * Definition: Role
 */
export interface Role {
    /** Unique ID of the role. */
    id?: string;
    /** Display name of the role. */
    displayName?: string;
}

/**
 * Definition: RoleCollection
 */
export interface RoleCollection {
    /** The collection of roles. */
    value: Array<Role>;
}

/**
 * Definition: Rule
 */
export interface Rule {
    /** Unique ID of the rule. */
    id?: string;
    /** Display name of the rule. */
    displayName?: string;
}

/**
 * Definition: RuleCollection
 */
export interface RuleCollection {
    /** The collection of rules. */
    value: Array<Rule>;
    /** URL to get the next page of rules. */
    nextLink?: string;
}

/**
 * Definition: JobSchedule
 */
export interface JobSchedule {
    /** The schedule at which to execute the job. */
    schedule?: Record<string, unknown>;
}

/**
 * Definition: DateJobSchedule
 */
export interface DateJobSchedule {
    /** The schedule at which to execute the job. */
    schedule: Record<string, unknown>;
}

/**
 * Definition: OccurrencesJobSchedule
 */
export interface OccurrencesJobSchedule {
    /** The schedule at which to execute the job. */
    schedule: Record<string, unknown>;
}

/**
 * Definition: JobScheduleEnd
 */
export interface JobScheduleEnd {
    /** Type of the job schedule end. */
    type?: string;
    /** The date when to end the scheduled job (YYYY-MM-DDTHH:MM:SSZ). */
    date?: string;
    /** The number of occurrences after which to end the scheduled job. */
    occurrences?: number;
}

/**
 * Definition: ScheduledJob
 */
export interface ScheduledJob {
    /** ETag used to prevent conflict in scheduled job updates. */
    etag?: string;
    /** Unique ID of the scheduled job. */
    id?: string;
    /** Display name of the scheduled job. */
    displayName: string;
    /** Detailed description of the scheduled job. */
    description?: string;
    /** The ID of the device group on which to execute the job. */
    group: string;
    /** The batching configuration for the scheduled job. */
    batch?: JobBatch;
    /** The cancellation threshold for the scheduled job. */
    cancellationThreshold?: JobCancellationThreshold;
    /** List of organizations of the job, only one organization is supported today, multiple organizations will be supported soon. */
    organizations?: Array<string>;
    /** The schedule at which to execute the job. */
    schedule: Record<string, unknown>;
    /** Whether the scheduled job is enabled. */
    enabled?: boolean;
    /** Whether the scheduled job has completed. */
    completed?: boolean;
}

/**
 * Definition: ScheduledJobStatic
 */
export interface ScheduledJobStatic {
    /** ETag used to prevent conflict in scheduled job updates. */
    etag?: string;
    /** Unique ID of the scheduled job. */
    id?: string;
    /** Display name of the scheduled job. */
    displayName: string;
    /** Detailed description of the scheduled job. */
    description?: string;
    /** The ID of the device group on which to execute the job. */
    group: string;
    /** The batching configuration for the scheduled job. */
    batch?: JobBatch;
    /** The cancellation threshold for the scheduled job. */
    cancellationThreshold?: JobCancellationThreshold;
    /** Data related to the operation being performed by this job. All entries must be of the same type. */
    data: Array<Record<string, unknown>>;
    /** List of organizations of the job, only one organization is supported today, multiple organizations will be supported soon. */
    organizations?: Array<string>;
    /** The schedule at which to execute the job. */
    schedule: Record<string, unknown>;
    /** Whether the scheduled job is enabled. */
    enabled?: boolean;
    /** Whether the scheduled job has completed. */
    completed?: boolean;
}

/**
 * Definition: ScheduledJobPatch
 */
export interface ScheduledJobPatch {
    /** ETag used to prevent conflict in scheduled job updates. */
    etag?: string;
    /** Unique ID of the scheduled job. */
    id?: string;
    /** The schedule at which to execute the job. */
    schedule: Record<string, unknown>;
    /** Whether the scheduled job is enabled. */
    enabled?: boolean;
    /** Whether the scheduled job has completed. */
    completed?: boolean;
}

/**
 * Definition: ScheduledJobCollection
 */
export interface ScheduledJobCollection {
    /** The collection of scheduled jobs. */
    value: Array<ScheduledJob>;
    /** URL to get the next page of scheduled jobs. */
    nextLink?: string;
}

/**
 * Definition: ScheduledJobJobCollection
 */
export interface ScheduledJobJobCollection {
    /** The collection of jobs. */
    value: Array<JobStatic>;
    /** URL to get the next page of jobs. */
    nextLink?: string;
}

/**
 * Definition: User
 */
export interface User {
    [key: string]: unknown;
}

/**
 * Definition: UserPatch
 */
export interface UserPatch {
    [key: string]: unknown;
}

/**
 * Definition: UserStatic
 */
export interface UserStatic {
    /** Unique ID of the user. */
    id?: string;
    /** Type of the user. */
    type?: string;
    /** List of role assignments that specify the permissions to access the application. */
    roles: Array<RoleAssignment>;
}

/**
 * Definition: EmailUser
 */
export interface EmailUser {
    /** Unique ID of the user. */
    id?: string;
    /** Type of the user. */
    type: string;
    /** List of role assignments that specify the permissions to access the application. */
    roles: Array<RoleAssignment>;
    /** Email address of the user. */
    email: string;
}

/**
 * Definition: ServicePrincipalUser
 */
export interface ServicePrincipalUser {
    /** Unique ID of the user. */
    id?: string;
    /** Type of the user. */
    type: string;
    /** List of role assignments that specify the permissions to access the application. */
    roles: Array<RoleAssignment>;
    /** The Microsoft Entra ID tenant ID of the service principal. */
    tenantId: string;
    /** The Microsoft Entra ID object ID of the service principal. */
    objectId: string;
}

/**
 * Definition: UserCollection
 */
export interface UserCollection {
    /** The collection of users. */
    value: Array<UserStatic>;
}

/**
 * Definition: RoleAssignment
 */
export interface RoleAssignment {
    /** ID of the role for this role assignment. */
    role: string;
}

/**
 * Definition: WebhookActionDevice
 */
export interface WebhookActionDevice {
    /** Unique ID of the device. */
    id?: string;
    /** ETag used to prevent conflict in device updates. */
    etag?: string;
    /** Display name of the device. */
    displayName?: string;
    /** The device template definition for the device. */
    instanceOf?: string;
    /** Whether the device is simulated. */
    simulated?: boolean;
    /** Whether the device has been approved to connect to IoT Central. */
    approved?: boolean;
    /** Whether resources have been allocated for the device. */
    provisioned?: boolean;
    /** Cloud properties that triggered the webhook. */
    cloudProperties?: Record<string, unknown>;
    /** Device properties that triggered the webhook. */
    properties?: Record<string, unknown>;
    /** Device telemetry that triggered the webhook. */
    telemetry?: Record<string, unknown>;
}

/**
 * Definition: WebhookActionBody
 */
export interface WebhookActionBody {
    /** Action that triggered the webhook. */
    action: Action;
    /** Application that triggered the webhook. */
    application: Application;
    /** Device that triggered the webhook. */
    device: WebhookActionDevice;
    /** Rule that triggered the webhook. */
    rule: Rule;
    /** Timestamp at which the webhook was triggered. */
    timestamp: string;
}

/**
 * Definition: WorkflowTrigger
 */
export interface WorkflowTrigger {
    /** Unique ID of the workflow trigger. */
    id?: string;
    /** Unique ID of the rule that is tied to the workflow trigger. */
    rule: string;
    /** Callback URL of the workflow Trigger. */
    url: string;
}

/**
 * Definition: DynamicValues
 */
export interface DynamicValues {
    [key: string]: unknown;
}

/**
 * Options for the getDevicesCloudProperties operation.
 */
export interface GetDevicesCloudPropertiesOptions extends ConnectorOperationOptions {
    /** The device template definition for the device. */
    instanceOf?: string;
}

/**
 * Options for the updateDevicesCloudProperties operation.
 */
export interface UpdateDevicesCloudPropertiesOptions extends ConnectorOperationOptions {
    /** The device template definition for the device. */
    instanceOf?: string;
}

/**
 * Options for the executeDevicesComponentCommand operation.
 */
export interface ExecuteDevicesComponentCommandOptions extends ConnectorOperationOptions {
    /** The device template definition for the device. */
    instanceOf?: string;
}

/**
 * Options for the setJobs operation.
 */
export interface SetJobsOptions extends ConnectorOperationOptions {
    /** Type of the job. */
    jobType?: string;
}

/**
 * Options for the setScheduledJobs operation.
 */
export interface SetScheduledJobsOptions extends ConnectorOperationOptions {
    /** Type of the job. */
    jobType?: string;
    /** How the scheduled job should end (not applied to device template migrations). */
    scheduledJobEndType?: string;
}

/**
 * Options for the updateScheduledJob operation.
 */
export interface UpdateScheduledJobOptions extends ConnectorOperationOptions {
    /** How the scheduled job should end (not applied to device template migrations). */
    scheduledJobEndType?: string;
}

/**
 * Options for the getDeviceCommandResponse operation.
 */
export interface GetDeviceCommandResponseOptions extends ConnectorOperationOptions {
    /** The device template definition for the device. */
    template?: string;
}

/**
 * Options for the getDeviceComponentCommandResponse operation.
 */
export interface GetDeviceComponentCommandResponseOptions extends ConnectorOperationOptions {
    /** The device template definition for the device. */
    template?: string;
}

/**
 * Options for the getDeviceComponentTelemetryValue operation.
 */
export interface GetDeviceComponentTelemetryValueOptions extends ConnectorOperationOptions {
    /** The device template definition for the device. */
    template?: string;
}

/**
 * Options for the getDeviceModuleCommandResponse operation.
 */
export interface GetDeviceModuleCommandResponseOptions extends ConnectorOperationOptions {
    /** The device template definition for the device. */
    template?: string;
}

/**
 * Options for the getDeviceModuleComponentCommandResponse operation.
 */
export interface GetDeviceModuleComponentCommandResponseOptions extends ConnectorOperationOptions {
    /** The device template definition for the device. */
    template?: string;
}

/**
 * Options for the getDeviceModuleComponentTelemetryValue operation.
 */
export interface GetDeviceModuleComponentTelemetryValueOptions extends ConnectorOperationOptions {
    /** The device template definition for the device. */
    template?: string;
}

/**
 * Options for the getDevicesModuleProperties operation.
 */
export interface GetDevicesModulePropertiesOptions extends ConnectorOperationOptions {
    /** The device template definition for the device. */
    template?: string;
}

/**
 * Options for the getDeviceModuleTelemetryValue operation.
 */
export interface GetDeviceModuleTelemetryValueOptions extends ConnectorOperationOptions {
    /** The device template definition for the device. */
    template?: string;
}

/**
 * Options for the getDevicesProperties operation.
 */
export interface GetDevicesPropertiesOptions extends ConnectorOperationOptions {
    /** The device template definition for the device. */
    template?: string;
}

/**
 * Options for the getDeviceTelemetryValue operation.
 */
export interface GetDeviceTelemetryValueOptions extends ConnectorOperationOptions {
    /** The device template definition for the device. */
    template?: string;
}

/**
 * Options for the runDevicesCommand operation.
 */
export interface RunDevicesCommandOptions extends ConnectorOperationOptions {
    /** The device template definition for the device. */
    template?: string;
}

/**
 * Options for the runDevicesComponentCommand operation.
 */
export interface RunDevicesComponentCommandOptions extends ConnectorOperationOptions {
    /** The device template definition for the device. */
    template?: string;
}

/**
 * Options for the runDevicesModuleCommand operation.
 */
export interface RunDevicesModuleCommandOptions extends ConnectorOperationOptions {
    /** The device template definition for the device. */
    template?: string;
}

/**
 * Options for the runDevicesModuleComponentCommand operation.
 */
export interface RunDevicesModuleComponentCommandOptions extends ConnectorOperationOptions {
    /** The device template definition for the device. */
    template?: string;
}

/**
 * Options for the updateDevicesModuleProperties operation.
 */
export interface UpdateDevicesModulePropertiesOptions extends ConnectorOperationOptions {
    /** The device template definition for the device. */
    template?: string;
}

/**
 * Options for the updateDevicesProperties operation.
 */
export interface UpdateDevicesPropertiesOptions extends ConnectorOperationOptions {
    /** The device template definition for the device. */
    template?: string;
}

/**
 * Options for the createUser operation.
 */
export interface CreateUserOptions extends ConnectorOperationOptions {
    /** Type of the user. */
    userType?: string;
}

/**
 * Options for the updateUser operation.
 */
export interface UpdateUserOptions extends ConnectorOperationOptions {
    /** Type of the user. */
    userType?: string;
}
// #endregion Types

export const AzureiotcentralTriggerOperations = {
    OnWorkflowCreateTrigger: "Workflow_CreateTrigger",
} as const;

export type AzureiotcentralTriggerOperation = typeof AzureiotcentralTriggerOperations[keyof typeof AzureiotcentralTriggerOperations];

export const AzureiotcentralTriggerParameters = {
    OnWorkflowCreateTrigger: {
        application: {
            name: "application",
            type: "string",
            required: true,
            description: "Unique ID of the application.",
            summary: "Application",
            dynamicValuesOperationId: "Applications_List",
        },
    },
} as const;

// #region Client

/**
 * Typed client for the azureiotcentral connector.
 */
export class AzureiotcentralClient extends ConnectorClientBase {

    /**
     * Creates a new AzureiotcentralClient.
     * @param connectionRuntimeUrl The connection runtime URL from Azure Portal.
     * @param credential The credential used for authentication.
     * @param options Optional connector client options.
     */
    constructor(connectionRuntimeUrl: string, credential: TokenCredential, options?: ConnectorClientOptions) {
        super(connectionRuntimeUrl, credential, options);
    }

    public get connectorName(): string {
        return "azureiotcentral";
    }

    /**
     * List device groups
     * @remarks Get the list of device groups in an application.
     */
    public listDeviceGroups(application: string, options: ConnectorOperationOptions = {}): ConnectorPagedAsyncIterableIterator<DeviceGroup> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/deviceGroups` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        return this.createPageable<DeviceGroupCollection, DeviceGroup>(
            requestPath,
            async (requestUrl, isFirstPage) => {
                const requestMethod = isFirstPage ? "GET" : "GET";
                const httpResponse = await this.sendWithTracingAsync<DeviceGroupCollection>("Azureiotcentral.listDeviceGroups", "DeviceGroups_List", requestMethod, requestUrl, undefined, options);

                return httpResponse.value as DeviceGroupCollection;
            },
            "value",
            "nextLink",
        );
    }

    /**
     * Get a device group
     * @remarks Get details about an existing device group by ID.
     */
    public async getDeviceGroup(deviceGroupId: string, application: string, options: ConnectorOperationOptions = {}): Promise<DeviceGroup> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/deviceGroups/${encodeURIComponent(String(deviceGroupId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceGroup>("Azureiotcentral.getDeviceGroup", "DeviceGroups_Get", "GET", requestUrl, undefined, options);

        return httpResponse.value as DeviceGroup;
    }

    /**
     * Create a device group
     * @remarks Create or update a device group.
     */
    public async setDeviceGroups(input: DeviceGroup, deviceGroupId: string, application: string, options: ConnectorOperationOptions = {}): Promise<DeviceGroup> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/deviceGroups/${encodeURIComponent(String(deviceGroupId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceGroup>("Azureiotcentral.setDeviceGroups", "DeviceGroups_Set", "PUT", requestUrl, input, options);

        return httpResponse.value as DeviceGroup;
    }

    /**
     * Delete device group
     * @remarks Delete an existing device group by ID
     */
    public async removeDeviceGroups(deviceGroupId: string, application: string, options: ConnectorOperationOptions = {}): Promise<void> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/deviceGroups/${encodeURIComponent(String(deviceGroupId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        await this.sendWithTracingAsync<void>("Azureiotcentral.removeDeviceGroups", "DeviceGroups_Remove", "DELETE", requestUrl, undefined, options);
    }

    /**
     * Get devices by device group ID
     * @remarks Get the list of devices in a device group in an application.
     */
    public getDeviceGroupsDevices(deviceGroupId: string, application: string, options: ConnectorOperationOptions = {}): ConnectorPagedAsyncIterableIterator<DeviceV1> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/deviceGroups/${encodeURIComponent(String(deviceGroupId))}/devices` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        return this.createPageable<DeviceGroupDeviceCollection, DeviceV1>(
            requestPath,
            async (requestUrl, isFirstPage) => {
                const requestMethod = isFirstPage ? "GET" : "GET";
                const httpResponse = await this.sendWithTracingAsync<DeviceGroupDeviceCollection>("Azureiotcentral.getDeviceGroupsDevices", "DeviceGroups_GetDevices", requestMethod, requestUrl, undefined, options);

                return httpResponse.value as DeviceGroupDeviceCollection;
            },
            "value",
            "nextLink",
        );
    }

    /**
     * Get device cloud properties (deprecated)
     * @remarks Get all cloud property values of a device by device ID (deprecated).
     */
    public async getDevicesCloudProperties(deviceId: string, application: string, options: GetDevicesCloudPropertiesOptions = {}): Promise<DeviceCloudProperties> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.instanceOf !== undefined) {
            queryParams.push(`instanceOf=${encodeURIComponent(String(options.instanceOf))}`);
        }
        const requestPath = `/api/preview/devices/${encodeURIComponent(String(deviceId))}/cloudProperties` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceCloudProperties>("Azureiotcentral.getDevicesCloudProperties", "Devices_GetCloudProperties", "GET", requestUrl, undefined, options);

        return httpResponse.value as DeviceCloudProperties;
    }

    /**
     * Update device cloud properties (deprecated)
     * @remarks Update all cloud property values of a device by device ID (deprecated).
     */
    public async updateDevicesCloudProperties(input: DeviceCloudProperties, deviceId: string, application: string, options: UpdateDevicesCloudPropertiesOptions = {}): Promise<DeviceCloudProperties> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.instanceOf !== undefined) {
            queryParams.push(`instanceOf=${encodeURIComponent(String(options.instanceOf))}`);
        }
        const requestPath = `/api/preview/devices/${encodeURIComponent(String(deviceId))}/cloudProperties` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceCloudProperties>("Azureiotcentral.updateDevicesCloudProperties", "Devices_UpdateCloudProperties", "PUT", requestUrl, input, options);

        return httpResponse.value as DeviceCloudProperties;
    }

    /**
     * Execute a device command (deprecated)
     * @remarks Execute a command on a device (deprecated).
     */
    public async executeDevicesComponentCommand(input: DeviceCommand, deviceId: string, componentName: string, commandName: string, application: string, options: ExecuteDevicesComponentCommandOptions = {}): Promise<DeviceCommand> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.instanceOf !== undefined) {
            queryParams.push(`instanceOf=${encodeURIComponent(String(options.instanceOf))}`);
        }
        const requestPath = `/api/preview/devices/${encodeURIComponent(String(deviceId))}/components/${encodeURIComponent(String(componentName))}/commands/${encodeURIComponent(String(commandName))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceCommand>("Azureiotcentral.executeDevicesComponentCommand", "Devices_ExecuteComponentCommand", "POST", requestUrl, input, options);

        return httpResponse.value as DeviceCommand;
    }

    /**
     * List relationships
     * @remarks List all relationships based on device ID.
     */
    public listDeviceRelationships(deviceId: string, application: string, options: ConnectorOperationOptions = {}): ConnectorPagedAsyncIterableIterator<DeviceRelationshipStatic> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/devices/${encodeURIComponent(String(deviceId))}/relationships` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        return this.createPageable<DeviceRelationshipCollection, DeviceRelationshipStatic>(
            requestPath,
            async (requestUrl, isFirstPage) => {
                const requestMethod = isFirstPage ? "GET" : "GET";
                const httpResponse = await this.sendWithTracingAsync<DeviceRelationshipCollection>("Azureiotcentral.listDeviceRelationships", "DeviceRelationships_List", requestMethod, requestUrl, undefined, options);

                return httpResponse.value as DeviceRelationshipCollection;
            },
            "value",
            "nextLink",
        );
    }

    /**
     * Get device relationship
     * @remarks Get details about an existing device relationship by device ID and by relationship ID.
     */
    public async getDeviceRelationship(deviceId: string, relationshipId: string, application: string, options: ConnectorOperationOptions = {}): Promise<DeviceRelationshipStatic> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/devices/${encodeURIComponent(String(deviceId))}/relationships/${encodeURIComponent(String(relationshipId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceRelationshipStatic>("Azureiotcentral.getDeviceRelationship", "DeviceRelationships_Get", "GET", requestUrl, undefined, options);

        return httpResponse.value as DeviceRelationshipStatic;
    }

    /**
     * Create a device relationship
     * @remarks Create a new device relationship between the given device and a new device given a device ID and a relationship ID.
     */
    public async setDeviceRelationships(input: DeviceRelationship, relationshipId: string, deviceId: string, application: string, options: ConnectorOperationOptions = {}): Promise<DeviceRelationshipStatic> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/devices/${encodeURIComponent(String(deviceId))}/relationships/${encodeURIComponent(String(relationshipId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceRelationshipStatic>("Azureiotcentral.setDeviceRelationships", "DeviceRelationships_Set", "PUT", requestUrl, input, options);

        return httpResponse.value as DeviceRelationshipStatic;
    }

    /**
     * Update a device relationship
     * @remarks Update an existing device relationship between the given device and a new device given a device ID and a relationship ID.
     */
    public async updateDeviceRelationship(input: DeviceRelationship, deviceId: string, relationshipId: string, application: string, options: ConnectorOperationOptions = {}): Promise<DeviceRelationshipStatic> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/devices/${encodeURIComponent(String(deviceId))}/relationships/${encodeURIComponent(String(relationshipId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceRelationshipStatic>("Azureiotcentral.updateDeviceRelationship", "DeviceRelationships_Update", "PATCH", requestUrl, input, options);

        return httpResponse.value as DeviceRelationshipStatic;
    }

    /**
     * Delete a device relationship
     * @remarks Delete an existing device relationship by device ID and relationship ID.
     */
    public async removeDeviceRelationships(deviceId: string, relationshipId: string, application: string, options: ConnectorOperationOptions = {}): Promise<void> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/devices/${encodeURIComponent(String(deviceId))}/relationships/${encodeURIComponent(String(relationshipId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        await this.sendWithTracingAsync<void>("Azureiotcentral.removeDeviceRelationships", "DeviceRelationships_Remove", "DELETE", requestUrl, undefined, options);
    }

    /**
     * List jobs
     * @remarks Get the list of jobs in an application.
     */
    public listJobs(application: string, options: ConnectorOperationOptions = {}): ConnectorPagedAsyncIterableIterator<JobStatic> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/jobs` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        return this.createPageable<JobCollection, JobStatic>(
            requestPath,
            async (requestUrl, isFirstPage) => {
                const requestMethod = isFirstPage ? "GET" : "GET";
                const httpResponse = await this.sendWithTracingAsync<JobCollection>("Azureiotcentral.listJobs", "Jobs_List", requestMethod, requestUrl, undefined, options);

                return httpResponse.value as JobCollection;
            },
            "value",
            "nextLink",
        );
    }

    /**
     * Get a job
     * @remarks Get details about an existing job by ID.
     */
    public async getJob(jobId: string, application: string, options: ConnectorOperationOptions = {}): Promise<JobStatic> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/jobs/${encodeURIComponent(String(jobId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<JobStatic>("Azureiotcentral.getJob", "Jobs_Get", "GET", requestUrl, undefined, options);

        return httpResponse.value as JobStatic;
    }

    /**
     * Create a job
     * @remarks Create and execute a new job via its job definition.
     */
    public async setJobs(input: Job, jobId: string, application: string, options: SetJobsOptions = {}): Promise<Job> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.jobType !== undefined) {
            queryParams.push(`job_type=${encodeURIComponent(String(options.jobType))}`);
        }
        const requestPath = `/api/ga_2022_07_31/jobs/${encodeURIComponent(String(jobId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<Job>("Azureiotcentral.setJobs", "Jobs_Set", "PUT", requestUrl, input, options);

        return httpResponse.value as Job;
    }

    /**
     * Get device statuses
     * @remarks Get the list of individual device statuses by job ID.
     */
    public getJobsDevices(jobId: string, application: string, options: ConnectorOperationOptions = {}): ConnectorPagedAsyncIterableIterator<JobDeviceStatus> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/jobs/${encodeURIComponent(String(jobId))}/devices` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        return this.createPageable<JobDeviceStatusCollection, JobDeviceStatus>(
            requestPath,
            async (requestUrl, isFirstPage) => {
                const requestMethod = isFirstPage ? "GET" : "GET";
                const httpResponse = await this.sendWithTracingAsync<JobDeviceStatusCollection>("Azureiotcentral.getJobsDevices", "Jobs_GetDevices", requestMethod, requestUrl, undefined, options);

                return httpResponse.value as JobDeviceStatusCollection;
            },
            "value",
            "nextLink",
        );
    }

    /**
     * Stop a running job
     * @remarks Stop execution of a job that is currently running.
     */
    public async stopJobs(jobId: string, application: string, options: ConnectorOperationOptions = {}): Promise<void> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/jobs/${encodeURIComponent(String(jobId))}/stop` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        await this.sendWithTracingAsync<void>("Azureiotcentral.stopJobs", "Jobs_Stop", "POST", requestUrl, undefined, options);
    }

    /**
     * Resume a stopped job
     * @remarks Resume execution of an existing stopped job.
     */
    public async resumeJobs(jobId: string, application: string, options: ConnectorOperationOptions = {}): Promise<void> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/jobs/${encodeURIComponent(String(jobId))}/resume` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        await this.sendWithTracingAsync<void>("Azureiotcentral.resumeJobs", "Jobs_Resume", "POST", requestUrl, undefined, options);
    }

    /**
     * Resume a job on failed devices
     * @remarks Execute a rerun of an existing job on all failed devices.
     */
    public async rerunJobs(jobId: string, rerunId: string, application: string, options: ConnectorOperationOptions = {}): Promise<JobStatic> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/jobs/${encodeURIComponent(String(jobId))}/rerun/${encodeURIComponent(String(rerunId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<JobStatic>("Azureiotcentral.rerunJobs", "Jobs_Rerun", "PUT", requestUrl, undefined, options);

        return httpResponse.value as JobStatic;
    }

    /**
     * List organizations
     * @remarks Get the list of organizations in an application.
     */
    public listOrganizations(application: string, options: ConnectorOperationOptions = {}): ConnectorPagedAsyncIterableIterator<Organization> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/organizations` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        return this.createPageable<OrganizationCollection, Organization>(
            requestPath,
            async (requestUrl, isFirstPage) => {
                const requestMethod = isFirstPage ? "GET" : "GET";
                const httpResponse = await this.sendWithTracingAsync<OrganizationCollection>("Azureiotcentral.listOrganizations", "Organizations_List", requestMethod, requestUrl, undefined, options);

                return httpResponse.value as OrganizationCollection;
            },
            "value",
            "nextLink",
        );
    }

    /**
     * Get an organization
     * @remarks Get details about an existing organization by ID.
     */
    public async getOrganization(organizationId: string, application: string, options: ConnectorOperationOptions = {}): Promise<Organization> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/organizations/${encodeURIComponent(String(organizationId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<Organization>("Azureiotcentral.getOrganization", "Organizations_Get", "GET", requestUrl, undefined, options);

        return httpResponse.value as Organization;
    }

    /**
     * Create an organization
     * @remarks Create a new organization.
     */
    public async setOrganizations(input: Organization, organizationId: string, application: string, options: ConnectorOperationOptions = {}): Promise<Organization> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/organizations/${encodeURIComponent(String(organizationId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<Organization>("Azureiotcentral.setOrganizations", "Organizations_Set", "PUT", requestUrl, input, options);

        return httpResponse.value as Organization;
    }

    /**
     * Delete organization
     * @remarks Delete an existing organization by ID
     */
    public async removeOrganizations(organizationId: string, application: string, options: ConnectorOperationOptions = {}): Promise<void> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/organizations/${encodeURIComponent(String(organizationId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        await this.sendWithTracingAsync<void>("Azureiotcentral.removeOrganizations", "Organizations_Remove", "DELETE", requestUrl, undefined, options);
    }

    /**
     * List scheduled jobs
     * @remarks Get the list of scheduled jobs in an application.
     */
    public listScheduledJobs(application: string, options: ConnectorOperationOptions = {}): ConnectorPagedAsyncIterableIterator<ScheduledJob> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/scheduledJobs` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        return this.createPageable<ScheduledJobCollection, ScheduledJob>(
            requestPath,
            async (requestUrl, isFirstPage) => {
                const requestMethod = isFirstPage ? "GET" : "GET";
                const httpResponse = await this.sendWithTracingAsync<ScheduledJobCollection>("Azureiotcentral.listScheduledJobs", "ScheduledJobs_List", requestMethod, requestUrl, undefined, options);

                return httpResponse.value as ScheduledJobCollection;
            },
            "value",
            "nextLink",
        );
    }

    /**
     * Get a scheduled job
     * @remarks Get details about an existing scheduled job by ID.
     */
    public async getScheduledJob(scheduledJobId: string, application: string, options: ConnectorOperationOptions = {}): Promise<ScheduledJob> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/scheduledJobs/${encodeURIComponent(String(scheduledJobId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<ScheduledJob>("Azureiotcentral.getScheduledJob", "ScheduledJobs_Get", "GET", requestUrl, undefined, options);

        return httpResponse.value as ScheduledJob;
    }

    /**
     * Create a scheduled job
     * @remarks Create and execute a new scheduled job via its job definition.
     */
    public async setScheduledJobs(input: ScheduledJob, scheduledJobId: string, application: string, options: SetScheduledJobsOptions = {}): Promise<ScheduledJob> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.jobType !== undefined) {
            queryParams.push(`job_type=${encodeURIComponent(String(options.jobType))}`);
        }
        if (options.scheduledJobEndType !== undefined) {
            queryParams.push(`scheduled_job_end_type=${encodeURIComponent(String(options.scheduledJobEndType))}`);
        }
        const requestPath = `/api/ga_2022_07_31/scheduledJobs/${encodeURIComponent(String(scheduledJobId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<ScheduledJob>("Azureiotcentral.setScheduledJobs", "ScheduledJobs_Set", "PUT", requestUrl, input, options);

        return httpResponse.value as ScheduledJob;
    }

    /**
     * Update a scheduled job
     * @remarks Update an existing scheduled job by ID.
     */
    public async updateScheduledJob(input: ScheduledJobPatch, scheduledJobId: string, application: string, options: UpdateScheduledJobOptions = {}): Promise<ScheduledJobStatic> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.scheduledJobEndType !== undefined) {
            queryParams.push(`scheduled_job_end_type=${encodeURIComponent(String(options.scheduledJobEndType))}`);
        }
        const requestPath = `/api/ga_2022_07_31/scheduledJobs/${encodeURIComponent(String(scheduledJobId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<ScheduledJobStatic>("Azureiotcentral.updateScheduledJob", "ScheduledJobs_Update", "PATCH", requestUrl, input, options);

        return httpResponse.value as ScheduledJobStatic;
    }

    /**
     * Delete a scheduled job
     * @remarks Delete an existing scheduled job by ID.
     */
    public async removeScheduledJobs(scheduledJobId: string, application: string, options: ConnectorOperationOptions = {}): Promise<void> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/scheduledJobs/${encodeURIComponent(String(scheduledJobId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        await this.sendWithTracingAsync<void>("Azureiotcentral.removeScheduledJobs", "ScheduledJobs_Remove", "DELETE", requestUrl, undefined, options);
    }

    /**
     * Get jobs by scheduled job ID
     * @remarks Get the list of jobs for a scheduled job definition.
     */
    public listScheduledJobsJobs(scheduledJobId: string, application: string, options: ConnectorOperationOptions = {}): ConnectorPagedAsyncIterableIterator<JobStatic> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/scheduledJobs/${encodeURIComponent(String(scheduledJobId))}/jobs` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        return this.createPageable<ScheduledJobJobCollection, JobStatic>(
            requestPath,
            async (requestUrl, isFirstPage) => {
                const requestMethod = isFirstPage ? "GET" : "GET";
                const httpResponse = await this.sendWithTracingAsync<ScheduledJobJobCollection>("Azureiotcentral.listScheduledJobsJobs", "ScheduledJobs_ListJobs", requestMethod, requestUrl, undefined, options);

                return httpResponse.value as ScheduledJobJobCollection;
            },
            "value",
            "nextLink",
        );
    }

    /**
     * Get a device by ID
     * @remarks Get details about an existing device by device ID.
     */
    public async getDevice(deviceId: string, application: string, options: ConnectorOperationOptions = {}): Promise<DeviceV1> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/v1/devices/${encodeURIComponent(String(deviceId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceV1>("Azureiotcentral.getDevice", "Devices_Get_V1", "GET", requestUrl, undefined, options);

        return httpResponse.value as DeviceV1;
    }

    /**
     * Get device command response
     * @remarks Get the last response for a device command.
     */
    public async getDeviceCommandResponse(deviceId: string, commandName: string, application: string, options: GetDeviceCommandResponseOptions = {}): Promise<DeviceCommandV1> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(options.template))}`);
        }
        const requestPath = `/api/v1/devices/${encodeURIComponent(String(deviceId))}/commands/${encodeURIComponent(String(commandName))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceCommandV1>("Azureiotcentral.getDeviceCommandResponse", "Devices_GetCommandResponse_V1", "GET", requestUrl, undefined, options);

        return httpResponse.value as DeviceCommandV1;
    }

    /**
     * Get component command response
     * @remarks Get the last response for a device component command.
     */
    public async getDeviceComponentCommandResponse(deviceId: string, componentName: string, commandName: string, application: string, options: GetDeviceComponentCommandResponseOptions = {}): Promise<DeviceComponentCommand> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(options.template))}`);
        }
        const requestPath = `/api/v1/devices/${encodeURIComponent(String(deviceId))}/components/${encodeURIComponent(String(componentName))}/commands/${encodeURIComponent(String(commandName))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceComponentCommand>("Azureiotcentral.getDeviceComponentCommandResponse", "Devices_GetComponentCommandResponse_V1", "GET", requestUrl, undefined, options);

        return httpResponse.value as DeviceComponentCommand;
    }

    /**
     * Get component telemetry value
     * @remarks Get the last telemetry value from a component.
     */
    public async getDeviceComponentTelemetryValue(deviceId: string, componentName: string, telemetryName: string, application: string, options: GetDeviceComponentTelemetryValueOptions = {}): Promise<DeviceComponentTelemetry> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(options.template))}`);
        }
        const requestPath = `/api/v1/devices/${encodeURIComponent(String(deviceId))}/components/${encodeURIComponent(String(componentName))}/telemetry/${encodeURIComponent(String(telemetryName))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceComponentTelemetry>("Azureiotcentral.getDeviceComponentTelemetryValue", "Devices_GetComponentTelemetryValue_V1", "GET", requestUrl, undefined, options);

        return httpResponse.value as DeviceComponentTelemetry;
    }

    /**
     * Get module command response
     * @remarks Get the last response for a device module command
     */
    public async getDeviceModuleCommandResponse(deviceId: string, module: string, commandName: string, application: string, options: GetDeviceModuleCommandResponseOptions = {}): Promise<DeviceModuleCommand> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(options.template))}`);
        }
        const requestPath = `/api/v1/devices/${encodeURIComponent(String(deviceId))}/modules/${encodeURIComponent(String(module))}/commands/${encodeURIComponent(String(commandName))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceModuleCommand>("Azureiotcentral.getDeviceModuleCommandResponse", "Devices_GetModuleCommandResponse_V1", "GET", requestUrl, undefined, options);

        return httpResponse.value as DeviceModuleCommand;
    }

    /**
     * Get module component command response
     * @remarks Get the last response for a module component command.
     */
    public async getDeviceModuleComponentCommandResponse(deviceId: string, module: string, componentName: string, commandName: string, application: string, options: GetDeviceModuleComponentCommandResponseOptions = {}): Promise<DeviceModuleComponentCommand> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(options.template))}`);
        }
        const requestPath = `/api/v1/devices/${encodeURIComponent(String(deviceId))}/modules/${encodeURIComponent(String(module))}/components/${encodeURIComponent(String(componentName))}/commands/${encodeURIComponent(String(commandName))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceModuleComponentCommand>("Azureiotcentral.getDeviceModuleComponentCommandResponse", "Devices_GetModuleComponentCommandResponse_V1", "GET", requestUrl, undefined, options);

        return httpResponse.value as DeviceModuleComponentCommand;
    }

    /**
     * Get module component telemetry value
     * @remarks Get the last telemetry value from a module component.
     */
    public async getDeviceModuleComponentTelemetryValue(deviceId: string, module: string, componentName: string, telemetryName: string, application: string, options: GetDeviceModuleComponentTelemetryValueOptions = {}): Promise<DeviceModuleComponentTelemetry> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(options.template))}`);
        }
        const requestPath = `/api/v1/devices/${encodeURIComponent(String(deviceId))}/modules/${encodeURIComponent(String(module))}/components/${encodeURIComponent(String(componentName))}/telemetry/${encodeURIComponent(String(telemetryName))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceModuleComponentTelemetry>("Azureiotcentral.getDeviceModuleComponentTelemetryValue", "Devices_GetModuleComponentTelemetryValue_V1", "GET", requestUrl, undefined, options);

        return httpResponse.value as DeviceModuleComponentTelemetry;
    }

    /**
     * Get module properties
     * @remarks Get all property values of a module.
     */
    public async getDevicesModuleProperties(deviceId: string, module: string, application: string, options: GetDevicesModulePropertiesOptions = {}): Promise<ModuleProperties> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(options.template))}`);
        }
        const requestPath = `/api/v1/devices/${encodeURIComponent(String(deviceId))}/modules/${encodeURIComponent(String(module))}/properties` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<ModuleProperties>("Azureiotcentral.getDevicesModuleProperties", "Devices_GetModuleProperties_V1", "GET", requestUrl, undefined, options);

        return httpResponse.value as ModuleProperties;
    }

    /**
     * Get module telemetry value
     * @remarks Get the last telemetry value from a module.
     */
    public async getDeviceModuleTelemetryValue(deviceId: string, module: string, telemetryName: string, application: string, options: GetDeviceModuleTelemetryValueOptions = {}): Promise<DeviceModuleTelemetry> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(options.template))}`);
        }
        const requestPath = `/api/v1/devices/${encodeURIComponent(String(deviceId))}/modules/${encodeURIComponent(String(module))}/telemetry/${encodeURIComponent(String(telemetryName))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceModuleTelemetry>("Azureiotcentral.getDeviceModuleTelemetryValue", "Devices_GetModuleTelemetryValue_V1", "GET", requestUrl, undefined, options);

        return httpResponse.value as DeviceModuleTelemetry;
    }

    /**
     * Get device properties
     * @remarks Get all property values of a device by device ID.
     */
    public async getDevicesProperties(deviceId: string, application: string, options: GetDevicesPropertiesOptions = {}): Promise<DeviceProperties> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(options.template))}`);
        }
        const requestPath = `/api/v1/devices/${encodeURIComponent(String(deviceId))}/properties` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceProperties>("Azureiotcentral.getDevicesProperties", "Devices_GetProperties_V1", "GET", requestUrl, undefined, options);

        return httpResponse.value as DeviceProperties;
    }

    /**
     * Get device telemetry value
     * @remarks Get the last telemetry value from a device.
     */
    public async getDeviceTelemetryValue(deviceId: string, telemetryName: string, application: string, options: GetDeviceTelemetryValueOptions = {}): Promise<DeviceTelemetry> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(options.template))}`);
        }
        const requestPath = `/api/v1/devices/${encodeURIComponent(String(deviceId))}/telemetry/${encodeURIComponent(String(telemetryName))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceTelemetry>("Azureiotcentral.getDeviceTelemetryValue", "Devices_GetTelemetryValue_V1", "GET", requestUrl, undefined, options);

        return httpResponse.value as DeviceTelemetry;
    }

    /**
     * List devices
     * @remarks Get the list of devices in an application.
     */
    public listDevices(application: string, options: ConnectorOperationOptions = {}): ConnectorPagedAsyncIterableIterator<DeviceV1> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/v1/devices` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        return this.createPageable<DeviceCollection, DeviceV1>(
            requestPath,
            async (requestUrl, isFirstPage) => {
                const requestMethod = isFirstPage ? "GET" : "GET";
                const httpResponse = await this.sendWithTracingAsync<DeviceCollection>("Azureiotcentral.listDevices", "Devices_List_V1", requestMethod, requestUrl, undefined, options);

                return httpResponse.value as DeviceCollection;
            },
            "value",
            "nextLink",
        );
    }

    /**
     * Delete a device
     * @remarks Delete an existing device by device ID.
     */
    public async removeDevices(deviceId: string, application: string, options: ConnectorOperationOptions = {}): Promise<void> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/v1/devices/${encodeURIComponent(String(deviceId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        await this.sendWithTracingAsync<void>("Azureiotcentral.removeDevices", "Devices_Remove_V1", "DELETE", requestUrl, undefined, options);
    }

    /**
     * Run a device command
     * @remarks Run a command on a device.
     */
    public async runDevicesCommand(input: DeviceCommandV1, deviceId: string, commandName: string, application: string, options: RunDevicesCommandOptions = {}): Promise<DeviceCommandV1> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(options.template))}`);
        }
        const requestPath = `/api/v1/devices/${encodeURIComponent(String(deviceId))}/commands/${encodeURIComponent(String(commandName))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceCommandV1>("Azureiotcentral.runDevicesCommand", "Devices_RunCommand_V1", "POST", requestUrl, input, options);

        return httpResponse.value as DeviceCommandV1;
    }

    /**
     * Run a component command
     * @remarks Run a command on a component.
     */
    public async runDevicesComponentCommand(input: DeviceComponentCommand, deviceId: string, componentName: string, commandName: string, application: string, options: RunDevicesComponentCommandOptions = {}): Promise<DeviceComponentCommand> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(options.template))}`);
        }
        const requestPath = `/api/v1/devices/${encodeURIComponent(String(deviceId))}/components/${encodeURIComponent(String(componentName))}/commands/${encodeURIComponent(String(commandName))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceComponentCommand>("Azureiotcentral.runDevicesComponentCommand", "Devices_RunComponentCommand_V1", "POST", requestUrl, input, options);

        return httpResponse.value as DeviceComponentCommand;
    }

    /**
     * Run a module command
     * @remarks Run a command on a module.
     */
    public async runDevicesModuleCommand(input: DeviceModuleCommand, deviceId: string, module: string, commandName: string, application: string, options: RunDevicesModuleCommandOptions = {}): Promise<DeviceModuleCommand> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(options.template))}`);
        }
        const requestPath = `/api/v1/devices/${encodeURIComponent(String(deviceId))}/modules/${encodeURIComponent(String(module))}/commands/${encodeURIComponent(String(commandName))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceModuleCommand>("Azureiotcentral.runDevicesModuleCommand", "Devices_RunModuleCommand_V1", "POST", requestUrl, input, options);

        return httpResponse.value as DeviceModuleCommand;
    }

    /**
     * Run a module component command
     * @remarks Run a command on a module component.
     */
    public async runDevicesModuleComponentCommand(input: DeviceModuleComponentCommand, deviceId: string, module: string, componentName: string, commandName: string, application: string, options: RunDevicesModuleComponentCommandOptions = {}): Promise<DeviceModuleComponentCommand> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(options.template))}`);
        }
        const requestPath = `/api/v1/devices/${encodeURIComponent(String(deviceId))}/modules/${encodeURIComponent(String(module))}/components/${encodeURIComponent(String(componentName))}/commands/${encodeURIComponent(String(commandName))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceModuleComponentCommand>("Azureiotcentral.runDevicesModuleComponentCommand", "Devices_RunModuleComponentCommand_V1", "POST", requestUrl, input, options);

        return httpResponse.value as DeviceModuleComponentCommand;
    }

    /**
     * Create or update a device
     * @remarks Create a new device or update an existing one by device ID.
     */
    public async setDevices(input: DeviceV2, deviceId: string, application: string, options: ConnectorOperationOptions = {}): Promise<DeviceV2> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/devices/${encodeURIComponent(String(deviceId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceV2>("Azureiotcentral.setDevices", "Devices_Set_V2", "PUT", requestUrl, input, options);

        return httpResponse.value as DeviceV2;
    }

    /**
     * Update module properties
     * @remarks Update all property values of a module.
     */
    public async updateDevicesModuleProperties(input: ModuleProperties, deviceId: string, module: string, application: string, options: UpdateDevicesModulePropertiesOptions = {}): Promise<ModuleProperties> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(options.template))}`);
        }
        const requestPath = `/api/v1/devices/${encodeURIComponent(String(deviceId))}/modules/${encodeURIComponent(String(module))}/properties` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<ModuleProperties>("Azureiotcentral.updateDevicesModuleProperties", "Devices_UpdateModuleProperties_V1", "PATCH", requestUrl, input, options);

        return httpResponse.value as ModuleProperties;
    }

    /**
     * Update device properties
     * @remarks Update all property values of a device by device ID.
     */
    public async updateDevicesProperties(input: DeviceProperties, deviceId: string, application: string, options: UpdateDevicesPropertiesOptions = {}): Promise<DeviceProperties> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(options.template))}`);
        }
        const requestPath = `/api/v1/devices/${encodeURIComponent(String(deviceId))}/properties` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceProperties>("Azureiotcentral.updateDevicesProperties", "Devices_UpdateProperties_V1", "PATCH", requestUrl, input, options);

        return httpResponse.value as DeviceProperties;
    }

    /**
     * Get a device template by ID
     * @remarks Get details about an existing device template by ID.
     */
    public async getDeviceTemplate(templateId: string, application: string, options: ConnectorOperationOptions = {}): Promise<DeviceTemplate> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/v1/deviceTemplates/${encodeURIComponent(String(templateId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<DeviceTemplate>("Azureiotcentral.getDeviceTemplate", "DeviceTemplates_Get_V1", "GET", requestUrl, undefined, options);

        return httpResponse.value as DeviceTemplate;
    }

    /**
     * List device templates
     * @remarks Get the list of device templates in an application.
     */
    public listDeviceTemplates(application: string, options: ConnectorOperationOptions = {}): ConnectorPagedAsyncIterableIterator<DeviceTemplate> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/v1/deviceTemplates` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        return this.createPageable<DeviceTemplateCollection, DeviceTemplate>(
            requestPath,
            async (requestUrl, isFirstPage) => {
                const requestMethod = isFirstPage ? "GET" : "GET";
                const httpResponse = await this.sendWithTracingAsync<DeviceTemplateCollection>("Azureiotcentral.listDeviceTemplates", "DeviceTemplates_List_V1", requestMethod, requestUrl, undefined, options);

                return httpResponse.value as DeviceTemplateCollection;
            },
            "value",
            "nextLink",
        );
    }

    /**
     * Delete a device template
     * @remarks Delete an existing device template by ID.
     */
    public async removeDeviceTemplates(templateId: string, application: string, options: ConnectorOperationOptions = {}): Promise<void> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/v1/deviceTemplates/${encodeURIComponent(String(templateId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        await this.sendWithTracingAsync<void>("Azureiotcentral.removeDeviceTemplates", "DeviceTemplates_Remove_V1", "DELETE", requestUrl, undefined, options);
    }

    /**
     * Get role
     * @remarks Get a role by ID.
     */
    public async getRole(roleId: string, application: string, options: ConnectorOperationOptions = {}): Promise<Role> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/v1/roles/${encodeURIComponent(String(roleId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<Role>("Azureiotcentral.getRole", "Roles_Get_V1", "GET", requestUrl, undefined, options);

        return httpResponse.value as Role;
    }

    /**
     * List roles
     * @remarks Get the list of roles in an application.
     */
    public listRoles(application: string, options: ConnectorOperationOptions = {}): ConnectorPagedAsyncIterableIterator<Role> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/v1/roles` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        return this.createPageable<RoleCollection, Role>(
            requestPath,
            async (requestUrl) => {
                const httpResponse = await this.sendWithTracingAsync<RoleCollection>("Azureiotcentral.listRoles", "Roles_List_V1", "GET", requestUrl, undefined, options);

                return httpResponse.value as RoleCollection;
            },
            "value",
            undefined,
        );
    }

    /**
     * Create user
     * @remarks Create a user in the application
     */
    public async createUser(input: User, userId: string, application: string, options: CreateUserOptions = {}): Promise<User> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.userType !== undefined) {
            queryParams.push(`user_type=${encodeURIComponent(String(options.userType))}`);
        }
        const requestPath = `/api/v1/users/${encodeURIComponent(String(userId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<User>("Azureiotcentral.createUser", "Users_Create_V1", "PUT", requestUrl, input, options);

        return httpResponse.value as User;
    }

    /**
     * Get user
     * @remarks Get a user by ID
     */
    public async getUser(userId: string, application: string, options: ConnectorOperationOptions = {}): Promise<UserStatic> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/v1/users/${encodeURIComponent(String(userId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<UserStatic>("Azureiotcentral.getUser", "Users_Get_V1", "GET", requestUrl, undefined, options);

        return httpResponse.value as UserStatic;
    }

    /**
     * List users
     * @remarks Get the list of users in an application
     */
    public listUsers(application: string, options: ConnectorOperationOptions = {}): ConnectorPagedAsyncIterableIterator<UserStatic> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/v1/users` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        return this.createPageable<UserCollection, UserStatic>(
            requestPath,
            async (requestUrl) => {
                const httpResponse = await this.sendWithTracingAsync<UserCollection>("Azureiotcentral.listUsers", "Users_List_V1", "GET", requestUrl, undefined, options);

                return httpResponse.value as UserCollection;
            },
            "value",
            undefined,
        );
    }

    /**
     * Delete user
     * @remarks Delete a user
     */
    public async removeUsers(userId: string, application: string, options: ConnectorOperationOptions = {}): Promise<void> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/v1/users/${encodeURIComponent(String(userId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        await this.sendWithTracingAsync<void>("Azureiotcentral.removeUsers", "Users_Remove_V1", "DELETE", requestUrl, undefined, options);
    }

    /**
     * Update user
     * @remarks Update a user in the application via patch
     */
    public async updateUser(input: UserPatch, userId: string, application: string, options: UpdateUserOptions = {}): Promise<UserPatch> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (options.userType !== undefined) {
            queryParams.push(`user_type=${encodeURIComponent(String(options.userType))}`);
        }
        const requestPath = `/api/v1/users/${encodeURIComponent(String(userId))}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<UserPatch>("Azureiotcentral.updateUser", "Users_Update_V1", "PATCH", requestUrl, input, options);

        return httpResponse.value as UserPatch;
    }

}

// #endregion Client
