// AzureiotcentralExtensions.ts - Auto-generated DirectClient SDK for TypeScript
// Do not edit this file directly.

import type { AbortSignalLike } from "@azure/abort-controller";
import { ConnectorClientBase } from "../azureConnectors/clientBase.ts";
import { ConnectorException } from "../azureConnectors/connectorException.ts";
import { ConnectorClientOptions } from "../azureConnectors/options.ts";
import { TokenProvider } from "../azureConnectors/authentication.ts";

// #region Types

/**
 * Definition: Action
 */
export interface Action {
    /** Unique ID of the action. */
    id?: string;
    /** Display name of the action. */
    displayName?: string;
    /** Type of the action. */
    type: string;
    /** Unique IDs of the rules that are tied to the action. */
    rules: Array<string>;
}

/**
 * Definition: EmailAction
 */
export interface EmailAction {
    /** Unique ID of the email action. */
    id?: string;
    /** Display name of the email action. */
    displayName?: string;
    /** Type of the email action. */
    type: string;
    /** Unique IDs of the rules that are tied to the email action. */
    rules: Array<string>;
}

/**
 * Definition: WebhookAction
 */
export interface WebhookAction {
    /** Unique ID of the webhook action. */
    id?: string;
    /** Display name of the webhook action. */
    displayName?: string;
    /** Type of the webhook action. */
    type: string;
    /** Unique IDs of the rules that are tied to the webhook action. */
    rules: Array<string>;
    /** Callback URL of the webhook. */
    url: string;
}

/**
 * Definition: PowerAutomateAction
 */
export interface PowerAutomateAction {
    /** Unique ID of the Power Automate action. */
    id?: string;
    /** Display name of the Power Automate action. */
    displayName?: string;
    /** Type of the Power Automate action. */
    type: string;
    /** Unique IDs of the rules that are tied to the Power Automate action. */
    rules: Array<string>;
}

/**
 * Definition: LogicAppsAction
 */
export interface LogicAppsAction {
    /** Unique ID of the Logic Apps action. */
    id?: string;
    /** Display name of the Logic Apps action. */
    displayName?: string;
    /** Type of the Logic Apps action. */
    type: string;
    /** Unique IDs of the rules that are tied to the Logic Apps action. */
    rules: Array<string>;
}

/**
 * Definition: ActionGroupsAction
 */
export interface ActionGroupsAction {
    /** Unique ID of the Action Groups action. */
    id?: string;
    /** Display name of the Action Groups action. */
    displayName?: string;
    /** Type of the Action Groups action. */
    type: string;
    /** Unique IDs of the rules that are tied to the Action Groups action. */
    rules: Array<string>;
}

/**
 * Definition: Application
 */
export interface Application {
    /** Unique ID of the application. */
    id?: string;
    /** Display name of the application. */
    displayName?: string;
    /** The URL subdomain of the application. */
    subdomain: string;
    /** The URL host of the application. */
    host: string;
}

/**
 * Definition: ApplicationCollection
 */
export interface ApplicationCollection {
    /** The collection of applications. */
    value: Array<Application>;
    /** URL to get the next page of applications. */
    nextLink?: string;
}

/**
 * Definition: Device
 */
export interface Device {
    /** Unique ID of the device. */
    id?: string;
    /** ETag used to prevent conflict in device updates. */
    etag?: string;
    /** Display name of the device. */
    displayName?: string;
    /** The device template definition for the device. */
    instanceOf?: string;
    /** Whether the device is simulated. */
    simulated?: boolean;
    /** Whether the device has been approved to connect to IoT Central. */
    approved?: boolean;
    /** Whether resources have been allocated for the device. */
    provisioned?: boolean;
}

/**
 * Definition: DeviceV1
 */
export interface DeviceV1 {
    /** Unique ID of the device. */
    id?: string;
    /** ETag used to prevent conflict in device updates. */
    etag?: string;
    /** Display name of the device. */
    displayName?: string;
    /** The device template definition for the device. */
    template?: string;
    /** Whether the device is simulated. */
    simulated?: boolean;
    /** Whether the device connection to IoT Central has been enabled. */
    enabled?: boolean;
    /** Whether resources have been allocated for the device. */
    provisioned?: boolean;
}

/**
 * Definition: DeviceV2
 */
export interface DeviceV2 {
    /** Unique ID of the device. */
    id?: string;
    /** ETag used to prevent conflict in device updates. */
    etag?: string;
    /** Display name of the device. */
    displayName?: string;
    /** The device template definition for the device. */
    template?: string;
    /** Whether the device is simulated. */
    simulated?: boolean;
    /** Whether the device connection to IoT Central has been enabled. */
    enabled?: boolean;
    /** List of organizations of the device, only one organization is supported today, multiple organizations will be supported soon. */
    organizations?: Array<string>;
    /** Whether resources have been allocated for the device. */
    provisioned?: boolean;
}

/**
 * Definition: DeviceCollection
 */
export interface DeviceCollection {
    /** The collection of devices. */
    value: Array<DeviceV1>;
    /** URL to get the next page of devices. */
    nextLink?: string;
}

/**
 * Definition: DeviceCloudProperties
 */
export interface DeviceCloudProperties {
    [key: string]: unknown;
}

/**
 * Definition: DeviceCommand
 */
export interface DeviceCommand {
    /** The request ID of the device command execution. */
    id?: string;
    /** The payload for the device command. */
    request?: unknown;
    /** The payload of the device command response. */
    response?: unknown;
    /** Connection timeout in seconds to wait for a disconnected device to come online. Defaults to 0 seconds. */
    connectionTimeout?: number;
    /** Response timeout in seconds to wait for a command completion on a device. Defaults to 30 seconds. */
    responseTimeout?: number;
    /** The status code of the device command response. */
    responseCode?: number;
}

/**
 * Definition: DeviceCommandV1
 */
export interface DeviceCommandV1 {
    /** The request ID of the device command execution. */
    id?: string;
    /** The payload for the device command. */
    request?: unknown;
    /** The payload of the device command response. */
    response?: unknown;
    /** Connection timeout in seconds to wait for a disconnected device to come online. Defaults to 0 seconds. */
    connectionTimeout?: number;
    /** Response timeout in seconds to wait for a command completion on a device. Defaults to 30 seconds. */
    responseTimeout?: number;
    /** The status code of the device command response. */
    responseCode?: number;
}

/**
 * Definition: DeviceComponentCommand
 */
export interface DeviceComponentCommand {
    /** The request ID of the device command execution. */
    id?: string;
    /** The payload for the device command. */
    request?: unknown;
    /** The payload of the device command response. */
    response?: unknown;
    /** Connection timeout in seconds to wait for a disconnected device to come online. Defaults to 0 seconds. */
    connectionTimeout?: number;
    /** Response timeout in seconds to wait for a command completion on a device. Defaults to 30 seconds. */
    responseTimeout?: number;
    /** The status code of the device command response. */
    responseCode?: number;
}

/**
 * Definition: DeviceModuleCommand
 */
export interface DeviceModuleCommand {
    /** The request ID of the device command execution. */
    id?: string;
    /** The payload for the device command. */
    request?: unknown;
    /** The payload of the device command response. */
    response?: unknown;
    /** Connection timeout in seconds to wait for a disconnected device to come online. Defaults to 0 seconds. */
    connectionTimeout?: number;
    /** Response timeout in seconds to wait for a command completion on a device. Defaults to 30 seconds. */
    responseTimeout?: number;
    /** The status code of the device command response. */
    responseCode?: number;
}

/**
 * Definition: DeviceModuleComponentCommand
 */
export interface DeviceModuleComponentCommand {
    /** The request ID of the device command execution. */
    id?: string;
    /** The payload for the device command. */
    request?: unknown;
    /** The payload of the device command response. */
    response?: unknown;
    /** Connection timeout in seconds to wait for a disconnected device to come online. Defaults to 0 seconds. */
    connectionTimeout?: number;
    /** Response timeout in seconds to wait for a command completion on a device. Defaults to 30 seconds. */
    responseTimeout?: number;
    /** The status code of the device command response. */
    responseCode?: number;
}

/**
 * Definition: DeviceGroup
 */
export interface DeviceGroup {
    /** Unique ID of the device group. */
    id?: string;
    /** Display name of the device group. */
    displayName: string;
    /** Query defining which devices should be in this group. */
    filter: string;
    /** Short summary of device group. */
    description?: string;
    /** ETag used to prevent conflict in device group updates. */
    etag?: string;
    /** List of organizations of the device group, only one organization is supported today, multiple organizations will be supported soon. */
    organizations?: Array<string>;
}

/**
 * Definition: DeviceGroupCollection
 */
export interface DeviceGroupCollection {
    /** The collection of device groups. */
    value: Array<DeviceGroup>;
    /** URL to get the next page of device groups. */
    nextLink?: string;
}

/**
 * Definition: DeviceGroupDeviceCollection
 */
export interface DeviceGroupDeviceCollection {
    /** The collection of devices belonging to the device group. */
    value: Array<DeviceV1>;
    /** URL to get the next page of devices in the group. */
    nextLink?: string;
}

/**
 * Definition: DeviceProperties
 */
export interface DeviceProperties {
    [key: string]: unknown;
}

/**
 * Definition: ModuleProperties
 */
export interface ModuleProperties {
    [key: string]: unknown;
}

/**
 * Definition: DeviceRelationship
 */
export interface DeviceRelationship {
    /** Unique ID of the device relationship. */
    id?: string;
    /** The device ID of the source (child) device. */
    target: string;
}

/**
 * Definition: DeviceRelationshipStatic
 */
export interface DeviceRelationshipStatic {
    /** Unique ID of the device relationship. */
    id?: string;
    /** The device ID of the source (parent) device. */
    source?: string;
    /** The device ID of the source (child) device. */
    target?: string;
}

/**
 * Definition: DeviceRelationshipCollection
 */
export interface DeviceRelationshipCollection {
    /** The collection of device relationships. */
    value: Array<DeviceRelationshipStatic>;
    /** URL to get the next page of device relationships. */
    nextLink?: string;
}

/**
 * Definition: DeviceTelemetry
 */
export interface DeviceTelemetry {
    /** The last known value of this device telemetry. */
    value?: unknown;
    /** String-formatted date representing the time when the telemetry value was sent. */
    timestamp?: string;
}

/**
 * Definition: DeviceComponentTelemetry
 */
export interface DeviceComponentTelemetry {
    /** The last known value of this device telemetry. */
    value?: unknown;
    /** String-formatted date representing the time when the telemetry value was sent. */
    timestamp?: string;
}

/**
 * Definition: DeviceModuleTelemetry
 */
export interface DeviceModuleTelemetry {
    /** The last known value of this device telemetry. */
    value?: unknown;
    /** String-formatted date representing the time when the telemetry value was sent. */
    timestamp?: string;
}

/**
 * Definition: DeviceModuleComponentTelemetry
 */
export interface DeviceModuleComponentTelemetry {
    /** The last known value of this device telemetry. */
    value?: unknown;
    /** String-formatted date representing the time when the telemetry value was sent. */
    timestamp?: string;
}

/**
 * Definition: DeviceTemplate
 */
export interface DeviceTemplate {
    /** Unique ID of the device template. */
    "@id"?: string;
    /** ETag used to prevent conflict in device template updates. */
    etag?: string;
    /** The JSON-LD types of this device template. */
    "@type": Array<string>;
    /** Display name of the device template. */
    displayName?: string;
    /** Detailed description of the device template. */
    description?: string;
    /** The capability model utilized by this device template. */
    capabilityModel?: unknown;
}

/**
 * Definition: DeviceTemplateCollection
 */
export interface DeviceTemplateCollection {
    /** The collection of device templates. */
    value: Array<DeviceTemplate>;
    /** URL to get the next page of device templates. */
    nextLink?: string;
}

/**
 * Definition: PropertyJobData
 */
export interface PropertyJobData {
    /** Data related to the operation being performed by this job. All entries must be of the same type. */
    data: Array<Record<string, unknown>>;
}

/**
 * Definition: CommandJobData
 */
export interface CommandJobData {
    /** Data related to the operation being performed by this job. All entries must be of the same type. */
    data: Array<Record<string, unknown>>;
}

/**
 * Definition: CloudPropertyJobData
 */
export interface CloudPropertyJobData {
    /** The collection of job data. */
    data: Array<Record<string, unknown>>;
}

/**
 * Definition: DeviceTemplateMigrationJobData
 */
export interface DeviceTemplateMigrationJobData {
    /** The collection of job data. */
    data: Array<Record<string, unknown>>;
}

/**
 * Definition: JobDeviceStatus
 */
export interface JobDeviceStatus {
    /** ID of the device whose job status is being provided. */
    id?: string;
    /** Indicates whether the job is starting, running, etc. for the given device. */
    status?: string;
}

/**
 * Definition: JobDeviceStatusCollection
 */
export interface JobDeviceStatusCollection {
    /** The collection of job device statuses. */
    value: Array<JobDeviceStatus>;
    /** URL to get the next page of job device statuses. */
    nextLink?: string;
}

/**
 * Definition: JobBatch
 */
export interface JobBatch {
    /** Whether batching is done on a specified number of devices or a percentage of the total devices. */
    type: string;
    /** The number or percentage of devices on which batching is done. */
    value: number;
}

/**
 * Definition: JobCancellationThreshold
 */
export interface JobCancellationThreshold {
    /** Whether the cancellation threshold is per a specified number of devices or a percentage of the total devices. */
    type: string;
    /** The number or percentage of devices on which the cancellation threshold is applied. */
    value: number;
    /** Whether the cancellation threshold applies per-batch or to the overall job. */
    batch?: boolean;
}

/**
 * Definition: Job
 */
export interface Job {
    /** Unique ID of the job. */
    id?: string;
    /** Display name of the job. */
    displayName: string;
    /** Detailed description of the job. */
    description?: string;
    /** The ID of the device group on which to execute the job. */
    group: string;
    /** The batching configuration for the job. */
    batch?: JobBatch;
    /** The cancellation threshold for the job. */
    cancellationThreshold?: JobCancellationThreshold;
    /** List of organizations of the job, only one organization is supported today, multiple organizations will be supported soon. */
    organizations?: Array<string>;
    /** Id of the scheduled job definition that created this job. */
    scheduledJobId?: string;
}

/**
 * Definition: JobStatic
 */
export interface JobStatic {
    /** Unique ID of the job. */
    id?: string;
    /** Display name of the job. */
    displayName: string;
    /** Detailed description of the job. */
    description?: string;
    /** The ID of the device group on which to execute the job. */
    group: string;
    /** The batching configuration for the job. */
    batch?: JobBatch;
    /** The cancellation threshold for the job. */
    cancellationThreshold?: JobCancellationThreshold;
    /** Data related to the operation being performed by this job. All entries must be of the same type. */
    data: Array<Record<string, unknown>>;
    /** List of organizations of the job, only one organization is supported today, multiple organizations will be supported soon. */
    organizations?: Array<string>;
    /** Id of the scheduled job definition that created this job. */
    scheduledJobId?: string;
}

/**
 * Definition: JobCollection
 */
export interface JobCollection {
    /** The collection of jobs. */
    value: Array<JobStatic>;
    /** URL to get the next page of jobs. */
    nextLink?: string;
}

/**
 * Definition: Organization
 */
export interface Organization {
    /** Unique ID of the organization. */
    id?: string;
    /** Display name of the organization. */
    displayName?: string;
    /** Display name of the parent organization. */
    parent?: string;
}

/**
 * Definition: OrganizationStatic
 */
export type OrganizationStatic = string;

/**
 * Definition: OrganizationCollection
 */
export interface OrganizationCollection {
    /** The collection of organizations. */
    value: Array<Organization>;
    /** URL to get the next page of organizations. */
    nextLink?: string;
}

/**
 * Definition: Role
 */
export interface Role {
    /** Unique ID of the role. */
    id?: string;
    /** Display name of the role. */
    displayName?: string;
}

/**
 * Definition: RoleCollection
 */
export interface RoleCollection {
    /** The collection of roles. */
    value: Array<Role>;
}

/**
 * Definition: Rule
 */
export interface Rule {
    /** Unique ID of the rule. */
    id?: string;
    /** Display name of the rule. */
    displayName?: string;
}

/**
 * Definition: RuleCollection
 */
export interface RuleCollection {
    /** The collection of rules. */
    value: Array<Rule>;
    /** URL to get the next page of rules. */
    nextLink?: string;
}

/**
 * Definition: JobSchedule
 */
export interface JobSchedule {
    /** The schedule at which to execute the job. */
    schedule?: Record<string, unknown>;
}

/**
 * Definition: DateJobSchedule
 */
export interface DateJobSchedule {
    /** The schedule at which to execute the job. */
    schedule: Record<string, unknown>;
}

/**
 * Definition: OccurrencesJobSchedule
 */
export interface OccurrencesJobSchedule {
    /** The schedule at which to execute the job. */
    schedule: Record<string, unknown>;
}

/**
 * Definition: JobScheduleEnd
 */
export interface JobScheduleEnd {
    /** Type of the job schedule end. */
    type?: string;
    /** The date when to end the scheduled job (YYYY-MM-DDTHH:MM:SSZ). */
    date?: string;
    /** The number of occurrences after which to end the scheduled job. */
    occurrences?: number;
}

/**
 * Definition: ScheduledJob
 */
export interface ScheduledJob {
    /** ETag used to prevent conflict in scheduled job updates. */
    etag?: string;
    /** Unique ID of the scheduled job. */
    id?: string;
    /** Display name of the scheduled job. */
    displayName: string;
    /** Detailed description of the scheduled job. */
    description?: string;
    /** The ID of the device group on which to execute the job. */
    group: string;
    /** The batching configuration for the scheduled job. */
    batch?: JobBatch;
    /** The cancellation threshold for the scheduled job. */
    cancellationThreshold?: JobCancellationThreshold;
    /** List of organizations of the job, only one organization is supported today, multiple organizations will be supported soon. */
    organizations?: Array<string>;
    /** The schedule at which to execute the job. */
    schedule: Record<string, unknown>;
    /** Whether the scheduled job is enabled. */
    enabled?: boolean;
    /** Whether the scheduled job has completed. */
    completed?: boolean;
}

/**
 * Definition: ScheduledJobStatic
 */
export interface ScheduledJobStatic {
    /** ETag used to prevent conflict in scheduled job updates. */
    etag?: string;
    /** Unique ID of the scheduled job. */
    id?: string;
    /** Display name of the scheduled job. */
    displayName: string;
    /** Detailed description of the scheduled job. */
    description?: string;
    /** The ID of the device group on which to execute the job. */
    group: string;
    /** The batching configuration for the scheduled job. */
    batch?: JobBatch;
    /** The cancellation threshold for the scheduled job. */
    cancellationThreshold?: JobCancellationThreshold;
    /** Data related to the operation being performed by this job. All entries must be of the same type. */
    data: Array<Record<string, unknown>>;
    /** List of organizations of the job, only one organization is supported today, multiple organizations will be supported soon. */
    organizations?: Array<string>;
    /** The schedule at which to execute the job. */
    schedule: Record<string, unknown>;
    /** Whether the scheduled job is enabled. */
    enabled?: boolean;
    /** Whether the scheduled job has completed. */
    completed?: boolean;
}

/**
 * Definition: ScheduledJobPatch
 */
export interface ScheduledJobPatch {
    /** ETag used to prevent conflict in scheduled job updates. */
    etag?: string;
    /** Unique ID of the scheduled job. */
    id?: string;
    /** The schedule at which to execute the job. */
    schedule: Record<string, unknown>;
    /** Whether the scheduled job is enabled. */
    enabled?: boolean;
    /** Whether the scheduled job has completed. */
    completed?: boolean;
}

/**
 * Definition: ScheduledJobCollection
 */
export interface ScheduledJobCollection {
    /** The collection of scheduled jobs. */
    value: Array<ScheduledJob>;
    /** URL to get the next page of scheduled jobs. */
    nextLink?: string;
}

/**
 * Definition: ScheduledJobJobCollection
 */
export interface ScheduledJobJobCollection {
    /** The collection of jobs. */
    value: Array<JobStatic>;
    /** URL to get the next page of jobs. */
    nextLink?: string;
}

/**
 * Definition: User
 */
export interface User {
    [key: string]: unknown;
}

/**
 * Definition: UserPatch
 */
export interface UserPatch {
    [key: string]: unknown;
}

/**
 * Definition: UserStatic
 */
export interface UserStatic {
    /** Unique ID of the user. */
    id?: string;
    /** Type of the user. */
    type?: string;
    /** List of role assignments that specify the permissions to access the application. */
    roles: Array<RoleAssignment>;
}

/**
 * Definition: EmailUser
 */
export interface EmailUser {
    /** Unique ID of the user. */
    id?: string;
    /** Type of the user. */
    type: string;
    /** List of role assignments that specify the permissions to access the application. */
    roles: Array<RoleAssignment>;
    /** Email address of the user. */
    email: string;
}

/**
 * Definition: ServicePrincipalUser
 */
export interface ServicePrincipalUser {
    /** Unique ID of the user. */
    id?: string;
    /** Type of the user. */
    type: string;
    /** List of role assignments that specify the permissions to access the application. */
    roles: Array<RoleAssignment>;
    /** The Microsoft Entra ID tenant ID of the service principal. */
    tenantId: string;
    /** The Microsoft Entra ID object ID of the service principal. */
    objectId: string;
}

/**
 * Definition: UserCollection
 */
export interface UserCollection {
    /** The collection of users. */
    value: Array<UserStatic>;
}

/**
 * Definition: RoleAssignment
 */
export interface RoleAssignment {
    /** ID of the role for this role assignment. */
    role: string;
}

/**
 * Definition: WebhookActionDevice
 */
export interface WebhookActionDevice {
    /** Unique ID of the device. */
    id?: string;
    /** ETag used to prevent conflict in device updates. */
    etag?: string;
    /** Display name of the device. */
    displayName?: string;
    /** The device template definition for the device. */
    instanceOf?: string;
    /** Whether the device is simulated. */
    simulated?: boolean;
    /** Whether the device has been approved to connect to IoT Central. */
    approved?: boolean;
    /** Whether resources have been allocated for the device. */
    provisioned?: boolean;
    /** Cloud properties that triggered the webhook. */
    cloudProperties?: Record<string, unknown>;
    /** Device properties that triggered the webhook. */
    properties?: Record<string, unknown>;
    /** Device telemetry that triggered the webhook. */
    telemetry?: Record<string, unknown>;
}

/**
 * Definition: WebhookActionBody
 */
export interface WebhookActionBody {
    /** Action that triggered the webhook. */
    action: Action;
    /** Application that triggered the webhook. */
    application: Application;
    /** Device that triggered the webhook. */
    device: WebhookActionDevice;
    /** Rule that triggered the webhook. */
    rule: Rule;
    /** Timestamp at which the webhook was triggered. */
    timestamp: string;
}

/**
 * Definition: WorkflowTrigger
 */
export interface WorkflowTrigger {
    /** Unique ID of the workflow trigger. */
    id?: string;
    /** Unique ID of the rule that is tied to the workflow trigger. */
    rule: string;
    /** Callback URL of the workflow Trigger. */
    url: string;
}

/**
 * Definition: DynamicValues
 */
export interface DynamicValues {
    [key: string]: unknown;
}
// #endregion Types

export const AzureiotcentralTriggerOperations = {
    OnWorkflowCreateTrigger: "Workflow_CreateTrigger",
} as const;

export type AzureiotcentralTriggerOperation = typeof AzureiotcentralTriggerOperations[keyof typeof AzureiotcentralTriggerOperations];

export const AzureiotcentralTriggerParameters = {
    OnWorkflowCreateTrigger: {
        application: {
            name: "application",
            type: "string",
            required: true,
            description: "Unique ID of the application.",
            summary: "Application",
            dynamicValuesOperationId: "Applications_List",
        },
    },
} as const;

// #region Client

/**
 * Typed client for the azureiotcentral connector.
 */
export class AzureiotcentralClient extends ConnectorClientBase {

    /**
     * Creates a new AzureiotcentralClient.
     * @param connectionRuntimeUrl The connection runtime URL from Azure Portal.
     * @param tokenProvider The token provider for authentication.
     * @param options Optional connector client options.
     */
    constructor(connectionRuntimeUrl: string, tokenProvider: TokenProvider, options?: ConnectorClientOptions) {
        super(connectionRuntimeUrl, tokenProvider, options);
    }

    public get connectorName(): string {
        return "azureiotcentral";
    }

    /**
     * List device groups
     * @remarks Get the list of device groups in an application.
     */
    public async deviceGroupsListAsync(application?: string, abortSignal?: AbortSignalLike): Promise<DeviceGroupCollection> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/deviceGroups` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceGroupCollection>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceGroupCollection;
    }

    /**
     * Get a device group
     * @remarks Get details about an existing device group by ID.
     */
    public async deviceGroupsGetAsync(deviceGroupId: string, application?: string, abortSignal?: AbortSignalLike): Promise<DeviceGroup> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/deviceGroups/${deviceGroupId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceGroup>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceGroup;
    }

    /**
     * Create a device group
     * @remarks Create or update a device group.
     */
    public async deviceGroupsSetAsync(input: DeviceGroup, deviceGroupId: string, application?: string, abortSignal?: AbortSignalLike): Promise<DeviceGroup> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/deviceGroups/${deviceGroupId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceGroup>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceGroup;
    }

    /**
     * Delete device group
     * @remarks Delete an existing device group by ID
     */
    public async deviceGroupsRemoveAsync(deviceGroupId: string, application?: string, abortSignal?: AbortSignalLike): Promise<void> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/deviceGroups/${deviceGroupId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<void>("DELETE", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `DELETE ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }
    }

    /**
     * Get devices by device group ID
     * @remarks Get the list of devices in a device group in an application.
     */
    public async deviceGroupsGetDevicesAsync(deviceGroupId: string, application?: string, abortSignal?: AbortSignalLike): Promise<DeviceGroupDeviceCollection> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/deviceGroups/${deviceGroupId}/devices` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceGroupDeviceCollection>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceGroupDeviceCollection;
    }

    /**
     * Get device cloud properties (deprecated)
     * @remarks Get all cloud property values of a device by device ID (deprecated).
     */
    public async devicesGetCloudPropertiesAsync(deviceId: string, application?: string, instanceOf?: string, abortSignal?: AbortSignalLike): Promise<DeviceCloudProperties> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (instanceOf !== undefined) {
            queryParams.push(`instanceOf=${encodeURIComponent(String(instanceOf))}`);
        }
        const requestPath = `/api/preview/devices/${deviceId}/cloudProperties` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceCloudProperties>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceCloudProperties;
    }

    /**
     * Update device cloud properties (deprecated)
     * @remarks Update all cloud property values of a device by device ID (deprecated).
     */
    public async devicesUpdateCloudPropertiesAsync(input: DeviceCloudProperties, deviceId: string, application?: string, instanceOf?: string, abortSignal?: AbortSignalLike): Promise<DeviceCloudProperties> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (instanceOf !== undefined) {
            queryParams.push(`instanceOf=${encodeURIComponent(String(instanceOf))}`);
        }
        const requestPath = `/api/preview/devices/${deviceId}/cloudProperties` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceCloudProperties>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceCloudProperties;
    }

    /**
     * Execute a device command (deprecated)
     * @remarks Execute a command on a device (deprecated).
     */
    public async devicesExecuteComponentCommandAsync(input: DeviceCommand, deviceId: string, componentName: string, commandName: string, application?: string, instanceOf?: string, abortSignal?: AbortSignalLike): Promise<DeviceCommand> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (instanceOf !== undefined) {
            queryParams.push(`instanceOf=${encodeURIComponent(String(instanceOf))}`);
        }
        const requestPath = `/api/preview/devices/${deviceId}/components/${componentName}/commands/${commandName}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceCommand>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceCommand;
    }

    /**
     * List relationships
     * @remarks List all relationships based on device ID.
     */
    public async deviceRelationshipsListAsync(deviceId: string, application?: string, abortSignal?: AbortSignalLike): Promise<DeviceRelationshipCollection> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/devices/${deviceId}/relationships` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceRelationshipCollection>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceRelationshipCollection;
    }

    /**
     * Get device relationship
     * @remarks Get details about an existing device relationship by device ID and by relationship ID.
     */
    public async deviceRelationshipsGetAsync(deviceId: string, relationshipId: string, application?: string, abortSignal?: AbortSignalLike): Promise<DeviceRelationshipStatic> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/devices/${deviceId}/relationships/${relationshipId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceRelationshipStatic>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceRelationshipStatic;
    }

    /**
     * Create a device relationship
     * @remarks Create a new device relationship between the given device and a new device given a device ID and a relationship ID.
     */
    public async deviceRelationshipsSetAsync(input: DeviceRelationship, relationshipId: string, deviceId: string, application?: string, abortSignal?: AbortSignalLike): Promise<DeviceRelationshipStatic> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/devices/${deviceId}/relationships/${relationshipId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceRelationshipStatic>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceRelationshipStatic;
    }

    /**
     * Update a device relationship
     * @remarks Update an existing device relationship between the given device and a new device given a device ID and a relationship ID.
     */
    public async deviceRelationshipsUpdateAsync(input: DeviceRelationship, deviceId: string, relationshipId: string, application?: string, abortSignal?: AbortSignalLike): Promise<DeviceRelationshipStatic> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/devices/${deviceId}/relationships/${relationshipId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceRelationshipStatic>("PATCH", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PATCH ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceRelationshipStatic;
    }

    /**
     * Delete a device relationship
     * @remarks Delete an existing device relationship by device ID and relationship ID.
     */
    public async deviceRelationshipsRemoveAsync(deviceId: string, relationshipId: string, application?: string, abortSignal?: AbortSignalLike): Promise<void> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/devices/${deviceId}/relationships/${relationshipId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<void>("DELETE", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `DELETE ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }
    }

    /**
     * List jobs
     * @remarks Get the list of jobs in an application.
     */
    public async jobsListAsync(application?: string, abortSignal?: AbortSignalLike): Promise<JobCollection> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/jobs` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<JobCollection>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as JobCollection;
    }

    /**
     * Get a job
     * @remarks Get details about an existing job by ID.
     */
    public async jobsGetAsync(jobId: string, application?: string, abortSignal?: AbortSignalLike): Promise<JobStatic> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/jobs/${jobId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<JobStatic>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as JobStatic;
    }

    /**
     * Create a job
     * @remarks Create and execute a new job via its job definition.
     */
    public async jobsSetAsync(input: Job, jobId: string, application?: string, jobType?: string, abortSignal?: AbortSignalLike): Promise<Job> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (jobType !== undefined) {
            queryParams.push(`job_type=${encodeURIComponent(String(jobType))}`);
        }
        const requestPath = `/api/ga_2022_07_31/jobs/${jobId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Job>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Job;
    }

    /**
     * Get device statuses
     * @remarks Get the list of individual device statuses by job ID.
     */
    public async jobsGetDevicesAsync(jobId: string, application?: string, abortSignal?: AbortSignalLike): Promise<JobDeviceStatusCollection> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/jobs/${jobId}/devices` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<JobDeviceStatusCollection>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as JobDeviceStatusCollection;
    }

    /**
     * Stop a running job
     * @remarks Stop execution of a job that is currently running.
     */
    public async jobsStopAsync(jobId: string, application?: string, abortSignal?: AbortSignalLike): Promise<void> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/jobs/${jobId}/stop` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<void>("POST", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }
    }

    /**
     * Resume a stopped job
     * @remarks Resume execution of an existing stopped job.
     */
    public async jobsResumeAsync(jobId: string, application?: string, abortSignal?: AbortSignalLike): Promise<void> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/jobs/${jobId}/resume` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<void>("POST", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }
    }

    /**
     * Resume a job on failed devices
     * @remarks Execute a rerun of an existing job on all failed devices.
     */
    public async jobsRerunAsync(jobId: string, rerunId: string, application?: string, abortSignal?: AbortSignalLike): Promise<JobStatic> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/jobs/${jobId}/rerun/${rerunId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<JobStatic>("PUT", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as JobStatic;
    }

    /**
     * List organizations
     * @remarks Get the list of organizations in an application.
     */
    public async organizationsListAsync(application?: string, abortSignal?: AbortSignalLike): Promise<OrganizationCollection> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/organizations` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<OrganizationCollection>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as OrganizationCollection;
    }

    /**
     * Get an organization
     * @remarks Get details about an existing organization by ID.
     */
    public async organizationsGetAsync(organizationId: string, application?: string, abortSignal?: AbortSignalLike): Promise<Organization> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/organizations/${organizationId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Organization>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Organization;
    }

    /**
     * Create an organization
     * @remarks Create a new organization.
     */
    public async organizationsSetAsync(input: Organization, organizationId: string, application?: string, abortSignal?: AbortSignalLike): Promise<Organization> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/organizations/${organizationId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Organization>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Organization;
    }

    /**
     * Delete organization
     * @remarks Delete an existing organization by ID
     */
    public async organizationsRemoveAsync(organizationId: string, application?: string, abortSignal?: AbortSignalLike): Promise<void> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/organizations/${organizationId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<void>("DELETE", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `DELETE ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }
    }

    /**
     * List scheduled jobs
     * @remarks Get the list of scheduled jobs in an application.
     */
    public async scheduledJobsListAsync(application?: string, abortSignal?: AbortSignalLike): Promise<ScheduledJobCollection> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/scheduledJobs` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<ScheduledJobCollection>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as ScheduledJobCollection;
    }

    /**
     * Get a scheduled job
     * @remarks Get details about an existing scheduled job by ID.
     */
    public async scheduledJobsGetAsync(scheduledJobId: string, application?: string, abortSignal?: AbortSignalLike): Promise<ScheduledJob> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/scheduledJobs/${scheduledJobId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<ScheduledJob>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as ScheduledJob;
    }

    /**
     * Create a scheduled job
     * @remarks Create and execute a new scheduled job via its job definition.
     */
    public async scheduledJobsSetAsync(input: ScheduledJob, scheduledJobId: string, application?: string, jobType?: string, scheduledJobEndType?: string, abortSignal?: AbortSignalLike): Promise<ScheduledJob> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (jobType !== undefined) {
            queryParams.push(`job_type=${encodeURIComponent(String(jobType))}`);
        }
        if (scheduledJobEndType !== undefined) {
            queryParams.push(`scheduled_job_end_type=${encodeURIComponent(String(scheduledJobEndType))}`);
        }
        const requestPath = `/api/ga_2022_07_31/scheduledJobs/${scheduledJobId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<ScheduledJob>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as ScheduledJob;
    }

    /**
     * Update a scheduled job
     * @remarks Update an existing scheduled job by ID.
     */
    public async scheduledJobsUpdateAsync(input: ScheduledJobPatch, scheduledJobId: string, application?: string, scheduledJobEndType?: string, abortSignal?: AbortSignalLike): Promise<ScheduledJobStatic> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (scheduledJobEndType !== undefined) {
            queryParams.push(`scheduled_job_end_type=${encodeURIComponent(String(scheduledJobEndType))}`);
        }
        const requestPath = `/api/ga_2022_07_31/scheduledJobs/${scheduledJobId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<ScheduledJobStatic>("PATCH", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PATCH ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as ScheduledJobStatic;
    }

    /**
     * Delete a scheduled job
     * @remarks Delete an existing scheduled job by ID.
     */
    public async scheduledJobsRemoveAsync(scheduledJobId: string, application?: string, abortSignal?: AbortSignalLike): Promise<void> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/scheduledJobs/${scheduledJobId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<void>("DELETE", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `DELETE ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }
    }

    /**
     * Get jobs by scheduled job ID
     * @remarks Get the list of jobs for a scheduled job definition.
     */
    public async scheduledJobsListJobsAsync(scheduledJobId: string, application?: string, abortSignal?: AbortSignalLike): Promise<ScheduledJobJobCollection> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/scheduledJobs/${scheduledJobId}/jobs` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<ScheduledJobJobCollection>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as ScheduledJobJobCollection;
    }

    /**
     * Get a device by ID
     * @remarks Get details about an existing device by device ID.
     */
    public async devicesGetAsync(deviceId: string, application?: string, abortSignal?: AbortSignalLike): Promise<DeviceV1> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/v1/devices/${deviceId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceV1>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceV1;
    }

    /**
     * Get device command response
     * @remarks Get the last response for a device command.
     */
    public async devicesGetCommandResponseAsync(deviceId: string, commandName: string, application?: string, template?: string, abortSignal?: AbortSignalLike): Promise<DeviceCommandV1> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(template))}`);
        }
        const requestPath = `/api/v1/devices/${deviceId}/commands/${commandName}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceCommandV1>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceCommandV1;
    }

    /**
     * Get component command response
     * @remarks Get the last response for a device component command.
     */
    public async devicesGetComponentCommandResponseAsync(deviceId: string, componentName: string, commandName: string, application?: string, template?: string, abortSignal?: AbortSignalLike): Promise<DeviceComponentCommand> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(template))}`);
        }
        const requestPath = `/api/v1/devices/${deviceId}/components/${componentName}/commands/${commandName}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceComponentCommand>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceComponentCommand;
    }

    /**
     * Get component telemetry value
     * @remarks Get the last telemetry value from a component.
     */
    public async devicesGetComponentTelemetryValueAsync(deviceId: string, componentName: string, telemetryName: string, application?: string, template?: string, abortSignal?: AbortSignalLike): Promise<DeviceComponentTelemetry> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(template))}`);
        }
        const requestPath = `/api/v1/devices/${deviceId}/components/${componentName}/telemetry/${telemetryName}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceComponentTelemetry>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceComponentTelemetry;
    }

    /**
     * Get module command response
     * @remarks Get the last response for a device module command
     */
    public async devicesGetModuleCommandResponseAsync(deviceId: string, module: string, commandName: string, application?: string, template?: string, abortSignal?: AbortSignalLike): Promise<DeviceModuleCommand> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(template))}`);
        }
        const requestPath = `/api/v1/devices/${deviceId}/modules/${module}/commands/${commandName}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceModuleCommand>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceModuleCommand;
    }

    /**
     * Get module component command response
     * @remarks Get the last response for a module component command.
     */
    public async devicesGetModuleComponentCommandResponseAsync(deviceId: string, module: string, componentName: string, commandName: string, application?: string, template?: string, abortSignal?: AbortSignalLike): Promise<DeviceModuleComponentCommand> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(template))}`);
        }
        const requestPath = `/api/v1/devices/${deviceId}/modules/${module}/components/${componentName}/commands/${commandName}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceModuleComponentCommand>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceModuleComponentCommand;
    }

    /**
     * Get module component telemetry value
     * @remarks Get the last telemetry value from a module component.
     */
    public async devicesGetModuleComponentTelemetryValueAsync(deviceId: string, module: string, componentName: string, telemetryName: string, application?: string, template?: string, abortSignal?: AbortSignalLike): Promise<DeviceModuleComponentTelemetry> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(template))}`);
        }
        const requestPath = `/api/v1/devices/${deviceId}/modules/${module}/components/${componentName}/telemetry/${telemetryName}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceModuleComponentTelemetry>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceModuleComponentTelemetry;
    }

    /**
     * Get module properties
     * @remarks Get all property values of a module.
     */
    public async devicesGetModulePropertiesAsync(deviceId: string, module: string, application?: string, template?: string, abortSignal?: AbortSignalLike): Promise<ModuleProperties> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(template))}`);
        }
        const requestPath = `/api/v1/devices/${deviceId}/modules/${module}/properties` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<ModuleProperties>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as ModuleProperties;
    }

    /**
     * Get module telemetry value
     * @remarks Get the last telemetry value from a module.
     */
    public async devicesGetModuleTelemetryValueAsync(deviceId: string, module: string, telemetryName: string, application?: string, template?: string, abortSignal?: AbortSignalLike): Promise<DeviceModuleTelemetry> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(template))}`);
        }
        const requestPath = `/api/v1/devices/${deviceId}/modules/${module}/telemetry/${telemetryName}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceModuleTelemetry>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceModuleTelemetry;
    }

    /**
     * Get device properties
     * @remarks Get all property values of a device by device ID.
     */
    public async devicesGetPropertiesAsync(deviceId: string, application?: string, template?: string, abortSignal?: AbortSignalLike): Promise<DeviceProperties> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(template))}`);
        }
        const requestPath = `/api/v1/devices/${deviceId}/properties` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceProperties>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceProperties;
    }

    /**
     * Get device telemetry value
     * @remarks Get the last telemetry value from a device.
     */
    public async devicesGetTelemetryValueAsync(deviceId: string, telemetryName: string, application?: string, template?: string, abortSignal?: AbortSignalLike): Promise<DeviceTelemetry> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(template))}`);
        }
        const requestPath = `/api/v1/devices/${deviceId}/telemetry/${telemetryName}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceTelemetry>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceTelemetry;
    }

    /**
     * List devices
     * @remarks Get the list of devices in an application.
     */
    public async devicesListAsync(application?: string, abortSignal?: AbortSignalLike): Promise<DeviceCollection> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/v1/devices` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceCollection>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceCollection;
    }

    /**
     * Delete a device
     * @remarks Delete an existing device by device ID.
     */
    public async devicesRemoveAsync(deviceId: string, application?: string, abortSignal?: AbortSignalLike): Promise<void> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/v1/devices/${deviceId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<void>("DELETE", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `DELETE ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }
    }

    /**
     * Run a device command
     * @remarks Run a command on a device.
     */
    public async devicesRunCommandAsync(input: DeviceCommandV1, deviceId: string, commandName: string, application?: string, template?: string, abortSignal?: AbortSignalLike): Promise<DeviceCommandV1> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(template))}`);
        }
        const requestPath = `/api/v1/devices/${deviceId}/commands/${commandName}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceCommandV1>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceCommandV1;
    }

    /**
     * Run a component command
     * @remarks Run a command on a component.
     */
    public async devicesRunComponentCommandAsync(input: DeviceComponentCommand, deviceId: string, componentName: string, commandName: string, application?: string, template?: string, abortSignal?: AbortSignalLike): Promise<DeviceComponentCommand> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(template))}`);
        }
        const requestPath = `/api/v1/devices/${deviceId}/components/${componentName}/commands/${commandName}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceComponentCommand>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceComponentCommand;
    }

    /**
     * Run a module command
     * @remarks Run a command on a module.
     */
    public async devicesRunModuleCommandAsync(input: DeviceModuleCommand, deviceId: string, module: string, commandName: string, application?: string, template?: string, abortSignal?: AbortSignalLike): Promise<DeviceModuleCommand> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(template))}`);
        }
        const requestPath = `/api/v1/devices/${deviceId}/modules/${module}/commands/${commandName}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceModuleCommand>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceModuleCommand;
    }

    /**
     * Run a module component command
     * @remarks Run a command on a module component.
     */
    public async devicesRunModuleComponentCommandAsync(input: DeviceModuleComponentCommand, deviceId: string, module: string, componentName: string, commandName: string, application?: string, template?: string, abortSignal?: AbortSignalLike): Promise<DeviceModuleComponentCommand> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(template))}`);
        }
        const requestPath = `/api/v1/devices/${deviceId}/modules/${module}/components/${componentName}/commands/${commandName}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceModuleComponentCommand>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceModuleComponentCommand;
    }

    /**
     * Create or update a device
     * @remarks Create a new device or update an existing one by device ID.
     */
    public async devicesSetAsync(input: DeviceV2, deviceId: string, application?: string, abortSignal?: AbortSignalLike): Promise<DeviceV2> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/ga_2022_07_31/devices/${deviceId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceV2>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceV2;
    }

    /**
     * Update module properties
     * @remarks Update all property values of a module.
     */
    public async devicesUpdateModulePropertiesAsync(input: ModuleProperties, deviceId: string, module: string, application?: string, template?: string, abortSignal?: AbortSignalLike): Promise<ModuleProperties> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(template))}`);
        }
        const requestPath = `/api/v1/devices/${deviceId}/modules/${module}/properties` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<ModuleProperties>("PATCH", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PATCH ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as ModuleProperties;
    }

    /**
     * Update device properties
     * @remarks Update all property values of a device by device ID.
     */
    public async devicesUpdatePropertiesAsync(input: DeviceProperties, deviceId: string, application?: string, template?: string, abortSignal?: AbortSignalLike): Promise<DeviceProperties> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (template !== undefined) {
            queryParams.push(`template=${encodeURIComponent(String(template))}`);
        }
        const requestPath = `/api/v1/devices/${deviceId}/properties` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceProperties>("PATCH", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PATCH ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceProperties;
    }

    /**
     * Get a device template by ID
     * @remarks Get details about an existing device template by ID.
     */
    public async deviceTemplatesGetAsync(templateId: string, application?: string, abortSignal?: AbortSignalLike): Promise<DeviceTemplate> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/v1/deviceTemplates/${templateId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceTemplate>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceTemplate;
    }

    /**
     * List device templates
     * @remarks Get the list of device templates in an application.
     */
    public async deviceTemplatesListAsync(application?: string, abortSignal?: AbortSignalLike): Promise<DeviceTemplateCollection> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/v1/deviceTemplates` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DeviceTemplateCollection>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DeviceTemplateCollection;
    }

    /**
     * Delete a device template
     * @remarks Delete an existing device template by ID.
     */
    public async deviceTemplatesRemoveAsync(templateId: string, application?: string, abortSignal?: AbortSignalLike): Promise<void> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/v1/deviceTemplates/${templateId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<void>("DELETE", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `DELETE ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }
    }

    /**
     * Get role
     * @remarks Get a role by ID.
     */
    public async rolesGetAsync(roleId: string, application?: string, abortSignal?: AbortSignalLike): Promise<Role> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/v1/roles/${roleId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Role>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Role;
    }

    /**
     * List roles
     * @remarks Get the list of roles in an application.
     */
    public async rolesListAsync(application?: string, abortSignal?: AbortSignalLike): Promise<RoleCollection> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/v1/roles` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<RoleCollection>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as RoleCollection;
    }

    /**
     * Create user
     * @remarks Create a user in the application
     */
    public async usersCreateAsync(input: User, userId: string, application?: string, userType?: string, abortSignal?: AbortSignalLike): Promise<User> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (userType !== undefined) {
            queryParams.push(`user_type=${encodeURIComponent(String(userType))}`);
        }
        const requestPath = `/api/v1/users/${userId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<User>("PUT", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PUT ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as User;
    }

    /**
     * Get user
     * @remarks Get a user by ID
     */
    public async usersGetAsync(userId: string, application?: string, abortSignal?: AbortSignalLike): Promise<UserStatic> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/v1/users/${userId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<UserStatic>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as UserStatic;
    }

    /**
     * List users
     * @remarks Get the list of users in an application
     */
    public async usersListAsync(application?: string, abortSignal?: AbortSignalLike): Promise<UserCollection> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/v1/users` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<UserCollection>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as UserCollection;
    }

    /**
     * Delete user
     * @remarks Delete a user
     */
    public async usersRemoveAsync(userId: string, application?: string, abortSignal?: AbortSignalLike): Promise<void> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        const requestPath = `/api/v1/users/${userId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<void>("DELETE", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `DELETE ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }
    }

    /**
     * Update user
     * @remarks Update a user in the application via patch
     */
    public async usersUpdateAsync(input: UserPatch, userId: string, application?: string, userType?: string, abortSignal?: AbortSignalLike): Promise<UserPatch> {
        const queryParams: string[] = [];
        if (application !== undefined) {
            queryParams.push(`application=${encodeURIComponent(String(application))}`);
        }
        if (userType !== undefined) {
            queryParams.push(`user_type=${encodeURIComponent(String(userType))}`);
        }
        const requestPath = `/api/v1/users/${userId}` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<UserPatch>("PATCH", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `PATCH ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as UserPatch;
    }

}

// #endregion Client
