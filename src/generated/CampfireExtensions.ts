// CampfireExtensions.ts - Auto-generated DirectClient SDK for TypeScript
// Do not edit this file directly.

import { ConnectorClientBase } from "../azureConnectors/clientBase.ts";
import { ConnectorException } from "../azureConnectors/connectorException.ts";
import { ConnectorClientOptions } from "../azureConnectors/options.ts";
import { TokenProvider } from "../azureConnectors/authentication.ts";

// #region Types

/**
 * Definition: AccountsResponse
 */
export interface AccountsResponse {
    /** Collection Of Accounts */
    accounts?: Array<Record<string, unknown>>;
}

/**
 * Definition: Account
 */
export interface Account {
    /** Id of the account */
    id?: number;
    /** Name of the account */
    name?: string;
    /** Link to the account */
    app_href?: string;
    /** Account Url */
    account_url?: string;
}

/**
 * Definition: RoomsResponse
 */
export interface RoomsResponse {
    /** Collection of Rooms */
    rooms?: Array<Record<string, unknown>>;
}

/**
 * Definition: Room
 */
export interface Room {
    /** Room name. */
    name?: string;
    /** Room Id. */
    id?: number;
    /** Boolean value, True if a room is locked. */
    locked?: boolean;
    /** Room topic or description. */
    topic?: string;
}

/**
 * Definition: MessagesResponse
 */
export interface MessagesResponse {
    /** Collection Of messages */
    messages?: Array<Record<string, unknown>>;
}

/**
 * Definition: Message
 */
export interface Message {
    /** Message Body. */
    body?: string;
    /** Message Id. */
    id?: number;
    /** Boolean value, True if a message is starred. */
    starred?: boolean;
}

/**
 * Definition: UploadResponse
 */
export interface UploadResponse {
    /** Collection of Uploads */
    uploads?: Array<Record<string, unknown>>;
}

/**
 * Definition: Upload
 */
export interface Upload {
    /** File Url. */
    full_url?: string;
    /** Room Id. */
    room_id?: number;
    /** File Id. */
    id?: number;
    /** Format of the uploaded file. */
    content_type?: string;
    /** File name. */
    name?: string;
    /** Timestamp of upload. */
    created_at?: string;
}

/**
 * Definition: CreateUploadResponse
 */
export interface CreateUploadResponse {
    upload?: Upload;
}

/**
 * Definition: CreateMessageResponse
 */
export interface CreateMessageResponse {
    /** A Campfire message */
    message?: Record<string, unknown>;
}

/**
 * Definition: UserResponse
 */
export interface UserResponse {
    user?: Record<string, unknown>;
}
// #endregion Types

export const CampfireTriggerOperations = {
    OnNewMessage: "OnNewMessage",
    OnNewRoom: "OnNewRoom",
    OnNewUpload: "OnNewUpload",
} as const;

export type CampfireTriggerOperation = typeof CampfireTriggerOperations[keyof typeof CampfireTriggerOperations];

export const CampfireTriggerParameters = {
    OnNewMessage: {
        account: {
            name: "account",
            type: "string",
            required: true,
            description: "Account in Campfire",
            summary: "Account",
            dynamicValuesOperationId: "ListAccounts",
        },
        roomId: {
            name: "roomId",
            type: "string",
            required: true,
            description: "A room associated with the account",
            summary: "RoomId",
            dynamicValuesOperationId: "ListRooms",
        },
    },
    OnNewRoom: {
        account: {
            name: "account",
            type: "string",
            required: true,
            description: "Account in Campfire",
            summary: "Account",
            dynamicValuesOperationId: "ListAccounts",
        },
    },
    OnNewUpload: {
        account: {
            name: "account",
            type: "string",
            required: true,
            description: "Account in Campfire",
            summary: "Account",
            dynamicValuesOperationId: "ListAccounts",
        },
        roomId: {
            name: "roomId",
            type: "string",
            required: true,
            description: "A room associated with the account",
            summary: "RoomId",
            dynamicValuesOperationId: "ListRooms",
        },
    },
} as const;

// #region Client

/**
 * Typed client for the campfire connector.
 */
export class CampfireClient extends ConnectorClientBase {

    /**
     * Creates a new CampfireClient.
     * @param connectionRuntimeUrl The connection runtime URL from Azure Portal.
     * @param tokenProvider The token provider for authentication.
     * @param options Optional connector client options.
     */
    constructor(connectionRuntimeUrl: string, tokenProvider: TokenProvider, options?: ConnectorClientOptions) {
        super(connectionRuntimeUrl, tokenProvider, options);
    }

    public get connectorName(): string {
        return "campfire";
    }

    /**
     * Create a message
     * @remarks Sends a message to the given room.
     */
    public async createMessageAsync(roomId: string, account?: string, message?: string, abortSignal?: AbortSignal): Promise<CreateMessageResponse> {
        const queryParams: string[] = [];
        if (account !== undefined) {
            queryParams.push(`account=${encodeURIComponent(String(account))}`);
        }
        if (message !== undefined) {
            queryParams.push(`message=${encodeURIComponent(String(message))}`);
        }
        const requestPath = `/room/${roomId}/speak.json` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<CreateMessageResponse>("POST", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as CreateMessageResponse;
    }

    /**
     * Get user by ID
     * @remarks Retrieves information about a user by given ID
     */
    public async getUserAsync(userId: string, account?: string, abortSignal?: AbortSignal): Promise<UserResponse> {
        const queryParams: string[] = [];
        if (account !== undefined) {
            queryParams.push(`account=${encodeURIComponent(String(account))}`);
        }
        const requestPath = `/users/${userId}.json` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<UserResponse>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as UserResponse;
    }

}

// #endregion Client
