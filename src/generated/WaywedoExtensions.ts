// WaywedoExtensions.ts - Auto-generated DirectClient SDK for TypeScript
// Do not edit this file directly.

import type { AbortSignalLike } from "@azure/abort-controller";
import { ConnectorClientBase } from "../azureConnectors/clientBase.ts";
import { ConnectorException } from "../azureConnectors/connectorException.ts";
import { ConnectorClientOptions } from "../azureConnectors/options.ts";
import { TokenProvider } from "../azureConnectors/authentication.ts";

// #region Types

/**
 * Add a comment to a checklist instance step
 */
export interface CommentAddInput {
    /** Select the checklist instance */
    instanceId: string;
    /** Select the step of the checklist instance */
    stepId: string;
    /** Select the user creating the comment */
    userId?: number;
    /** Specify the comment to add */
    message: string;
    /** The name of the bot creating the comment */
    bot: string;
}

/**
 * Create Checklist Instance
 */
export interface ChecklistInstancesInput {
    /** Select a checklist master */
    procedureId: number;
    /** The title of the checklist instance */
    title: string;
    /** Select the user to add as a collaborator */
    userId?: number;
    /** Select the company role to add as a collaborator. */
    companyRoleId?: number;
    /** The name of the bot creating the instance */
    bot: string;
}

/**
 * Complete a Checklist Step
 */
export interface ChecklistStepsCompleteInput {
    /** Select the user completing the step */
    userId: number;
    /** The name of the bot that completed the checklist step */
    bot: string;
}

/**
 * Response for Complete a Checklist Step
 */
export interface ChecklistStepsCompleteResponse {
    [key: string]: unknown;
}

/**
 * Add Collaborators to a Checklist Instance
 */
export interface CollaboratorsAddInput {
    /** Select the checklist instance */
    checklistInstanceId: string;
    userIds?: Array<number>;
    companyRoleIds?: Array<number>;
}

/**
 * Response for Add Collaborators to a Checklist Instance
 */
export interface CollaboratorsAddResponse {
    [key: string]: unknown;
}

/**
 * Add a New User
 */
export interface UsersInput {
    /** The first name of the user. */
    firstName: string;
    /** The last name of the user. */
    lastName: string;
    /** The email address of the user. */
    email: string;
    /** Select a security role. */
    securityRole: number;
    companyRoles?: Array<number>;
    /** Select the user's timezone. */
    timeZoneId: string;
}

/**
 * Response for Add a New User
 */
export interface UsersPostResponse {
    [key: string]: unknown;
}

/**
 * When a checklist instance is started
 */
export interface ChecklistCreateWebHookInput {
    /** The URL to call when action occurs. */
    callbackUrl: string;
}

/**
 * When a comment is added to a checklist
 */
export interface NewCommentWebHookInput {
    /** The URL to call when action occurs. */
    callbackUrl: string;
}

/**
 * When a checklist is finished
 */
export interface FinishChecklistWebHookInput {
    /** The URL to call when action occurs. */
    callbackUrl: string;
}

/**
 * When a supervisor is invited
 */
export interface InviteSupervisorWebHookInput {
    /** The URL to call when action occurs. */
    callbackUrl: string;
}

/**
 * When a procedure is accepted
 */
export interface GenerateAcceptancePDFWebHookInput {
    /** The URL to call when action occurs. */
    callbackUrl: string;
}

/**
 * When a checklist step is completed
 */
export interface ChecklistStepCompletedWebHookInput {
    /** The URL to call when action occurs. */
    callbackUrl: string;
}

/**
 * Definition: ChecklistInstance
 */
export interface ChecklistInstance {
    /** The unique ID of the checklist instance. */
    id?: string;
    /** The unique ID of the procedure. */
    procedureId?: number;
    /** The title of the checklist instance. */
    title?: string;
    /** The URL to the checklist instance. */
    url?: string;
    /** The date and time the checklist instance was started. */
    created?: string;
    /** The email address of the user who started the checklist instance. */
    createdBy?: string;
    /** The date and time the checklist instance was finished. */
    finished?: string;
    /** The earliest date and time the checklist instance could be due. */
    earliestDue?: string;
    /** The latest date and time the checklist instance could be due. */
    latestDue?: string;
}

/**
 * Definition: ChecklistStep
 */
export interface ChecklistStep {
    /** The unique ID of the instance step. */
    id?: string;
    /** The title of the instance step. */
    title?: string;
    /** The number of the instance step. */
    ordinal?: number;
}

/**
 * Definition: CompletedStep
 */
export interface CompletedStep {
    /** The unique ID of the checklist step completion. */
    id?: string;
    /** The unique ID of the checklist instance. */
    instanceId?: string;
    /** The unique ID of the procedure. */
    procedureId?: number;
    /** The date and time the checklist step was completed. */
    created?: string;
    /** The email address of the user that completed the checklist step. */
    createdBy?: string;
    /** The date and time the checklist step was due. */
    due?: string;
    step?: ChecklistStep;
    /** The list of choices made if the step is a decision point. */
    decisionChoices?: Array<string>;
}

/**
 * Definition: ChecklistComment
 */
export interface ChecklistComment {
    /** The unique ID of the comment. */
    id?: string;
    /** The type of comment; either 1 (standard) or 2 (attachment). */
    type?: number;
    /** The unique ID of the procedure. */
    procedureId?: number;
    /** The title of the procedure. */
    procedureTitle?: string;
    /** The unique ID of the checklist instance. */
    instanceId?: string;
    /** The unique ID of the checklist step. */
    stepId?: string;
    /** The title of the checklist instance. */
    instanceTitle?: string;
    /** The comment text added to the step. */
    comment?: string;
    /** The date and time the comment was created. */
    created?: string;
    /** The email address of the user who created the comment. */
    createdBy?: string;
    /** The URL to the attached file. Valid for 5 minutes. */
    imageUrl?: string;
}

/**
 * Definition: Procedure
 */
export interface Procedure {
    /** The unique ID of the procedure. */
    id?: number;
    /** The title of the procedure. */
    title?: string;
    /** The publishing status of the procedure. */
    status?: string;
    /** The summary of the procedure. */
    summary?: string;
    /** The email of the user that created the procedure. */
    createdBy?: string;
    /** The URL of the procedure. */
    url?: string;
    /** Wether the procedure is restricted or not. */
    restricted?: boolean;
    /** The type of procedure; 1 (standard) or 2 (checklist) */
    type?: number;
}

/**
 * Definition: ProcedureAcceptance
 */
export interface ProcedureAcceptance {
    /** The unique ID of the procedure acceptance */
    id?: number;
    /** The date and time the procedure was accepted. */
    accepted?: string;
    /** The email address of the user that accepted the procedure. */
    createdBy?: string;
    /** The file name of the generated PDF. */
    fileName?: string;
    /** The URL to the generated PDF. Valid for 5 minutes. */
    attachmentUrl?: string;
    /** The title of the accepted procedure. */
    procedureTitle?: string;
}

/**
 * Definition: User
 */
export interface User {
    /** The unique ID of the user. */
    id?: number;
    /** The first name of the user. */
    firstName?: string;
    /** The last name of the user. */
    lastName?: string;
    /** The email address of the user. */
    email?: string;
}

/**
 * Definition: WebhookRegistered
 */
export interface WebhookRegistered {
    /** The unique ID of the Webhook registration. */
    id?: number;
    /** The URL to call when action occurs. */
    callbackurl?: string;
    /** The date and time the webhook was registered. */
    created?: string;
    /** The name of the webhook event. */
    webHookEvent?: string;
}
// #endregion Types

export const WaywedoTriggerOperations = {
    OnChecklistCreateWebHook: "Checklist_Create_WebHook",
    OnChecklistStepCompletedWebHook: "Checklist_Step_Completed_WebHook",
    OnFinishChecklistWebHook: "Finish_Checklist_WebHook",
    OnGenerateAcceptancePDFWebHook: "Generate_Acceptance_PDF_WebHook",
    OnInviteSupervisorWebHook: "Invite_Supervisor_WebHook",
    OnNewCommentWebHook: "New_Comment_WebHook",
} as const;

export type WaywedoTriggerOperation = typeof WaywedoTriggerOperations[keyof typeof WaywedoTriggerOperations];

// #region Client

/**
 * Typed client for the waywedo connector.
 */
export class WaywedoClient extends ConnectorClientBase {

    /**
     * Creates a new WaywedoClient.
     * @param connectionRuntimeUrl The connection runtime URL from Azure Portal.
     * @param tokenProvider The token provider for authentication.
     * @param options Optional connector client options.
     */
    constructor(connectionRuntimeUrl: string, tokenProvider: TokenProvider, options?: ConnectorClientOptions) {
        super(connectionRuntimeUrl, tokenProvider, options);
    }

    public get connectorName(): string {
        return "waywedo";
    }

    /**
     * Add a comment to a checklist instance step
     * @remarks Adds a new comment to an Activated Checklist Instance step.
     */
    public async commentAddAsync(input: CommentAddInput, abortSignal?: AbortSignalLike): Promise<ChecklistComment> {
        const requestPath = `/v1/ChecklistInstanceComments`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<ChecklistComment>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as ChecklistComment;
    }

    /**
     * Create Checklist Instance
     * @remarks Creates a new activated checklist instance.
     */
    public async checklistInstancesAsync(input: ChecklistInstancesInput, abortSignal?: AbortSignalLike): Promise<ChecklistInstance> {
        const requestPath = `/v1/ChecklistInstances`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<ChecklistInstance>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as ChecklistInstance;
    }

    /**
     * Get A Checklist Instance
     * @remarks Retrieve a specific checklist instance by ID
     */
    public async checklistInstancesGetAsync(instanceId: string, abortSignal?: AbortSignalLike): Promise<ChecklistInstance> {
        const requestPath = `/v1/ChecklistInstances/${instanceId}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<ChecklistInstance>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as ChecklistInstance;
    }

    /**
     * Get Checklist Instance Activity
     * @remarks Retrieve all activity for a checklist instance
     */
    public async checklistInstancesActivityAsync(instanceId: string, abortSignal?: AbortSignalLike): Promise<Array<Record<string, unknown>>> {
        const requestPath = `/v1/ChecklistInstances/${instanceId}/Activity`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<Record<string, unknown>>>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<Record<string, unknown>>;
    }

    /**
     * Find Checklist Step
     * @remarks Find a step of a checklist instance
     */
    public async findStepsAsync(instanceId: string, query?: string, abortSignal?: AbortSignalLike): Promise<Array<ChecklistStep>> {
        const queryParams: string[] = [];
        if (query !== undefined) {
            queryParams.push(`query=${encodeURIComponent(String(query))}`);
        }
        const requestPath = `/v1/ChecklistInstances/${instanceId}/Steps` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<ChecklistStep>>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<ChecklistStep>;
    }

    /**
     * Get A Checklist Step
     * @remarks Retrieve a specific checklist step by ID
     */
    public async checklistStepsGetAsync(instanceId: string, stepId: string, abortSignal?: AbortSignalLike): Promise<ChecklistStep> {
        const requestPath = `/v1/ChecklistInstances/${instanceId}/Steps/${stepId}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<ChecklistStep>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as ChecklistStep;
    }

    /**
     * Complete a Checklist Step
     * @remarks Mark a specific step in a checklist instance as complete.
     */
    public async checklistStepsCompleteAsync(input: ChecklistStepsCompleteInput, instanceId: string, stepId: string, abortSignal?: AbortSignalLike): Promise<ChecklistStepsCompleteResponse> {
        const requestPath = `/v1/ChecklistInstances/${instanceId}/Steps/${stepId}/Complete`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<ChecklistStepsCompleteResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as ChecklistStepsCompleteResponse;
    }

    /**
     * Add Collaborators to a Checklist Instance
     * @remarks Adds one or more collaborators to a checklist instance.
     */
    public async collaboratorsAddAsync(input: CollaboratorsAddInput, abortSignal?: AbortSignalLike): Promise<CollaboratorsAddResponse> {
        const requestPath = `/v1/Collaborators`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<CollaboratorsAddResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as CollaboratorsAddResponse;
    }

    /**
     * Find Checklist
     * @remarks Search for a checklist by title
     */
    public async findChecklistAsync(query?: string, type?: string, abortSignal?: AbortSignalLike): Promise<Array<Procedure>> {
        const queryParams: string[] = [];
        if (query !== undefined) {
            queryParams.push(`query=${encodeURIComponent(String(query))}`);
        }
        if (type !== undefined) {
            queryParams.push(`type=${encodeURIComponent(String(type))}`);
        }
        const requestPath = `/v1/Procedures` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<Procedure>>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<Procedure>;
    }

    /**
     * Get A Checklist Or Procedure
     * @remarks Retrieve a specific checklist or procedure by ID
     */
    public async proceduresGetAsync(procedureId: string, abortSignal?: AbortSignalLike): Promise<Procedure> {
        const requestPath = `/v1/Procedures/${procedureId}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Procedure>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Procedure;
    }

    /**
     * Find Checklist Instance
     * @remarks Search for a checklist instance by title.
     */
    public async findChecklistInstancesAsync(procedureId: string, query?: string, abortSignal?: AbortSignalLike): Promise<Array<ChecklistInstance>> {
        const queryParams: string[] = [];
        if (query !== undefined) {
            queryParams.push(`query=${encodeURIComponent(String(query))}`);
        }
        const requestPath = `/v1/Procedures/${procedureId}/ChecklistInstances` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<ChecklistInstance>>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<ChecklistInstance>;
    }

    /**
     * Find User
     * @remarks Search for a user by name or email.
     */
    public async findUserAsync(query?: string, abortSignal?: AbortSignalLike): Promise<Array<User>> {
        const queryParams: string[] = [];
        if (query !== undefined) {
            queryParams.push(`query=${encodeURIComponent(String(query))}`);
        }
        const requestPath = `/v1/Users` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<Array<User>>("GET", requestUrl, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as Array<User>;
    }

    /**
     * Add a New User
     * @remarks Creates and invites a new user.
     */
    public async usersAsync(input: UsersInput, abortSignal?: AbortSignalLike): Promise<UsersPostResponse> {
        const requestPath = `/v1/Users`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<UsersPostResponse>("POST", requestUrl, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as UsersPostResponse;
    }

}

// #endregion Client
