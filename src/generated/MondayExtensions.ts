// MondayExtensions.ts - Auto-generated DirectClient SDK for TypeScript
// Do not edit this file directly.

import { ConnectorClientBase } from "../azureConnectors/clientBase.ts";
import { ConnectorException } from "../azureConnectors/connectorException.ts";
import { ConnectorClientOptions } from "../azureConnectors/options.ts";
import { TokenProvider } from "../azureConnectors/authentication.ts";

// #region Types

/**
 * When an item is created
 */
export interface WebhookCreateItemInput {
    /** Select a workspace from the drop-down or specify a workspace ID. */
    workspaceId: string;
    /** Select a board from the drop-down or specify a board ID. */
    boardId: string;
    /** Specify the callback URL */
    callbackUrl: string;
}

/**
 * Response for When an item is created
 */
export interface WebhookCreateItemResponse {
    [key: string]: unknown;
}

/**
 * When a new update is posted
 */
export interface WebhookCreateUpdateInput {
    /** Select a workspace from the drop-down or specify a workspace ID. */
    workspaceId: string;
    /** Select a board from the drop-down or specify a board ID. */
    boardId: string;
    /** Specify the callback URL. */
    callbackUrl: string;
}

/**
 * Response for When a new update is posted
 */
export interface WebhookCreateUpdateResponse {
    [key: string]: unknown;
}

/**
 * When an item's name changes
 */
export interface WebhookChangeNameInput {
    /** Select a workspace from the drop-down or specify a workspace ID. */
    workspaceId: string;
    /** Select a board from the drop-down or specify a board ID. */
    boardId: string;
    /** Specify the callback URL. */
    callbackUrl: string;
}

/**
 * Response for When an item's name changes
 */
export interface WebhookChangeNameResponse {
    [key: string]: unknown;
}

/**
 * When a subitem's name changes
 */
export interface WebhookChangeSubitemNameInput {
    /** Select a workspace from the drop-down or specify a workspace ID. */
    workspaceId: string;
    /** Select a board from the drop-down or specify a board ID. */
    boardId: string;
    /** Specify the callback URL. */
    callbackUrl: string;
}

/**
 * Response for When a subitem's name changes
 */
export interface WebhookChangeSubitemNameResponse {
    [key: string]: unknown;
}

/**
 * When a subitem is created
 */
export interface WebhookCreateSubitemInput {
    /** Select a workspace from the drop-down or specify a workspace ID. */
    workspaceId: string;
    /** Select a board from the drop-down or specify a board ID. */
    boardId: string;
    /** Specify the callback URL. */
    callbackUrl: string;
}

/**
 * Response for When a subitem is created
 */
export interface WebhookCreateSubitemResponse {
    [key: string]: unknown;
}

/**
 * When a column changes
 */
export interface WebhookColumnChangesInput {
    /** Select a workspace from the drop-down or specify a workspace ID. */
    workspaceId: string;
    /** Select a board from the drop-down or specify a board ID. */
    boardId: string;
    /** Select a column from the drop-down. */
    columnId: string;
    /** Specify the callback URL. */
    callbackUrl: string;
}

/**
 * Response for When a column changes
 */
export interface WebhookColumnChangesResponse {
    [key: string]: unknown;
}

/**
 * When any column changes
 */
export interface WebhookAnyColumnChangesInput {
    /** Select a workspace from the drop-down or specify a workspace ID. */
    workspaceId: string;
    /** Select a board from the drop-down or specify a board ID. */
    boardId: string;
    /** Specify the callback URL. */
    callbackUrl: string;
}

/**
 * Response for When any column changes
 */
export interface WebhookAnyColumnChangesResponse {
    [key: string]: unknown;
}

/**
 * When any subitem column changes
 */
export interface WebhookSubitemColumnChangesInput {
    /** Select a workspace from the drop-down or specify a workspace ID. */
    workspaceId: string;
    /** Select a board from the drop-down or specify a board ID. */
    boardId: string;
    /** Specify the callback URL. */
    callbackUrl: string;
}

/**
 * Response for When any subitem column changes
 */
export interface WebhookSubitemColumnChangesResponse {
    [key: string]: unknown;
}

/**
 * Create an item
 */
export interface CreateItemInput {
    workspaceId: FieldWorkspace;
    boardId: FieldBoard;
    /** Select a group from the drop-down or specify a group ID. */
    groupId: string;
    /** Specify the name of the item you want to create. */
    itemName: string;
    columnValues?: DynamicResponseGetListSchema;
}

/**
 * Response for Create an item
 */
export interface CreateItemResponse {
    data?: Record<string, unknown>;
}

/**
 * Duplicate a board
 */
export interface DuplicateBoardInput {
    /** Select the workspace for the board you want to duplicate. */
    sourceWorkspaceId: string;
    /** Select the board to duplicate. */
    sourceBoardId: string;
    /** Specify the name of the duplicated board. */
    duplicatedBoardName?: string;
    /** Select the duplication type from the drop-down. */
    duplicationType: string;
    /** Select the destination workspace for the duplicated board. */
    destinationWorkspaceId?: string;
    /** Select the destination folder for the duplicated board. */
    destinationFolder?: string;
    /** Select whether to keep board subscribers. */
    keepBoardSubscribers: boolean;
}

/**
 * Response for Duplicate a board
 */
export interface DuplicateBoardResponse {
    /** The ID of the duplicated board */
    id?: string;
}

/**
 * Create a board
 */
export interface CreateBoardInput {
    /** Specify the ID of a monday.com workspace. */
    workspaceId: string;
    /** Specify the desired name of the board you want to create. */
    boardName: string;
}

/**
 * Response for Create a board
 */
export interface CreateBoardResponse {
    data?: Record<string, unknown>;
}

/**
 * Create a column
 */
export interface CreateColumnInput {
    workspaceId: FieldWorkspace;
    boardId: FieldBoard;
    /** Specify the title of the column you want to create. */
    title: string;
    /** Specify the description of the column you want to create. */
    description?: string;
    /** Select a column type from the drop-down. */
    columnType: string;
}

/**
 * Response for Create a column
 */
export interface CreateColumnResponse {
    data?: Record<string, unknown>;
}

/**
 * Create a group
 */
export interface CreateGroupInput {
    workspaceId: FieldWorkspace;
    boardId: FieldBoard;
    /** Specify your desired group name. */
    groupName: string;
}

/**
 * Response for Create a group
 */
export interface CreateGroupResponse {
    data?: Record<string, unknown>;
}

/**
 * Update an item's column value
 */
export interface UpdateItemColumnInput {
    /** Select the workspace the item belongs to from the drop-down. */
    workspaceId: string;
    /** Select the board the item belongs to from the drop-down. */
    boardId: string;
    columnId: FieldColumnsToUpdateSingleColumn;
    /** Specify the ID of the item to update. */
    itemId: string;
    columnValues: DynamicResponseGetSingleColumnSchema;
}

/**
 * Response for Update an item's column value
 */
export interface UpdateItemColumnResponse {
    data?: Record<string, unknown>;
}

/**
 * Update multiple item column values
 */
export interface UpdateMultipleItemColumnsInput {
    /** Select the workspace the item belongs to from the drop-down. */
    workspaceId: string;
    /** Select the board the item belongs to from the drop-down. */
    boardId: string;
    /** Specify the ID of the item to update. */
    itemId: string;
    /** Specify a new name for the item. Leave blank to keep the current name. */
    itemName?: string;
    columnValues?: DynamicResponseGetListSchema;
}

/**
 * Response for Update multiple item column values
 */
export interface UpdateMultipleItemColumnsResponse {
    data?: Record<string, unknown>;
}

/**
 * Move an item to a group
 */
export interface MoveItemToGroupInput {
    workspaceId: FieldWorkspace;
    boardId: FieldBoard;
    /** Select the group to move the item to from the drop-down. */
    groupId: string;
    /** Specify the ID of the item to move. */
    itemId: string;
}

/**
 * Response for Move an item to a group
 */
export interface MoveItemToGroupResponse {
    data?: Record<string, unknown>;
}

/**
 * Create a notification
 */
export interface CreateNotificationInput {
    /** Select a user from the drop-down or specify a user ID. */
    userId: string;
    /** Specify a board ID or item ID the notification will go to. */
    targetId: string;
    /** Specify the notification text. */
    text: string;
}

/**
 * Response for Create a notification
 */
export interface CreateNotificationResponse {
    /** Data */
    data?: Record<string, unknown>;
}

/**
 * Create a subitem
 */
export interface CreateSubitemInput {
    workspaceId: FieldWorkspace;
    /** Select a board from the drop-down or specify a board ID. */
    boardId: string;
    /** Select a parent item from the drop-down or specify a parent item ID. */
    parentItemId: string;
    /** Specify a name for the subitem. */
    itemName: string;
    columnValues?: DynamicResponseGetListSchema;
}

/**
 * Response for Create a subitem
 */
export interface CreateSubitemResponse {
    data?: Record<string, unknown>;
}

/**
 * Create an update
 */
export interface CreateUpdateInput {
    workspaceId: FieldWorkspace;
    boardId: FieldBoard;
    /** Select a group from the drop-down or specify a group ID. */
    groupId: string;
    /** Select an item from the drop-down or specify an item ID. */
    itemId: string;
    /** Specify text for the update. */
    body: string;
}

/**
 * Response for Create an update
 */
export interface CreateUpdateResponse {
    data?: Record<string, unknown>;
}

/**
 * Create a workspace
 */
export interface CreateWorkspaceInput {
    /** Specify a name for the new workspace. */
    name: string;
    /** Specify a description for the new workspace. */
    description?: string;
}

/**
 * Response for Create a workspace
 */
export interface CreateWorkspaceResponse {
    data?: Record<string, unknown>;
}

/**
 * Response for Get tags
 */
export interface GetTagsResponse {
    data?: Record<string, unknown>;
}

/**
 * Response for Get users
 */
export interface GetUsersResponse {
    data?: Record<string, unknown>;
}

/**
 * Definition: DynamicResponseGetListSchema
 */
export interface DynamicResponseGetListSchema {
    [key: string]: unknown;
}

/**
 * Definition: DynamicResponseGetSingleColumnSchema
 */
export interface DynamicResponseGetSingleColumnSchema {
    [key: string]: unknown;
}

/**
 * Definition: DynamicResponseGetListSchemaGet
 */
export interface DynamicResponseGetListSchemaGet {
    [key: string]: unknown;
}

/**
 * Definition: DynamicGetGetItemsSchema
 */
export interface DynamicGetGetItemsSchema {
    [key: string]: unknown;
}

/**
 * Definition: DynamicGetColumnNamesForWebhook
 */
export interface DynamicGetColumnNamesForWebhook {
    [key: string]: unknown;
}

/**
 * Definition: DynamicGetColumnNamesForUpdateWebhook
 */
export interface DynamicGetColumnNamesForUpdateWebhook {
    [key: string]: unknown;
}

/**
 * Definition: DynamicGetSubitemColumnNamesForWebhook
 */
export interface DynamicGetSubitemColumnNamesForWebhook {
    [key: string]: unknown;
}

/**
 * Definition: GetSubitemColumnNamesForGetSubitems
 */
export interface GetSubitemColumnNamesForGetSubitems {
    [key: string]: unknown;
}

/**
 * Definition: FieldWorkspace
 */
export type FieldWorkspace = string;

/**
 * Definition: FieldBoard
 */
export type FieldBoard = string;

/**
 * Definition: FieldColumnsToUpdateSingleColumn
 */
export type FieldColumnsToUpdateSingleColumn = string;
// #endregion Types

export const MondayTriggerOperations = {
    OnWebhookAnyColumnChanges: "WebhookAnyColumnChanges",
    OnWebhookChangeName: "WebhookChangeName",
    OnWebhookChangeSubitemName: "WebhookChangeSubitemName",
    OnWebhookColumnChanges: "WebhookColumnChanges",
    OnWebhookCreateItem: "WebhookCreateItem",
    OnWebhookCreateSubitem: "WebhookCreateSubitem",
    OnWebhookCreateUpdate: "WebhookCreateUpdate",
    OnWebhookSubitemColumnChanges: "WebhookSubitemColumnChanges",
} as const;

export type MondayTriggerOperation = typeof MondayTriggerOperations[keyof typeof MondayTriggerOperations];

// #region Client

/**
 * Typed client for the monday connector.
 */
export class MondayClient extends ConnectorClientBase {

    /**
     * Creates a new MondayClient.
     * @param connectionRuntimeUrl The connection runtime URL from Azure Portal.
     * @param tokenProvider The token provider for authentication.
     * @param options Optional connector client options.
     */
    constructor(connectionRuntimeUrl: string, tokenProvider: TokenProvider, options?: ConnectorClientOptions) {
        super(connectionRuntimeUrl, tokenProvider, options);
    }

    public get connectorName(): string {
        return "monday";
    }

    /**
     * Create an item
     * @remarks Creates an item in the selected monday.com board and group.
     */
    public async createItemAsync(input: CreateItemInput, abortSignal?: AbortSignal): Promise<CreateItemResponse> {
        const requestPath = `/executePowerAutomateAction/CreateItem`;
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<CreateItemResponse>("POST", url, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as CreateItemResponse;
    }

    /**
     * Duplicate a board
     * @remarks Duplicates a board in monday.com.
     */
    public async duplicateBoardAsync(input: DuplicateBoardInput, abortSignal?: AbortSignal): Promise<DuplicateBoardResponse> {
        const requestPath = `/executePowerAutomateAction/DuplicateBoard`;
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DuplicateBoardResponse>("POST", url, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DuplicateBoardResponse;
    }

    /**
     * Create a board
     * @remarks Creates a board in monday.com.
     */
    public async createBoardAsync(input: CreateBoardInput, abortSignal?: AbortSignal): Promise<CreateBoardResponse> {
        const requestPath = `/executePowerAutomateAction/CreateBoard`;
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<CreateBoardResponse>("POST", url, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as CreateBoardResponse;
    }

    /**
     * Create a column
     * @remarks Creates a column in monday.com for the specified board.
     */
    public async createColumnAsync(input: CreateColumnInput, abortSignal?: AbortSignal): Promise<CreateColumnResponse> {
        const requestPath = `/executePowerAutomateAction/CreateColumn`;
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<CreateColumnResponse>("POST", url, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as CreateColumnResponse;
    }

    /**
     * Create a group
     * @remarks Creates a group in monday.com for the specified board.
     */
    public async createGroupAsync(input: CreateGroupInput, abortSignal?: AbortSignal): Promise<CreateGroupResponse> {
        const requestPath = `/executePowerAutomateAction/CreateGroup`;
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<CreateGroupResponse>("POST", url, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as CreateGroupResponse;
    }

    /**
     * Update an item's column value
     * @remarks Updates a single item's column. If the value is blank, the column value will be cleared.
     */
    public async updateItemColumnAsync(input: UpdateItemColumnInput, abortSignal?: AbortSignal): Promise<UpdateItemColumnResponse> {
        const requestPath = `/executePowerAutomateAction/UpdateItemColumn`;
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<UpdateItemColumnResponse>("POST", url, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as UpdateItemColumnResponse;
    }

    /**
     * Update multiple item column values
     * @remarks Updates multiple item columns. Leave the column value blank to keep the current value.
     */
    public async updateMultipleItemColumnsAsync(input: UpdateMultipleItemColumnsInput, abortSignal?: AbortSignal): Promise<UpdateMultipleItemColumnsResponse> {
        const requestPath = `/executePowerAutomateAction/UpdateMultipleItemColumns`;
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<UpdateMultipleItemColumnsResponse>("POST", url, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as UpdateMultipleItemColumnsResponse;
    }

    /**
     * Move an item to a group
     * @remarks Moves an item to another group on the same board.
     */
    public async moveItemToGroupAsync(input: MoveItemToGroupInput, abortSignal?: AbortSignal): Promise<MoveItemToGroupResponse> {
        const requestPath = `/executePowerAutomateAction/MoveItemToGroup`;
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<MoveItemToGroupResponse>("POST", url, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as MoveItemToGroupResponse;
    }

    /**
     * Create a notification
     * @remarks Creates a notification in monday.com for a user for either a specific board or item.
     */
    public async createNotificationAsync(input: CreateNotificationInput, abortSignal?: AbortSignal): Promise<CreateNotificationResponse> {
        const requestPath = `/executePowerAutomateAction/CreateNotification`;
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<CreateNotificationResponse>("POST", url, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as CreateNotificationResponse;
    }

    /**
     * Create a subitem
     * @remarks Creates a subitem in monday.com for the selected board and parent item.
     */
    public async createSubitemAsync(input: CreateSubitemInput, abortSignal?: AbortSignal): Promise<CreateSubitemResponse> {
        const requestPath = `/executePowerAutomateAction/CreateSubitem`;
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<CreateSubitemResponse>("POST", url, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as CreateSubitemResponse;
    }

    /**
     * Get subitems
     * @remarks Gets subitems in monday.com for a specific item in a board.
     */
    public async getSubitemsAsync(workspaceId?: string, boardId?: string, itemId?: string, abortSignal?: AbortSignal): Promise<GetSubitemColumnNamesForGetSubitems> {
        const queryParams: string[] = [];
        if (workspaceId !== undefined) {
            queryParams.push(`workspaceId=${encodeURIComponent(String(workspaceId))}`);
        }
        if (boardId !== undefined) {
            queryParams.push(`boardId=${encodeURIComponent(String(boardId))}`);
        }
        if (itemId !== undefined) {
            queryParams.push(`itemId=${encodeURIComponent(String(itemId))}`);
        }
        const requestPath = `/getData/getSubitems` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<GetSubitemColumnNamesForGetSubitems>("GET", url, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as GetSubitemColumnNamesForGetSubitems;
    }

    /**
     * Create an update
     * @remarks Creates an update in monday.com for a specific item.
     */
    public async createUpdateAsync(input: CreateUpdateInput, abortSignal?: AbortSignal): Promise<CreateUpdateResponse> {
        const requestPath = `/executePowerAutomateAction/CreateUpdate`;
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<CreateUpdateResponse>("POST", url, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as CreateUpdateResponse;
    }

    /**
     * Get an item by ID
     * @remarks Gets a monday.com item using the specified item ID and board.
     */
    public async getItemByIdAsync(itemId?: string, workspaceId?: string, boardId?: string, abortSignal?: AbortSignal): Promise<DynamicResponseGetListSchemaGet> {
        const queryParams: string[] = [];
        if (itemId !== undefined) {
            queryParams.push(`itemId=${encodeURIComponent(String(itemId))}`);
        }
        if (workspaceId !== undefined) {
            queryParams.push(`workspaceId=${encodeURIComponent(String(workspaceId))}`);
        }
        if (boardId !== undefined) {
            queryParams.push(`boardId=${encodeURIComponent(String(boardId))}`);
        }
        const requestPath = `/getData/getItemById` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DynamicResponseGetListSchemaGet>("GET", url, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DynamicResponseGetListSchemaGet;
    }

    /**
     * Create a workspace
     * @remarks Creates a monday.com workspace with a user-specified name and description.
     */
    public async createWorkspaceAsync(input: CreateWorkspaceInput, abortSignal?: AbortSignal): Promise<CreateWorkspaceResponse> {
        const requestPath = `/executePowerAutomateAction/CreateWorkspaceV2`;
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<CreateWorkspaceResponse>("POST", url, undefined, input, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `POST ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as CreateWorkspaceResponse;
    }

    /**
     * Get items
     * @remarks Gets items in monday.com for a specific board and group. Filter items by column values using the filters found under advanced options. More info on filtering can be found here: https://plugingenie.com/docs/filtering-items.
     */
    public async getItemsAsync(workspaceId?: string, boardId?: string, groupId?: string, filter1Column?: string, filter1Operator?: string, filter1Value?: string, filter2Column?: string, filter2Operator?: string, filter2Value?: string, filter3Column?: string, filter3Operator?: string, filter3Value?: string, filter4Column?: string, filter4Operator?: string, filter4Value?: string, abortSignal?: AbortSignal): Promise<DynamicGetGetItemsSchema> {
        const queryParams: string[] = [];
        if (workspaceId !== undefined) {
            queryParams.push(`workspaceId=${encodeURIComponent(String(workspaceId))}`);
        }
        if (boardId !== undefined) {
            queryParams.push(`boardId=${encodeURIComponent(String(boardId))}`);
        }
        if (groupId !== undefined) {
            queryParams.push(`groupId=${encodeURIComponent(String(groupId))}`);
        }
        if (filter1Column !== undefined) {
            queryParams.push(`filter1Column=${encodeURIComponent(String(filter1Column))}`);
        }
        if (filter1Operator !== undefined) {
            queryParams.push(`filter1Operator=${encodeURIComponent(String(filter1Operator))}`);
        }
        if (filter1Value !== undefined) {
            queryParams.push(`filter1Value=${encodeURIComponent(String(filter1Value))}`);
        }
        if (filter2Column !== undefined) {
            queryParams.push(`filter2Column=${encodeURIComponent(String(filter2Column))}`);
        }
        if (filter2Operator !== undefined) {
            queryParams.push(`filter2Operator=${encodeURIComponent(String(filter2Operator))}`);
        }
        if (filter2Value !== undefined) {
            queryParams.push(`filter2Value=${encodeURIComponent(String(filter2Value))}`);
        }
        if (filter3Column !== undefined) {
            queryParams.push(`filter3Column=${encodeURIComponent(String(filter3Column))}`);
        }
        if (filter3Operator !== undefined) {
            queryParams.push(`filter3Operator=${encodeURIComponent(String(filter3Operator))}`);
        }
        if (filter3Value !== undefined) {
            queryParams.push(`filter3Value=${encodeURIComponent(String(filter3Value))}`);
        }
        if (filter4Column !== undefined) {
            queryParams.push(`filter4Column=${encodeURIComponent(String(filter4Column))}`);
        }
        if (filter4Operator !== undefined) {
            queryParams.push(`filter4Operator=${encodeURIComponent(String(filter4Operator))}`);
        }
        if (filter4Value !== undefined) {
            queryParams.push(`filter4Value=${encodeURIComponent(String(filter4Value))}`);
        }
        const requestPath = `/getData/getItemsV2` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<DynamicGetGetItemsSchema>("GET", url, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as DynamicGetGetItemsSchema;
    }

    /**
     * Get tags
     * @remarks This operation gets monday.com tags.
     */
    public async getTagsAsync(abortSignal?: AbortSignal): Promise<GetTagsResponse> {
        const requestPath = `/getData/getTagsV2`;
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<GetTagsResponse>("GET", url, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as GetTagsResponse;
    }

    /**
     * Get users
     * @remarks This operation gets the name, email, and ID of users in your monday.com account.
     */
    public async getUsersAsync(abortSignal?: AbortSignal): Promise<GetUsersResponse> {
        const requestPath = `/getData/getUsersV2`;
        const url = this.resolveUrl(requestPath);
        const httpResponse = await this.httpClient.sendAsync<GetUsersResponse>("GET", url, undefined, undefined, abortSignal);

        if (!httpResponse.isSuccessStatusCode) {
            throw new ConnectorException(this.connectorName, `GET ${requestPath}`, httpResponse.statusCode, httpResponse.text);
        }

        return httpResponse.value as GetUsersResponse;
    }

}

// #endregion Client
