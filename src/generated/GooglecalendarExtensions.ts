// GooglecalendarExtensions.ts - Auto-generated DirectClient SDK for TypeScript
// Do not edit this file directly.

import type { TokenCredential } from "@azure/core-auth";
import { ConnectorClientBase } from "../azureConnectors/clientBase.ts";
import type { ConnectorPagedAsyncIterableIterator } from "../azureConnectors/clientBase.ts";
import type { ConnectorClientOptions, ConnectorOperationOptions } from "../azureConnectors/options.ts";

// #region Types

/**
 * Definition: CalendarList
 */
export interface CalendarList {
    /** Calendars in the list. */
    items?: Array<CalendarListEntry>;
}

/**
 * Definition: CalendarListEntry
 */
export interface CalendarListEntry {
    /** Identifier of the calendar. */
    id?: string;
    /** Title of the calendar. */
    summary?: string;
    /** Description of the calendar. */
    description?: string;
    /** Geographic location of the calendar as free-form text. */
    location?: string;
    /** The time zone of the calendar. */
    timeZone?: string;
}

/**
 * Definition: CalendarEventList
 */
export interface CalendarEventList {
    /** Events in the calendar. */
    items?: Array<ResponseEvent>;
}

/**
 * Definition: CalendarEventChangedList
 */
export interface CalendarEventChangedList {
    /** Changed events in the calendar. */
    items?: Array<ResponseEventWithActionType>;
}

/**
 * Definition: ResponseEvent
 */
export interface ResponseEvent {
    /** A summary of the event. */
    summary?: string;
    /** The (inclusive) start time of the event. */
    start?: string;
    /** The (exclusive) end time of the event. */
    end?: string;
    /** Description of the event. */
    description?: string;
    /** Geographic location of the event as free-form text. */
    location?: string;
    /** Status of the event. Possible values are "confirmed", "tentative", or "cancelled". */
    status?: string;
    /** Unique ID of the list. */
    htmlLink?: string;
    /** Opaque identifier of the event. */
    id?: string;
    /** The event's attendees. */
    attendees?: string;
    /** The creator of the event. */
    creator?: string;
    /** The organizer of the event. */
    organizer?: string;
    /** Creation time of the event. */
    created?: string;
    /** Last modification time of the event. */
    updated?: string;
    /** Whether the end time is actually unspecified. An end time is still             provided for compatibility reasons, even if this attribute is set to True. */
    endTimeUnspecified?: boolean;
}

/**
 * Definition: RequestEvent
 */
export interface RequestEvent {
    /** A summary of the event. */
    summary?: string;
    /** The (inclusive) start time of the event. */
    start: string;
    /** The (exclusive) end time of the event. */
    end: string;
    /** Description of the event. */
    description?: string;
    /** Geographic location of the event as free-form text. */
    location?: string;
    /** Enter a comma-separated list of attendee emails. */
    attendees?: string;
    /** Status of the event. Possible values are "confirmed", "tentative", or "cancelled". */
    status?: string;
    /** Is all-day event? */
    isAllDay?: boolean;
}

/**
 * Definition: PatchEvent
 */
export interface PatchEvent {
    /** A summary of the event. */
    summary?: string;
    /** The (inclusive) start time of the event. */
    start?: string;
    /** The (exclusive) end time of the event. */
    end?: string;
    /** Description of the event. */
    description?: string;
    /** Geographic location of the event as free-form text. */
    location?: string;
    /** Enter a comma-separated list of attendee emails. */
    attendees?: string;
    /** Status of the event. Possible values are "confirmed", "tentative", or "cancelled. */
    status?: string;
    /** Is all-day event? */
    isAllDay?: boolean;
}

/**
 * Definition: ResponseEventWithActionType
 */
export interface ResponseEventWithActionType {
    /** Type of action which changed the event in change trigger. */
    actionType?: string;
    /** A summary of the event. */
    summary?: string;
    /** The (inclusive) start time of the event. */
    start?: string;
    /** The (exclusive) end time of the event. */
    end?: string;
    /** Description of the event. */
    description?: string;
    /** Geographic location of the event as free-form text. */
    location?: string;
    /** Status of the event. Possible values are "confirmed", "tentative", or "cancelled". */
    status?: string;
    /** Unique ID of the list. */
    htmlLink?: string;
    /** Opaque identifier of the event. */
    id?: string;
    /** The event's attendees. */
    attendees?: string;
    /** The creator of the event. */
    creator?: string;
    /** The organizer of the event. */
    organizer?: string;
    /** Creation time of the event. */
    created?: string;
    /** Last modification time of the event. */
    updated?: string;
    /** Whether the end time is actually unspecified. An end time is still             provided for compatibility reasons, even if this attribute is set to True. */
    endTimeUnspecified?: boolean;
}

/**
 * Definition: Object
 */
export interface ObjectEntity {
    [key: string]: unknown;
}

/**
 * Options for the listCalendars operation.
 */
export interface ListCalendarsOptions extends ConnectorOperationOptions {
    /** The minimum access role users must have to view calendars. */
    minAccessRole?: string;
}

/**
 * Options for the listEvents operation.
 */
export interface ListEventsOptions extends ConnectorOperationOptions {
    /** Time format: yyyy-MM-ddTHH:mm:ss.fffZ (2016-07-20T08:00:00.000Z). */
    timeMin?: string;
    /** Time format: yyyy-MM-ddTHH:mm:ss.fffZ (2016-07-20T09:00:59.000Z). */
    timeMax?: string;
    /** Text search terms to find events that match these terms in any fields */
    q?: string;
}
// #endregion Types

export const GooglecalendarTriggerOperations = {
    OnChangedEventInCalendar: "OnChangedEventInCalendar",
    OnDeletedEventInCalendar: "OnDeletedEventInCalendar",
    OnEventStarted: "OnEventStarted",
    OnNewEventInCalendar: "OnNewEventInCalendar",
    OnUpdatedEventInCalendar: "OnUpdatedEventInCalendar",
} as const;

export type GooglecalendarTriggerOperation = typeof GooglecalendarTriggerOperations[keyof typeof GooglecalendarTriggerOperations];

export const GooglecalendarTriggerParameters = {
    OnChangedEventInCalendar: {
        calendarId: {
            name: "calendar_id",
            type: "string",
            required: true,
            description: "Unique ID of the calendar to fetch events from.",
            summary: "Calendar ID",
            dynamicValuesOperationId: "ListCalendars",
        },
        singleEvents: {
            name: "singleEvents",
            type: "boolean",
            required: false,
            description: "Whether to expand recurring events into instances.",
            summary: "Expand Recurring Events?",
        },
    },
    OnDeletedEventInCalendar: {
        calendarId: {
            name: "calendar_id",
            type: "string",
            required: true,
            description: "Unique ID of the calendar to fetch events from.",
            summary: "Calendar ID",
            dynamicValuesOperationId: "ListCalendars",
        },
    },
    OnEventStarted: {
        calendarId: {
            name: "calendar_id",
            type: "string",
            required: true,
            description: "Unique ID of the calendar to fetch events from.",
            summary: "Calendar ID",
            dynamicValuesOperationId: "ListCalendars",
        },
    },
    OnNewEventInCalendar: {
        calendarId: {
            name: "calendar_id",
            type: "string",
            required: true,
            description: "Unique ID of the calendar to fetch events from.",
            summary: "Calendar ID",
            dynamicValuesOperationId: "ListCalendars",
        },
    },
    OnUpdatedEventInCalendar: {
        calendarId: {
            name: "calendar_id",
            type: "string",
            required: true,
            description: "Unique ID of the calendar to fetch events from.",
            summary: "Calendar ID",
            dynamicValuesOperationId: "ListCalendars",
        },
    },
} as const;

// #region Client

/**
 * Typed client for the googlecalendar connector.
 */
export class GooglecalendarClient extends ConnectorClientBase {

    /**
     * Creates a new GooglecalendarClient.
     * @param connectionRuntimeUrl The connection runtime URL from Azure Portal.
     * @param credential The credential used for authentication.
     * @param options Optional connector client options.
     */
    constructor(connectionRuntimeUrl: string, credential: TokenCredential, options?: ConnectorClientOptions) {
        super(connectionRuntimeUrl, credential, options);
    }

    public get connectorName(): string {
        return "googlecalendar";
    }

    /**
     * List calendars
     * @remarks This operation is used to list all calendars in your Google account.
     */
    public listCalendars(options: ListCalendarsOptions = {}): ConnectorPagedAsyncIterableIterator<CalendarListEntry> {
        const queryParams: string[] = [];
        if (options.minAccessRole !== undefined) {
            queryParams.push(`minAccessRole=${encodeURIComponent(String(options.minAccessRole))}`);
        }
        const requestPath = `/users/me/calendarList` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        return this.createPageable<CalendarList, CalendarListEntry>(
            requestPath,
            async (requestUrl) => {
                const httpResponse = await this.sendWithTracingAsync<CalendarList>("Googlecalendar.listCalendars", "ListCalendars", "GET", requestUrl, undefined, options);

                return httpResponse.value as CalendarList;
            },
            "items",
            undefined,
        );
    }

    /**
     * List the events on a calendar
     * @remarks This operation is used to return the first page of arbitrarily ordered events on the selected calendar.
     */
    public listEvents(calendarId: string, options: ListEventsOptions = {}): ConnectorPagedAsyncIterableIterator<ResponseEvent> {
        const queryParams: string[] = [];
        if (options.timeMin !== undefined) {
            queryParams.push(`timeMin=${encodeURIComponent(String(options.timeMin))}`);
        }
        if (options.timeMax !== undefined) {
            queryParams.push(`timeMax=${encodeURIComponent(String(options.timeMax))}`);
        }
        if (options.q !== undefined) {
            queryParams.push(`q=${encodeURIComponent(String(options.q))}`);
        }
        const requestPath = `/calendars/${encodeURIComponent(String(calendarId))}/events` + (queryParams.length > 0 ? "?" + queryParams.join("&") : "");
        return this.createPageable<CalendarEventList, ResponseEvent>(
            requestPath,
            async (requestUrl) => {
                const httpResponse = await this.sendWithTracingAsync<CalendarEventList>("Googlecalendar.listEvents", "ListEvents", "GET", requestUrl, undefined, options);

                return httpResponse.value as CalendarEventList;
            },
            "items",
            undefined,
        );
    }

    /**
     * Create an event
     * @remarks This operation is used to create an event on a specific calendar.
     */
    public async createEvent(input: RequestEvent, calendarId: string, options: ConnectorOperationOptions = {}): Promise<ResponseEvent> {
        const requestPath = `/calendars/${encodeURIComponent(String(calendarId))}/events`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<ResponseEvent>("Googlecalendar.createEvent", "CreateEvent", "POST", requestUrl, input, options);

        return httpResponse.value as ResponseEvent;
    }

    /**
     * Get an event
     * @remarks This operation is used to get the details of a specific event from a calendar.
     */
    public async getEvent(calendarId: string, eventId: string, options: ConnectorOperationOptions = {}): Promise<ResponseEvent> {
        const requestPath = `/calendars/${encodeURIComponent(String(calendarId))}/events/${encodeURIComponent(String(eventId))}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<ResponseEvent>("Googlecalendar.getEvent", "GetEvent", "GET", requestUrl, undefined, options);

        return httpResponse.value as ResponseEvent;
    }

    /**
     * Delete an event
     * @remarks This operation is used to delete an event from a calendar.
     */
    public async deleteEvent(calendarId: string, eventId: string, options: ConnectorOperationOptions = {}): Promise<ObjectEntity> {
        const requestPath = `/calendars/${encodeURIComponent(String(calendarId))}/events/${encodeURIComponent(String(eventId))}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<ObjectEntity>("Googlecalendar.deleteEvent", "DeleteEvent", "DELETE", requestUrl, undefined, options);

        return httpResponse.value as ObjectEntity;
    }

    /**
     * Update an event
     * @remarks This operation is used to update an existing event on a calendar.
     */
    public async updateEvent(input: PatchEvent, calendarId: string, eventId: string, options: ConnectorOperationOptions = {}): Promise<ResponseEvent> {
        const requestPath = `/calendars/${encodeURIComponent(String(calendarId))}/events/${encodeURIComponent(String(eventId))}`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<ResponseEvent>("Googlecalendar.updateEvent", "UpdateEvent", "PATCH", requestUrl, input, options);

        return httpResponse.value as ResponseEvent;
    }

}

// #endregion Client
