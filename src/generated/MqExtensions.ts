// MqExtensions.ts - Auto-generated DirectClient SDK for TypeScript
// Do not edit this file directly.

import type { TokenCredential } from "@azure/core-auth";
import { ConnectorClientBase } from "../azureConnectors/clientBase.ts";
import type { ConnectorClientOptions, ConnectorOperationOptions } from "../azureConnectors/options.ts";

// #region Types

/**
 * Response for Send message
 */
export interface SendResponse {
    ItemInternalId?: string;
    MessageData?: string;
    MessageId?: string;
    CorrelationId?: string;
}

/**
 * Definition: ProcedureMetadata
 */
export interface ProcedureMetadata {
    name?: string;
    title?: string;
    schema?: Record<string, unknown>;
}

/**
 * Definition: Object
 */
export interface ObjectEntity {
    [key: string]: unknown;
}

/**
 * Definition: Item
 */
export interface Item {
    ItemInternalId?: string;
    MessageData: string;
    MessageId?: string;
    CorrelationId?: string;
    PutDateTime?: string;
    UserIdentifier?: string;
    PutApplicationName?: string;
    PutApplicationType?: string;
    Format?: string;
    AccountingToken?: string;
    Ccsid?: number;
    GroupId?: string;
    LogicalSequenceNumber?: number;
    MessageType?: string;
    Offset?: number;
    OriginalLength?: number;
    Persistence?: string;
    Priority?: number;
    ReplyToQueue?: string;
    ReplyToQueueManager?: string;
}

/**
 * Definition: ItemsList
 */
export interface ItemsList {
    value?: Array<Item>;
}

/**
 * Definition: SingleGetValidOptions
 */
export interface SingleGetValidOptions {
    /** When specified, overrides the queue defined in the connection. */
    Queue?: string;
    /** Base64 string that represents a byte array with 24 bytes. */
    MessageId?: string;
    /** Base64 string that represents a byte array with 24 bytes. */
    CorrelationId?: string;
    /** Base64 string that represents a byte array with 24 bytes. */
    GroupId?: string;
    /** Base64 string that represents a byte array with 16 bytes. */
    MessageToken?: string;
    /** Offset in bytes of the data in the physical message from the start of the logical message. */
    Offset?: number;
    /** Sequence number of a logical message within a group. */
    LogicalSequenceNumber?: number;
    /** Indicates if additional message info should be included. Expects true or false. */
    IncludeInfo?: string;
    /** Wait time for a message to appear in the queue, expects an XML duration or hh:mm:ss. */
    Timeout?: string;
}

/**
 * Definition: MultipleGetValidOptions
 */
export interface MultipleGetValidOptions {
    /** When specified, overrides the queue defined in the connection. */
    Queue?: string;
    /** Base64 string that represents a byte array with 24 bytes. */
    MessageId?: string;
    /** Base64 string that represents a byte array with 24 bytes. */
    CorrelationId?: string;
    /** Base64 string that represents a byte array with 24 bytes. */
    GroupId?: string;
    /** Base64 string that represents a byte array with 16 bytes. */
    MessageToken?: string;
    /** Offset in bytes of the data in the physical message from the start of the logical message. */
    Offset?: number;
    /** Sequence number of a logical message within a group. */
    LogicalSequenceNumber?: number;
    /** Indicates if additional message info should be included. Expects true or false. */
    IncludeInfo?: string;
    /** Wait time for a message to appear in the queue, expects an XML duration or hh:mm:ss. */
    Timeout?: string;
    /** Maximum number of messages to be retrieved from the queue. */
    BatchSize?: number;
}

/**
 * Definition: SendValidDataOptions
 */
export interface SendValidDataOptions {
    /** When specified, overrides the queue defined in the connection. */
    Queue?: string;
    /** Message data */
    Message: string;
    /** Message type */
    MessageType?: string;
    /** Base64 string that represents a byte array with 24 bytes. */
    CorrelationId?: string;
    /** Base64 string that represents a byte array with 24 bytes. */
    MessageId?: string;
    /** Reply-to queue */
    ReplyToQueue?: string;
    /** Reply-to queue manager */
    ReplyToQueueManager?: string;
    /** Character set for the message. */
    CodeCharSetId?: number;
    /** Offset in bytes of the data in the physical message from the start of the logical message. */
    Offset?: number;
    /** Format field for the message. */
    Format?: string;
}

/**
 * Definition: SendValidDataOptionsArray
 */
export interface SendValidDataOptionsArray {
    messages: Array<SendValidDataOptions>;
}
// #endregion Types

// #region Client

/**
 * Typed client for the mq connector.
 */
export class MqClient extends ConnectorClientBase {

    /**
     * Creates a new MqClient.
     * @param connectionRuntimeUrl The connection runtime URL from Azure Portal.
     * @param credential The credential used for authentication.
     * @param options Optional connector client options.
     */
    constructor(connectionRuntimeUrl: string, credential: TokenCredential, options?: ConnectorClientOptions) {
        super(connectionRuntimeUrl, credential, options);
    }

    public get connectorName(): string {
        return "mq";
    }

    /**
     * Delete message
     * @remarks Deletes one message from the queue, by doing a destructive get.
     */
    public async delete(input: SingleGetValidOptions, options: ConnectorOperationOptions = {}): Promise<Item> {
        const requestPath = `/v2/delete`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<Item>("Mq.delete", "DeleteV2", "POST", requestUrl, input, options);

        return httpResponse.value as Item;
    }

    /**
     * Delete messages
     * @remarks Deletes messages from the queue, by doing a destructive get.
     */
    public async deleteAll(input: MultipleGetValidOptions, options: ConnectorOperationOptions = {}): Promise<ItemsList> {
        const requestPath = `/v2/deleteall`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<ItemsList>("Mq.deleteAll", "DeleteAllV2", "POST", requestUrl, input, options);

        return httpResponse.value as ItemsList;
    }

    /**
     * Browse message
     * @remarks Browse one message from the queue.
     */
    public async read(input: SingleGetValidOptions, options: ConnectorOperationOptions = {}): Promise<Item> {
        const requestPath = `/v2/read`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<Item>("Mq.read", "ReadV2", "POST", requestUrl, input, options);

        return httpResponse.value as Item;
    }

    /**
     * Browse messages
     * @remarks Browse messages in a queue.
     */
    public async readAll(input: MultipleGetValidOptions, options: ConnectorOperationOptions = {}): Promise<ItemsList> {
        const requestPath = `/v2/readall`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<ItemsList>("Mq.readAll", "ReadAllV2", "POST", requestUrl, input, options);

        return httpResponse.value as ItemsList;
    }

    /**
     * Receive message
     * @remarks Returns one message from the queue, by doing a destructive get.
     */
    public async receive(input: SingleGetValidOptions, options: ConnectorOperationOptions = {}): Promise<Item> {
        const requestPath = `/v2/receive`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<Item>("Mq.receive", "ReceiveV2", "POST", requestUrl, input, options);

        return httpResponse.value as Item;
    }

    /**
     * Receive messages
     * @remarks Returns messages from the queue, by doing a destructive get.
     */
    public async receiveAll(input: MultipleGetValidOptions, options: ConnectorOperationOptions = {}): Promise<ItemsList> {
        const requestPath = `/v2/receiveall`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<ItemsList>("Mq.receiveAll", "ReceiveAllV2", "POST", requestUrl, input, options);

        return httpResponse.value as ItemsList;
    }

    /**
     * Send message
     * @remarks Send a message to a queue.
     */
    public async send(input: SendValidDataOptions, options: ConnectorOperationOptions = {}): Promise<SendResponse> {
        const requestPath = `/v2/send`;
        const requestUrl = this.resolveUrl(requestPath);
        const httpResponse = await this.sendWithTracingAsync<SendResponse>("Mq.send", "SendV2", "POST", requestUrl, input, options);

        return httpResponse.value as SendResponse;
    }

}

// #endregion Client
